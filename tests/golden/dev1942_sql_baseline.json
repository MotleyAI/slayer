{
  "dual_phase/filter_position::bigquery": "WITH _cm_amount_sum_partition_by_ordered_at_region AS (\n  SELECT\n    `_stage_inner`.`monthly___region` AS `region`,\n    `_stage_inner`.`monthly___ordered_at_month` AS `ordered_at_month`,\n    `_stage_inner`.`monthly___amount_sum_partition_by_ordered_at_region` AS `amount_sum_partition_by_ordered_at_region`\n  FROM (\n    SELECT\n      monthly.region AS `monthly___region`,\n      DATE_TRUNC(monthly.ordered_at, MONTH) AS `monthly___ordered_at_month`,\n      CAST(SUM(monthly.amount) AS FLOAT64) AS `monthly___amount_sum_partition_by_ordered_at_region`\n    FROM monthly AS monthly\n    GROUP BY\n      monthly.region,\n      DATE_TRUNC(monthly.ordered_at, MONTH)\n  ) AS _stage_inner\n), _cm_monthly___min_partition_by_region AS (\n  SELECT\n    `_base`.`monthly___region` AS `monthly___region`,\n    MIN(_base._v) AS `monthly___min_amount_sum_partition_by_ordered_at_region`\n  FROM (\n    SELECT\n      monthly.region AS `monthly___region`,\n      monthly.region AS _ek0,\n      DATE_TRUNC(monthly.ordered_at, MONTH) AS _ek1,\n      MAX(\n        _cm_amount_sum_partition_by_ordered_at_region.`amount_sum_partition_by_ordered_at_region`\n      ) AS _v\n    FROM monthly AS monthly\n    LEFT JOIN _cm_amount_sum_partition_by_ordered_at_region\n      ON monthly.region IS NOT DISTINCT FROM _cm_amount_sum_partition_by_ordered_at_region.`region`\n      AND DATE_TRUNC(monthly.ordered_at, MONTH) IS NOT DISTINCT FROM _cm_amount_sum_partition_by_ordered_at_region.`ordered_at_month`\n    GROUP BY\n      monthly.region,\n      monthly.region,\n      DATE_TRUNC(monthly.ordered_at, MONTH)\n  ) AS _base\n  GROUP BY\n    `_base`.`monthly___region`\n), _base AS (\n  SELECT\n    monthly.region AS `monthly___region`,\n    DATE_TRUNC(monthly.ordered_at, MONTH) AS `monthly___ordered_at`,\n    SUM(\n      monthly.amount * `_cm_monthly___min_partition_by_region`.`monthly___min_amount_sum_partition_by_ordered_at_region`\n    ) AS `monthly___b`\n  FROM monthly AS monthly\n  LEFT JOIN _cm_monthly___min_partition_by_region\n    ON monthly.region IS NOT DISTINCT FROM _cm_monthly___min_partition_by_region.`monthly___region`\n  GROUP BY\n    monthly.region,\n    DATE_TRUNC(monthly.ordered_at, MONTH)\n)\nSELECT\n  _base.`monthly___region`,\n  _base.`monthly___ordered_at`,\n  _base.`monthly___b`\nFROM _base\nLEFT JOIN _cm_monthly___min_partition_by_region\n  ON _base.`monthly___region` IS NOT DISTINCT FROM _cm_monthly___min_partition_by_region.`monthly___region`\nWHERE\n  _cm_monthly___min_partition_by_region.`monthly___min_amount_sum_partition_by_ordered_at_region` > 5",
  "dual_phase/filter_position::duckdb": "WITH _cm_amount_sum_partition_by_ordered_at_region AS (\n  SELECT\n    _stage_inner.\"monthly.region\" AS \"region\",\n    _stage_inner.\"monthly.ordered_at_month\" AS \"ordered_at_month\",\n    _stage_inner.\"monthly.amount_sum_partition_by_ordered_at_region\" AS \"amount_sum_partition_by_ordered_at_region\"\n  FROM (\n    SELECT\n      monthly.region AS \"monthly.region\",\n      DATE_TRUNC('MONTH', monthly.ordered_at) AS \"monthly.ordered_at_month\",\n      CAST(SUM(monthly.amount) AS DOUBLE) AS \"monthly.amount_sum_partition_by_ordered_at_region\"\n    FROM monthly AS monthly\n    GROUP BY\n      monthly.region,\n      DATE_TRUNC('MONTH', monthly.ordered_at)\n  ) AS _stage_inner\n), _cm_monthly___min_partition_by_region AS (\n  SELECT\n    _base.\"monthly.region\" AS \"monthly.region\",\n    MIN(_base._v) AS \"monthly.min_amount_sum_partition_by_ordered_at_region\"\n  FROM (\n    SELECT\n      monthly.region AS \"monthly.region\",\n      monthly.region AS _ek0,\n      DATE_TRUNC('MONTH', monthly.ordered_at) AS _ek1,\n      MAX(\n        _cm_amount_sum_partition_by_ordered_at_region.\"amount_sum_partition_by_ordered_at_region\"\n      ) AS _v\n    FROM monthly AS monthly\n    LEFT JOIN _cm_amount_sum_partition_by_ordered_at_region\n      ON monthly.region IS NOT DISTINCT FROM _cm_amount_sum_partition_by_ordered_at_region.\"region\"\n      AND DATE_TRUNC('MONTH', monthly.ordered_at) IS NOT DISTINCT FROM _cm_amount_sum_partition_by_ordered_at_region.\"ordered_at_month\"\n    GROUP BY\n      monthly.region,\n      monthly.region,\n      DATE_TRUNC('MONTH', monthly.ordered_at)\n  ) AS _base\n  GROUP BY\n    _base.\"monthly.region\"\n), _base AS (\n  SELECT\n    monthly.region AS \"monthly.region\",\n    DATE_TRUNC('MONTH', monthly.ordered_at) AS \"monthly.ordered_at\",\n    SUM(\n      monthly.amount * _cm_monthly___min_partition_by_region.\"monthly.min_amount_sum_partition_by_ordered_at_region\"\n    ) AS \"monthly.b\"\n  FROM monthly AS monthly\n  LEFT JOIN _cm_monthly___min_partition_by_region\n    ON monthly.region IS NOT DISTINCT FROM _cm_monthly___min_partition_by_region.\"monthly.region\"\n  GROUP BY\n    monthly.region,\n    DATE_TRUNC('MONTH', monthly.ordered_at)\n)\nSELECT\n  _base.\"monthly.region\",\n  _base.\"monthly.ordered_at\",\n  _base.\"monthly.b\"\nFROM _base\nLEFT JOIN _cm_monthly___min_partition_by_region\n  ON _base.\"monthly.region\" IS NOT DISTINCT FROM _cm_monthly___min_partition_by_region.\"monthly.region\"\nWHERE\n  _cm_monthly___min_partition_by_region.\"monthly.min_amount_sum_partition_by_ordered_at_region\" > 5",
  "dual_phase/filter_position::mysql": "WITH _cm_amount_sum_partition_by_ordered_at_region AS (\n  SELECT\n    _stage_inner.`monthly.region` AS `region`,\n    _stage_inner.`monthly.ordered_at_month` AS `ordered_at_month`,\n    _stage_inner.`monthly.amount_sum_partition_by_ordered_at_region` AS `amount_sum_partition_by_ordered_at_region`\n  FROM (\n    SELECT\n      monthly.region AS `monthly.region`,\n      STR_TO_DATE(CONCAT(YEAR(monthly.ordered_at), ' ', MONTH(monthly.ordered_at), ' 1'), '%Y %c %e') AS `monthly.ordered_at_month`,\n      CAST(SUM(monthly.amount) AS DOUBLE) AS `monthly.amount_sum_partition_by_ordered_at_region`\n    FROM monthly AS monthly\n    GROUP BY\n      monthly.region,\n      STR_TO_DATE(CONCAT(YEAR(monthly.ordered_at), ' ', MONTH(monthly.ordered_at), ' 1'), '%Y %c %e')\n  ) AS _stage_inner\n), _cm_monthly___min_partition_by_region AS (\n  SELECT\n    _base.`monthly.region` AS `monthly.region`,\n    MIN(_base._v) AS `monthly.min_amount_sum_partition_by_ordered_at_region`\n  FROM (\n    SELECT\n      monthly.region AS `monthly.region`,\n      monthly.region AS _ek0,\n      STR_TO_DATE(CONCAT(YEAR(monthly.ordered_at), ' ', MONTH(monthly.ordered_at), ' 1'), '%Y %c %e') AS _ek1,\n      MAX(\n        _cm_amount_sum_partition_by_ordered_at_region.`amount_sum_partition_by_ordered_at_region`\n      ) AS _v\n    FROM monthly AS monthly\n    LEFT JOIN _cm_amount_sum_partition_by_ordered_at_region\n      ON monthly.region <=> _cm_amount_sum_partition_by_ordered_at_region.`region`\n      AND STR_TO_DATE(CONCAT(YEAR(monthly.ordered_at), ' ', MONTH(monthly.ordered_at), ' 1'), '%Y %c %e') <=> _cm_amount_sum_partition_by_ordered_at_region.`ordered_at_month`\n    GROUP BY\n      monthly.region,\n      monthly.region,\n      STR_TO_DATE(CONCAT(YEAR(monthly.ordered_at), ' ', MONTH(monthly.ordered_at), ' 1'), '%Y %c %e')\n  ) AS _base\n  GROUP BY\n    _base.`monthly.region`\n), _base AS (\n  SELECT\n    monthly.region AS `monthly.region`,\n    STR_TO_DATE(CONCAT(YEAR(monthly.ordered_at), ' ', MONTH(monthly.ordered_at), ' 1'), '%Y %c %e') AS `monthly.ordered_at`,\n    SUM(\n      monthly.amount * _cm_monthly___min_partition_by_region.`monthly.min_amount_sum_partition_by_ordered_at_region`\n    ) AS `monthly.b`\n  FROM monthly AS monthly\n  LEFT JOIN _cm_monthly___min_partition_by_region\n    ON monthly.region <=> _cm_monthly___min_partition_by_region.`monthly.region`\n  GROUP BY\n    monthly.region,\n    STR_TO_DATE(CONCAT(YEAR(monthly.ordered_at), ' ', MONTH(monthly.ordered_at), ' 1'), '%Y %c %e')\n)\nSELECT\n  _base.`monthly.region`,\n  _base.`monthly.ordered_at`,\n  _base.`monthly.b`\nFROM _base\nLEFT JOIN _cm_monthly___min_partition_by_region\n  ON _base.`monthly.region` <=> _cm_monthly___min_partition_by_region.`monthly.region`\nWHERE\n  _cm_monthly___min_partition_by_region.`monthly.min_amount_sum_partition_by_ordered_at_region` > 5",
  "dual_phase/filter_position::postgres": "WITH _cm_amount_sum_partition_by_ordered_at_region AS (\n  SELECT\n    _stage_inner.\"monthly.region\" AS \"region\",\n    _stage_inner.\"monthly.ordered_at_month\" AS \"ordered_at_month\",\n    _stage_inner.\"monthly.amount_sum_partition_by_ordered_at_region\" AS \"amount_sum_partition_by_ordered_at_region\"\n  FROM (\n    SELECT\n      monthly.region AS \"monthly.region\",\n      DATE_TRUNC('MONTH', monthly.ordered_at) AS \"monthly.ordered_at_month\",\n      CAST(SUM(monthly.amount) AS DOUBLE PRECISION) AS \"monthly.amount_sum_partition_by_ordered_at_region\"\n    FROM monthly AS monthly\n    GROUP BY\n      monthly.region,\n      DATE_TRUNC('MONTH', monthly.ordered_at)\n  ) AS _stage_inner\n), _cm_monthly___min_partition_by_region AS (\n  SELECT\n    _base.\"monthly.region\" AS \"monthly.region\",\n    MIN(_base._v) AS \"monthly.min_amount_sum_partition_by_ordered_at_region\"\n  FROM (\n    SELECT\n      monthly.region AS \"monthly.region\",\n      monthly.region AS _ek0,\n      DATE_TRUNC('MONTH', monthly.ordered_at) AS _ek1,\n      MAX(\n        _cm_amount_sum_partition_by_ordered_at_region.\"amount_sum_partition_by_ordered_at_region\"\n      ) AS _v\n    FROM monthly AS monthly\n    LEFT JOIN _cm_amount_sum_partition_by_ordered_at_region\n      ON monthly.region IS NOT DISTINCT FROM _cm_amount_sum_partition_by_ordered_at_region.\"region\"\n      AND DATE_TRUNC('MONTH', monthly.ordered_at) IS NOT DISTINCT FROM _cm_amount_sum_partition_by_ordered_at_region.\"ordered_at_month\"\n    GROUP BY\n      monthly.region,\n      monthly.region,\n      DATE_TRUNC('MONTH', monthly.ordered_at)\n  ) AS _base\n  GROUP BY\n    _base.\"monthly.region\"\n), _base AS (\n  SELECT\n    monthly.region AS \"monthly.region\",\n    DATE_TRUNC('MONTH', monthly.ordered_at) AS \"monthly.ordered_at\",\n    SUM(\n      monthly.amount * _cm_monthly___min_partition_by_region.\"monthly.min_amount_sum_partition_by_ordered_at_region\"\n    ) AS \"monthly.b\"\n  FROM monthly AS monthly\n  LEFT JOIN _cm_monthly___min_partition_by_region\n    ON monthly.region IS NOT DISTINCT FROM _cm_monthly___min_partition_by_region.\"monthly.region\"\n  GROUP BY\n    monthly.region,\n    DATE_TRUNC('MONTH', monthly.ordered_at)\n)\nSELECT\n  _base.\"monthly.region\",\n  _base.\"monthly.ordered_at\",\n  _base.\"monthly.b\"\nFROM _base\nLEFT JOIN _cm_monthly___min_partition_by_region\n  ON _base.\"monthly.region\" IS NOT DISTINCT FROM _cm_monthly___min_partition_by_region.\"monthly.region\"\nWHERE\n  _cm_monthly___min_partition_by_region.\"monthly.min_amount_sum_partition_by_ordered_at_region\" > 5",
  "dual_phase/filter_position::snowflake": "WITH _cm_amount_sum_partition_by_ordered_at_region AS (\n  SELECT\n    _stage_inner.\"monthly.region\" AS \"region\",\n    _stage_inner.\"monthly.ordered_at_month\" AS \"ordered_at_month\",\n    _stage_inner.\"monthly.amount_sum_partition_by_ordered_at_region\" AS \"amount_sum_partition_by_ordered_at_region\"\n  FROM (\n    SELECT\n      monthly.region AS \"monthly.region\",\n      DATE_TRUNC('MONTH', monthly.ordered_at) AS \"monthly.ordered_at_month\",\n      CAST(SUM(monthly.amount) AS DOUBLE) AS \"monthly.amount_sum_partition_by_ordered_at_region\"\n    FROM monthly AS monthly\n    GROUP BY\n      monthly.region,\n      DATE_TRUNC('MONTH', monthly.ordered_at)\n  ) AS _stage_inner\n), _cm_monthly___min_partition_by_region AS (\n  SELECT\n    _base.\"monthly.region\" AS \"monthly.region\",\n    MIN(_base._v) AS \"monthly.min_amount_sum_partition_by_ordered_at_region\"\n  FROM (\n    SELECT\n      monthly.region AS \"monthly.region\",\n      monthly.region AS _ek0,\n      DATE_TRUNC('MONTH', monthly.ordered_at) AS _ek1,\n      MAX(\n        _cm_amount_sum_partition_by_ordered_at_region.\"amount_sum_partition_by_ordered_at_region\"\n      ) AS _v\n    FROM monthly AS monthly\n    LEFT JOIN _cm_amount_sum_partition_by_ordered_at_region\n      ON monthly.region IS NOT DISTINCT FROM _cm_amount_sum_partition_by_ordered_at_region.\"region\"\n      AND DATE_TRUNC('MONTH', monthly.ordered_at) IS NOT DISTINCT FROM _cm_amount_sum_partition_by_ordered_at_region.\"ordered_at_month\"\n    GROUP BY\n      monthly.region,\n      monthly.region,\n      DATE_TRUNC('MONTH', monthly.ordered_at)\n  ) AS _base\n  GROUP BY\n    _base.\"monthly.region\"\n), _base AS (\n  SELECT\n    monthly.region AS \"monthly.region\",\n    DATE_TRUNC('MONTH', monthly.ordered_at) AS \"monthly.ordered_at\",\n    SUM(\n      monthly.amount * _cm_monthly___min_partition_by_region.\"monthly.min_amount_sum_partition_by_ordered_at_region\"\n    ) AS \"monthly.b\"\n  FROM monthly AS monthly\n  LEFT JOIN _cm_monthly___min_partition_by_region\n    ON monthly.region IS NOT DISTINCT FROM _cm_monthly___min_partition_by_region.\"monthly.region\"\n  GROUP BY\n    monthly.region,\n    DATE_TRUNC('MONTH', monthly.ordered_at)\n)\nSELECT\n  _base.\"monthly.region\",\n  _base.\"monthly.ordered_at\",\n  _base.\"monthly.b\"\nFROM _base\nLEFT JOIN _cm_monthly___min_partition_by_region\n  ON _base.\"monthly.region\" IS NOT DISTINCT FROM _cm_monthly___min_partition_by_region.\"monthly.region\"\nWHERE\n  _cm_monthly___min_partition_by_region.\"monthly.min_amount_sum_partition_by_ordered_at_region\" > 5",
  "dual_phase/filter_position::sqlite": "WITH _cm_amount_sum_partition_by_ordered_at_region AS (\n  SELECT\n    _stage_inner.\"monthly.region\" AS \"region\",\n    _stage_inner.\"monthly.ordered_at_month\" AS \"ordered_at_month\",\n    _stage_inner.\"monthly.amount_sum_partition_by_ordered_at_region\" AS \"amount_sum_partition_by_ordered_at_region\"\n  FROM (\n    SELECT\n      monthly.region AS \"monthly.region\",\n      STRFTIME('%Y-%m-01', monthly.ordered_at) AS \"monthly.ordered_at_month\",\n      CAST(SUM(monthly.amount) AS REAL) AS \"monthly.amount_sum_partition_by_ordered_at_region\"\n    FROM monthly AS monthly\n    GROUP BY\n      monthly.region,\n      STRFTIME('%Y-%m-01', monthly.ordered_at)\n  ) AS _stage_inner\n), _cm_monthly___min_partition_by_region AS (\n  SELECT\n    _base.\"monthly.region\" AS \"monthly.region\",\n    MIN(_base._v) AS \"monthly.min_amount_sum_partition_by_ordered_at_region\"\n  FROM (\n    SELECT\n      monthly.region AS \"monthly.region\",\n      monthly.region AS _ek0,\n      STRFTIME('%Y-%m-01', monthly.ordered_at) AS _ek1,\n      MAX(\n        _cm_amount_sum_partition_by_ordered_at_region.\"amount_sum_partition_by_ordered_at_region\"\n      ) AS _v\n    FROM monthly AS monthly\n    LEFT JOIN _cm_amount_sum_partition_by_ordered_at_region\n      ON monthly.region IS _cm_amount_sum_partition_by_ordered_at_region.\"region\"\n      AND STRFTIME('%Y-%m-01', monthly.ordered_at) IS _cm_amount_sum_partition_by_ordered_at_region.\"ordered_at_month\"\n    GROUP BY\n      monthly.region,\n      monthly.region,\n      STRFTIME('%Y-%m-01', monthly.ordered_at)\n  ) AS _base\n  GROUP BY\n    _base.\"monthly.region\"\n), _base AS (\n  SELECT\n    monthly.region AS \"monthly.region\",\n    STRFTIME('%Y-%m-01', monthly.ordered_at) AS \"monthly.ordered_at\",\n    SUM(\n      monthly.amount * _cm_monthly___min_partition_by_region.\"monthly.min_amount_sum_partition_by_ordered_at_region\"\n    ) AS \"monthly.b\"\n  FROM monthly AS monthly\n  LEFT JOIN _cm_monthly___min_partition_by_region\n    ON monthly.region IS _cm_monthly___min_partition_by_region.\"monthly.region\"\n  GROUP BY\n    monthly.region,\n    STRFTIME('%Y-%m-01', monthly.ordered_at)\n)\nSELECT\n  _base.\"monthly.region\",\n  _base.\"monthly.ordered_at\",\n  _base.\"monthly.b\"\nFROM _base\nLEFT JOIN _cm_monthly___min_partition_by_region\n  ON _base.\"monthly.region\" IS _cm_monthly___min_partition_by_region.\"monthly.region\"\nWHERE\n  _cm_monthly___min_partition_by_region.\"monthly.min_amount_sum_partition_by_ordered_at_region\" > 5",
  "dual_phase/filter_position::tsql": "WITH _cm_amount_sum_partition_by_ordered_at_region AS (\n  SELECT\n    _stage_inner.[monthly___region] AS [region],\n    _stage_inner.[monthly___ordered_at_month] AS [ordered_at_month],\n    _stage_inner.[monthly___amount_sum_partition_by_ordered_at_region] AS [amount_sum_partition_by_ordered_at_region]\n  FROM (\n    SELECT\n      monthly.region AS [monthly___region],\n      DATETRUNC(MONTH, monthly.ordered_at) AS [monthly___ordered_at_month],\n      CAST(SUM(monthly.amount) AS FLOAT) AS [monthly___amount_sum_partition_by_ordered_at_region]\n    FROM monthly AS monthly\n    GROUP BY\n      monthly.region,\n      DATETRUNC(MONTH, monthly.ordered_at)\n  ) AS _stage_inner\n), _cm_monthly___min_partition_by_region AS (\n  SELECT\n    _base.[monthly___region] AS [monthly___region],\n    MIN(_base._v) AS [monthly___min_amount_sum_partition_by_ordered_at_region]\n  FROM (\n    SELECT\n      monthly.region AS [monthly___region],\n      monthly.region AS _ek0,\n      DATETRUNC(MONTH, monthly.ordered_at) AS _ek1,\n      MAX(\n        _cm_amount_sum_partition_by_ordered_at_region.[amount_sum_partition_by_ordered_at_region]\n      ) AS _v\n    FROM monthly AS monthly\n    LEFT JOIN _cm_amount_sum_partition_by_ordered_at_region\n      ON (\n        monthly.region = _cm_amount_sum_partition_by_ordered_at_region.[region]\n        OR (\n          monthly.region IS NULL\n          AND _cm_amount_sum_partition_by_ordered_at_region.[region] IS NULL\n        )\n      )\n      AND (\n        DATETRUNC(MONTH, monthly.ordered_at) = _cm_amount_sum_partition_by_ordered_at_region.[ordered_at_month]\n        OR (\n          DATETRUNC(MONTH, monthly.ordered_at) IS NULL\n          AND _cm_amount_sum_partition_by_ordered_at_region.[ordered_at_month] IS NULL\n        )\n      )\n    GROUP BY\n      monthly.region,\n      monthly.region,\n      DATETRUNC(MONTH, monthly.ordered_at)\n  ) AS _base\n  GROUP BY\n    _base.[monthly___region]\n), _base AS (\n  SELECT\n    monthly.region AS [monthly___region],\n    DATETRUNC(month, monthly.ordered_at) AS [monthly___ordered_at],\n    SUM(\n      monthly.amount * _cm_monthly___min_partition_by_region.[monthly___min_amount_sum_partition_by_ordered_at_region]\n    ) AS [monthly___b]\n  FROM monthly AS monthly\n  LEFT JOIN _cm_monthly___min_partition_by_region\n    ON (\n      monthly.region = _cm_monthly___min_partition_by_region.[monthly___region]\n      OR (\n        monthly.region IS NULL\n        AND _cm_monthly___min_partition_by_region.[monthly___region] IS NULL\n      )\n    )\n  GROUP BY\n    monthly.region,\n    DATETRUNC(month, monthly.ordered_at)\n)\nSELECT\n  _base.[monthly___region],\n  _base.[monthly___ordered_at],\n  _base.[monthly___b]\nFROM _base\nLEFT JOIN _cm_monthly___min_partition_by_region\n  ON (\n    _base.[monthly___region] = _cm_monthly___min_partition_by_region.[monthly___region]\n    OR (\n      _base.[monthly___region] IS NULL\n      AND _cm_monthly___min_partition_by_region.[monthly___region] IS NULL\n    )\n  )\nWHERE\n  _cm_monthly___min_partition_by_region.[monthly___min_amount_sum_partition_by_ordered_at_region] > 5",
  "dual_phase/measure::bigquery": "WITH _cm_amount_sum_partition_by_ordered_at_region AS (\n  SELECT\n    `_stage_inner`.`monthly___region` AS `region`,\n    `_stage_inner`.`monthly___ordered_at_month` AS `ordered_at_month`,\n    `_stage_inner`.`monthly___amount_sum_partition_by_ordered_at_region` AS `amount_sum_partition_by_ordered_at_region`\n  FROM (\n    SELECT\n      monthly.region AS `monthly___region`,\n      DATE_TRUNC(monthly.ordered_at, MONTH) AS `monthly___ordered_at_month`,\n      CAST(SUM(monthly.amount) AS FLOAT64) AS `monthly___amount_sum_partition_by_ordered_at_region`\n    FROM monthly AS monthly\n    GROUP BY\n      monthly.region,\n      DATE_TRUNC(monthly.ordered_at, MONTH)\n  ) AS _stage_inner\n), _cm_monthly___min_partition_by_region AS (\n  SELECT\n    `_base`.`monthly___region` AS `monthly___region`,\n    MIN(_base._v) AS `monthly___a`\n  FROM (\n    SELECT\n      monthly.region AS `monthly___region`,\n      monthly.region AS _ek0,\n      DATE_TRUNC(monthly.ordered_at, MONTH) AS _ek1,\n      MAX(\n        _cm_amount_sum_partition_by_ordered_at_region.`amount_sum_partition_by_ordered_at_region`\n      ) AS _v\n    FROM monthly AS monthly\n    LEFT JOIN _cm_amount_sum_partition_by_ordered_at_region\n      ON monthly.region IS NOT DISTINCT FROM _cm_amount_sum_partition_by_ordered_at_region.`region`\n      AND DATE_TRUNC(monthly.ordered_at, MONTH) IS NOT DISTINCT FROM _cm_amount_sum_partition_by_ordered_at_region.`ordered_at_month`\n    GROUP BY\n      monthly.region,\n      monthly.region,\n      DATE_TRUNC(monthly.ordered_at, MONTH)\n  ) AS _base\n  GROUP BY\n    `_base`.`monthly___region`\n), _base AS (\n  SELECT\n    monthly.region AS `monthly___region`,\n    DATE_TRUNC(monthly.ordered_at, MONTH) AS `monthly___ordered_at`,\n    SUM(monthly.amount * `_cm_monthly___min_partition_by_region`.`monthly___a`) AS `monthly___b`\n  FROM monthly AS monthly\n  LEFT JOIN _cm_monthly___min_partition_by_region\n    ON monthly.region IS NOT DISTINCT FROM _cm_monthly___min_partition_by_region.`monthly___region`\n  GROUP BY\n    monthly.region,\n    DATE_TRUNC(monthly.ordered_at, MONTH)\n)\nSELECT\n  _base.`monthly___region`,\n  _base.`monthly___ordered_at`,\n  _cm_monthly___min_partition_by_region.`monthly___a`,\n  _base.`monthly___b`\nFROM _base\nLEFT JOIN _cm_monthly___min_partition_by_region\n  ON _base.`monthly___region` IS NOT DISTINCT FROM _cm_monthly___min_partition_by_region.`monthly___region`",
  "dual_phase/measure::duckdb": "WITH _cm_amount_sum_partition_by_ordered_at_region AS (\n  SELECT\n    _stage_inner.\"monthly.region\" AS \"region\",\n    _stage_inner.\"monthly.ordered_at_month\" AS \"ordered_at_month\",\n    _stage_inner.\"monthly.amount_sum_partition_by_ordered_at_region\" AS \"amount_sum_partition_by_ordered_at_region\"\n  FROM (\n    SELECT\n      monthly.region AS \"monthly.region\",\n      DATE_TRUNC('MONTH', monthly.ordered_at) AS \"monthly.ordered_at_month\",\n      CAST(SUM(monthly.amount) AS DOUBLE) AS \"monthly.amount_sum_partition_by_ordered_at_region\"\n    FROM monthly AS monthly\n    GROUP BY\n      monthly.region,\n      DATE_TRUNC('MONTH', monthly.ordered_at)\n  ) AS _stage_inner\n), _cm_monthly___min_partition_by_region AS (\n  SELECT\n    _base.\"monthly.region\" AS \"monthly.region\",\n    MIN(_base._v) AS \"monthly.a\"\n  FROM (\n    SELECT\n      monthly.region AS \"monthly.region\",\n      monthly.region AS _ek0,\n      DATE_TRUNC('MONTH', monthly.ordered_at) AS _ek1,\n      MAX(\n        _cm_amount_sum_partition_by_ordered_at_region.\"amount_sum_partition_by_ordered_at_region\"\n      ) AS _v\n    FROM monthly AS monthly\n    LEFT JOIN _cm_amount_sum_partition_by_ordered_at_region\n      ON monthly.region IS NOT DISTINCT FROM _cm_amount_sum_partition_by_ordered_at_region.\"region\"\n      AND DATE_TRUNC('MONTH', monthly.ordered_at) IS NOT DISTINCT FROM _cm_amount_sum_partition_by_ordered_at_region.\"ordered_at_month\"\n    GROUP BY\n      monthly.region,\n      monthly.region,\n      DATE_TRUNC('MONTH', monthly.ordered_at)\n  ) AS _base\n  GROUP BY\n    _base.\"monthly.region\"\n), _base AS (\n  SELECT\n    monthly.region AS \"monthly.region\",\n    DATE_TRUNC('MONTH', monthly.ordered_at) AS \"monthly.ordered_at\",\n    SUM(monthly.amount * _cm_monthly___min_partition_by_region.\"monthly.a\") AS \"monthly.b\"\n  FROM monthly AS monthly\n  LEFT JOIN _cm_monthly___min_partition_by_region\n    ON monthly.region IS NOT DISTINCT FROM _cm_monthly___min_partition_by_region.\"monthly.region\"\n  GROUP BY\n    monthly.region,\n    DATE_TRUNC('MONTH', monthly.ordered_at)\n)\nSELECT\n  _base.\"monthly.region\",\n  _base.\"monthly.ordered_at\",\n  _cm_monthly___min_partition_by_region.\"monthly.a\",\n  _base.\"monthly.b\"\nFROM _base\nLEFT JOIN _cm_monthly___min_partition_by_region\n  ON _base.\"monthly.region\" IS NOT DISTINCT FROM _cm_monthly___min_partition_by_region.\"monthly.region\"",
  "dual_phase/measure::mysql": "WITH _cm_amount_sum_partition_by_ordered_at_region AS (\n  SELECT\n    _stage_inner.`monthly.region` AS `region`,\n    _stage_inner.`monthly.ordered_at_month` AS `ordered_at_month`,\n    _stage_inner.`monthly.amount_sum_partition_by_ordered_at_region` AS `amount_sum_partition_by_ordered_at_region`\n  FROM (\n    SELECT\n      monthly.region AS `monthly.region`,\n      STR_TO_DATE(CONCAT(YEAR(monthly.ordered_at), ' ', MONTH(monthly.ordered_at), ' 1'), '%Y %c %e') AS `monthly.ordered_at_month`,\n      CAST(SUM(monthly.amount) AS DOUBLE) AS `monthly.amount_sum_partition_by_ordered_at_region`\n    FROM monthly AS monthly\n    GROUP BY\n      monthly.region,\n      STR_TO_DATE(CONCAT(YEAR(monthly.ordered_at), ' ', MONTH(monthly.ordered_at), ' 1'), '%Y %c %e')\n  ) AS _stage_inner\n), _cm_monthly___min_partition_by_region AS (\n  SELECT\n    _base.`monthly.region` AS `monthly.region`,\n    MIN(_base._v) AS `monthly.a`\n  FROM (\n    SELECT\n      monthly.region AS `monthly.region`,\n      monthly.region AS _ek0,\n      STR_TO_DATE(CONCAT(YEAR(monthly.ordered_at), ' ', MONTH(monthly.ordered_at), ' 1'), '%Y %c %e') AS _ek1,\n      MAX(\n        _cm_amount_sum_partition_by_ordered_at_region.`amount_sum_partition_by_ordered_at_region`\n      ) AS _v\n    FROM monthly AS monthly\n    LEFT JOIN _cm_amount_sum_partition_by_ordered_at_region\n      ON monthly.region <=> _cm_amount_sum_partition_by_ordered_at_region.`region`\n      AND STR_TO_DATE(CONCAT(YEAR(monthly.ordered_at), ' ', MONTH(monthly.ordered_at), ' 1'), '%Y %c %e') <=> _cm_amount_sum_partition_by_ordered_at_region.`ordered_at_month`\n    GROUP BY\n      monthly.region,\n      monthly.region,\n      STR_TO_DATE(CONCAT(YEAR(monthly.ordered_at), ' ', MONTH(monthly.ordered_at), ' 1'), '%Y %c %e')\n  ) AS _base\n  GROUP BY\n    _base.`monthly.region`\n), _base AS (\n  SELECT\n    monthly.region AS `monthly.region`,\n    STR_TO_DATE(CONCAT(YEAR(monthly.ordered_at), ' ', MONTH(monthly.ordered_at), ' 1'), '%Y %c %e') AS `monthly.ordered_at`,\n    SUM(monthly.amount * _cm_monthly___min_partition_by_region.`monthly.a`) AS `monthly.b`\n  FROM monthly AS monthly\n  LEFT JOIN _cm_monthly___min_partition_by_region\n    ON monthly.region <=> _cm_monthly___min_partition_by_region.`monthly.region`\n  GROUP BY\n    monthly.region,\n    STR_TO_DATE(CONCAT(YEAR(monthly.ordered_at), ' ', MONTH(monthly.ordered_at), ' 1'), '%Y %c %e')\n)\nSELECT\n  _base.`monthly.region`,\n  _base.`monthly.ordered_at`,\n  _cm_monthly___min_partition_by_region.`monthly.a`,\n  _base.`monthly.b`\nFROM _base\nLEFT JOIN _cm_monthly___min_partition_by_region\n  ON _base.`monthly.region` <=> _cm_monthly___min_partition_by_region.`monthly.region`",
  "dual_phase/measure::postgres": "WITH _cm_amount_sum_partition_by_ordered_at_region AS (\n  SELECT\n    _stage_inner.\"monthly.region\" AS \"region\",\n    _stage_inner.\"monthly.ordered_at_month\" AS \"ordered_at_month\",\n    _stage_inner.\"monthly.amount_sum_partition_by_ordered_at_region\" AS \"amount_sum_partition_by_ordered_at_region\"\n  FROM (\n    SELECT\n      monthly.region AS \"monthly.region\",\n      DATE_TRUNC('MONTH', monthly.ordered_at) AS \"monthly.ordered_at_month\",\n      CAST(SUM(monthly.amount) AS DOUBLE PRECISION) AS \"monthly.amount_sum_partition_by_ordered_at_region\"\n    FROM monthly AS monthly\n    GROUP BY\n      monthly.region,\n      DATE_TRUNC('MONTH', monthly.ordered_at)\n  ) AS _stage_inner\n), _cm_monthly___min_partition_by_region AS (\n  SELECT\n    _base.\"monthly.region\" AS \"monthly.region\",\n    MIN(_base._v) AS \"monthly.a\"\n  FROM (\n    SELECT\n      monthly.region AS \"monthly.region\",\n      monthly.region AS _ek0,\n      DATE_TRUNC('MONTH', monthly.ordered_at) AS _ek1,\n      MAX(\n        _cm_amount_sum_partition_by_ordered_at_region.\"amount_sum_partition_by_ordered_at_region\"\n      ) AS _v\n    FROM monthly AS monthly\n    LEFT JOIN _cm_amount_sum_partition_by_ordered_at_region\n      ON monthly.region IS NOT DISTINCT FROM _cm_amount_sum_partition_by_ordered_at_region.\"region\"\n      AND DATE_TRUNC('MONTH', monthly.ordered_at) IS NOT DISTINCT FROM _cm_amount_sum_partition_by_ordered_at_region.\"ordered_at_month\"\n    GROUP BY\n      monthly.region,\n      monthly.region,\n      DATE_TRUNC('MONTH', monthly.ordered_at)\n  ) AS _base\n  GROUP BY\n    _base.\"monthly.region\"\n), _base AS (\n  SELECT\n    monthly.region AS \"monthly.region\",\n    DATE_TRUNC('MONTH', monthly.ordered_at) AS \"monthly.ordered_at\",\n    SUM(monthly.amount * _cm_monthly___min_partition_by_region.\"monthly.a\") AS \"monthly.b\"\n  FROM monthly AS monthly\n  LEFT JOIN _cm_monthly___min_partition_by_region\n    ON monthly.region IS NOT DISTINCT FROM _cm_monthly___min_partition_by_region.\"monthly.region\"\n  GROUP BY\n    monthly.region,\n    DATE_TRUNC('MONTH', monthly.ordered_at)\n)\nSELECT\n  _base.\"monthly.region\",\n  _base.\"monthly.ordered_at\",\n  _cm_monthly___min_partition_by_region.\"monthly.a\",\n  _base.\"monthly.b\"\nFROM _base\nLEFT JOIN _cm_monthly___min_partition_by_region\n  ON _base.\"monthly.region\" IS NOT DISTINCT FROM _cm_monthly___min_partition_by_region.\"monthly.region\"",
  "dual_phase/measure::snowflake": "WITH _cm_amount_sum_partition_by_ordered_at_region AS (\n  SELECT\n    _stage_inner.\"monthly.region\" AS \"region\",\n    _stage_inner.\"monthly.ordered_at_month\" AS \"ordered_at_month\",\n    _stage_inner.\"monthly.amount_sum_partition_by_ordered_at_region\" AS \"amount_sum_partition_by_ordered_at_region\"\n  FROM (\n    SELECT\n      monthly.region AS \"monthly.region\",\n      DATE_TRUNC('MONTH', monthly.ordered_at) AS \"monthly.ordered_at_month\",\n      CAST(SUM(monthly.amount) AS DOUBLE) AS \"monthly.amount_sum_partition_by_ordered_at_region\"\n    FROM monthly AS monthly\n    GROUP BY\n      monthly.region,\n      DATE_TRUNC('MONTH', monthly.ordered_at)\n  ) AS _stage_inner\n), _cm_monthly___min_partition_by_region AS (\n  SELECT\n    _base.\"monthly.region\" AS \"monthly.region\",\n    MIN(_base._v) AS \"monthly.a\"\n  FROM (\n    SELECT\n      monthly.region AS \"monthly.region\",\n      monthly.region AS _ek0,\n      DATE_TRUNC('MONTH', monthly.ordered_at) AS _ek1,\n      MAX(\n        _cm_amount_sum_partition_by_ordered_at_region.\"amount_sum_partition_by_ordered_at_region\"\n      ) AS _v\n    FROM monthly AS monthly\n    LEFT JOIN _cm_amount_sum_partition_by_ordered_at_region\n      ON monthly.region IS NOT DISTINCT FROM _cm_amount_sum_partition_by_ordered_at_region.\"region\"\n      AND DATE_TRUNC('MONTH', monthly.ordered_at) IS NOT DISTINCT FROM _cm_amount_sum_partition_by_ordered_at_region.\"ordered_at_month\"\n    GROUP BY\n      monthly.region,\n      monthly.region,\n      DATE_TRUNC('MONTH', monthly.ordered_at)\n  ) AS _base\n  GROUP BY\n    _base.\"monthly.region\"\n), _base AS (\n  SELECT\n    monthly.region AS \"monthly.region\",\n    DATE_TRUNC('MONTH', monthly.ordered_at) AS \"monthly.ordered_at\",\n    SUM(monthly.amount * _cm_monthly___min_partition_by_region.\"monthly.a\") AS \"monthly.b\"\n  FROM monthly AS monthly\n  LEFT JOIN _cm_monthly___min_partition_by_region\n    ON monthly.region IS NOT DISTINCT FROM _cm_monthly___min_partition_by_region.\"monthly.region\"\n  GROUP BY\n    monthly.region,\n    DATE_TRUNC('MONTH', monthly.ordered_at)\n)\nSELECT\n  _base.\"monthly.region\",\n  _base.\"monthly.ordered_at\",\n  _cm_monthly___min_partition_by_region.\"monthly.a\",\n  _base.\"monthly.b\"\nFROM _base\nLEFT JOIN _cm_monthly___min_partition_by_region\n  ON _base.\"monthly.region\" IS NOT DISTINCT FROM _cm_monthly___min_partition_by_region.\"monthly.region\"",
  "dual_phase/measure::sqlite": "WITH _cm_amount_sum_partition_by_ordered_at_region AS (\n  SELECT\n    _stage_inner.\"monthly.region\" AS \"region\",\n    _stage_inner.\"monthly.ordered_at_month\" AS \"ordered_at_month\",\n    _stage_inner.\"monthly.amount_sum_partition_by_ordered_at_region\" AS \"amount_sum_partition_by_ordered_at_region\"\n  FROM (\n    SELECT\n      monthly.region AS \"monthly.region\",\n      STRFTIME('%Y-%m-01', monthly.ordered_at) AS \"monthly.ordered_at_month\",\n      CAST(SUM(monthly.amount) AS REAL) AS \"monthly.amount_sum_partition_by_ordered_at_region\"\n    FROM monthly AS monthly\n    GROUP BY\n      monthly.region,\n      STRFTIME('%Y-%m-01', monthly.ordered_at)\n  ) AS _stage_inner\n), _cm_monthly___min_partition_by_region AS (\n  SELECT\n    _base.\"monthly.region\" AS \"monthly.region\",\n    MIN(_base._v) AS \"monthly.a\"\n  FROM (\n    SELECT\n      monthly.region AS \"monthly.region\",\n      monthly.region AS _ek0,\n      STRFTIME('%Y-%m-01', monthly.ordered_at) AS _ek1,\n      MAX(\n        _cm_amount_sum_partition_by_ordered_at_region.\"amount_sum_partition_by_ordered_at_region\"\n      ) AS _v\n    FROM monthly AS monthly\n    LEFT JOIN _cm_amount_sum_partition_by_ordered_at_region\n      ON monthly.region IS _cm_amount_sum_partition_by_ordered_at_region.\"region\"\n      AND STRFTIME('%Y-%m-01', monthly.ordered_at) IS _cm_amount_sum_partition_by_ordered_at_region.\"ordered_at_month\"\n    GROUP BY\n      monthly.region,\n      monthly.region,\n      STRFTIME('%Y-%m-01', monthly.ordered_at)\n  ) AS _base\n  GROUP BY\n    _base.\"monthly.region\"\n), _base AS (\n  SELECT\n    monthly.region AS \"monthly.region\",\n    STRFTIME('%Y-%m-01', monthly.ordered_at) AS \"monthly.ordered_at\",\n    SUM(monthly.amount * _cm_monthly___min_partition_by_region.\"monthly.a\") AS \"monthly.b\"\n  FROM monthly AS monthly\n  LEFT JOIN _cm_monthly___min_partition_by_region\n    ON monthly.region IS _cm_monthly___min_partition_by_region.\"monthly.region\"\n  GROUP BY\n    monthly.region,\n    STRFTIME('%Y-%m-01', monthly.ordered_at)\n)\nSELECT\n  _base.\"monthly.region\",\n  _base.\"monthly.ordered_at\",\n  _cm_monthly___min_partition_by_region.\"monthly.a\",\n  _base.\"monthly.b\"\nFROM _base\nLEFT JOIN _cm_monthly___min_partition_by_region\n  ON _base.\"monthly.region\" IS _cm_monthly___min_partition_by_region.\"monthly.region\"",
  "dual_phase/measure::tsql": "WITH _cm_amount_sum_partition_by_ordered_at_region AS (\n  SELECT\n    _stage_inner.[monthly___region] AS [region],\n    _stage_inner.[monthly___ordered_at_month] AS [ordered_at_month],\n    _stage_inner.[monthly___amount_sum_partition_by_ordered_at_region] AS [amount_sum_partition_by_ordered_at_region]\n  FROM (\n    SELECT\n      monthly.region AS [monthly___region],\n      DATETRUNC(MONTH, monthly.ordered_at) AS [monthly___ordered_at_month],\n      CAST(SUM(monthly.amount) AS FLOAT) AS [monthly___amount_sum_partition_by_ordered_at_region]\n    FROM monthly AS monthly\n    GROUP BY\n      monthly.region,\n      DATETRUNC(MONTH, monthly.ordered_at)\n  ) AS _stage_inner\n), _cm_monthly___min_partition_by_region AS (\n  SELECT\n    _base.[monthly___region] AS [monthly___region],\n    MIN(_base._v) AS [monthly___a]\n  FROM (\n    SELECT\n      monthly.region AS [monthly___region],\n      monthly.region AS _ek0,\n      DATETRUNC(MONTH, monthly.ordered_at) AS _ek1,\n      MAX(\n        _cm_amount_sum_partition_by_ordered_at_region.[amount_sum_partition_by_ordered_at_region]\n      ) AS _v\n    FROM monthly AS monthly\n    LEFT JOIN _cm_amount_sum_partition_by_ordered_at_region\n      ON (\n        monthly.region = _cm_amount_sum_partition_by_ordered_at_region.[region]\n        OR (\n          monthly.region IS NULL\n          AND _cm_amount_sum_partition_by_ordered_at_region.[region] IS NULL\n        )\n      )\n      AND (\n        DATETRUNC(MONTH, monthly.ordered_at) = _cm_amount_sum_partition_by_ordered_at_region.[ordered_at_month]\n        OR (\n          DATETRUNC(MONTH, monthly.ordered_at) IS NULL\n          AND _cm_amount_sum_partition_by_ordered_at_region.[ordered_at_month] IS NULL\n        )\n      )\n    GROUP BY\n      monthly.region,\n      monthly.region,\n      DATETRUNC(MONTH, monthly.ordered_at)\n  ) AS _base\n  GROUP BY\n    _base.[monthly___region]\n), _base AS (\n  SELECT\n    monthly.region AS [monthly___region],\n    DATETRUNC(month, monthly.ordered_at) AS [monthly___ordered_at],\n    SUM(monthly.amount * _cm_monthly___min_partition_by_region.[monthly___a]) AS [monthly___b]\n  FROM monthly AS monthly\n  LEFT JOIN _cm_monthly___min_partition_by_region\n    ON (\n      monthly.region = _cm_monthly___min_partition_by_region.[monthly___region]\n      OR (\n        monthly.region IS NULL\n        AND _cm_monthly___min_partition_by_region.[monthly___region] IS NULL\n      )\n    )\n  GROUP BY\n    monthly.region,\n    DATETRUNC(month, monthly.ordered_at)\n)\nSELECT\n  _base.[monthly___region],\n  _base.[monthly___ordered_at],\n  _cm_monthly___min_partition_by_region.[monthly___a],\n  _base.[monthly___b]\nFROM _base\nLEFT JOIN _cm_monthly___min_partition_by_region\n  ON (\n    _base.[monthly___region] = _cm_monthly___min_partition_by_region.[monthly___region]\n    OR (\n      _base.[monthly___region] IS NULL\n      AND _cm_monthly___min_partition_by_region.[monthly___region] IS NULL\n    )\n  )",
  "dual_phase/order_position::bigquery": "WITH _cm_amount_sum_partition_by_ordered_at_region AS (\n  SELECT\n    `_stage_inner`.`monthly___region` AS `region`,\n    `_stage_inner`.`monthly___ordered_at_month` AS `ordered_at_month`,\n    `_stage_inner`.`monthly___amount_sum_partition_by_ordered_at_region` AS `amount_sum_partition_by_ordered_at_region`\n  FROM (\n    SELECT\n      monthly.region AS `monthly___region`,\n      DATE_TRUNC(monthly.ordered_at, MONTH) AS `monthly___ordered_at_month`,\n      CAST(SUM(monthly.amount) AS FLOAT64) AS `monthly___amount_sum_partition_by_ordered_at_region`\n    FROM monthly AS monthly\n    GROUP BY\n      monthly.region,\n      DATE_TRUNC(monthly.ordered_at, MONTH)\n  ) AS _stage_inner\n), _cm_monthly___min_partition_by_region AS (\n  SELECT\n    `_base`.`monthly___region` AS `monthly___region`,\n    MIN(_base._v) AS `monthly___a`\n  FROM (\n    SELECT\n      monthly.region AS `monthly___region`,\n      monthly.region AS _ek0,\n      DATE_TRUNC(monthly.ordered_at, MONTH) AS _ek1,\n      MAX(\n        _cm_amount_sum_partition_by_ordered_at_region.`amount_sum_partition_by_ordered_at_region`\n      ) AS _v\n    FROM monthly AS monthly\n    LEFT JOIN _cm_amount_sum_partition_by_ordered_at_region\n      ON monthly.region IS NOT DISTINCT FROM _cm_amount_sum_partition_by_ordered_at_region.`region`\n      AND DATE_TRUNC(monthly.ordered_at, MONTH) IS NOT DISTINCT FROM _cm_amount_sum_partition_by_ordered_at_region.`ordered_at_month`\n    GROUP BY\n      monthly.region,\n      monthly.region,\n      DATE_TRUNC(monthly.ordered_at, MONTH)\n  ) AS _base\n  GROUP BY\n    `_base`.`monthly___region`\n), _base AS (\n  SELECT\n    monthly.region AS `monthly___region`,\n    DATE_TRUNC(monthly.ordered_at, MONTH) AS `monthly___ordered_at`,\n    SUM(monthly.amount * `_cm_monthly___min_partition_by_region`.`monthly___a`) AS `monthly___b`\n  FROM monthly AS monthly\n  LEFT JOIN _cm_monthly___min_partition_by_region\n    ON monthly.region IS NOT DISTINCT FROM _cm_monthly___min_partition_by_region.`monthly___region`\n  GROUP BY\n    monthly.region,\n    DATE_TRUNC(monthly.ordered_at, MONTH)\n)\nSELECT\n  _base.`monthly___region`,\n  _base.`monthly___ordered_at`,\n  _cm_monthly___min_partition_by_region.`monthly___a`,\n  _base.`monthly___b`\nFROM _base\nLEFT JOIN _cm_monthly___min_partition_by_region\n  ON _base.`monthly___region` IS NOT DISTINCT FROM _cm_monthly___min_partition_by_region.`monthly___region`\nORDER BY\n  _cm_monthly___min_partition_by_region.`monthly___a` DESC",
  "dual_phase/order_position::duckdb": "WITH _cm_amount_sum_partition_by_ordered_at_region AS (\n  SELECT\n    _stage_inner.\"monthly.region\" AS \"region\",\n    _stage_inner.\"monthly.ordered_at_month\" AS \"ordered_at_month\",\n    _stage_inner.\"monthly.amount_sum_partition_by_ordered_at_region\" AS \"amount_sum_partition_by_ordered_at_region\"\n  FROM (\n    SELECT\n      monthly.region AS \"monthly.region\",\n      DATE_TRUNC('MONTH', monthly.ordered_at) AS \"monthly.ordered_at_month\",\n      CAST(SUM(monthly.amount) AS DOUBLE) AS \"monthly.amount_sum_partition_by_ordered_at_region\"\n    FROM monthly AS monthly\n    GROUP BY\n      monthly.region,\n      DATE_TRUNC('MONTH', monthly.ordered_at)\n  ) AS _stage_inner\n), _cm_monthly___min_partition_by_region AS (\n  SELECT\n    _base.\"monthly.region\" AS \"monthly.region\",\n    MIN(_base._v) AS \"monthly.a\"\n  FROM (\n    SELECT\n      monthly.region AS \"monthly.region\",\n      monthly.region AS _ek0,\n      DATE_TRUNC('MONTH', monthly.ordered_at) AS _ek1,\n      MAX(\n        _cm_amount_sum_partition_by_ordered_at_region.\"amount_sum_partition_by_ordered_at_region\"\n      ) AS _v\n    FROM monthly AS monthly\n    LEFT JOIN _cm_amount_sum_partition_by_ordered_at_region\n      ON monthly.region IS NOT DISTINCT FROM _cm_amount_sum_partition_by_ordered_at_region.\"region\"\n      AND DATE_TRUNC('MONTH', monthly.ordered_at) IS NOT DISTINCT FROM _cm_amount_sum_partition_by_ordered_at_region.\"ordered_at_month\"\n    GROUP BY\n      monthly.region,\n      monthly.region,\n      DATE_TRUNC('MONTH', monthly.ordered_at)\n  ) AS _base\n  GROUP BY\n    _base.\"monthly.region\"\n), _base AS (\n  SELECT\n    monthly.region AS \"monthly.region\",\n    DATE_TRUNC('MONTH', monthly.ordered_at) AS \"monthly.ordered_at\",\n    SUM(monthly.amount * _cm_monthly___min_partition_by_region.\"monthly.a\") AS \"monthly.b\"\n  FROM monthly AS monthly\n  LEFT JOIN _cm_monthly___min_partition_by_region\n    ON monthly.region IS NOT DISTINCT FROM _cm_monthly___min_partition_by_region.\"monthly.region\"\n  GROUP BY\n    monthly.region,\n    DATE_TRUNC('MONTH', monthly.ordered_at)\n)\nSELECT\n  _base.\"monthly.region\",\n  _base.\"monthly.ordered_at\",\n  _cm_monthly___min_partition_by_region.\"monthly.a\",\n  _base.\"monthly.b\"\nFROM _base\nLEFT JOIN _cm_monthly___min_partition_by_region\n  ON _base.\"monthly.region\" IS NOT DISTINCT FROM _cm_monthly___min_partition_by_region.\"monthly.region\"\nORDER BY\n  _cm_monthly___min_partition_by_region.\"monthly.a\" DESC",
  "dual_phase/order_position::mysql": "WITH _cm_amount_sum_partition_by_ordered_at_region AS (\n  SELECT\n    _stage_inner.`monthly.region` AS `region`,\n    _stage_inner.`monthly.ordered_at_month` AS `ordered_at_month`,\n    _stage_inner.`monthly.amount_sum_partition_by_ordered_at_region` AS `amount_sum_partition_by_ordered_at_region`\n  FROM (\n    SELECT\n      monthly.region AS `monthly.region`,\n      STR_TO_DATE(CONCAT(YEAR(monthly.ordered_at), ' ', MONTH(monthly.ordered_at), ' 1'), '%Y %c %e') AS `monthly.ordered_at_month`,\n      CAST(SUM(monthly.amount) AS DOUBLE) AS `monthly.amount_sum_partition_by_ordered_at_region`\n    FROM monthly AS monthly\n    GROUP BY\n      monthly.region,\n      STR_TO_DATE(CONCAT(YEAR(monthly.ordered_at), ' ', MONTH(monthly.ordered_at), ' 1'), '%Y %c %e')\n  ) AS _stage_inner\n), _cm_monthly___min_partition_by_region AS (\n  SELECT\n    _base.`monthly.region` AS `monthly.region`,\n    MIN(_base._v) AS `monthly.a`\n  FROM (\n    SELECT\n      monthly.region AS `monthly.region`,\n      monthly.region AS _ek0,\n      STR_TO_DATE(CONCAT(YEAR(monthly.ordered_at), ' ', MONTH(monthly.ordered_at), ' 1'), '%Y %c %e') AS _ek1,\n      MAX(\n        _cm_amount_sum_partition_by_ordered_at_region.`amount_sum_partition_by_ordered_at_region`\n      ) AS _v\n    FROM monthly AS monthly\n    LEFT JOIN _cm_amount_sum_partition_by_ordered_at_region\n      ON monthly.region <=> _cm_amount_sum_partition_by_ordered_at_region.`region`\n      AND STR_TO_DATE(CONCAT(YEAR(monthly.ordered_at), ' ', MONTH(monthly.ordered_at), ' 1'), '%Y %c %e') <=> _cm_amount_sum_partition_by_ordered_at_region.`ordered_at_month`\n    GROUP BY\n      monthly.region,\n      monthly.region,\n      STR_TO_DATE(CONCAT(YEAR(monthly.ordered_at), ' ', MONTH(monthly.ordered_at), ' 1'), '%Y %c %e')\n  ) AS _base\n  GROUP BY\n    _base.`monthly.region`\n), _base AS (\n  SELECT\n    monthly.region AS `monthly.region`,\n    STR_TO_DATE(CONCAT(YEAR(monthly.ordered_at), ' ', MONTH(monthly.ordered_at), ' 1'), '%Y %c %e') AS `monthly.ordered_at`,\n    SUM(monthly.amount * _cm_monthly___min_partition_by_region.`monthly.a`) AS `monthly.b`\n  FROM monthly AS monthly\n  LEFT JOIN _cm_monthly___min_partition_by_region\n    ON monthly.region <=> _cm_monthly___min_partition_by_region.`monthly.region`\n  GROUP BY\n    monthly.region,\n    STR_TO_DATE(CONCAT(YEAR(monthly.ordered_at), ' ', MONTH(monthly.ordered_at), ' 1'), '%Y %c %e')\n)\nSELECT\n  _base.`monthly.region`,\n  _base.`monthly.ordered_at`,\n  _cm_monthly___min_partition_by_region.`monthly.a`,\n  _base.`monthly.b`\nFROM _base\nLEFT JOIN _cm_monthly___min_partition_by_region\n  ON _base.`monthly.region` <=> _cm_monthly___min_partition_by_region.`monthly.region`\nORDER BY\n  _cm_monthly___min_partition_by_region.`monthly.a` DESC",
  "dual_phase/order_position::postgres": "WITH _cm_amount_sum_partition_by_ordered_at_region AS (\n  SELECT\n    _stage_inner.\"monthly.region\" AS \"region\",\n    _stage_inner.\"monthly.ordered_at_month\" AS \"ordered_at_month\",\n    _stage_inner.\"monthly.amount_sum_partition_by_ordered_at_region\" AS \"amount_sum_partition_by_ordered_at_region\"\n  FROM (\n    SELECT\n      monthly.region AS \"monthly.region\",\n      DATE_TRUNC('MONTH', monthly.ordered_at) AS \"monthly.ordered_at_month\",\n      CAST(SUM(monthly.amount) AS DOUBLE PRECISION) AS \"monthly.amount_sum_partition_by_ordered_at_region\"\n    FROM monthly AS monthly\n    GROUP BY\n      monthly.region,\n      DATE_TRUNC('MONTH', monthly.ordered_at)\n  ) AS _stage_inner\n), _cm_monthly___min_partition_by_region AS (\n  SELECT\n    _base.\"monthly.region\" AS \"monthly.region\",\n    MIN(_base._v) AS \"monthly.a\"\n  FROM (\n    SELECT\n      monthly.region AS \"monthly.region\",\n      monthly.region AS _ek0,\n      DATE_TRUNC('MONTH', monthly.ordered_at) AS _ek1,\n      MAX(\n        _cm_amount_sum_partition_by_ordered_at_region.\"amount_sum_partition_by_ordered_at_region\"\n      ) AS _v\n    FROM monthly AS monthly\n    LEFT JOIN _cm_amount_sum_partition_by_ordered_at_region\n      ON monthly.region IS NOT DISTINCT FROM _cm_amount_sum_partition_by_ordered_at_region.\"region\"\n      AND DATE_TRUNC('MONTH', monthly.ordered_at) IS NOT DISTINCT FROM _cm_amount_sum_partition_by_ordered_at_region.\"ordered_at_month\"\n    GROUP BY\n      monthly.region,\n      monthly.region,\n      DATE_TRUNC('MONTH', monthly.ordered_at)\n  ) AS _base\n  GROUP BY\n    _base.\"monthly.region\"\n), _base AS (\n  SELECT\n    monthly.region AS \"monthly.region\",\n    DATE_TRUNC('MONTH', monthly.ordered_at) AS \"monthly.ordered_at\",\n    SUM(monthly.amount * _cm_monthly___min_partition_by_region.\"monthly.a\") AS \"monthly.b\"\n  FROM monthly AS monthly\n  LEFT JOIN _cm_monthly___min_partition_by_region\n    ON monthly.region IS NOT DISTINCT FROM _cm_monthly___min_partition_by_region.\"monthly.region\"\n  GROUP BY\n    monthly.region,\n    DATE_TRUNC('MONTH', monthly.ordered_at)\n)\nSELECT\n  _base.\"monthly.region\",\n  _base.\"monthly.ordered_at\",\n  _cm_monthly___min_partition_by_region.\"monthly.a\",\n  _base.\"monthly.b\"\nFROM _base\nLEFT JOIN _cm_monthly___min_partition_by_region\n  ON _base.\"monthly.region\" IS NOT DISTINCT FROM _cm_monthly___min_partition_by_region.\"monthly.region\"\nORDER BY\n  _cm_monthly___min_partition_by_region.\"monthly.a\" DESC NULLS LAST",
  "dual_phase/order_position::snowflake": "WITH _cm_amount_sum_partition_by_ordered_at_region AS (\n  SELECT\n    _stage_inner.\"monthly.region\" AS \"region\",\n    _stage_inner.\"monthly.ordered_at_month\" AS \"ordered_at_month\",\n    _stage_inner.\"monthly.amount_sum_partition_by_ordered_at_region\" AS \"amount_sum_partition_by_ordered_at_region\"\n  FROM (\n    SELECT\n      monthly.region AS \"monthly.region\",\n      DATE_TRUNC('MONTH', monthly.ordered_at) AS \"monthly.ordered_at_month\",\n      CAST(SUM(monthly.amount) AS DOUBLE) AS \"monthly.amount_sum_partition_by_ordered_at_region\"\n    FROM monthly AS monthly\n    GROUP BY\n      monthly.region,\n      DATE_TRUNC('MONTH', monthly.ordered_at)\n  ) AS _stage_inner\n), _cm_monthly___min_partition_by_region AS (\n  SELECT\n    _base.\"monthly.region\" AS \"monthly.region\",\n    MIN(_base._v) AS \"monthly.a\"\n  FROM (\n    SELECT\n      monthly.region AS \"monthly.region\",\n      monthly.region AS _ek0,\n      DATE_TRUNC('MONTH', monthly.ordered_at) AS _ek1,\n      MAX(\n        _cm_amount_sum_partition_by_ordered_at_region.\"amount_sum_partition_by_ordered_at_region\"\n      ) AS _v\n    FROM monthly AS monthly\n    LEFT JOIN _cm_amount_sum_partition_by_ordered_at_region\n      ON monthly.region IS NOT DISTINCT FROM _cm_amount_sum_partition_by_ordered_at_region.\"region\"\n      AND DATE_TRUNC('MONTH', monthly.ordered_at) IS NOT DISTINCT FROM _cm_amount_sum_partition_by_ordered_at_region.\"ordered_at_month\"\n    GROUP BY\n      monthly.region,\n      monthly.region,\n      DATE_TRUNC('MONTH', monthly.ordered_at)\n  ) AS _base\n  GROUP BY\n    _base.\"monthly.region\"\n), _base AS (\n  SELECT\n    monthly.region AS \"monthly.region\",\n    DATE_TRUNC('MONTH', monthly.ordered_at) AS \"monthly.ordered_at\",\n    SUM(monthly.amount * _cm_monthly___min_partition_by_region.\"monthly.a\") AS \"monthly.b\"\n  FROM monthly AS monthly\n  LEFT JOIN _cm_monthly___min_partition_by_region\n    ON monthly.region IS NOT DISTINCT FROM _cm_monthly___min_partition_by_region.\"monthly.region\"\n  GROUP BY\n    monthly.region,\n    DATE_TRUNC('MONTH', monthly.ordered_at)\n)\nSELECT\n  _base.\"monthly.region\",\n  _base.\"monthly.ordered_at\",\n  _cm_monthly___min_partition_by_region.\"monthly.a\",\n  _base.\"monthly.b\"\nFROM _base\nLEFT JOIN _cm_monthly___min_partition_by_region\n  ON _base.\"monthly.region\" IS NOT DISTINCT FROM _cm_monthly___min_partition_by_region.\"monthly.region\"\nORDER BY\n  _cm_monthly___min_partition_by_region.\"monthly.a\" DESC NULLS LAST",
  "dual_phase/order_position::sqlite": "WITH _cm_amount_sum_partition_by_ordered_at_region AS (\n  SELECT\n    _stage_inner.\"monthly.region\" AS \"region\",\n    _stage_inner.\"monthly.ordered_at_month\" AS \"ordered_at_month\",\n    _stage_inner.\"monthly.amount_sum_partition_by_ordered_at_region\" AS \"amount_sum_partition_by_ordered_at_region\"\n  FROM (\n    SELECT\n      monthly.region AS \"monthly.region\",\n      STRFTIME('%Y-%m-01', monthly.ordered_at) AS \"monthly.ordered_at_month\",\n      CAST(SUM(monthly.amount) AS REAL) AS \"monthly.amount_sum_partition_by_ordered_at_region\"\n    FROM monthly AS monthly\n    GROUP BY\n      monthly.region,\n      STRFTIME('%Y-%m-01', monthly.ordered_at)\n  ) AS _stage_inner\n), _cm_monthly___min_partition_by_region AS (\n  SELECT\n    _base.\"monthly.region\" AS \"monthly.region\",\n    MIN(_base._v) AS \"monthly.a\"\n  FROM (\n    SELECT\n      monthly.region AS \"monthly.region\",\n      monthly.region AS _ek0,\n      STRFTIME('%Y-%m-01', monthly.ordered_at) AS _ek1,\n      MAX(\n        _cm_amount_sum_partition_by_ordered_at_region.\"amount_sum_partition_by_ordered_at_region\"\n      ) AS _v\n    FROM monthly AS monthly\n    LEFT JOIN _cm_amount_sum_partition_by_ordered_at_region\n      ON monthly.region IS _cm_amount_sum_partition_by_ordered_at_region.\"region\"\n      AND STRFTIME('%Y-%m-01', monthly.ordered_at) IS _cm_amount_sum_partition_by_ordered_at_region.\"ordered_at_month\"\n    GROUP BY\n      monthly.region,\n      monthly.region,\n      STRFTIME('%Y-%m-01', monthly.ordered_at)\n  ) AS _base\n  GROUP BY\n    _base.\"monthly.region\"\n), _base AS (\n  SELECT\n    monthly.region AS \"monthly.region\",\n    STRFTIME('%Y-%m-01', monthly.ordered_at) AS \"monthly.ordered_at\",\n    SUM(monthly.amount * _cm_monthly___min_partition_by_region.\"monthly.a\") AS \"monthly.b\"\n  FROM monthly AS monthly\n  LEFT JOIN _cm_monthly___min_partition_by_region\n    ON monthly.region IS _cm_monthly___min_partition_by_region.\"monthly.region\"\n  GROUP BY\n    monthly.region,\n    STRFTIME('%Y-%m-01', monthly.ordered_at)\n)\nSELECT\n  _base.\"monthly.region\",\n  _base.\"monthly.ordered_at\",\n  _cm_monthly___min_partition_by_region.\"monthly.a\",\n  _base.\"monthly.b\"\nFROM _base\nLEFT JOIN _cm_monthly___min_partition_by_region\n  ON _base.\"monthly.region\" IS _cm_monthly___min_partition_by_region.\"monthly.region\"\nORDER BY\n  _cm_monthly___min_partition_by_region.\"monthly.a\" DESC",
  "dual_phase/order_position::tsql": "WITH _cm_amount_sum_partition_by_ordered_at_region AS (\n  SELECT\n    _stage_inner.[monthly___region] AS [region],\n    _stage_inner.[monthly___ordered_at_month] AS [ordered_at_month],\n    _stage_inner.[monthly___amount_sum_partition_by_ordered_at_region] AS [amount_sum_partition_by_ordered_at_region]\n  FROM (\n    SELECT\n      monthly.region AS [monthly___region],\n      DATETRUNC(MONTH, monthly.ordered_at) AS [monthly___ordered_at_month],\n      CAST(SUM(monthly.amount) AS FLOAT) AS [monthly___amount_sum_partition_by_ordered_at_region]\n    FROM monthly AS monthly\n    GROUP BY\n      monthly.region,\n      DATETRUNC(MONTH, monthly.ordered_at)\n  ) AS _stage_inner\n), _cm_monthly___min_partition_by_region AS (\n  SELECT\n    _base.[monthly___region] AS [monthly___region],\n    MIN(_base._v) AS [monthly___a]\n  FROM (\n    SELECT\n      monthly.region AS [monthly___region],\n      monthly.region AS _ek0,\n      DATETRUNC(MONTH, monthly.ordered_at) AS _ek1,\n      MAX(\n        _cm_amount_sum_partition_by_ordered_at_region.[amount_sum_partition_by_ordered_at_region]\n      ) AS _v\n    FROM monthly AS monthly\n    LEFT JOIN _cm_amount_sum_partition_by_ordered_at_region\n      ON (\n        monthly.region = _cm_amount_sum_partition_by_ordered_at_region.[region]\n        OR (\n          monthly.region IS NULL\n          AND _cm_amount_sum_partition_by_ordered_at_region.[region] IS NULL\n        )\n      )\n      AND (\n        DATETRUNC(MONTH, monthly.ordered_at) = _cm_amount_sum_partition_by_ordered_at_region.[ordered_at_month]\n        OR (\n          DATETRUNC(MONTH, monthly.ordered_at) IS NULL\n          AND _cm_amount_sum_partition_by_ordered_at_region.[ordered_at_month] IS NULL\n        )\n      )\n    GROUP BY\n      monthly.region,\n      monthly.region,\n      DATETRUNC(MONTH, monthly.ordered_at)\n  ) AS _base\n  GROUP BY\n    _base.[monthly___region]\n), _base AS (\n  SELECT\n    monthly.region AS [monthly___region],\n    DATETRUNC(month, monthly.ordered_at) AS [monthly___ordered_at],\n    SUM(monthly.amount * _cm_monthly___min_partition_by_region.[monthly___a]) AS [monthly___b]\n  FROM monthly AS monthly\n  LEFT JOIN _cm_monthly___min_partition_by_region\n    ON (\n      monthly.region = _cm_monthly___min_partition_by_region.[monthly___region]\n      OR (\n        monthly.region IS NULL\n        AND _cm_monthly___min_partition_by_region.[monthly___region] IS NULL\n      )\n    )\n  GROUP BY\n    monthly.region,\n    DATETRUNC(month, monthly.ordered_at)\n)\nSELECT\n  _base.[monthly___region],\n  _base.[monthly___ordered_at],\n  _cm_monthly___min_partition_by_region.[monthly___a],\n  _base.[monthly___b]\nFROM _base\nLEFT JOIN _cm_monthly___min_partition_by_region\n  ON (\n    _base.[monthly___region] = _cm_monthly___min_partition_by_region.[monthly___region]\n    OR (\n      _base.[monthly___region] IS NULL\n      AND _cm_monthly___min_partition_by_region.[monthly___region] IS NULL\n    )\n  )\nORDER BY\n  _cm_monthly___min_partition_by_region.[monthly___a] DESC",
  "dual_phase/with_transform::bigquery": "SELECT\n    `monthly___region`,\n    `monthly___ordered_at`,\n    `monthly___a`,\n    `monthly___b`,\n    `monthly___c`\nFROM (\nWITH _cm_amount_sum_partition_by_ordered_at_region AS (\n  SELECT\n    `_stage_inner`.`monthly___region` AS `region`,\n    `_stage_inner`.`monthly___ordered_at_month` AS `ordered_at_month`,\n    `_stage_inner`.`monthly___amount_sum_partition_by_ordered_at_region` AS `amount_sum_partition_by_ordered_at_region`\n  FROM (\n    SELECT\n      monthly.region AS `monthly___region`,\n      DATE_TRUNC(monthly.ordered_at, MONTH) AS `monthly___ordered_at_month`,\n      CAST(SUM(monthly.amount) AS FLOAT64) AS `monthly___amount_sum_partition_by_ordered_at_region`\n    FROM monthly AS monthly\n    GROUP BY\n      monthly.region,\n      DATE_TRUNC(monthly.ordered_at, MONTH)\n  ) AS _stage_inner\n), _cm_monthly___min_partition_by_region AS (\n  SELECT\n    `_base`.`monthly___region` AS `monthly___region`,\n    MIN(_base._v) AS `monthly___a`\n  FROM (\n    SELECT\n      monthly.region AS `monthly___region`,\n      monthly.region AS _ek0,\n      DATE_TRUNC(monthly.ordered_at, MONTH) AS _ek1,\n      MAX(\n        _cm_amount_sum_partition_by_ordered_at_region.`amount_sum_partition_by_ordered_at_region`\n      ) AS _v\n    FROM monthly AS monthly\n    LEFT JOIN _cm_amount_sum_partition_by_ordered_at_region\n      ON monthly.region IS NOT DISTINCT FROM _cm_amount_sum_partition_by_ordered_at_region.`region`\n      AND DATE_TRUNC(monthly.ordered_at, MONTH) IS NOT DISTINCT FROM _cm_amount_sum_partition_by_ordered_at_region.`ordered_at_month`\n    GROUP BY\n      monthly.region,\n      monthly.region,\n      DATE_TRUNC(monthly.ordered_at, MONTH)\n  ) AS _base\n  GROUP BY\n    `_base`.`monthly___region`\n), _base AS (\n  SELECT\n    monthly.region AS `monthly___region`,\n    DATE_TRUNC(monthly.ordered_at, MONTH) AS `monthly___ordered_at`,\n    SUM(monthly.amount * `_cm_monthly___min_partition_by_region`.`monthly___a`) AS `monthly___b`,\n    SUM(monthly.amount) AS `monthly___amount_sum`\n  FROM monthly AS monthly\n  LEFT JOIN _cm_monthly___min_partition_by_region\n    ON monthly.region IS NOT DISTINCT FROM _cm_monthly___min_partition_by_region.`monthly___region`\n  GROUP BY\n    monthly.region,\n    DATE_TRUNC(monthly.ordered_at, MONTH)\n), base AS (\n  SELECT\n    _base.`monthly___region`,\n    _base.`monthly___ordered_at`,\n    _cm_monthly___min_partition_by_region.`monthly___a`,\n    _base.`monthly___b`,\n    _base.`monthly___amount_sum`\n  FROM _base\n  LEFT JOIN _cm_monthly___min_partition_by_region\n    ON _base.`monthly___region` IS NOT DISTINCT FROM _cm_monthly___min_partition_by_region.`monthly___region`\n), step1 AS (\n  SELECT\n    `monthly___region`,\n    `monthly___ordered_at`,\n    `monthly___b`,\n    `monthly___amount_sum`,\n    `monthly___a`,\n    SUM(`monthly___amount_sum`) OVER (PARTITION BY `monthly___region` ORDER BY `monthly___ordered_at`) AS `monthly___c`\n  FROM base\n)\nSELECT\n  `monthly___region`,\n  `monthly___ordered_at`,\n  `monthly___b`,\n  `monthly___amount_sum`,\n  `monthly___a`,\n  `monthly___c`\nFROM step1\n) AS _outer",
  "dual_phase/with_transform::duckdb": "SELECT\n    \"monthly.region\",\n    \"monthly.ordered_at\",\n    \"monthly.a\",\n    \"monthly.b\",\n    \"monthly.c\"\nFROM (\nWITH _cm_amount_sum_partition_by_ordered_at_region AS (\n  SELECT\n    _stage_inner.\"monthly.region\" AS \"region\",\n    _stage_inner.\"monthly.ordered_at_month\" AS \"ordered_at_month\",\n    _stage_inner.\"monthly.amount_sum_partition_by_ordered_at_region\" AS \"amount_sum_partition_by_ordered_at_region\"\n  FROM (\n    SELECT\n      monthly.region AS \"monthly.region\",\n      DATE_TRUNC('MONTH', monthly.ordered_at) AS \"monthly.ordered_at_month\",\n      CAST(SUM(monthly.amount) AS DOUBLE) AS \"monthly.amount_sum_partition_by_ordered_at_region\"\n    FROM monthly AS monthly\n    GROUP BY\n      monthly.region,\n      DATE_TRUNC('MONTH', monthly.ordered_at)\n  ) AS _stage_inner\n), _cm_monthly___min_partition_by_region AS (\n  SELECT\n    _base.\"monthly.region\" AS \"monthly.region\",\n    MIN(_base._v) AS \"monthly.a\"\n  FROM (\n    SELECT\n      monthly.region AS \"monthly.region\",\n      monthly.region AS _ek0,\n      DATE_TRUNC('MONTH', monthly.ordered_at) AS _ek1,\n      MAX(\n        _cm_amount_sum_partition_by_ordered_at_region.\"amount_sum_partition_by_ordered_at_region\"\n      ) AS _v\n    FROM monthly AS monthly\n    LEFT JOIN _cm_amount_sum_partition_by_ordered_at_region\n      ON monthly.region IS NOT DISTINCT FROM _cm_amount_sum_partition_by_ordered_at_region.\"region\"\n      AND DATE_TRUNC('MONTH', monthly.ordered_at) IS NOT DISTINCT FROM _cm_amount_sum_partition_by_ordered_at_region.\"ordered_at_month\"\n    GROUP BY\n      monthly.region,\n      monthly.region,\n      DATE_TRUNC('MONTH', monthly.ordered_at)\n  ) AS _base\n  GROUP BY\n    _base.\"monthly.region\"\n), _base AS (\n  SELECT\n    monthly.region AS \"monthly.region\",\n    DATE_TRUNC('MONTH', monthly.ordered_at) AS \"monthly.ordered_at\",\n    SUM(monthly.amount * _cm_monthly___min_partition_by_region.\"monthly.a\") AS \"monthly.b\",\n    SUM(monthly.amount) AS \"monthly.amount_sum\"\n  FROM monthly AS monthly\n  LEFT JOIN _cm_monthly___min_partition_by_region\n    ON monthly.region IS NOT DISTINCT FROM _cm_monthly___min_partition_by_region.\"monthly.region\"\n  GROUP BY\n    monthly.region,\n    DATE_TRUNC('MONTH', monthly.ordered_at)\n), base AS (\n  SELECT\n    _base.\"monthly.region\",\n    _base.\"monthly.ordered_at\",\n    _cm_monthly___min_partition_by_region.\"monthly.a\",\n    _base.\"monthly.b\",\n    _base.\"monthly.amount_sum\"\n  FROM _base\n  LEFT JOIN _cm_monthly___min_partition_by_region\n    ON _base.\"monthly.region\" IS NOT DISTINCT FROM _cm_monthly___min_partition_by_region.\"monthly.region\"\n), step1 AS (\n  SELECT\n    \"monthly.region\",\n    \"monthly.ordered_at\",\n    \"monthly.b\",\n    \"monthly.amount_sum\",\n    \"monthly.a\",\n    SUM(\"monthly.amount_sum\") OVER (PARTITION BY \"monthly.region\" ORDER BY \"monthly.ordered_at\") AS \"monthly.c\"\n  FROM base\n)\nSELECT\n  \"monthly.region\",\n  \"monthly.ordered_at\",\n  \"monthly.b\",\n  \"monthly.amount_sum\",\n  \"monthly.a\",\n  \"monthly.c\"\nFROM step1\n) AS _outer",
  "dual_phase/with_transform::mysql": "SELECT\n    `monthly.region`,\n    `monthly.ordered_at`,\n    `monthly.a`,\n    `monthly.b`,\n    `monthly.c`\nFROM (\nWITH _cm_amount_sum_partition_by_ordered_at_region AS (\n  SELECT\n    _stage_inner.`monthly.region` AS `region`,\n    _stage_inner.`monthly.ordered_at_month` AS `ordered_at_month`,\n    _stage_inner.`monthly.amount_sum_partition_by_ordered_at_region` AS `amount_sum_partition_by_ordered_at_region`\n  FROM (\n    SELECT\n      monthly.region AS `monthly.region`,\n      STR_TO_DATE(CONCAT(YEAR(monthly.ordered_at), ' ', MONTH(monthly.ordered_at), ' 1'), '%Y %c %e') AS `monthly.ordered_at_month`,\n      CAST(SUM(monthly.amount) AS DOUBLE) AS `monthly.amount_sum_partition_by_ordered_at_region`\n    FROM monthly AS monthly\n    GROUP BY\n      monthly.region,\n      STR_TO_DATE(CONCAT(YEAR(monthly.ordered_at), ' ', MONTH(monthly.ordered_at), ' 1'), '%Y %c %e')\n  ) AS _stage_inner\n), _cm_monthly___min_partition_by_region AS (\n  SELECT\n    _base.`monthly.region` AS `monthly.region`,\n    MIN(_base._v) AS `monthly.a`\n  FROM (\n    SELECT\n      monthly.region AS `monthly.region`,\n      monthly.region AS _ek0,\n      STR_TO_DATE(CONCAT(YEAR(monthly.ordered_at), ' ', MONTH(monthly.ordered_at), ' 1'), '%Y %c %e') AS _ek1,\n      MAX(\n        _cm_amount_sum_partition_by_ordered_at_region.`amount_sum_partition_by_ordered_at_region`\n      ) AS _v\n    FROM monthly AS monthly\n    LEFT JOIN _cm_amount_sum_partition_by_ordered_at_region\n      ON monthly.region <=> _cm_amount_sum_partition_by_ordered_at_region.`region`\n      AND STR_TO_DATE(CONCAT(YEAR(monthly.ordered_at), ' ', MONTH(monthly.ordered_at), ' 1'), '%Y %c %e') <=> _cm_amount_sum_partition_by_ordered_at_region.`ordered_at_month`\n    GROUP BY\n      monthly.region,\n      monthly.region,\n      STR_TO_DATE(CONCAT(YEAR(monthly.ordered_at), ' ', MONTH(monthly.ordered_at), ' 1'), '%Y %c %e')\n  ) AS _base\n  GROUP BY\n    _base.`monthly.region`\n), _base AS (\n  SELECT\n    monthly.region AS `monthly.region`,\n    STR_TO_DATE(CONCAT(YEAR(monthly.ordered_at), ' ', MONTH(monthly.ordered_at), ' 1'), '%Y %c %e') AS `monthly.ordered_at`,\n    SUM(monthly.amount * _cm_monthly___min_partition_by_region.`monthly.a`) AS `monthly.b`,\n    SUM(monthly.amount) AS `monthly.amount_sum`\n  FROM monthly AS monthly\n  LEFT JOIN _cm_monthly___min_partition_by_region\n    ON monthly.region <=> _cm_monthly___min_partition_by_region.`monthly.region`\n  GROUP BY\n    monthly.region,\n    STR_TO_DATE(CONCAT(YEAR(monthly.ordered_at), ' ', MONTH(monthly.ordered_at), ' 1'), '%Y %c %e')\n), base AS (\n  SELECT\n    _base.`monthly.region`,\n    _base.`monthly.ordered_at`,\n    _cm_monthly___min_partition_by_region.`monthly.a`,\n    _base.`monthly.b`,\n    _base.`monthly.amount_sum`\n  FROM _base\n  LEFT JOIN _cm_monthly___min_partition_by_region\n    ON _base.`monthly.region` <=> _cm_monthly___min_partition_by_region.`monthly.region`\n), step1 AS (\n  SELECT\n    `monthly.region`,\n    `monthly.ordered_at`,\n    `monthly.b`,\n    `monthly.amount_sum`,\n    `monthly.a`,\n    SUM(`monthly.amount_sum`) OVER (PARTITION BY `monthly.region` ORDER BY `monthly.ordered_at`) AS `monthly.c`\n  FROM base\n)\nSELECT\n  `monthly.region`,\n  `monthly.ordered_at`,\n  `monthly.b`,\n  `monthly.amount_sum`,\n  `monthly.a`,\n  `monthly.c`\nFROM step1\n) AS _outer",
  "dual_phase/with_transform::postgres": "SELECT\n    \"monthly.region\",\n    \"monthly.ordered_at\",\n    \"monthly.a\",\n    \"monthly.b\",\n    \"monthly.c\"\nFROM (\nWITH _cm_amount_sum_partition_by_ordered_at_region AS (\n  SELECT\n    _stage_inner.\"monthly.region\" AS \"region\",\n    _stage_inner.\"monthly.ordered_at_month\" AS \"ordered_at_month\",\n    _stage_inner.\"monthly.amount_sum_partition_by_ordered_at_region\" AS \"amount_sum_partition_by_ordered_at_region\"\n  FROM (\n    SELECT\n      monthly.region AS \"monthly.region\",\n      DATE_TRUNC('MONTH', monthly.ordered_at) AS \"monthly.ordered_at_month\",\n      CAST(SUM(monthly.amount) AS DOUBLE PRECISION) AS \"monthly.amount_sum_partition_by_ordered_at_region\"\n    FROM monthly AS monthly\n    GROUP BY\n      monthly.region,\n      DATE_TRUNC('MONTH', monthly.ordered_at)\n  ) AS _stage_inner\n), _cm_monthly___min_partition_by_region AS (\n  SELECT\n    _base.\"monthly.region\" AS \"monthly.region\",\n    MIN(_base._v) AS \"monthly.a\"\n  FROM (\n    SELECT\n      monthly.region AS \"monthly.region\",\n      monthly.region AS _ek0,\n      DATE_TRUNC('MONTH', monthly.ordered_at) AS _ek1,\n      MAX(\n        _cm_amount_sum_partition_by_ordered_at_region.\"amount_sum_partition_by_ordered_at_region\"\n      ) AS _v\n    FROM monthly AS monthly\n    LEFT JOIN _cm_amount_sum_partition_by_ordered_at_region\n      ON monthly.region IS NOT DISTINCT FROM _cm_amount_sum_partition_by_ordered_at_region.\"region\"\n      AND DATE_TRUNC('MONTH', monthly.ordered_at) IS NOT DISTINCT FROM _cm_amount_sum_partition_by_ordered_at_region.\"ordered_at_month\"\n    GROUP BY\n      monthly.region,\n      monthly.region,\n      DATE_TRUNC('MONTH', monthly.ordered_at)\n  ) AS _base\n  GROUP BY\n    _base.\"monthly.region\"\n), _base AS (\n  SELECT\n    monthly.region AS \"monthly.region\",\n    DATE_TRUNC('MONTH', monthly.ordered_at) AS \"monthly.ordered_at\",\n    SUM(monthly.amount * _cm_monthly___min_partition_by_region.\"monthly.a\") AS \"monthly.b\",\n    SUM(monthly.amount) AS \"monthly.amount_sum\"\n  FROM monthly AS monthly\n  LEFT JOIN _cm_monthly___min_partition_by_region\n    ON monthly.region IS NOT DISTINCT FROM _cm_monthly___min_partition_by_region.\"monthly.region\"\n  GROUP BY\n    monthly.region,\n    DATE_TRUNC('MONTH', monthly.ordered_at)\n), base AS (\n  SELECT\n    _base.\"monthly.region\",\n    _base.\"monthly.ordered_at\",\n    _cm_monthly___min_partition_by_region.\"monthly.a\",\n    _base.\"monthly.b\",\n    _base.\"monthly.amount_sum\"\n  FROM _base\n  LEFT JOIN _cm_monthly___min_partition_by_region\n    ON _base.\"monthly.region\" IS NOT DISTINCT FROM _cm_monthly___min_partition_by_region.\"monthly.region\"\n), step1 AS (\n  SELECT\n    \"monthly.region\",\n    \"monthly.ordered_at\",\n    \"monthly.b\",\n    \"monthly.amount_sum\",\n    \"monthly.a\",\n    SUM(\"monthly.amount_sum\") OVER (PARTITION BY \"monthly.region\" ORDER BY \"monthly.ordered_at\") AS \"monthly.c\"\n  FROM base\n)\nSELECT\n  \"monthly.region\",\n  \"monthly.ordered_at\",\n  \"monthly.b\",\n  \"monthly.amount_sum\",\n  \"monthly.a\",\n  \"monthly.c\"\nFROM step1\n) AS _outer",
  "dual_phase/with_transform::snowflake": "SELECT\n    \"monthly.region\",\n    \"monthly.ordered_at\",\n    \"monthly.a\",\n    \"monthly.b\",\n    \"monthly.c\"\nFROM (\nWITH _cm_amount_sum_partition_by_ordered_at_region AS (\n  SELECT\n    _stage_inner.\"monthly.region\" AS \"region\",\n    _stage_inner.\"monthly.ordered_at_month\" AS \"ordered_at_month\",\n    _stage_inner.\"monthly.amount_sum_partition_by_ordered_at_region\" AS \"amount_sum_partition_by_ordered_at_region\"\n  FROM (\n    SELECT\n      monthly.region AS \"monthly.region\",\n      DATE_TRUNC('MONTH', monthly.ordered_at) AS \"monthly.ordered_at_month\",\n      CAST(SUM(monthly.amount) AS DOUBLE) AS \"monthly.amount_sum_partition_by_ordered_at_region\"\n    FROM monthly AS monthly\n    GROUP BY\n      monthly.region,\n      DATE_TRUNC('MONTH', monthly.ordered_at)\n  ) AS _stage_inner\n), _cm_monthly___min_partition_by_region AS (\n  SELECT\n    _base.\"monthly.region\" AS \"monthly.region\",\n    MIN(_base._v) AS \"monthly.a\"\n  FROM (\n    SELECT\n      monthly.region AS \"monthly.region\",\n      monthly.region AS _ek0,\n      DATE_TRUNC('MONTH', monthly.ordered_at) AS _ek1,\n      MAX(\n        _cm_amount_sum_partition_by_ordered_at_region.\"amount_sum_partition_by_ordered_at_region\"\n      ) AS _v\n    FROM monthly AS monthly\n    LEFT JOIN _cm_amount_sum_partition_by_ordered_at_region\n      ON monthly.region IS NOT DISTINCT FROM _cm_amount_sum_partition_by_ordered_at_region.\"region\"\n      AND DATE_TRUNC('MONTH', monthly.ordered_at) IS NOT DISTINCT FROM _cm_amount_sum_partition_by_ordered_at_region.\"ordered_at_month\"\n    GROUP BY\n      monthly.region,\n      monthly.region,\n      DATE_TRUNC('MONTH', monthly.ordered_at)\n  ) AS _base\n  GROUP BY\n    _base.\"monthly.region\"\n), _base AS (\n  SELECT\n    monthly.region AS \"monthly.region\",\n    DATE_TRUNC('MONTH', monthly.ordered_at) AS \"monthly.ordered_at\",\n    SUM(monthly.amount * _cm_monthly___min_partition_by_region.\"monthly.a\") AS \"monthly.b\",\n    SUM(monthly.amount) AS \"monthly.amount_sum\"\n  FROM monthly AS monthly\n  LEFT JOIN _cm_monthly___min_partition_by_region\n    ON monthly.region IS NOT DISTINCT FROM _cm_monthly___min_partition_by_region.\"monthly.region\"\n  GROUP BY\n    monthly.region,\n    DATE_TRUNC('MONTH', monthly.ordered_at)\n), base AS (\n  SELECT\n    _base.\"monthly.region\",\n    _base.\"monthly.ordered_at\",\n    _cm_monthly___min_partition_by_region.\"monthly.a\",\n    _base.\"monthly.b\",\n    _base.\"monthly.amount_sum\"\n  FROM _base\n  LEFT JOIN _cm_monthly___min_partition_by_region\n    ON _base.\"monthly.region\" IS NOT DISTINCT FROM _cm_monthly___min_partition_by_region.\"monthly.region\"\n), step1 AS (\n  SELECT\n    \"monthly.region\",\n    \"monthly.ordered_at\",\n    \"monthly.b\",\n    \"monthly.amount_sum\",\n    \"monthly.a\",\n    SUM(\"monthly.amount_sum\") OVER (PARTITION BY \"monthly.region\" ORDER BY \"monthly.ordered_at\") AS \"monthly.c\"\n  FROM base\n)\nSELECT\n  \"monthly.region\",\n  \"monthly.ordered_at\",\n  \"monthly.b\",\n  \"monthly.amount_sum\",\n  \"monthly.a\",\n  \"monthly.c\"\nFROM step1\n) AS _outer",
  "dual_phase/with_transform::sqlite": "SELECT\n    \"monthly.region\",\n    \"monthly.ordered_at\",\n    \"monthly.a\",\n    \"monthly.b\",\n    \"monthly.c\"\nFROM (\nWITH _cm_amount_sum_partition_by_ordered_at_region AS (\n  SELECT\n    _stage_inner.\"monthly.region\" AS \"region\",\n    _stage_inner.\"monthly.ordered_at_month\" AS \"ordered_at_month\",\n    _stage_inner.\"monthly.amount_sum_partition_by_ordered_at_region\" AS \"amount_sum_partition_by_ordered_at_region\"\n  FROM (\n    SELECT\n      monthly.region AS \"monthly.region\",\n      STRFTIME('%Y-%m-01', monthly.ordered_at) AS \"monthly.ordered_at_month\",\n      CAST(SUM(monthly.amount) AS REAL) AS \"monthly.amount_sum_partition_by_ordered_at_region\"\n    FROM monthly AS monthly\n    GROUP BY\n      monthly.region,\n      STRFTIME('%Y-%m-01', monthly.ordered_at)\n  ) AS _stage_inner\n), _cm_monthly___min_partition_by_region AS (\n  SELECT\n    _base.\"monthly.region\" AS \"monthly.region\",\n    MIN(_base._v) AS \"monthly.a\"\n  FROM (\n    SELECT\n      monthly.region AS \"monthly.region\",\n      monthly.region AS _ek0,\n      STRFTIME('%Y-%m-01', monthly.ordered_at) AS _ek1,\n      MAX(\n        _cm_amount_sum_partition_by_ordered_at_region.\"amount_sum_partition_by_ordered_at_region\"\n      ) AS _v\n    FROM monthly AS monthly\n    LEFT JOIN _cm_amount_sum_partition_by_ordered_at_region\n      ON monthly.region IS _cm_amount_sum_partition_by_ordered_at_region.\"region\"\n      AND STRFTIME('%Y-%m-01', monthly.ordered_at) IS _cm_amount_sum_partition_by_ordered_at_region.\"ordered_at_month\"\n    GROUP BY\n      monthly.region,\n      monthly.region,\n      STRFTIME('%Y-%m-01', monthly.ordered_at)\n  ) AS _base\n  GROUP BY\n    _base.\"monthly.region\"\n), _base AS (\n  SELECT\n    monthly.region AS \"monthly.region\",\n    STRFTIME('%Y-%m-01', monthly.ordered_at) AS \"monthly.ordered_at\",\n    SUM(monthly.amount * _cm_monthly___min_partition_by_region.\"monthly.a\") AS \"monthly.b\",\n    SUM(monthly.amount) AS \"monthly.amount_sum\"\n  FROM monthly AS monthly\n  LEFT JOIN _cm_monthly___min_partition_by_region\n    ON monthly.region IS _cm_monthly___min_partition_by_region.\"monthly.region\"\n  GROUP BY\n    monthly.region,\n    STRFTIME('%Y-%m-01', monthly.ordered_at)\n), base AS (\n  SELECT\n    _base.\"monthly.region\",\n    _base.\"monthly.ordered_at\",\n    _cm_monthly___min_partition_by_region.\"monthly.a\",\n    _base.\"monthly.b\",\n    _base.\"monthly.amount_sum\"\n  FROM _base\n  LEFT JOIN _cm_monthly___min_partition_by_region\n    ON _base.\"monthly.region\" IS _cm_monthly___min_partition_by_region.\"monthly.region\"\n), step1 AS (\n  SELECT\n    \"monthly.region\",\n    \"monthly.ordered_at\",\n    \"monthly.b\",\n    \"monthly.amount_sum\",\n    \"monthly.a\",\n    SUM(\"monthly.amount_sum\") OVER (PARTITION BY \"monthly.region\" ORDER BY \"monthly.ordered_at\") AS \"monthly.c\"\n  FROM base\n)\nSELECT\n  \"monthly.region\",\n  \"monthly.ordered_at\",\n  \"monthly.b\",\n  \"monthly.amount_sum\",\n  \"monthly.a\",\n  \"monthly.c\"\nFROM step1\n) AS _outer",
  "dual_phase/with_transform::tsql": "WITH _cm_amount_sum_partition_by_ordered_at_region AS (\n  SELECT\n    _stage_inner.[monthly___region] AS [region],\n    _stage_inner.[monthly___ordered_at_month] AS [ordered_at_month],\n    _stage_inner.[monthly___amount_sum_partition_by_ordered_at_region] AS [amount_sum_partition_by_ordered_at_region]\n  FROM (\n    SELECT\n      monthly.region AS [monthly___region],\n      DATETRUNC(MONTH, monthly.ordered_at) AS [monthly___ordered_at_month],\n      CAST(SUM(monthly.amount) AS FLOAT) AS [monthly___amount_sum_partition_by_ordered_at_region]\n    FROM monthly AS monthly\n    GROUP BY\n      monthly.region,\n      DATETRUNC(MONTH, monthly.ordered_at)\n  ) AS _stage_inner\n), _cm_monthly___min_partition_by_region AS (\n  SELECT\n    _base.[monthly___region] AS [monthly___region],\n    MIN(_base._v) AS [monthly___a]\n  FROM (\n    SELECT\n      monthly.region AS [monthly___region],\n      monthly.region AS _ek0,\n      DATETRUNC(MONTH, monthly.ordered_at) AS _ek1,\n      MAX(\n        _cm_amount_sum_partition_by_ordered_at_region.[amount_sum_partition_by_ordered_at_region]\n      ) AS _v\n    FROM monthly AS monthly\n    LEFT JOIN _cm_amount_sum_partition_by_ordered_at_region\n      ON (\n        monthly.region = _cm_amount_sum_partition_by_ordered_at_region.[region]\n        OR (\n          monthly.region IS NULL\n          AND _cm_amount_sum_partition_by_ordered_at_region.[region] IS NULL\n        )\n      )\n      AND (\n        DATETRUNC(MONTH, monthly.ordered_at) = _cm_amount_sum_partition_by_ordered_at_region.[ordered_at_month]\n        OR (\n          DATETRUNC(MONTH, monthly.ordered_at) IS NULL\n          AND _cm_amount_sum_partition_by_ordered_at_region.[ordered_at_month] IS NULL\n        )\n      )\n    GROUP BY\n      monthly.region,\n      monthly.region,\n      DATETRUNC(MONTH, monthly.ordered_at)\n  ) AS _base\n  GROUP BY\n    _base.[monthly___region]\n), _base AS (\n  SELECT\n    monthly.region AS [monthly___region],\n    DATETRUNC(MONTH, monthly.ordered_at) AS [monthly___ordered_at],\n    SUM(monthly.amount * _cm_monthly___min_partition_by_region.[monthly___a]) AS [monthly___b],\n    SUM(monthly.amount) AS [monthly___amount_sum]\n  FROM monthly AS monthly\n  LEFT JOIN _cm_monthly___min_partition_by_region\n    ON (\n      monthly.region = _cm_monthly___min_partition_by_region.[monthly___region]\n      OR (\n        monthly.region IS NULL\n        AND _cm_monthly___min_partition_by_region.[monthly___region] IS NULL\n      )\n    )\n  GROUP BY\n    monthly.region,\n    DATETRUNC(MONTH, monthly.ordered_at)\n), base AS (\n  SELECT\n    _base.[monthly___region] AS [monthly___region],\n    _base.[monthly___ordered_at] AS [monthly___ordered_at],\n    _cm_monthly___min_partition_by_region.[monthly___a] AS [monthly___a],\n    _base.[monthly___b] AS [monthly___b],\n    _base.[monthly___amount_sum] AS [monthly___amount_sum]\n  FROM _base\n  LEFT JOIN _cm_monthly___min_partition_by_region\n    ON (\n      _base.[monthly___region] = _cm_monthly___min_partition_by_region.[monthly___region]\n      OR (\n        _base.[monthly___region] IS NULL\n        AND _cm_monthly___min_partition_by_region.[monthly___region] IS NULL\n      )\n    )\n), step1 AS (\n  SELECT\n    [monthly___region] AS [monthly___region],\n    [monthly___ordered_at] AS [monthly___ordered_at],\n    [monthly___b] AS [monthly___b],\n    [monthly___amount_sum] AS [monthly___amount_sum],\n    [monthly___a] AS [monthly___a],\n    SUM([monthly___amount_sum]) OVER (PARTITION BY [monthly___region] ORDER BY [monthly___ordered_at]) AS [monthly___c]\n  FROM base\n)\nSELECT\n  [monthly___region],\n  [monthly___ordered_at],\n  [monthly___a],\n  [monthly___b],\n  [monthly___c]\nFROM (\n  SELECT\n    [monthly___region] AS [monthly___region],\n    [monthly___ordered_at] AS [monthly___ordered_at],\n    [monthly___b] AS [monthly___b],\n    [monthly___amount_sum] AS [monthly___amount_sum],\n    [monthly___a] AS [monthly___a],\n    [monthly___c] AS [monthly___c]\n  FROM step1\n) AS _outer",
  "population/dual_phase_with_filter::bigquery": "WITH _cm_spend_sum_partition_by_plan_code_tier AS (\n  SELECT\n    `_stage_inner`.`customers___plan_code` AS `plan_code`,\n    `_stage_inner`.`customers___tier` AS `tier`,\n    `_stage_inner`.`customers___spend_sum_partition_by_plan_code_tier` AS `spend_sum_partition_by_plan_code_tier`\n  FROM (\n    SELECT\n      customers.plan_code AS `customers___plan_code`,\n      customers.tier AS `customers___tier`,\n      CAST(SUM(customers.spend) AS FLOAT64) AS `customers___spend_sum_partition_by_plan_code_tier`\n    FROM customers AS customers\n    WHERE\n      EXISTS(\n        SELECT\n          1\n        FROM orders AS orders\n        WHERE\n          customers.id = orders.customer_id AND orders.status = 'ok'\n      )\n    GROUP BY\n      customers.plan_code,\n      customers.tier\n  ) AS _stage_inner\n), _cm_customers___avg_partition_by_tier AS (\n  SELECT\n    `_base`.`customers___tier` AS `customers___tier`,\n    CAST(AVG(_base._v) AS FLOAT64) AS `customers___a`\n  FROM (\n    SELECT\n      customers.tier AS `customers___tier`,\n      customers.plan_code AS _ek0,\n      customers.tier AS _ek1,\n      MAX(\n        _cm_spend_sum_partition_by_plan_code_tier.`spend_sum_partition_by_plan_code_tier`\n      ) AS _v\n    FROM customers AS customers\n    LEFT JOIN _cm_spend_sum_partition_by_plan_code_tier\n      ON customers.plan_code IS NOT DISTINCT FROM _cm_spend_sum_partition_by_plan_code_tier.`plan_code`\n      AND customers.tier IS NOT DISTINCT FROM _cm_spend_sum_partition_by_plan_code_tier.`tier`\n    WHERE\n      EXISTS(\n        SELECT\n          1\n        FROM orders AS orders\n        WHERE\n          customers.id = orders.customer_id AND orders.status = 'ok'\n      )\n    GROUP BY\n      customers.tier,\n      customers.plan_code,\n      customers.tier\n  ) AS _base\n  GROUP BY\n    `_base`.`customers___tier`\n), _base AS (\n  SELECT\n    customers.tier AS `customers___tier`,\n    SUM(customers.spend * `_cm_customers___avg_partition_by_tier`.`customers___a`) AS `customers___m`\n  FROM customers AS customers\n  LEFT JOIN _cm_customers___avg_partition_by_tier\n    ON customers.tier IS NOT DISTINCT FROM _cm_customers___avg_partition_by_tier.`customers___tier`\n  WHERE\n    EXISTS(\n      SELECT\n        1\n      FROM orders AS orders\n      WHERE\n        customers.id = orders.customer_id AND orders.status = 'ok'\n    )\n  GROUP BY\n    customers.tier\n)\nSELECT\n  _base.`customers___tier`,\n  _cm_customers___avg_partition_by_tier.`customers___a`,\n  _base.`customers___m`\nFROM _base\nLEFT JOIN _cm_customers___avg_partition_by_tier\n  ON _base.`customers___tier` IS NOT DISTINCT FROM _cm_customers___avg_partition_by_tier.`customers___tier`",
  "population/dual_phase_with_filter::duckdb": "WITH _cm_spend_sum_partition_by_plan_code_tier AS (\n  SELECT\n    _stage_inner.\"customers.plan_code\" AS \"plan_code\",\n    _stage_inner.\"customers.tier\" AS \"tier\",\n    _stage_inner.\"customers.spend_sum_partition_by_plan_code_tier\" AS \"spend_sum_partition_by_plan_code_tier\"\n  FROM (\n    SELECT\n      customers.plan_code AS \"customers.plan_code\",\n      customers.tier AS \"customers.tier\",\n      CAST(SUM(customers.spend) AS DOUBLE) AS \"customers.spend_sum_partition_by_plan_code_tier\"\n    FROM customers AS customers\n    WHERE\n      EXISTS(\n        SELECT\n          1\n        FROM orders AS orders\n        WHERE\n          customers.id = orders.customer_id AND orders.status = 'ok'\n      )\n    GROUP BY\n      customers.plan_code,\n      customers.tier\n  ) AS _stage_inner\n), _cm_customers___avg_partition_by_tier AS (\n  SELECT\n    _base.\"customers.tier\" AS \"customers.tier\",\n    CAST(AVG(_base._v) AS DOUBLE) AS \"customers.a\"\n  FROM (\n    SELECT\n      customers.tier AS \"customers.tier\",\n      customers.plan_code AS _ek0,\n      customers.tier AS _ek1,\n      MAX(\n        _cm_spend_sum_partition_by_plan_code_tier.\"spend_sum_partition_by_plan_code_tier\"\n      ) AS _v\n    FROM customers AS customers\n    LEFT JOIN _cm_spend_sum_partition_by_plan_code_tier\n      ON customers.plan_code IS NOT DISTINCT FROM _cm_spend_sum_partition_by_plan_code_tier.\"plan_code\"\n      AND customers.tier IS NOT DISTINCT FROM _cm_spend_sum_partition_by_plan_code_tier.\"tier\"\n    WHERE\n      EXISTS(\n        SELECT\n          1\n        FROM orders AS orders\n        WHERE\n          customers.id = orders.customer_id AND orders.status = 'ok'\n      )\n    GROUP BY\n      customers.tier,\n      customers.plan_code,\n      customers.tier\n  ) AS _base\n  GROUP BY\n    _base.\"customers.tier\"\n), _base AS (\n  SELECT\n    customers.tier AS \"customers.tier\",\n    SUM(customers.spend * _cm_customers___avg_partition_by_tier.\"customers.a\") AS \"customers.m\"\n  FROM customers AS customers\n  LEFT JOIN _cm_customers___avg_partition_by_tier\n    ON customers.tier IS NOT DISTINCT FROM _cm_customers___avg_partition_by_tier.\"customers.tier\"\n  WHERE\n    EXISTS(\n      SELECT\n        1\n      FROM orders AS orders\n      WHERE\n        customers.id = orders.customer_id AND orders.status = 'ok'\n    )\n  GROUP BY\n    customers.tier\n)\nSELECT\n  _base.\"customers.tier\",\n  _cm_customers___avg_partition_by_tier.\"customers.a\",\n  _base.\"customers.m\"\nFROM _base\nLEFT JOIN _cm_customers___avg_partition_by_tier\n  ON _base.\"customers.tier\" IS NOT DISTINCT FROM _cm_customers___avg_partition_by_tier.\"customers.tier\"",
  "population/dual_phase_with_filter::mysql": "WITH _cm_spend_sum_partition_by_plan_code_tier AS (\n  SELECT\n    _stage_inner.`customers.plan_code` AS `plan_code`,\n    _stage_inner.`customers.tier` AS `tier`,\n    _stage_inner.`customers.spend_sum_partition_by_plan_code_tier` AS `spend_sum_partition_by_plan_code_tier`\n  FROM (\n    SELECT\n      customers.plan_code AS `customers.plan_code`,\n      customers.tier AS `customers.tier`,\n      CAST(SUM(customers.spend) AS DOUBLE) AS `customers.spend_sum_partition_by_plan_code_tier`\n    FROM customers AS customers\n    WHERE\n      EXISTS(\n        SELECT\n          1\n        FROM orders AS orders\n        WHERE\n          customers.id = orders.customer_id AND orders.status = 'ok'\n      )\n    GROUP BY\n      customers.plan_code,\n      customers.tier\n  ) AS _stage_inner\n), _cm_customers___avg_partition_by_tier AS (\n  SELECT\n    _base.`customers.tier` AS `customers.tier`,\n    CAST(AVG(_base._v) AS DOUBLE) AS `customers.a`\n  FROM (\n    SELECT\n      customers.tier AS `customers.tier`,\n      customers.plan_code AS _ek0,\n      customers.tier AS _ek1,\n      MAX(\n        _cm_spend_sum_partition_by_plan_code_tier.`spend_sum_partition_by_plan_code_tier`\n      ) AS _v\n    FROM customers AS customers\n    LEFT JOIN _cm_spend_sum_partition_by_plan_code_tier\n      ON customers.plan_code <=> _cm_spend_sum_partition_by_plan_code_tier.`plan_code`\n      AND customers.tier <=> _cm_spend_sum_partition_by_plan_code_tier.`tier`\n    WHERE\n      EXISTS(\n        SELECT\n          1\n        FROM orders AS orders\n        WHERE\n          customers.id = orders.customer_id AND orders.status = 'ok'\n      )\n    GROUP BY\n      customers.tier,\n      customers.plan_code,\n      customers.tier\n  ) AS _base\n  GROUP BY\n    _base.`customers.tier`\n), _base AS (\n  SELECT\n    customers.tier AS `customers.tier`,\n    SUM(customers.spend * _cm_customers___avg_partition_by_tier.`customers.a`) AS `customers.m`\n  FROM customers AS customers\n  LEFT JOIN _cm_customers___avg_partition_by_tier\n    ON customers.tier <=> _cm_customers___avg_partition_by_tier.`customers.tier`\n  WHERE\n    EXISTS(\n      SELECT\n        1\n      FROM orders AS orders\n      WHERE\n        customers.id = orders.customer_id AND orders.status = 'ok'\n    )\n  GROUP BY\n    customers.tier\n)\nSELECT\n  _base.`customers.tier`,\n  _cm_customers___avg_partition_by_tier.`customers.a`,\n  _base.`customers.m`\nFROM _base\nLEFT JOIN _cm_customers___avg_partition_by_tier\n  ON _base.`customers.tier` <=> _cm_customers___avg_partition_by_tier.`customers.tier`",
  "population/dual_phase_with_filter::postgres": "WITH _cm_spend_sum_partition_by_plan_code_tier AS (\n  SELECT\n    _stage_inner.\"customers.plan_code\" AS \"plan_code\",\n    _stage_inner.\"customers.tier\" AS \"tier\",\n    _stage_inner.\"customers.spend_sum_partition_by_plan_code_tier\" AS \"spend_sum_partition_by_plan_code_tier\"\n  FROM (\n    SELECT\n      customers.plan_code AS \"customers.plan_code\",\n      customers.tier AS \"customers.tier\",\n      CAST(SUM(customers.spend) AS DOUBLE PRECISION) AS \"customers.spend_sum_partition_by_plan_code_tier\"\n    FROM customers AS customers\n    WHERE\n      EXISTS(\n        SELECT\n          1\n        FROM orders AS orders\n        WHERE\n          customers.id = orders.customer_id AND orders.status = 'ok'\n      )\n    GROUP BY\n      customers.plan_code,\n      customers.tier\n  ) AS _stage_inner\n), _cm_customers___avg_partition_by_tier AS (\n  SELECT\n    _base.\"customers.tier\" AS \"customers.tier\",\n    CAST(AVG(_base._v) AS DOUBLE PRECISION) AS \"customers.a\"\n  FROM (\n    SELECT\n      customers.tier AS \"customers.tier\",\n      customers.plan_code AS _ek0,\n      customers.tier AS _ek1,\n      MAX(\n        _cm_spend_sum_partition_by_plan_code_tier.\"spend_sum_partition_by_plan_code_tier\"\n      ) AS _v\n    FROM customers AS customers\n    LEFT JOIN _cm_spend_sum_partition_by_plan_code_tier\n      ON customers.plan_code IS NOT DISTINCT FROM _cm_spend_sum_partition_by_plan_code_tier.\"plan_code\"\n      AND customers.tier IS NOT DISTINCT FROM _cm_spend_sum_partition_by_plan_code_tier.\"tier\"\n    WHERE\n      EXISTS(\n        SELECT\n          1\n        FROM orders AS orders\n        WHERE\n          customers.id = orders.customer_id AND orders.status = 'ok'\n      )\n    GROUP BY\n      customers.tier,\n      customers.plan_code,\n      customers.tier\n  ) AS _base\n  GROUP BY\n    _base.\"customers.tier\"\n), _base AS (\n  SELECT\n    customers.tier AS \"customers.tier\",\n    SUM(customers.spend * _cm_customers___avg_partition_by_tier.\"customers.a\") AS \"customers.m\"\n  FROM customers AS customers\n  LEFT JOIN _cm_customers___avg_partition_by_tier\n    ON customers.tier IS NOT DISTINCT FROM _cm_customers___avg_partition_by_tier.\"customers.tier\"\n  WHERE\n    EXISTS(\n      SELECT\n        1\n      FROM orders AS orders\n      WHERE\n        customers.id = orders.customer_id AND orders.status = 'ok'\n    )\n  GROUP BY\n    customers.tier\n)\nSELECT\n  _base.\"customers.tier\",\n  _cm_customers___avg_partition_by_tier.\"customers.a\",\n  _base.\"customers.m\"\nFROM _base\nLEFT JOIN _cm_customers___avg_partition_by_tier\n  ON _base.\"customers.tier\" IS NOT DISTINCT FROM _cm_customers___avg_partition_by_tier.\"customers.tier\"",
  "population/dual_phase_with_filter::snowflake": "WITH _cm_spend_sum_partition_by_plan_code_tier AS (\n  SELECT\n    _stage_inner.\"customers.plan_code\" AS \"plan_code\",\n    _stage_inner.\"customers.tier\" AS \"tier\",\n    _stage_inner.\"customers.spend_sum_partition_by_plan_code_tier\" AS \"spend_sum_partition_by_plan_code_tier\"\n  FROM (\n    SELECT\n      customers.plan_code AS \"customers.plan_code\",\n      customers.tier AS \"customers.tier\",\n      CAST(SUM(customers.spend) AS DOUBLE) AS \"customers.spend_sum_partition_by_plan_code_tier\"\n    FROM customers AS customers\n    WHERE\n      EXISTS(\n        SELECT\n          1\n        FROM orders AS orders\n        WHERE\n          customers.id = orders.customer_id AND orders.status = 'ok'\n      )\n    GROUP BY\n      customers.plan_code,\n      customers.tier\n  ) AS _stage_inner\n), _cm_customers___avg_partition_by_tier AS (\n  SELECT\n    _base.\"customers.tier\" AS \"customers.tier\",\n    CAST(AVG(_base._v) AS DOUBLE) AS \"customers.a\"\n  FROM (\n    SELECT\n      customers.tier AS \"customers.tier\",\n      customers.plan_code AS _ek0,\n      customers.tier AS _ek1,\n      MAX(\n        _cm_spend_sum_partition_by_plan_code_tier.\"spend_sum_partition_by_plan_code_tier\"\n      ) AS _v\n    FROM customers AS customers\n    LEFT JOIN _cm_spend_sum_partition_by_plan_code_tier\n      ON customers.plan_code IS NOT DISTINCT FROM _cm_spend_sum_partition_by_plan_code_tier.\"plan_code\"\n      AND customers.tier IS NOT DISTINCT FROM _cm_spend_sum_partition_by_plan_code_tier.\"tier\"\n    WHERE\n      EXISTS(\n        SELECT\n          1\n        FROM orders AS orders\n        WHERE\n          customers.id = orders.customer_id AND orders.status = 'ok'\n      )\n    GROUP BY\n      customers.tier,\n      customers.plan_code,\n      customers.tier\n  ) AS _base\n  GROUP BY\n    _base.\"customers.tier\"\n), _base AS (\n  SELECT\n    customers.tier AS \"customers.tier\",\n    SUM(customers.spend * _cm_customers___avg_partition_by_tier.\"customers.a\") AS \"customers.m\"\n  FROM customers AS customers\n  LEFT JOIN _cm_customers___avg_partition_by_tier\n    ON customers.tier IS NOT DISTINCT FROM _cm_customers___avg_partition_by_tier.\"customers.tier\"\n  WHERE\n    EXISTS(\n      SELECT\n        1\n      FROM orders AS orders\n      WHERE\n        customers.id = orders.customer_id AND orders.status = 'ok'\n    )\n  GROUP BY\n    customers.tier\n)\nSELECT\n  _base.\"customers.tier\",\n  _cm_customers___avg_partition_by_tier.\"customers.a\",\n  _base.\"customers.m\"\nFROM _base\nLEFT JOIN _cm_customers___avg_partition_by_tier\n  ON _base.\"customers.tier\" IS NOT DISTINCT FROM _cm_customers___avg_partition_by_tier.\"customers.tier\"",
  "population/dual_phase_with_filter::sqlite": "WITH _cm_spend_sum_partition_by_plan_code_tier AS (\n  SELECT\n    _stage_inner.\"customers.plan_code\" AS \"plan_code\",\n    _stage_inner.\"customers.tier\" AS \"tier\",\n    _stage_inner.\"customers.spend_sum_partition_by_plan_code_tier\" AS \"spend_sum_partition_by_plan_code_tier\"\n  FROM (\n    SELECT\n      customers.plan_code AS \"customers.plan_code\",\n      customers.tier AS \"customers.tier\",\n      CAST(SUM(customers.spend) AS REAL) AS \"customers.spend_sum_partition_by_plan_code_tier\"\n    FROM customers AS customers\n    WHERE\n      EXISTS(\n        SELECT\n          1\n        FROM orders AS orders\n        WHERE\n          customers.id = orders.customer_id AND orders.status = 'ok'\n      )\n    GROUP BY\n      customers.plan_code,\n      customers.tier\n  ) AS _stage_inner\n), _cm_customers___avg_partition_by_tier AS (\n  SELECT\n    _base.\"customers.tier\" AS \"customers.tier\",\n    CAST(AVG(_base._v) AS REAL) AS \"customers.a\"\n  FROM (\n    SELECT\n      customers.tier AS \"customers.tier\",\n      customers.plan_code AS _ek0,\n      customers.tier AS _ek1,\n      MAX(\n        _cm_spend_sum_partition_by_plan_code_tier.\"spend_sum_partition_by_plan_code_tier\"\n      ) AS _v\n    FROM customers AS customers\n    LEFT JOIN _cm_spend_sum_partition_by_plan_code_tier\n      ON customers.plan_code IS _cm_spend_sum_partition_by_plan_code_tier.\"plan_code\"\n      AND customers.tier IS _cm_spend_sum_partition_by_plan_code_tier.\"tier\"\n    WHERE\n      EXISTS(\n        SELECT\n          1\n        FROM orders AS orders\n        WHERE\n          customers.id = orders.customer_id AND orders.status = 'ok'\n      )\n    GROUP BY\n      customers.tier,\n      customers.plan_code,\n      customers.tier\n  ) AS _base\n  GROUP BY\n    _base.\"customers.tier\"\n), _base AS (\n  SELECT\n    customers.tier AS \"customers.tier\",\n    SUM(customers.spend * _cm_customers___avg_partition_by_tier.\"customers.a\") AS \"customers.m\"\n  FROM customers AS customers\n  LEFT JOIN _cm_customers___avg_partition_by_tier\n    ON customers.tier IS _cm_customers___avg_partition_by_tier.\"customers.tier\"\n  WHERE\n    EXISTS(\n      SELECT\n        1\n      FROM orders AS orders\n      WHERE\n        customers.id = orders.customer_id AND orders.status = 'ok'\n    )\n  GROUP BY\n    customers.tier\n)\nSELECT\n  _base.\"customers.tier\",\n  _cm_customers___avg_partition_by_tier.\"customers.a\",\n  _base.\"customers.m\"\nFROM _base\nLEFT JOIN _cm_customers___avg_partition_by_tier\n  ON _base.\"customers.tier\" IS _cm_customers___avg_partition_by_tier.\"customers.tier\"",
  "population/dual_phase_with_filter::tsql": "WITH _cm_spend_sum_partition_by_plan_code_tier AS (\n  SELECT\n    _stage_inner.[customers___plan_code] AS [plan_code],\n    _stage_inner.[customers___tier] AS [tier],\n    _stage_inner.[customers___spend_sum_partition_by_plan_code_tier] AS [spend_sum_partition_by_plan_code_tier]\n  FROM (\n    SELECT\n      customers.plan_code AS [customers___plan_code],\n      customers.tier AS [customers___tier],\n      CAST(SUM(customers.spend) AS FLOAT) AS [customers___spend_sum_partition_by_plan_code_tier]\n    FROM customers AS customers\n    WHERE\n      EXISTS(\n        SELECT\n          1\n        FROM orders AS orders\n        WHERE\n          customers.id = orders.customer_id AND orders.status = 'ok'\n      )\n    GROUP BY\n      customers.plan_code,\n      customers.tier\n  ) AS _stage_inner\n), _cm_customers___avg_partition_by_tier AS (\n  SELECT\n    _base.[customers___tier] AS [customers___tier],\n    CAST(AVG(_base._v) AS FLOAT) AS [customers___a]\n  FROM (\n    SELECT\n      customers.tier AS [customers___tier],\n      customers.plan_code AS _ek0,\n      customers.tier AS _ek1,\n      MAX(\n        _cm_spend_sum_partition_by_plan_code_tier.[spend_sum_partition_by_plan_code_tier]\n      ) AS _v\n    FROM customers AS customers\n    LEFT JOIN _cm_spend_sum_partition_by_plan_code_tier\n      ON (\n        customers.plan_code = _cm_spend_sum_partition_by_plan_code_tier.[plan_code]\n        OR (\n          customers.plan_code IS NULL\n          AND _cm_spend_sum_partition_by_plan_code_tier.[plan_code] IS NULL\n        )\n      )\n      AND (\n        customers.tier = _cm_spend_sum_partition_by_plan_code_tier.[tier]\n        OR (\n          customers.tier IS NULL\n          AND _cm_spend_sum_partition_by_plan_code_tier.[tier] IS NULL\n        )\n      )\n    WHERE\n      EXISTS(\n        SELECT\n          1\n        FROM orders AS orders\n        WHERE\n          customers.id = orders.customer_id AND orders.status = 'ok'\n      )\n    GROUP BY\n      customers.tier,\n      customers.plan_code,\n      customers.tier\n  ) AS _base\n  GROUP BY\n    _base.[customers___tier]\n), _base AS (\n  SELECT\n    customers.tier AS [customers___tier],\n    SUM(customers.spend * _cm_customers___avg_partition_by_tier.[customers___a]) AS [customers___m]\n  FROM customers AS customers\n  LEFT JOIN _cm_customers___avg_partition_by_tier\n    ON (\n      customers.tier = _cm_customers___avg_partition_by_tier.[customers___tier]\n      OR (\n        customers.tier IS NULL\n        AND _cm_customers___avg_partition_by_tier.[customers___tier] IS NULL\n      )\n    )\n  WHERE\n    EXISTS(\n      SELECT\n        1\n      FROM orders AS orders\n      WHERE\n        customers.id = orders.customer_id AND orders.status = 'ok'\n    )\n  GROUP BY\n    customers.tier\n)\nSELECT\n  _base.[customers___tier],\n  _cm_customers___avg_partition_by_tier.[customers___a],\n  _base.[customers___m]\nFROM _base\nLEFT JOIN _cm_customers___avg_partition_by_tier\n  ON (\n    _base.[customers___tier] = _cm_customers___avg_partition_by_tier.[customers___tier]\n    OR (\n      _base.[customers___tier] IS NULL\n      AND _cm_customers___avg_partition_by_tier.[customers___tier] IS NULL\n    )\n  )",
  "population/mixed_reagg_constituent::bigquery": "WITH _cm_spend_sum_partition_by_plan_code_tier AS (\n  SELECT\n    `_stage_inner`.`customers___plan_code` AS `plan_code`,\n    `_stage_inner`.`customers___tier` AS `tier`,\n    `_stage_inner`.`customers___spend_sum_partition_by_plan_code_tier` AS `spend_sum_partition_by_plan_code_tier`\n  FROM (\n    SELECT\n      customers.plan_code AS `customers___plan_code`,\n      customers.tier AS `customers___tier`,\n      CAST(SUM(customers.spend) AS FLOAT64) AS `customers___spend_sum_partition_by_plan_code_tier`\n    FROM customers AS customers\n    WHERE\n      EXISTS(\n        SELECT\n          1\n        FROM orders AS orders\n        WHERE\n          customers.id = orders.customer_id AND orders.status = 'ok'\n      )\n    GROUP BY\n      customers.plan_code,\n      customers.tier\n  ) AS _stage_inner\n), _cm_avg_spend_sum_partition_by_plan_code_tier AS (\n  SELECT\n    _stage_inner.`customers___tier` AS `tier`,\n    _stage_inner.`customers___avg_spend_sum_partition_by_plan_code_tier` AS `avg_spend_sum_partition_by_plan_code_tier`\n  FROM (\n    SELECT\n      `_base`.`customers___tier` AS `customers___tier`,\n      CAST(AVG(_base._v) AS FLOAT64) AS `customers___avg_spend_sum_partition_by_plan_code_tier`\n    FROM (\n      SELECT\n        customers.tier AS `customers___tier`,\n        customers.plan_code AS _ek0,\n        customers.tier AS _ek1,\n        MAX(\n          _cm_spend_sum_partition_by_plan_code_tier.`spend_sum_partition_by_plan_code_tier`\n        ) AS _v\n      FROM customers AS customers\n      LEFT JOIN _cm_spend_sum_partition_by_plan_code_tier\n        ON customers.plan_code IS NOT DISTINCT FROM _cm_spend_sum_partition_by_plan_code_tier.`plan_code`\n        AND customers.tier IS NOT DISTINCT FROM _cm_spend_sum_partition_by_plan_code_tier.`tier`\n      WHERE\n        EXISTS(\n          SELECT\n            1\n          FROM orders AS orders\n          WHERE\n            customers.id = orders.customer_id AND orders.status = 'ok'\n        )\n      GROUP BY\n        customers.tier,\n        customers.plan_code,\n        customers.tier\n    ) AS _base\n    GROUP BY\n      `_base`.`customers___tier`\n  ) AS _stage_inner\n)\nSELECT\n  customers.tier AS `customers___tier`,\n  SUM(\n    customers.spend * _cm_avg_spend_sum_partition_by_plan_code_tier.`avg_spend_sum_partition_by_plan_code_tier`\n  ) AS `customers___m`\nFROM customers AS customers\nLEFT JOIN _cm_avg_spend_sum_partition_by_plan_code_tier\n  ON customers.tier IS NOT DISTINCT FROM _cm_avg_spend_sum_partition_by_plan_code_tier.`tier`\nWHERE\n  EXISTS(\n    SELECT\n      1\n    FROM orders AS orders\n    WHERE\n      customers.id = orders.customer_id AND orders.status = 'ok'\n  )\nGROUP BY\n  customers.tier",
  "population/mixed_reagg_constituent::duckdb": "WITH _cm_spend_sum_partition_by_plan_code_tier AS (\n  SELECT\n    _stage_inner.\"customers.plan_code\" AS \"plan_code\",\n    _stage_inner.\"customers.tier\" AS \"tier\",\n    _stage_inner.\"customers.spend_sum_partition_by_plan_code_tier\" AS \"spend_sum_partition_by_plan_code_tier\"\n  FROM (\n    SELECT\n      customers.plan_code AS \"customers.plan_code\",\n      customers.tier AS \"customers.tier\",\n      CAST(SUM(customers.spend) AS DOUBLE) AS \"customers.spend_sum_partition_by_plan_code_tier\"\n    FROM customers AS customers\n    WHERE\n      EXISTS(\n        SELECT\n          1\n        FROM orders AS orders\n        WHERE\n          customers.id = orders.customer_id AND orders.status = 'ok'\n      )\n    GROUP BY\n      customers.plan_code,\n      customers.tier\n  ) AS _stage_inner\n), _cm_avg_spend_sum_partition_by_plan_code_tier AS (\n  SELECT\n    _stage_inner.\"customers.tier\" AS \"tier\",\n    _stage_inner.\"customers.avg_spend_sum_partition_by_plan_code_tier\" AS \"avg_spend_sum_partition_by_plan_code_tier\"\n  FROM (\n    SELECT\n      _base.\"customers.tier\" AS \"customers.tier\",\n      CAST(AVG(_base._v) AS DOUBLE) AS \"customers.avg_spend_sum_partition_by_plan_code_tier\"\n    FROM (\n      SELECT\n        customers.tier AS \"customers.tier\",\n        customers.plan_code AS _ek0,\n        customers.tier AS _ek1,\n        MAX(\n          _cm_spend_sum_partition_by_plan_code_tier.\"spend_sum_partition_by_plan_code_tier\"\n        ) AS _v\n      FROM customers AS customers\n      LEFT JOIN _cm_spend_sum_partition_by_plan_code_tier\n        ON customers.plan_code IS NOT DISTINCT FROM _cm_spend_sum_partition_by_plan_code_tier.\"plan_code\"\n        AND customers.tier IS NOT DISTINCT FROM _cm_spend_sum_partition_by_plan_code_tier.\"tier\"\n      WHERE\n        EXISTS(\n          SELECT\n            1\n          FROM orders AS orders\n          WHERE\n            customers.id = orders.customer_id AND orders.status = 'ok'\n        )\n      GROUP BY\n        customers.tier,\n        customers.plan_code,\n        customers.tier\n    ) AS _base\n    GROUP BY\n      _base.\"customers.tier\"\n  ) AS _stage_inner\n)\nSELECT\n  customers.tier AS \"customers.tier\",\n  SUM(\n    customers.spend * _cm_avg_spend_sum_partition_by_plan_code_tier.\"avg_spend_sum_partition_by_plan_code_tier\"\n  ) AS \"customers.m\"\nFROM customers AS customers\nLEFT JOIN _cm_avg_spend_sum_partition_by_plan_code_tier\n  ON customers.tier IS NOT DISTINCT FROM _cm_avg_spend_sum_partition_by_plan_code_tier.\"tier\"\nWHERE\n  EXISTS(\n    SELECT\n      1\n    FROM orders AS orders\n    WHERE\n      customers.id = orders.customer_id AND orders.status = 'ok'\n  )\nGROUP BY\n  customers.tier",
  "population/mixed_reagg_constituent::mysql": "WITH _cm_spend_sum_partition_by_plan_code_tier AS (\n  SELECT\n    _stage_inner.`customers.plan_code` AS `plan_code`,\n    _stage_inner.`customers.tier` AS `tier`,\n    _stage_inner.`customers.spend_sum_partition_by_plan_code_tier` AS `spend_sum_partition_by_plan_code_tier`\n  FROM (\n    SELECT\n      customers.plan_code AS `customers.plan_code`,\n      customers.tier AS `customers.tier`,\n      CAST(SUM(customers.spend) AS DOUBLE) AS `customers.spend_sum_partition_by_plan_code_tier`\n    FROM customers AS customers\n    WHERE\n      EXISTS(\n        SELECT\n          1\n        FROM orders AS orders\n        WHERE\n          customers.id = orders.customer_id AND orders.status = 'ok'\n      )\n    GROUP BY\n      customers.plan_code,\n      customers.tier\n  ) AS _stage_inner\n), _cm_avg_spend_sum_partition_by_plan_code_tier AS (\n  SELECT\n    _stage_inner.`customers.tier` AS `tier`,\n    _stage_inner.`customers.avg_spend_sum_partition_by_plan_code_tier` AS `avg_spend_sum_partition_by_plan_code_tier`\n  FROM (\n    SELECT\n      _base.`customers.tier` AS `customers.tier`,\n      CAST(AVG(_base._v) AS DOUBLE) AS `customers.avg_spend_sum_partition_by_plan_code_tier`\n    FROM (\n      SELECT\n        customers.tier AS `customers.tier`,\n        customers.plan_code AS _ek0,\n        customers.tier AS _ek1,\n        MAX(\n          _cm_spend_sum_partition_by_plan_code_tier.`spend_sum_partition_by_plan_code_tier`\n        ) AS _v\n      FROM customers AS customers\n      LEFT JOIN _cm_spend_sum_partition_by_plan_code_tier\n        ON customers.plan_code <=> _cm_spend_sum_partition_by_plan_code_tier.`plan_code`\n        AND customers.tier <=> _cm_spend_sum_partition_by_plan_code_tier.`tier`\n      WHERE\n        EXISTS(\n          SELECT\n            1\n          FROM orders AS orders\n          WHERE\n            customers.id = orders.customer_id AND orders.status = 'ok'\n        )\n      GROUP BY\n        customers.tier,\n        customers.plan_code,\n        customers.tier\n    ) AS _base\n    GROUP BY\n      _base.`customers.tier`\n  ) AS _stage_inner\n)\nSELECT\n  customers.tier AS `customers.tier`,\n  SUM(\n    customers.spend * _cm_avg_spend_sum_partition_by_plan_code_tier.`avg_spend_sum_partition_by_plan_code_tier`\n  ) AS `customers.m`\nFROM customers AS customers\nLEFT JOIN _cm_avg_spend_sum_partition_by_plan_code_tier\n  ON customers.tier <=> _cm_avg_spend_sum_partition_by_plan_code_tier.`tier`\nWHERE\n  EXISTS(\n    SELECT\n      1\n    FROM orders AS orders\n    WHERE\n      customers.id = orders.customer_id AND orders.status = 'ok'\n  )\nGROUP BY\n  customers.tier",
  "population/mixed_reagg_constituent::postgres": "WITH _cm_spend_sum_partition_by_plan_code_tier AS (\n  SELECT\n    _stage_inner.\"customers.plan_code\" AS \"plan_code\",\n    _stage_inner.\"customers.tier\" AS \"tier\",\n    _stage_inner.\"customers.spend_sum_partition_by_plan_code_tier\" AS \"spend_sum_partition_by_plan_code_tier\"\n  FROM (\n    SELECT\n      customers.plan_code AS \"customers.plan_code\",\n      customers.tier AS \"customers.tier\",\n      CAST(SUM(customers.spend) AS DOUBLE PRECISION) AS \"customers.spend_sum_partition_by_plan_code_tier\"\n    FROM customers AS customers\n    WHERE\n      EXISTS(\n        SELECT\n          1\n        FROM orders AS orders\n        WHERE\n          customers.id = orders.customer_id AND orders.status = 'ok'\n      )\n    GROUP BY\n      customers.plan_code,\n      customers.tier\n  ) AS _stage_inner\n), _cm_avg_spend_sum_partition_by_plan_code_tier AS (\n  SELECT\n    _stage_inner.\"customers.tier\" AS \"tier\",\n    _stage_inner.\"customers.avg_spend_sum_partition_by_plan_code_tier\" AS \"avg_spend_sum_partition_by_plan_code_tier\"\n  FROM (\n    SELECT\n      _base.\"customers.tier\" AS \"customers.tier\",\n      CAST(AVG(_base._v) AS DOUBLE PRECISION) AS \"customers.avg_spend_sum_partition_by_plan_code_tier\"\n    FROM (\n      SELECT\n        customers.tier AS \"customers.tier\",\n        customers.plan_code AS _ek0,\n        customers.tier AS _ek1,\n        MAX(\n          _cm_spend_sum_partition_by_plan_code_tier.\"spend_sum_partition_by_plan_code_tier\"\n        ) AS _v\n      FROM customers AS customers\n      LEFT JOIN _cm_spend_sum_partition_by_plan_code_tier\n        ON customers.plan_code IS NOT DISTINCT FROM _cm_spend_sum_partition_by_plan_code_tier.\"plan_code\"\n        AND customers.tier IS NOT DISTINCT FROM _cm_spend_sum_partition_by_plan_code_tier.\"tier\"\n      WHERE\n        EXISTS(\n          SELECT\n            1\n          FROM orders AS orders\n          WHERE\n            customers.id = orders.customer_id AND orders.status = 'ok'\n        )\n      GROUP BY\n        customers.tier,\n        customers.plan_code,\n        customers.tier\n    ) AS _base\n    GROUP BY\n      _base.\"customers.tier\"\n  ) AS _stage_inner\n)\nSELECT\n  customers.tier AS \"customers.tier\",\n  SUM(\n    customers.spend * _cm_avg_spend_sum_partition_by_plan_code_tier.\"avg_spend_sum_partition_by_plan_code_tier\"\n  ) AS \"customers.m\"\nFROM customers AS customers\nLEFT JOIN _cm_avg_spend_sum_partition_by_plan_code_tier\n  ON customers.tier IS NOT DISTINCT FROM _cm_avg_spend_sum_partition_by_plan_code_tier.\"tier\"\nWHERE\n  EXISTS(\n    SELECT\n      1\n    FROM orders AS orders\n    WHERE\n      customers.id = orders.customer_id AND orders.status = 'ok'\n  )\nGROUP BY\n  customers.tier",
  "population/mixed_reagg_constituent::snowflake": "WITH _cm_spend_sum_partition_by_plan_code_tier AS (\n  SELECT\n    _stage_inner.\"customers.plan_code\" AS \"plan_code\",\n    _stage_inner.\"customers.tier\" AS \"tier\",\n    _stage_inner.\"customers.spend_sum_partition_by_plan_code_tier\" AS \"spend_sum_partition_by_plan_code_tier\"\n  FROM (\n    SELECT\n      customers.plan_code AS \"customers.plan_code\",\n      customers.tier AS \"customers.tier\",\n      CAST(SUM(customers.spend) AS DOUBLE) AS \"customers.spend_sum_partition_by_plan_code_tier\"\n    FROM customers AS customers\n    WHERE\n      EXISTS(\n        SELECT\n          1\n        FROM orders AS orders\n        WHERE\n          customers.id = orders.customer_id AND orders.status = 'ok'\n      )\n    GROUP BY\n      customers.plan_code,\n      customers.tier\n  ) AS _stage_inner\n), _cm_avg_spend_sum_partition_by_plan_code_tier AS (\n  SELECT\n    _stage_inner.\"customers.tier\" AS \"tier\",\n    _stage_inner.\"customers.avg_spend_sum_partition_by_plan_code_tier\" AS \"avg_spend_sum_partition_by_plan_code_tier\"\n  FROM (\n    SELECT\n      _base.\"customers.tier\" AS \"customers.tier\",\n      CAST(AVG(_base._v) AS DOUBLE) AS \"customers.avg_spend_sum_partition_by_plan_code_tier\"\n    FROM (\n      SELECT\n        customers.tier AS \"customers.tier\",\n        customers.plan_code AS _ek0,\n        customers.tier AS _ek1,\n        MAX(\n          _cm_spend_sum_partition_by_plan_code_tier.\"spend_sum_partition_by_plan_code_tier\"\n        ) AS _v\n      FROM customers AS customers\n      LEFT JOIN _cm_spend_sum_partition_by_plan_code_tier\n        ON customers.plan_code IS NOT DISTINCT FROM _cm_spend_sum_partition_by_plan_code_tier.\"plan_code\"\n        AND customers.tier IS NOT DISTINCT FROM _cm_spend_sum_partition_by_plan_code_tier.\"tier\"\n      WHERE\n        EXISTS(\n          SELECT\n            1\n          FROM orders AS orders\n          WHERE\n            customers.id = orders.customer_id AND orders.status = 'ok'\n        )\n      GROUP BY\n        customers.tier,\n        customers.plan_code,\n        customers.tier\n    ) AS _base\n    GROUP BY\n      _base.\"customers.tier\"\n  ) AS _stage_inner\n)\nSELECT\n  customers.tier AS \"customers.tier\",\n  SUM(\n    customers.spend * _cm_avg_spend_sum_partition_by_plan_code_tier.\"avg_spend_sum_partition_by_plan_code_tier\"\n  ) AS \"customers.m\"\nFROM customers AS customers\nLEFT JOIN _cm_avg_spend_sum_partition_by_plan_code_tier\n  ON customers.tier IS NOT DISTINCT FROM _cm_avg_spend_sum_partition_by_plan_code_tier.\"tier\"\nWHERE\n  EXISTS(\n    SELECT\n      1\n    FROM orders AS orders\n    WHERE\n      customers.id = orders.customer_id AND orders.status = 'ok'\n  )\nGROUP BY\n  customers.tier",
  "population/mixed_reagg_constituent::sqlite": "WITH _cm_spend_sum_partition_by_plan_code_tier AS (\n  SELECT\n    _stage_inner.\"customers.plan_code\" AS \"plan_code\",\n    _stage_inner.\"customers.tier\" AS \"tier\",\n    _stage_inner.\"customers.spend_sum_partition_by_plan_code_tier\" AS \"spend_sum_partition_by_plan_code_tier\"\n  FROM (\n    SELECT\n      customers.plan_code AS \"customers.plan_code\",\n      customers.tier AS \"customers.tier\",\n      CAST(SUM(customers.spend) AS REAL) AS \"customers.spend_sum_partition_by_plan_code_tier\"\n    FROM customers AS customers\n    WHERE\n      EXISTS(\n        SELECT\n          1\n        FROM orders AS orders\n        WHERE\n          customers.id = orders.customer_id AND orders.status = 'ok'\n      )\n    GROUP BY\n      customers.plan_code,\n      customers.tier\n  ) AS _stage_inner\n), _cm_avg_spend_sum_partition_by_plan_code_tier AS (\n  SELECT\n    _stage_inner.\"customers.tier\" AS \"tier\",\n    _stage_inner.\"customers.avg_spend_sum_partition_by_plan_code_tier\" AS \"avg_spend_sum_partition_by_plan_code_tier\"\n  FROM (\n    SELECT\n      _base.\"customers.tier\" AS \"customers.tier\",\n      CAST(AVG(_base._v) AS REAL) AS \"customers.avg_spend_sum_partition_by_plan_code_tier\"\n    FROM (\n      SELECT\n        customers.tier AS \"customers.tier\",\n        customers.plan_code AS _ek0,\n        customers.tier AS _ek1,\n        MAX(\n          _cm_spend_sum_partition_by_plan_code_tier.\"spend_sum_partition_by_plan_code_tier\"\n        ) AS _v\n      FROM customers AS customers\n      LEFT JOIN _cm_spend_sum_partition_by_plan_code_tier\n        ON customers.plan_code IS _cm_spend_sum_partition_by_plan_code_tier.\"plan_code\"\n        AND customers.tier IS _cm_spend_sum_partition_by_plan_code_tier.\"tier\"\n      WHERE\n        EXISTS(\n          SELECT\n            1\n          FROM orders AS orders\n          WHERE\n            customers.id = orders.customer_id AND orders.status = 'ok'\n        )\n      GROUP BY\n        customers.tier,\n        customers.plan_code,\n        customers.tier\n    ) AS _base\n    GROUP BY\n      _base.\"customers.tier\"\n  ) AS _stage_inner\n)\nSELECT\n  customers.tier AS \"customers.tier\",\n  SUM(\n    customers.spend * _cm_avg_spend_sum_partition_by_plan_code_tier.\"avg_spend_sum_partition_by_plan_code_tier\"\n  ) AS \"customers.m\"\nFROM customers AS customers\nLEFT JOIN _cm_avg_spend_sum_partition_by_plan_code_tier\n  ON customers.tier IS _cm_avg_spend_sum_partition_by_plan_code_tier.\"tier\"\nWHERE\n  EXISTS(\n    SELECT\n      1\n    FROM orders AS orders\n    WHERE\n      customers.id = orders.customer_id AND orders.status = 'ok'\n  )\nGROUP BY\n  customers.tier",
  "population/mixed_reagg_constituent::tsql": "WITH _cm_spend_sum_partition_by_plan_code_tier AS (\n  SELECT\n    _stage_inner.[customers___plan_code] AS [plan_code],\n    _stage_inner.[customers___tier] AS [tier],\n    _stage_inner.[customers___spend_sum_partition_by_plan_code_tier] AS [spend_sum_partition_by_plan_code_tier]\n  FROM (\n    SELECT\n      customers.plan_code AS [customers___plan_code],\n      customers.tier AS [customers___tier],\n      CAST(SUM(customers.spend) AS FLOAT) AS [customers___spend_sum_partition_by_plan_code_tier]\n    FROM customers AS customers\n    WHERE\n      EXISTS(\n        SELECT\n          1\n        FROM orders AS orders\n        WHERE\n          customers.id = orders.customer_id AND orders.status = 'ok'\n      )\n    GROUP BY\n      customers.plan_code,\n      customers.tier\n  ) AS _stage_inner\n), _cm_avg_spend_sum_partition_by_plan_code_tier AS (\n  SELECT\n    _stage_inner.[customers___tier] AS [tier],\n    _stage_inner.[customers___avg_spend_sum_partition_by_plan_code_tier] AS [avg_spend_sum_partition_by_plan_code_tier]\n  FROM (\n    SELECT\n      _base.[customers___tier] AS [customers___tier],\n      CAST(AVG(_base._v) AS FLOAT) AS [customers___avg_spend_sum_partition_by_plan_code_tier]\n    FROM (\n      SELECT\n        customers.tier AS [customers___tier],\n        customers.plan_code AS _ek0,\n        customers.tier AS _ek1,\n        MAX(\n          _cm_spend_sum_partition_by_plan_code_tier.[spend_sum_partition_by_plan_code_tier]\n        ) AS _v\n      FROM customers AS customers\n      LEFT JOIN _cm_spend_sum_partition_by_plan_code_tier\n        ON (\n          customers.plan_code = _cm_spend_sum_partition_by_plan_code_tier.[plan_code]\n          OR (\n            customers.plan_code IS NULL\n            AND _cm_spend_sum_partition_by_plan_code_tier.[plan_code] IS NULL\n          )\n        )\n        AND (\n          customers.tier = _cm_spend_sum_partition_by_plan_code_tier.[tier]\n          OR (\n            customers.tier IS NULL\n            AND _cm_spend_sum_partition_by_plan_code_tier.[tier] IS NULL\n          )\n        )\n      WHERE\n        EXISTS(\n          SELECT\n            1\n          FROM orders AS orders\n          WHERE\n            customers.id = orders.customer_id AND orders.status = 'ok'\n        )\n      GROUP BY\n        customers.tier,\n        customers.plan_code,\n        customers.tier\n    ) AS _base\n    GROUP BY\n      _base.[customers___tier]\n  ) AS _stage_inner\n)\nSELECT\n  customers.tier AS [customers___tier],\n  SUM(\n    customers.spend * _cm_avg_spend_sum_partition_by_plan_code_tier.[avg_spend_sum_partition_by_plan_code_tier]\n  ) AS [customers___m]\nFROM customers AS customers\nLEFT JOIN _cm_avg_spend_sum_partition_by_plan_code_tier\n  ON (\n    customers.tier = _cm_avg_spend_sum_partition_by_plan_code_tier.[tier]\n    OR (\n      customers.tier IS NULL\n      AND _cm_avg_spend_sum_partition_by_plan_code_tier.[tier] IS NULL\n    )\n  )\nWHERE\n  EXISTS(\n    SELECT\n      1\n    FROM orders AS orders\n    WHERE\n      customers.id = orders.customer_id AND orders.status = 'ok'\n  )\nGROUP BY\n  customers.tier"
}
