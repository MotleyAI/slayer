{
  "lifted/cross_model_partition::bigquery": "WITH _base AS (\n  SELECT\n    orders.status AS `orders___status`\n  FROM orders AS orders\n  GROUP BY\n    orders.status\n), _cm_orders__amount_customers_discount_sum_partition_by_status AS (\n  SELECT\n    orders.status AS `orders___status`,\n    SUM(orders.amount - customers.discount) AS `orders___m`\n  FROM orders AS orders\n  LEFT JOIN customers AS customers\n    ON orders.customer_id = customers.id\n  GROUP BY\n    orders.status\n)\nSELECT\n  _base.`orders___status`,\n  _cm_orders__amount_customers_discount_sum_partition_by_status.`orders___m`\nFROM _base\nLEFT JOIN _cm_orders__amount_customers_discount_sum_partition_by_status\n  ON _base.`orders___status` IS NOT DISTINCT FROM _cm_orders__amount_customers_discount_sum_partition_by_status.`orders___status`",
  "lifted/cross_model_partition::duckdb": "WITH _base AS (\n  SELECT\n    orders.status AS \"orders.status\"\n  FROM orders AS orders\n  GROUP BY\n    orders.status\n), _cm_orders__amount_customers_discount_sum_partition_by_status AS (\n  SELECT\n    orders.status AS \"orders.status\",\n    SUM(orders.amount - customers.discount) AS \"orders.m\"\n  FROM orders AS orders\n  LEFT JOIN customers AS customers\n    ON orders.customer_id = customers.id\n  GROUP BY\n    orders.status\n)\nSELECT\n  _base.\"orders.status\",\n  _cm_orders__amount_customers_discount_sum_partition_by_status.\"orders.m\"\nFROM _base\nLEFT JOIN _cm_orders__amount_customers_discount_sum_partition_by_status\n  ON _base.\"orders.status\" IS NOT DISTINCT FROM _cm_orders__amount_customers_discount_sum_partition_by_status.\"orders.status\"",
  "lifted/cross_model_partition::mysql": "WITH _base AS (\n  SELECT\n    orders.status AS `orders.status`\n  FROM orders AS orders\n  GROUP BY\n    orders.status\n), _cm_orders__amount_customers_discount_sum_partition_by_status AS (\n  SELECT\n    orders.status AS `orders.status`,\n    SUM(orders.amount - customers.discount) AS `orders.m`\n  FROM orders AS orders\n  LEFT JOIN customers AS customers\n    ON orders.customer_id = customers.id\n  GROUP BY\n    orders.status\n)\nSELECT\n  _base.`orders.status`,\n  _cm_orders__amount_customers_discount_sum_partition_by_status.`orders.m`\nFROM _base\nLEFT JOIN _cm_orders__amount_customers_discount_sum_partition_by_status\n  ON _base.`orders.status` <=> _cm_orders__amount_customers_discount_sum_partition_by_status.`orders.status`",
  "lifted/cross_model_partition::postgres": "WITH _base AS (\n  SELECT\n    orders.status AS \"orders.status\"\n  FROM orders AS orders\n  GROUP BY\n    orders.status\n), _cm_orders__amount_customers_discount_sum_partition_by_status AS (\n  SELECT\n    orders.status AS \"orders.status\",\n    SUM(orders.amount - customers.discount) AS \"orders.m\"\n  FROM orders AS orders\n  LEFT JOIN customers AS customers\n    ON orders.customer_id = customers.id\n  GROUP BY\n    orders.status\n)\nSELECT\n  _base.\"orders.status\",\n  _cm_orders__amount_customers_discount_sum_partition_by_status.\"orders.m\"\nFROM _base\nLEFT JOIN _cm_orders__amount_customers_discount_sum_partition_by_status\n  ON _base.\"orders.status\" IS NOT DISTINCT FROM _cm_orders__amount_customers_discount_sum_partition_by_status.\"orders.status\"",
  "lifted/cross_model_partition::snowflake": "WITH _base AS (\n  SELECT\n    orders.status AS \"orders.status\"\n  FROM orders AS orders\n  GROUP BY\n    orders.status\n), _cm_orders__amount_customers_discount_sum_partition_by_status AS (\n  SELECT\n    orders.status AS \"orders.status\",\n    SUM(orders.amount - customers.discount) AS \"orders.m\"\n  FROM orders AS orders\n  LEFT JOIN customers AS customers\n    ON orders.customer_id = customers.id\n  GROUP BY\n    orders.status\n)\nSELECT\n  _base.\"orders.status\",\n  _cm_orders__amount_customers_discount_sum_partition_by_status.\"orders.m\"\nFROM _base\nLEFT JOIN _cm_orders__amount_customers_discount_sum_partition_by_status\n  ON _base.\"orders.status\" IS NOT DISTINCT FROM _cm_orders__amount_customers_discount_sum_partition_by_status.\"orders.status\"",
  "lifted/cross_model_partition::sqlite": "WITH _base AS (\n  SELECT\n    orders.status AS \"orders.status\"\n  FROM orders AS orders\n  GROUP BY\n    orders.status\n), _cm_orders__amount_customers_discount_sum_partition_by_status AS (\n  SELECT\n    orders.status AS \"orders.status\",\n    SUM(orders.amount - customers.discount) AS \"orders.m\"\n  FROM orders AS orders\n  LEFT JOIN customers AS customers\n    ON orders.customer_id = customers.id\n  GROUP BY\n    orders.status\n)\nSELECT\n  _base.\"orders.status\",\n  _cm_orders__amount_customers_discount_sum_partition_by_status.\"orders.m\"\nFROM _base\nLEFT JOIN _cm_orders__amount_customers_discount_sum_partition_by_status\n  ON _base.\"orders.status\" IS _cm_orders__amount_customers_discount_sum_partition_by_status.\"orders.status\"",
  "lifted/cross_model_partition::tsql": "WITH _base AS (\n  SELECT\n    orders.status AS [orders___status]\n  FROM orders AS orders\n  GROUP BY\n    orders.status\n), _cm_orders__amount_customers_discount_sum_partition_by_status AS (\n  SELECT\n    orders.status AS [orders___status],\n    SUM(orders.amount - customers.discount) AS [orders___m]\n  FROM orders AS orders\n  LEFT JOIN customers AS customers\n    ON orders.customer_id = customers.id\n  GROUP BY\n    orders.status\n)\nSELECT\n  _base.[orders___status],\n  _cm_orders__amount_customers_discount_sum_partition_by_status.[orders___m]\nFROM _base\nLEFT JOIN _cm_orders__amount_customers_discount_sum_partition_by_status\n  ON (\n    _base.[orders___status] = _cm_orders__amount_customers_discount_sum_partition_by_status.[orders___status]\n    OR (\n      _base.[orders___status] IS NULL\n      AND _cm_orders__amount_customers_discount_sum_partition_by_status.[orders___status] IS NULL\n    )\n  )",
  "lifted/filtered_leaf_joined::bigquery": "WITH _base AS (\n  SELECT\n    orders.status AS `orders___status`\n  FROM orders AS orders\n  GROUP BY\n    orders.status\n), _cm_orders__amount_customers_north_spend_sum AS (\n  SELECT\n    orders.status AS `orders___status`,\n    SUM(\n      orders.amount - CASE WHEN customers__regions.name = 'North' THEN customers.spend END\n    ) AS `orders___m`\n  FROM orders AS orders\n  LEFT JOIN customers AS customers\n    ON orders.customer_id = customers.id\n  LEFT JOIN regions AS customers__regions\n    ON customers.region_id = customers__regions.id\n  GROUP BY\n    orders.status\n)\nSELECT\n  _base.`orders___status`,\n  _cm_orders__amount_customers_north_spend_sum.`orders___m`\nFROM _base\nLEFT JOIN _cm_orders__amount_customers_north_spend_sum\n  ON _base.`orders___status` IS NOT DISTINCT FROM _cm_orders__amount_customers_north_spend_sum.`orders___status`",
  "lifted/filtered_leaf_joined::duckdb": "WITH _base AS (\n  SELECT\n    orders.status AS \"orders.status\"\n  FROM orders AS orders\n  GROUP BY\n    orders.status\n), _cm_orders__amount_customers_north_spend_sum AS (\n  SELECT\n    orders.status AS \"orders.status\",\n    SUM(\n      orders.amount - CASE WHEN customers__regions.name = 'North' THEN customers.spend END\n    ) AS \"orders.m\"\n  FROM orders AS orders\n  LEFT JOIN customers AS customers\n    ON orders.customer_id = customers.id\n  LEFT JOIN regions AS customers__regions\n    ON customers.region_id = customers__regions.id\n  GROUP BY\n    orders.status\n)\nSELECT\n  _base.\"orders.status\",\n  _cm_orders__amount_customers_north_spend_sum.\"orders.m\"\nFROM _base\nLEFT JOIN _cm_orders__amount_customers_north_spend_sum\n  ON _base.\"orders.status\" IS NOT DISTINCT FROM _cm_orders__amount_customers_north_spend_sum.\"orders.status\"",
  "lifted/filtered_leaf_joined::mysql": "WITH _base AS (\n  SELECT\n    orders.status AS `orders.status`\n  FROM orders AS orders\n  GROUP BY\n    orders.status\n), _cm_orders__amount_customers_north_spend_sum AS (\n  SELECT\n    orders.status AS `orders.status`,\n    SUM(\n      orders.amount - CASE WHEN customers__regions.name = 'North' THEN customers.spend END\n    ) AS `orders.m`\n  FROM orders AS orders\n  LEFT JOIN customers AS customers\n    ON orders.customer_id = customers.id\n  LEFT JOIN regions AS customers__regions\n    ON customers.region_id = customers__regions.id\n  GROUP BY\n    orders.status\n)\nSELECT\n  _base.`orders.status`,\n  _cm_orders__amount_customers_north_spend_sum.`orders.m`\nFROM _base\nLEFT JOIN _cm_orders__amount_customers_north_spend_sum\n  ON _base.`orders.status` <=> _cm_orders__amount_customers_north_spend_sum.`orders.status`",
  "lifted/filtered_leaf_joined::postgres": "WITH _base AS (\n  SELECT\n    orders.status AS \"orders.status\"\n  FROM orders AS orders\n  GROUP BY\n    orders.status\n), _cm_orders__amount_customers_north_spend_sum AS (\n  SELECT\n    orders.status AS \"orders.status\",\n    SUM(\n      orders.amount - CASE WHEN customers__regions.name = 'North' THEN customers.spend END\n    ) AS \"orders.m\"\n  FROM orders AS orders\n  LEFT JOIN customers AS customers\n    ON orders.customer_id = customers.id\n  LEFT JOIN regions AS customers__regions\n    ON customers.region_id = customers__regions.id\n  GROUP BY\n    orders.status\n)\nSELECT\n  _base.\"orders.status\",\n  _cm_orders__amount_customers_north_spend_sum.\"orders.m\"\nFROM _base\nLEFT JOIN _cm_orders__amount_customers_north_spend_sum\n  ON _base.\"orders.status\" IS NOT DISTINCT FROM _cm_orders__amount_customers_north_spend_sum.\"orders.status\"",
  "lifted/filtered_leaf_joined::snowflake": "WITH _base AS (\n  SELECT\n    orders.status AS \"orders.status\"\n  FROM orders AS orders\n  GROUP BY\n    orders.status\n), _cm_orders__amount_customers_north_spend_sum AS (\n  SELECT\n    orders.status AS \"orders.status\",\n    SUM(\n      orders.amount - CASE WHEN customers__regions.name = 'North' THEN customers.spend END\n    ) AS \"orders.m\"\n  FROM orders AS orders\n  LEFT JOIN customers AS customers\n    ON orders.customer_id = customers.id\n  LEFT JOIN regions AS customers__regions\n    ON customers.region_id = customers__regions.id\n  GROUP BY\n    orders.status\n)\nSELECT\n  _base.\"orders.status\",\n  _cm_orders__amount_customers_north_spend_sum.\"orders.m\"\nFROM _base\nLEFT JOIN _cm_orders__amount_customers_north_spend_sum\n  ON _base.\"orders.status\" IS NOT DISTINCT FROM _cm_orders__amount_customers_north_spend_sum.\"orders.status\"",
  "lifted/filtered_leaf_joined::sqlite": "WITH _base AS (\n  SELECT\n    orders.status AS \"orders.status\"\n  FROM orders AS orders\n  GROUP BY\n    orders.status\n), _cm_orders__amount_customers_north_spend_sum AS (\n  SELECT\n    orders.status AS \"orders.status\",\n    SUM(\n      orders.amount - CASE WHEN customers__regions.name = 'North' THEN customers.spend END\n    ) AS \"orders.m\"\n  FROM orders AS orders\n  LEFT JOIN customers AS customers\n    ON orders.customer_id = customers.id\n  LEFT JOIN regions AS customers__regions\n    ON customers.region_id = customers__regions.id\n  GROUP BY\n    orders.status\n)\nSELECT\n  _base.\"orders.status\",\n  _cm_orders__amount_customers_north_spend_sum.\"orders.m\"\nFROM _base\nLEFT JOIN _cm_orders__amount_customers_north_spend_sum\n  ON _base.\"orders.status\" IS _cm_orders__amount_customers_north_spend_sum.\"orders.status\"",
  "lifted/filtered_leaf_joined::tsql": "WITH _base AS (\n  SELECT\n    orders.status AS [orders___status]\n  FROM orders AS orders\n  GROUP BY\n    orders.status\n), _cm_orders__amount_customers_north_spend_sum AS (\n  SELECT\n    orders.status AS [orders___status],\n    SUM(\n      orders.amount - CASE WHEN customers__regions.name = 'North' THEN customers.spend END\n    ) AS [orders___m]\n  FROM orders AS orders\n  LEFT JOIN customers AS customers\n    ON orders.customer_id = customers.id\n  LEFT JOIN regions AS customers__regions\n    ON customers.region_id = customers__regions.id\n  GROUP BY\n    orders.status\n)\nSELECT\n  _base.[orders___status],\n  _cm_orders__amount_customers_north_spend_sum.[orders___m]\nFROM _base\nLEFT JOIN _cm_orders__amount_customers_north_spend_sum\n  ON (\n    _base.[orders___status] = _cm_orders__amount_customers_north_spend_sum.[orders___status]\n    OR (\n      _base.[orders___status] IS NULL\n      AND _cm_orders__amount_customers_north_spend_sum.[orders___status] IS NULL\n    )\n  )",
  "lifted/filtered_operand::bigquery": "SELECT\n  sales.region AS `sales___region`,\n  SUM(CASE WHEN sales.product = 'Q' THEN sales.amount END - 1) AS `sales___m`\nFROM sales AS sales\nGROUP BY\n  sales.region",
  "lifted/filtered_operand::duckdb": "SELECT\n  sales.region AS \"sales.region\",\n  SUM(CASE WHEN sales.product = 'Q' THEN sales.amount END - 1) AS \"sales.m\"\nFROM sales AS sales\nGROUP BY\n  sales.region",
  "lifted/filtered_operand::mysql": "SELECT\n  sales.region AS `sales.region`,\n  SUM(CASE WHEN sales.product = 'Q' THEN sales.amount END - 1) AS `sales.m`\nFROM sales AS sales\nGROUP BY\n  sales.region",
  "lifted/filtered_operand::postgres": "SELECT\n  sales.region AS \"sales.region\",\n  SUM(CASE WHEN sales.product = 'Q' THEN sales.amount END - 1) AS \"sales.m\"\nFROM sales AS sales\nGROUP BY\n  sales.region",
  "lifted/filtered_operand::snowflake": "SELECT\n  sales.region AS \"sales.region\",\n  SUM(CASE WHEN sales.product = 'Q' THEN sales.amount END - 1) AS \"sales.m\"\nFROM sales AS sales\nGROUP BY\n  sales.region",
  "lifted/filtered_operand::sqlite": "SELECT\n  sales.region AS \"sales.region\",\n  SUM(CASE WHEN sales.product = 'Q' THEN sales.amount END - 1) AS \"sales.m\"\nFROM sales AS sales\nGROUP BY\n  sales.region",
  "lifted/filtered_operand::tsql": "SELECT\n  sales.region AS [sales___region],\n  SUM(CASE WHEN sales.product = 'Q' THEN sales.amount END - 1) AS [sales___m]\nFROM sales AS sales\nGROUP BY\n  sales.region",
  "lifted/host_homed_arith::bigquery": "WITH _base AS (\n  SELECT\n    orders.status AS `orders___status`\n  FROM orders AS orders\n  GROUP BY\n    orders.status\n), _cm_orders__amount_customers_discount_sum AS (\n  SELECT\n    orders.status AS `orders___status`,\n    SUM(orders.amount - customers.discount) AS `orders___m`\n  FROM orders AS orders\n  LEFT JOIN customers AS customers\n    ON orders.customer_id = customers.id\n  GROUP BY\n    orders.status\n)\nSELECT\n  _base.`orders___status`,\n  _cm_orders__amount_customers_discount_sum.`orders___m`\nFROM _base\nLEFT JOIN _cm_orders__amount_customers_discount_sum\n  ON _base.`orders___status` IS NOT DISTINCT FROM _cm_orders__amount_customers_discount_sum.`orders___status`",
  "lifted/host_homed_arith::duckdb": "WITH _base AS (\n  SELECT\n    orders.status AS \"orders.status\"\n  FROM orders AS orders\n  GROUP BY\n    orders.status\n), _cm_orders__amount_customers_discount_sum AS (\n  SELECT\n    orders.status AS \"orders.status\",\n    SUM(orders.amount - customers.discount) AS \"orders.m\"\n  FROM orders AS orders\n  LEFT JOIN customers AS customers\n    ON orders.customer_id = customers.id\n  GROUP BY\n    orders.status\n)\nSELECT\n  _base.\"orders.status\",\n  _cm_orders__amount_customers_discount_sum.\"orders.m\"\nFROM _base\nLEFT JOIN _cm_orders__amount_customers_discount_sum\n  ON _base.\"orders.status\" IS NOT DISTINCT FROM _cm_orders__amount_customers_discount_sum.\"orders.status\"",
  "lifted/host_homed_arith::mysql": "WITH _base AS (\n  SELECT\n    orders.status AS `orders.status`\n  FROM orders AS orders\n  GROUP BY\n    orders.status\n), _cm_orders__amount_customers_discount_sum AS (\n  SELECT\n    orders.status AS `orders.status`,\n    SUM(orders.amount - customers.discount) AS `orders.m`\n  FROM orders AS orders\n  LEFT JOIN customers AS customers\n    ON orders.customer_id = customers.id\n  GROUP BY\n    orders.status\n)\nSELECT\n  _base.`orders.status`,\n  _cm_orders__amount_customers_discount_sum.`orders.m`\nFROM _base\nLEFT JOIN _cm_orders__amount_customers_discount_sum\n  ON _base.`orders.status` <=> _cm_orders__amount_customers_discount_sum.`orders.status`",
  "lifted/host_homed_arith::postgres": "WITH _base AS (\n  SELECT\n    orders.status AS \"orders.status\"\n  FROM orders AS orders\n  GROUP BY\n    orders.status\n), _cm_orders__amount_customers_discount_sum AS (\n  SELECT\n    orders.status AS \"orders.status\",\n    SUM(orders.amount - customers.discount) AS \"orders.m\"\n  FROM orders AS orders\n  LEFT JOIN customers AS customers\n    ON orders.customer_id = customers.id\n  GROUP BY\n    orders.status\n)\nSELECT\n  _base.\"orders.status\",\n  _cm_orders__amount_customers_discount_sum.\"orders.m\"\nFROM _base\nLEFT JOIN _cm_orders__amount_customers_discount_sum\n  ON _base.\"orders.status\" IS NOT DISTINCT FROM _cm_orders__amount_customers_discount_sum.\"orders.status\"",
  "lifted/host_homed_arith::snowflake": "WITH _base AS (\n  SELECT\n    orders.status AS \"orders.status\"\n  FROM orders AS orders\n  GROUP BY\n    orders.status\n), _cm_orders__amount_customers_discount_sum AS (\n  SELECT\n    orders.status AS \"orders.status\",\n    SUM(orders.amount - customers.discount) AS \"orders.m\"\n  FROM orders AS orders\n  LEFT JOIN customers AS customers\n    ON orders.customer_id = customers.id\n  GROUP BY\n    orders.status\n)\nSELECT\n  _base.\"orders.status\",\n  _cm_orders__amount_customers_discount_sum.\"orders.m\"\nFROM _base\nLEFT JOIN _cm_orders__amount_customers_discount_sum\n  ON _base.\"orders.status\" IS NOT DISTINCT FROM _cm_orders__amount_customers_discount_sum.\"orders.status\"",
  "lifted/host_homed_arith::sqlite": "WITH _base AS (\n  SELECT\n    orders.status AS \"orders.status\"\n  FROM orders AS orders\n  GROUP BY\n    orders.status\n), _cm_orders__amount_customers_discount_sum AS (\n  SELECT\n    orders.status AS \"orders.status\",\n    SUM(orders.amount - customers.discount) AS \"orders.m\"\n  FROM orders AS orders\n  LEFT JOIN customers AS customers\n    ON orders.customer_id = customers.id\n  GROUP BY\n    orders.status\n)\nSELECT\n  _base.\"orders.status\",\n  _cm_orders__amount_customers_discount_sum.\"orders.m\"\nFROM _base\nLEFT JOIN _cm_orders__amount_customers_discount_sum\n  ON _base.\"orders.status\" IS _cm_orders__amount_customers_discount_sum.\"orders.status\"",
  "lifted/host_homed_arith::tsql": "WITH _base AS (\n  SELECT\n    orders.status AS [orders___status]\n  FROM orders AS orders\n  GROUP BY\n    orders.status\n), _cm_orders__amount_customers_discount_sum AS (\n  SELECT\n    orders.status AS [orders___status],\n    SUM(orders.amount - customers.discount) AS [orders___m]\n  FROM orders AS orders\n  LEFT JOIN customers AS customers\n    ON orders.customer_id = customers.id\n  GROUP BY\n    orders.status\n)\nSELECT\n  _base.[orders___status],\n  _cm_orders__amount_customers_discount_sum.[orders___m]\nFROM _base\nLEFT JOIN _cm_orders__amount_customers_discount_sum\n  ON (\n    _base.[orders___status] = _cm_orders__amount_customers_discount_sum.[orders___status]\n    OR (\n      _base.[orders___status] IS NULL\n      AND _cm_orders__amount_customers_discount_sum.[orders___status] IS NULL\n    )\n  )",
  "lifted/mixed_collapse::bigquery": "WITH base_2 AS (\n  SELECT\n    monthly.region AS `monthly___region`,\n    DATE_TRUNC(monthly.ordered_at, MONTH) AS `monthly___ordered_at_month`,\n    SUM(monthly.amount) AS `monthly___amount_sum_partition_by_ordered_at_region`\n  FROM monthly AS monthly\n  GROUP BY\n    monthly.region,\n    DATE_TRUNC(monthly.ordered_at, MONTH)\n), step1 AS (\n  SELECT\n    `monthly___region`,\n    `monthly___ordered_at_month`,\n    `monthly___amount_sum_partition_by_ordered_at_region`,\n    FIRST_VALUE(`monthly___amount_sum_partition_by_ordered_at_region`) OVER (\n      PARTITION BY `monthly___region`\n      ORDER BY `monthly___ordered_at_month` DESC\n      ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING\n    ) AS `monthly___last`\n  FROM base_2\n), _cm_last AS (\n  SELECT\n    `_stage_inner`.`monthly___region` AS `region`,\n    `_stage_inner`.`monthly___ordered_at_month` AS `ordered_at_month`,\n    `_stage_inner`.`monthly___last` AS `last`\n  FROM (\n    SELECT\n      `monthly___region`,\n      `monthly___ordered_at_month`,\n      `monthly___last`\n    FROM (\n      SELECT\n        `monthly___region`,\n        `monthly___ordered_at_month`,\n        `monthly___amount_sum_partition_by_ordered_at_region`,\n        `monthly___last`\n      FROM step1\n    ) AS _outer\n  ) AS _stage_inner\n), _cm_max_last AS (\n  SELECT\n    _stage_inner.`monthly___region` AS `region`,\n    _stage_inner.`monthly___max_last` AS `max_last`\n  FROM (\n    SELECT\n      `_base`.`monthly___region` AS `monthly___region`,\n      MAX(_base._v) AS `monthly___max_last`\n    FROM (\n      SELECT\n        monthly.region AS `monthly___region`,\n        monthly.region AS _ek0,\n        DATE_TRUNC(monthly.ordered_at, MONTH) AS _ek1,\n        MAX(_cm_last.`last`) AS _v\n      FROM monthly AS monthly\n      LEFT JOIN _cm_last\n        ON monthly.region IS NOT DISTINCT FROM _cm_last.`region`\n        AND DATE_TRUNC(monthly.ordered_at, MONTH) IS NOT DISTINCT FROM _cm_last.`ordered_at_month`\n      GROUP BY\n        monthly.region,\n        monthly.region,\n        DATE_TRUNC(monthly.ordered_at, MONTH)\n    ) AS _base\n    GROUP BY\n      `_base`.`monthly___region`\n  ) AS _stage_inner\n)\nSELECT\n  DATE_TRUNC(monthly.ordered_at, MONTH) AS `monthly___ordered_at`,\n  SUM(monthly.amount * _cm_max_last.`max_last`) AS `monthly___m`\nFROM monthly AS monthly\nLEFT JOIN _cm_max_last\n  ON monthly.region IS NOT DISTINCT FROM _cm_max_last.`region`\nGROUP BY\n  DATE_TRUNC(monthly.ordered_at, MONTH)",
  "lifted/mixed_collapse::duckdb": "WITH base_2 AS (\n  SELECT\n    monthly.region AS \"monthly.region\",\n    DATE_TRUNC('MONTH', monthly.ordered_at) AS \"monthly.ordered_at_month\",\n    SUM(monthly.amount) AS \"monthly.amount_sum_partition_by_ordered_at_region\"\n  FROM monthly AS monthly\n  GROUP BY\n    monthly.region,\n    DATE_TRUNC('MONTH', monthly.ordered_at)\n), step1 AS (\n  SELECT\n    \"monthly.region\",\n    \"monthly.ordered_at_month\",\n    \"monthly.amount_sum_partition_by_ordered_at_region\",\n    FIRST_VALUE(\"monthly.amount_sum_partition_by_ordered_at_region\") OVER (\n      PARTITION BY \"monthly.region\"\n      ORDER BY \"monthly.ordered_at_month\" DESC\n      ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING\n    ) AS \"monthly.last\"\n  FROM base_2\n), _cm_last AS (\n  SELECT\n    _stage_inner.\"monthly.region\" AS \"region\",\n    _stage_inner.\"monthly.ordered_at_month\" AS \"ordered_at_month\",\n    _stage_inner.\"monthly.last\" AS \"last\"\n  FROM (\n    SELECT\n      \"monthly.region\",\n      \"monthly.ordered_at_month\",\n      \"monthly.last\"\n    FROM (\n      SELECT\n        \"monthly.region\",\n        \"monthly.ordered_at_month\",\n        \"monthly.amount_sum_partition_by_ordered_at_region\",\n        \"monthly.last\"\n      FROM step1\n    ) AS _outer\n  ) AS _stage_inner\n), _cm_max_last AS (\n  SELECT\n    _stage_inner.\"monthly.region\" AS \"region\",\n    _stage_inner.\"monthly.max_last\" AS \"max_last\"\n  FROM (\n    SELECT\n      _base.\"monthly.region\" AS \"monthly.region\",\n      MAX(_base._v) AS \"monthly.max_last\"\n    FROM (\n      SELECT\n        monthly.region AS \"monthly.region\",\n        monthly.region AS _ek0,\n        DATE_TRUNC('MONTH', monthly.ordered_at) AS _ek1,\n        MAX(_cm_last.\"last\") AS _v\n      FROM monthly AS monthly\n      LEFT JOIN _cm_last\n        ON monthly.region IS NOT DISTINCT FROM _cm_last.\"region\"\n        AND DATE_TRUNC('MONTH', monthly.ordered_at) IS NOT DISTINCT FROM _cm_last.\"ordered_at_month\"\n      GROUP BY\n        monthly.region,\n        monthly.region,\n        DATE_TRUNC('MONTH', monthly.ordered_at)\n    ) AS _base\n    GROUP BY\n      _base.\"monthly.region\"\n  ) AS _stage_inner\n)\nSELECT\n  DATE_TRUNC('MONTH', monthly.ordered_at) AS \"monthly.ordered_at\",\n  SUM(monthly.amount * _cm_max_last.\"max_last\") AS \"monthly.m\"\nFROM monthly AS monthly\nLEFT JOIN _cm_max_last\n  ON monthly.region IS NOT DISTINCT FROM _cm_max_last.\"region\"\nGROUP BY\n  DATE_TRUNC('MONTH', monthly.ordered_at)",
  "lifted/mixed_collapse::mysql": "WITH base_2 AS (\n  SELECT\n    monthly.region AS `monthly.region`,\n    STR_TO_DATE(CONCAT(YEAR(monthly.ordered_at), ' ', MONTH(monthly.ordered_at), ' 1'), '%Y %c %e') AS `monthly.ordered_at_month`,\n    SUM(monthly.amount) AS `monthly.amount_sum_partition_by_ordered_at_region`\n  FROM monthly AS monthly\n  GROUP BY\n    monthly.region,\n    STR_TO_DATE(CONCAT(YEAR(monthly.ordered_at), ' ', MONTH(monthly.ordered_at), ' 1'), '%Y %c %e')\n), step1 AS (\n  SELECT\n    `monthly.region`,\n    `monthly.ordered_at_month`,\n    `monthly.amount_sum_partition_by_ordered_at_region`,\n    FIRST_VALUE(`monthly.amount_sum_partition_by_ordered_at_region`) OVER (\n      PARTITION BY `monthly.region`\n      ORDER BY `monthly.ordered_at_month` DESC\n      ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING\n    ) AS `monthly.last`\n  FROM base_2\n), _cm_last AS (\n  SELECT\n    _stage_inner.`monthly.region` AS `region`,\n    _stage_inner.`monthly.ordered_at_month` AS `ordered_at_month`,\n    _stage_inner.`monthly.last` AS `last`\n  FROM (\n    SELECT\n      `monthly.region`,\n      `monthly.ordered_at_month`,\n      `monthly.last`\n    FROM (\n      SELECT\n        `monthly.region`,\n        `monthly.ordered_at_month`,\n        `monthly.amount_sum_partition_by_ordered_at_region`,\n        `monthly.last`\n      FROM step1\n    ) AS _outer\n  ) AS _stage_inner\n), _cm_max_last AS (\n  SELECT\n    _stage_inner.`monthly.region` AS `region`,\n    _stage_inner.`monthly.max_last` AS `max_last`\n  FROM (\n    SELECT\n      _base.`monthly.region` AS `monthly.region`,\n      MAX(_base._v) AS `monthly.max_last`\n    FROM (\n      SELECT\n        monthly.region AS `monthly.region`,\n        monthly.region AS _ek0,\n        STR_TO_DATE(CONCAT(YEAR(monthly.ordered_at), ' ', MONTH(monthly.ordered_at), ' 1'), '%Y %c %e') AS _ek1,\n        MAX(_cm_last.`last`) AS _v\n      FROM monthly AS monthly\n      LEFT JOIN _cm_last\n        ON monthly.region <=> _cm_last.`region`\n        AND STR_TO_DATE(CONCAT(YEAR(monthly.ordered_at), ' ', MONTH(monthly.ordered_at), ' 1'), '%Y %c %e') <=> _cm_last.`ordered_at_month`\n      GROUP BY\n        monthly.region,\n        monthly.region,\n        STR_TO_DATE(CONCAT(YEAR(monthly.ordered_at), ' ', MONTH(monthly.ordered_at), ' 1'), '%Y %c %e')\n    ) AS _base\n    GROUP BY\n      _base.`monthly.region`\n  ) AS _stage_inner\n)\nSELECT\n  STR_TO_DATE(CONCAT(YEAR(monthly.ordered_at), ' ', MONTH(monthly.ordered_at), ' 1'), '%Y %c %e') AS `monthly.ordered_at`,\n  SUM(monthly.amount * _cm_max_last.`max_last`) AS `monthly.m`\nFROM monthly AS monthly\nLEFT JOIN _cm_max_last\n  ON monthly.region <=> _cm_max_last.`region`\nGROUP BY\n  STR_TO_DATE(CONCAT(YEAR(monthly.ordered_at), ' ', MONTH(monthly.ordered_at), ' 1'), '%Y %c %e')",
  "lifted/mixed_collapse::postgres": "WITH base_2 AS (\n  SELECT\n    monthly.region AS \"monthly.region\",\n    DATE_TRUNC('MONTH', monthly.ordered_at) AS \"monthly.ordered_at_month\",\n    SUM(monthly.amount) AS \"monthly.amount_sum_partition_by_ordered_at_region\"\n  FROM monthly AS monthly\n  GROUP BY\n    monthly.region,\n    DATE_TRUNC('MONTH', monthly.ordered_at)\n), step1 AS (\n  SELECT\n    \"monthly.region\",\n    \"monthly.ordered_at_month\",\n    \"monthly.amount_sum_partition_by_ordered_at_region\",\n    FIRST_VALUE(\"monthly.amount_sum_partition_by_ordered_at_region\") OVER (\n      PARTITION BY \"monthly.region\"\n      ORDER BY \"monthly.ordered_at_month\" DESC\n      ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING\n    ) AS \"monthly.last\"\n  FROM base_2\n), _cm_last AS (\n  SELECT\n    _stage_inner.\"monthly.region\" AS \"region\",\n    _stage_inner.\"monthly.ordered_at_month\" AS \"ordered_at_month\",\n    _stage_inner.\"monthly.last\" AS \"last\"\n  FROM (\n    SELECT\n      \"monthly.region\",\n      \"monthly.ordered_at_month\",\n      \"monthly.last\"\n    FROM (\n      SELECT\n        \"monthly.region\",\n        \"monthly.ordered_at_month\",\n        \"monthly.amount_sum_partition_by_ordered_at_region\",\n        \"monthly.last\"\n      FROM step1\n    ) AS _outer\n  ) AS _stage_inner\n), _cm_max_last AS (\n  SELECT\n    _stage_inner.\"monthly.region\" AS \"region\",\n    _stage_inner.\"monthly.max_last\" AS \"max_last\"\n  FROM (\n    SELECT\n      _base.\"monthly.region\" AS \"monthly.region\",\n      MAX(_base._v) AS \"monthly.max_last\"\n    FROM (\n      SELECT\n        monthly.region AS \"monthly.region\",\n        monthly.region AS _ek0,\n        DATE_TRUNC('MONTH', monthly.ordered_at) AS _ek1,\n        MAX(_cm_last.\"last\") AS _v\n      FROM monthly AS monthly\n      LEFT JOIN _cm_last\n        ON monthly.region IS NOT DISTINCT FROM _cm_last.\"region\"\n        AND DATE_TRUNC('MONTH', monthly.ordered_at) IS NOT DISTINCT FROM _cm_last.\"ordered_at_month\"\n      GROUP BY\n        monthly.region,\n        monthly.region,\n        DATE_TRUNC('MONTH', monthly.ordered_at)\n    ) AS _base\n    GROUP BY\n      _base.\"monthly.region\"\n  ) AS _stage_inner\n)\nSELECT\n  DATE_TRUNC('MONTH', monthly.ordered_at) AS \"monthly.ordered_at\",\n  SUM(monthly.amount * _cm_max_last.\"max_last\") AS \"monthly.m\"\nFROM monthly AS monthly\nLEFT JOIN _cm_max_last\n  ON monthly.region IS NOT DISTINCT FROM _cm_max_last.\"region\"\nGROUP BY\n  DATE_TRUNC('MONTH', monthly.ordered_at)",
  "lifted/mixed_collapse::snowflake": "WITH base_2 AS (\n  SELECT\n    monthly.region AS \"monthly.region\",\n    DATE_TRUNC('MONTH', monthly.ordered_at) AS \"monthly.ordered_at_month\",\n    SUM(monthly.amount) AS \"monthly.amount_sum_partition_by_ordered_at_region\"\n  FROM monthly AS monthly\n  GROUP BY\n    monthly.region,\n    DATE_TRUNC('MONTH', monthly.ordered_at)\n), step1 AS (\n  SELECT\n    \"monthly.region\",\n    \"monthly.ordered_at_month\",\n    \"monthly.amount_sum_partition_by_ordered_at_region\",\n    FIRST_VALUE(\"monthly.amount_sum_partition_by_ordered_at_region\") OVER (PARTITION BY \"monthly.region\" ORDER BY \"monthly.ordered_at_month\" DESC) AS \"monthly.last\"\n  FROM base_2\n), _cm_last AS (\n  SELECT\n    _stage_inner.\"monthly.region\" AS \"region\",\n    _stage_inner.\"monthly.ordered_at_month\" AS \"ordered_at_month\",\n    _stage_inner.\"monthly.last\" AS \"last\"\n  FROM (\n    SELECT\n      \"monthly.region\",\n      \"monthly.ordered_at_month\",\n      \"monthly.last\"\n    FROM (\n      SELECT\n        \"monthly.region\",\n        \"monthly.ordered_at_month\",\n        \"monthly.amount_sum_partition_by_ordered_at_region\",\n        \"monthly.last\"\n      FROM step1\n    ) AS _outer\n  ) AS _stage_inner\n), _cm_max_last AS (\n  SELECT\n    _stage_inner.\"monthly.region\" AS \"region\",\n    _stage_inner.\"monthly.max_last\" AS \"max_last\"\n  FROM (\n    SELECT\n      _base.\"monthly.region\" AS \"monthly.region\",\n      MAX(_base._v) AS \"monthly.max_last\"\n    FROM (\n      SELECT\n        monthly.region AS \"monthly.region\",\n        monthly.region AS _ek0,\n        DATE_TRUNC('MONTH', monthly.ordered_at) AS _ek1,\n        MAX(_cm_last.\"last\") AS _v\n      FROM monthly AS monthly\n      LEFT JOIN _cm_last\n        ON monthly.region IS NOT DISTINCT FROM _cm_last.\"region\"\n        AND DATE_TRUNC('MONTH', monthly.ordered_at) IS NOT DISTINCT FROM _cm_last.\"ordered_at_month\"\n      GROUP BY\n        monthly.region,\n        monthly.region,\n        DATE_TRUNC('MONTH', monthly.ordered_at)\n    ) AS _base\n    GROUP BY\n      _base.\"monthly.region\"\n  ) AS _stage_inner\n)\nSELECT\n  DATE_TRUNC('MONTH', monthly.ordered_at) AS \"monthly.ordered_at\",\n  SUM(monthly.amount * _cm_max_last.\"max_last\") AS \"monthly.m\"\nFROM monthly AS monthly\nLEFT JOIN _cm_max_last\n  ON monthly.region IS NOT DISTINCT FROM _cm_max_last.\"region\"\nGROUP BY\n  DATE_TRUNC('MONTH', monthly.ordered_at)",
  "lifted/mixed_collapse::sqlite": "WITH base_2 AS (\n  SELECT\n    monthly.region AS \"monthly.region\",\n    STRFTIME('%Y-%m-01', monthly.ordered_at) AS \"monthly.ordered_at_month\",\n    SUM(monthly.amount) AS \"monthly.amount_sum_partition_by_ordered_at_region\"\n  FROM monthly AS monthly\n  GROUP BY\n    monthly.region,\n    STRFTIME('%Y-%m-01', monthly.ordered_at)\n), step1 AS (\n  SELECT\n    \"monthly.region\",\n    \"monthly.ordered_at_month\",\n    \"monthly.amount_sum_partition_by_ordered_at_region\",\n    FIRST_VALUE(\"monthly.amount_sum_partition_by_ordered_at_region\") OVER (\n      PARTITION BY \"monthly.region\"\n      ORDER BY \"monthly.ordered_at_month\" DESC\n      ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING\n    ) AS \"monthly.last\"\n  FROM base_2\n), _cm_last AS (\n  SELECT\n    _stage_inner.\"monthly.region\" AS \"region\",\n    _stage_inner.\"monthly.ordered_at_month\" AS \"ordered_at_month\",\n    _stage_inner.\"monthly.last\" AS \"last\"\n  FROM (\n    SELECT\n      \"monthly.region\",\n      \"monthly.ordered_at_month\",\n      \"monthly.last\"\n    FROM (\n      SELECT\n        \"monthly.region\",\n        \"monthly.ordered_at_month\",\n        \"monthly.amount_sum_partition_by_ordered_at_region\",\n        \"monthly.last\"\n      FROM step1\n    ) AS _outer\n  ) AS _stage_inner\n), _cm_max_last AS (\n  SELECT\n    _stage_inner.\"monthly.region\" AS \"region\",\n    _stage_inner.\"monthly.max_last\" AS \"max_last\"\n  FROM (\n    SELECT\n      _base.\"monthly.region\" AS \"monthly.region\",\n      MAX(_base._v) AS \"monthly.max_last\"\n    FROM (\n      SELECT\n        monthly.region AS \"monthly.region\",\n        monthly.region AS _ek0,\n        STRFTIME('%Y-%m-01', monthly.ordered_at) AS _ek1,\n        MAX(_cm_last.\"last\") AS _v\n      FROM monthly AS monthly\n      LEFT JOIN _cm_last\n        ON monthly.region IS _cm_last.\"region\"\n        AND STRFTIME('%Y-%m-01', monthly.ordered_at) IS _cm_last.\"ordered_at_month\"\n      GROUP BY\n        monthly.region,\n        monthly.region,\n        STRFTIME('%Y-%m-01', monthly.ordered_at)\n    ) AS _base\n    GROUP BY\n      _base.\"monthly.region\"\n  ) AS _stage_inner\n)\nSELECT\n  STRFTIME('%Y-%m-01', monthly.ordered_at) AS \"monthly.ordered_at\",\n  SUM(monthly.amount * _cm_max_last.\"max_last\") AS \"monthly.m\"\nFROM monthly AS monthly\nLEFT JOIN _cm_max_last\n  ON monthly.region IS _cm_max_last.\"region\"\nGROUP BY\n  STRFTIME('%Y-%m-01', monthly.ordered_at)",
  "lifted/mixed_collapse::tsql": "WITH base_2 AS (\n  SELECT\n    monthly.region AS [monthly___region],\n    DATETRUNC(MONTH, monthly.ordered_at) AS [monthly___ordered_at_month],\n    SUM(monthly.amount) AS [monthly___amount_sum_partition_by_ordered_at_region]\n  FROM monthly AS monthly\n  GROUP BY\n    monthly.region,\n    DATETRUNC(MONTH, monthly.ordered_at)\n), step1 AS (\n  SELECT\n    [monthly___region] AS [monthly___region],\n    [monthly___ordered_at_month] AS [monthly___ordered_at_month],\n    [monthly___amount_sum_partition_by_ordered_at_region] AS [monthly___amount_sum_partition_by_ordered_at_region],\n    FIRST_VALUE([monthly___amount_sum_partition_by_ordered_at_region]) OVER (\n      PARTITION BY [monthly___region]\n      ORDER BY [monthly___ordered_at_month] DESC\n      ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING\n    ) AS [monthly___last]\n  FROM base_2\n), _cm_last AS (\n  SELECT\n    _stage_inner.[monthly___region] AS [region],\n    _stage_inner.[monthly___ordered_at_month] AS [ordered_at_month],\n    _stage_inner.[monthly___last] AS [last]\n  FROM (\n    SELECT\n      [monthly___region] AS [monthly___region],\n      [monthly___ordered_at_month] AS [monthly___ordered_at_month],\n      [monthly___last] AS [monthly___last]\n    FROM (\n      SELECT\n        [monthly___region] AS [monthly___region],\n        [monthly___ordered_at_month] AS [monthly___ordered_at_month],\n        [monthly___amount_sum_partition_by_ordered_at_region] AS [monthly___amount_sum_partition_by_ordered_at_region],\n        [monthly___last] AS [monthly___last]\n      FROM step1\n    ) AS _outer\n  ) AS _stage_inner\n), _cm_max_last AS (\n  SELECT\n    _stage_inner.[monthly___region] AS [region],\n    _stage_inner.[monthly___max_last] AS [max_last]\n  FROM (\n    SELECT\n      _base.[monthly___region] AS [monthly___region],\n      MAX(_base._v) AS [monthly___max_last]\n    FROM (\n      SELECT\n        monthly.region AS [monthly___region],\n        monthly.region AS _ek0,\n        DATETRUNC(MONTH, monthly.ordered_at) AS _ek1,\n        MAX(_cm_last.[last]) AS _v\n      FROM monthly AS monthly\n      LEFT JOIN _cm_last\n        ON (\n          monthly.region = _cm_last.[region]\n          OR (\n            monthly.region IS NULL AND _cm_last.[region] IS NULL\n          )\n        )\n        AND (\n          DATETRUNC(MONTH, monthly.ordered_at) = _cm_last.[ordered_at_month]\n          OR (\n            DATETRUNC(MONTH, monthly.ordered_at) IS NULL\n            AND _cm_last.[ordered_at_month] IS NULL\n          )\n        )\n      GROUP BY\n        monthly.region,\n        monthly.region,\n        DATETRUNC(MONTH, monthly.ordered_at)\n    ) AS _base\n    GROUP BY\n      _base.[monthly___region]\n  ) AS _stage_inner\n)\nSELECT\n  DATETRUNC(month, monthly.ordered_at) AS [monthly___ordered_at],\n  SUM(monthly.amount * _cm_max_last.[max_last]) AS [monthly___m]\nFROM monthly AS monthly\nLEFT JOIN _cm_max_last\n  ON (\n    monthly.region = _cm_max_last.[region]\n    OR (\n      monthly.region IS NULL AND _cm_max_last.[region] IS NULL\n    )\n  )\nGROUP BY\n  DATETRUNC(month, monthly.ordered_at)",
  "lifted/mixed_reagg::bigquery": "WITH _cm_amount_sum_partition_by_ordered_at_region AS (\n  SELECT\n    `_stage_inner`.`monthly___region` AS `region`,\n    `_stage_inner`.`monthly___ordered_at_month` AS `ordered_at_month`,\n    `_stage_inner`.`monthly___amount_sum_partition_by_ordered_at_region` AS `amount_sum_partition_by_ordered_at_region`\n  FROM (\n    SELECT\n      monthly.region AS `monthly___region`,\n      DATE_TRUNC(monthly.ordered_at, MONTH) AS `monthly___ordered_at_month`,\n      CAST(SUM(monthly.amount) AS FLOAT64) AS `monthly___amount_sum_partition_by_ordered_at_region`\n    FROM monthly AS monthly\n    GROUP BY\n      monthly.region,\n      DATE_TRUNC(monthly.ordered_at, MONTH)\n  ) AS _stage_inner\n), _cm_min_amount_sum_partition_by_ordered_at_region AS (\n  SELECT\n    _stage_inner.`monthly___region` AS `region`,\n    _stage_inner.`monthly___min_amount_sum_partition_by_ordered_at_region` AS `min_amount_sum_partition_by_ordered_at_region`\n  FROM (\n    SELECT\n      `_base`.`monthly___region` AS `monthly___region`,\n      MIN(_base._v) AS `monthly___min_amount_sum_partition_by_ordered_at_region`\n    FROM (\n      SELECT\n        monthly.region AS `monthly___region`,\n        monthly.region AS _ek0,\n        DATE_TRUNC(monthly.ordered_at, MONTH) AS _ek1,\n        MAX(\n          _cm_amount_sum_partition_by_ordered_at_region.`amount_sum_partition_by_ordered_at_region`\n        ) AS _v\n      FROM monthly AS monthly\n      LEFT JOIN _cm_amount_sum_partition_by_ordered_at_region\n        ON monthly.region IS NOT DISTINCT FROM _cm_amount_sum_partition_by_ordered_at_region.`region`\n        AND DATE_TRUNC(monthly.ordered_at, MONTH) IS NOT DISTINCT FROM _cm_amount_sum_partition_by_ordered_at_region.`ordered_at_month`\n      GROUP BY\n        monthly.region,\n        monthly.region,\n        DATE_TRUNC(monthly.ordered_at, MONTH)\n    ) AS _base\n    GROUP BY\n      `_base`.`monthly___region`\n  ) AS _stage_inner\n)\nSELECT\n  DATE_TRUNC(monthly.ordered_at, MONTH) AS `monthly___ordered_at`,\n  SUM(\n    monthly.amount * _cm_min_amount_sum_partition_by_ordered_at_region.`min_amount_sum_partition_by_ordered_at_region`\n  ) AS `monthly___m`\nFROM monthly AS monthly\nLEFT JOIN _cm_min_amount_sum_partition_by_ordered_at_region\n  ON monthly.region IS NOT DISTINCT FROM _cm_min_amount_sum_partition_by_ordered_at_region.`region`\nGROUP BY\n  DATE_TRUNC(monthly.ordered_at, MONTH)",
  "lifted/mixed_reagg::duckdb": "WITH _cm_amount_sum_partition_by_ordered_at_region AS (\n  SELECT\n    _stage_inner.\"monthly.region\" AS \"region\",\n    _stage_inner.\"monthly.ordered_at_month\" AS \"ordered_at_month\",\n    _stage_inner.\"monthly.amount_sum_partition_by_ordered_at_region\" AS \"amount_sum_partition_by_ordered_at_region\"\n  FROM (\n    SELECT\n      monthly.region AS \"monthly.region\",\n      DATE_TRUNC('MONTH', monthly.ordered_at) AS \"monthly.ordered_at_month\",\n      CAST(SUM(monthly.amount) AS DOUBLE) AS \"monthly.amount_sum_partition_by_ordered_at_region\"\n    FROM monthly AS monthly\n    GROUP BY\n      monthly.region,\n      DATE_TRUNC('MONTH', monthly.ordered_at)\n  ) AS _stage_inner\n), _cm_min_amount_sum_partition_by_ordered_at_region AS (\n  SELECT\n    _stage_inner.\"monthly.region\" AS \"region\",\n    _stage_inner.\"monthly.min_amount_sum_partition_by_ordered_at_region\" AS \"min_amount_sum_partition_by_ordered_at_region\"\n  FROM (\n    SELECT\n      _base.\"monthly.region\" AS \"monthly.region\",\n      MIN(_base._v) AS \"monthly.min_amount_sum_partition_by_ordered_at_region\"\n    FROM (\n      SELECT\n        monthly.region AS \"monthly.region\",\n        monthly.region AS _ek0,\n        DATE_TRUNC('MONTH', monthly.ordered_at) AS _ek1,\n        MAX(\n          _cm_amount_sum_partition_by_ordered_at_region.\"amount_sum_partition_by_ordered_at_region\"\n        ) AS _v\n      FROM monthly AS monthly\n      LEFT JOIN _cm_amount_sum_partition_by_ordered_at_region\n        ON monthly.region IS NOT DISTINCT FROM _cm_amount_sum_partition_by_ordered_at_region.\"region\"\n        AND DATE_TRUNC('MONTH', monthly.ordered_at) IS NOT DISTINCT FROM _cm_amount_sum_partition_by_ordered_at_region.\"ordered_at_month\"\n      GROUP BY\n        monthly.region,\n        monthly.region,\n        DATE_TRUNC('MONTH', monthly.ordered_at)\n    ) AS _base\n    GROUP BY\n      _base.\"monthly.region\"\n  ) AS _stage_inner\n)\nSELECT\n  DATE_TRUNC('MONTH', monthly.ordered_at) AS \"monthly.ordered_at\",\n  SUM(\n    monthly.amount * _cm_min_amount_sum_partition_by_ordered_at_region.\"min_amount_sum_partition_by_ordered_at_region\"\n  ) AS \"monthly.m\"\nFROM monthly AS monthly\nLEFT JOIN _cm_min_amount_sum_partition_by_ordered_at_region\n  ON monthly.region IS NOT DISTINCT FROM _cm_min_amount_sum_partition_by_ordered_at_region.\"region\"\nGROUP BY\n  DATE_TRUNC('MONTH', monthly.ordered_at)",
  "lifted/mixed_reagg::mysql": "WITH _cm_amount_sum_partition_by_ordered_at_region AS (\n  SELECT\n    _stage_inner.`monthly.region` AS `region`,\n    _stage_inner.`monthly.ordered_at_month` AS `ordered_at_month`,\n    _stage_inner.`monthly.amount_sum_partition_by_ordered_at_region` AS `amount_sum_partition_by_ordered_at_region`\n  FROM (\n    SELECT\n      monthly.region AS `monthly.region`,\n      STR_TO_DATE(CONCAT(YEAR(monthly.ordered_at), ' ', MONTH(monthly.ordered_at), ' 1'), '%Y %c %e') AS `monthly.ordered_at_month`,\n      CAST(SUM(monthly.amount) AS DOUBLE) AS `monthly.amount_sum_partition_by_ordered_at_region`\n    FROM monthly AS monthly\n    GROUP BY\n      monthly.region,\n      STR_TO_DATE(CONCAT(YEAR(monthly.ordered_at), ' ', MONTH(monthly.ordered_at), ' 1'), '%Y %c %e')\n  ) AS _stage_inner\n), _cm_min_amount_sum_partition_by_ordered_at_region AS (\n  SELECT\n    _stage_inner.`monthly.region` AS `region`,\n    _stage_inner.`monthly.min_amount_sum_partition_by_ordered_at_region` AS `min_amount_sum_partition_by_ordered_at_region`\n  FROM (\n    SELECT\n      _base.`monthly.region` AS `monthly.region`,\n      MIN(_base._v) AS `monthly.min_amount_sum_partition_by_ordered_at_region`\n    FROM (\n      SELECT\n        monthly.region AS `monthly.region`,\n        monthly.region AS _ek0,\n        STR_TO_DATE(CONCAT(YEAR(monthly.ordered_at), ' ', MONTH(monthly.ordered_at), ' 1'), '%Y %c %e') AS _ek1,\n        MAX(\n          _cm_amount_sum_partition_by_ordered_at_region.`amount_sum_partition_by_ordered_at_region`\n        ) AS _v\n      FROM monthly AS monthly\n      LEFT JOIN _cm_amount_sum_partition_by_ordered_at_region\n        ON monthly.region <=> _cm_amount_sum_partition_by_ordered_at_region.`region`\n        AND STR_TO_DATE(CONCAT(YEAR(monthly.ordered_at), ' ', MONTH(monthly.ordered_at), ' 1'), '%Y %c %e') <=> _cm_amount_sum_partition_by_ordered_at_region.`ordered_at_month`\n      GROUP BY\n        monthly.region,\n        monthly.region,\n        STR_TO_DATE(CONCAT(YEAR(monthly.ordered_at), ' ', MONTH(monthly.ordered_at), ' 1'), '%Y %c %e')\n    ) AS _base\n    GROUP BY\n      _base.`monthly.region`\n  ) AS _stage_inner\n)\nSELECT\n  STR_TO_DATE(CONCAT(YEAR(monthly.ordered_at), ' ', MONTH(monthly.ordered_at), ' 1'), '%Y %c %e') AS `monthly.ordered_at`,\n  SUM(\n    monthly.amount * _cm_min_amount_sum_partition_by_ordered_at_region.`min_amount_sum_partition_by_ordered_at_region`\n  ) AS `monthly.m`\nFROM monthly AS monthly\nLEFT JOIN _cm_min_amount_sum_partition_by_ordered_at_region\n  ON monthly.region <=> _cm_min_amount_sum_partition_by_ordered_at_region.`region`\nGROUP BY\n  STR_TO_DATE(CONCAT(YEAR(monthly.ordered_at), ' ', MONTH(monthly.ordered_at), ' 1'), '%Y %c %e')",
  "lifted/mixed_reagg::postgres": "WITH _cm_amount_sum_partition_by_ordered_at_region AS (\n  SELECT\n    _stage_inner.\"monthly.region\" AS \"region\",\n    _stage_inner.\"monthly.ordered_at_month\" AS \"ordered_at_month\",\n    _stage_inner.\"monthly.amount_sum_partition_by_ordered_at_region\" AS \"amount_sum_partition_by_ordered_at_region\"\n  FROM (\n    SELECT\n      monthly.region AS \"monthly.region\",\n      DATE_TRUNC('MONTH', monthly.ordered_at) AS \"monthly.ordered_at_month\",\n      CAST(SUM(monthly.amount) AS DOUBLE PRECISION) AS \"monthly.amount_sum_partition_by_ordered_at_region\"\n    FROM monthly AS monthly\n    GROUP BY\n      monthly.region,\n      DATE_TRUNC('MONTH', monthly.ordered_at)\n  ) AS _stage_inner\n), _cm_min_amount_sum_partition_by_ordered_at_region AS (\n  SELECT\n    _stage_inner.\"monthly.region\" AS \"region\",\n    _stage_inner.\"monthly.min_amount_sum_partition_by_ordered_at_region\" AS \"min_amount_sum_partition_by_ordered_at_region\"\n  FROM (\n    SELECT\n      _base.\"monthly.region\" AS \"monthly.region\",\n      MIN(_base._v) AS \"monthly.min_amount_sum_partition_by_ordered_at_region\"\n    FROM (\n      SELECT\n        monthly.region AS \"monthly.region\",\n        monthly.region AS _ek0,\n        DATE_TRUNC('MONTH', monthly.ordered_at) AS _ek1,\n        MAX(\n          _cm_amount_sum_partition_by_ordered_at_region.\"amount_sum_partition_by_ordered_at_region\"\n        ) AS _v\n      FROM monthly AS monthly\n      LEFT JOIN _cm_amount_sum_partition_by_ordered_at_region\n        ON monthly.region IS NOT DISTINCT FROM _cm_amount_sum_partition_by_ordered_at_region.\"region\"\n        AND DATE_TRUNC('MONTH', monthly.ordered_at) IS NOT DISTINCT FROM _cm_amount_sum_partition_by_ordered_at_region.\"ordered_at_month\"\n      GROUP BY\n        monthly.region,\n        monthly.region,\n        DATE_TRUNC('MONTH', monthly.ordered_at)\n    ) AS _base\n    GROUP BY\n      _base.\"monthly.region\"\n  ) AS _stage_inner\n)\nSELECT\n  DATE_TRUNC('MONTH', monthly.ordered_at) AS \"monthly.ordered_at\",\n  SUM(\n    monthly.amount * _cm_min_amount_sum_partition_by_ordered_at_region.\"min_amount_sum_partition_by_ordered_at_region\"\n  ) AS \"monthly.m\"\nFROM monthly AS monthly\nLEFT JOIN _cm_min_amount_sum_partition_by_ordered_at_region\n  ON monthly.region IS NOT DISTINCT FROM _cm_min_amount_sum_partition_by_ordered_at_region.\"region\"\nGROUP BY\n  DATE_TRUNC('MONTH', monthly.ordered_at)",
  "lifted/mixed_reagg::snowflake": "WITH _cm_amount_sum_partition_by_ordered_at_region AS (\n  SELECT\n    _stage_inner.\"monthly.region\" AS \"region\",\n    _stage_inner.\"monthly.ordered_at_month\" AS \"ordered_at_month\",\n    _stage_inner.\"monthly.amount_sum_partition_by_ordered_at_region\" AS \"amount_sum_partition_by_ordered_at_region\"\n  FROM (\n    SELECT\n      monthly.region AS \"monthly.region\",\n      DATE_TRUNC('MONTH', monthly.ordered_at) AS \"monthly.ordered_at_month\",\n      CAST(SUM(monthly.amount) AS DOUBLE) AS \"monthly.amount_sum_partition_by_ordered_at_region\"\n    FROM monthly AS monthly\n    GROUP BY\n      monthly.region,\n      DATE_TRUNC('MONTH', monthly.ordered_at)\n  ) AS _stage_inner\n), _cm_min_amount_sum_partition_by_ordered_at_region AS (\n  SELECT\n    _stage_inner.\"monthly.region\" AS \"region\",\n    _stage_inner.\"monthly.min_amount_sum_partition_by_ordered_at_region\" AS \"min_amount_sum_partition_by_ordered_at_region\"\n  FROM (\n    SELECT\n      _base.\"monthly.region\" AS \"monthly.region\",\n      MIN(_base._v) AS \"monthly.min_amount_sum_partition_by_ordered_at_region\"\n    FROM (\n      SELECT\n        monthly.region AS \"monthly.region\",\n        monthly.region AS _ek0,\n        DATE_TRUNC('MONTH', monthly.ordered_at) AS _ek1,\n        MAX(\n          _cm_amount_sum_partition_by_ordered_at_region.\"amount_sum_partition_by_ordered_at_region\"\n        ) AS _v\n      FROM monthly AS monthly\n      LEFT JOIN _cm_amount_sum_partition_by_ordered_at_region\n        ON monthly.region IS NOT DISTINCT FROM _cm_amount_sum_partition_by_ordered_at_region.\"region\"\n        AND DATE_TRUNC('MONTH', monthly.ordered_at) IS NOT DISTINCT FROM _cm_amount_sum_partition_by_ordered_at_region.\"ordered_at_month\"\n      GROUP BY\n        monthly.region,\n        monthly.region,\n        DATE_TRUNC('MONTH', monthly.ordered_at)\n    ) AS _base\n    GROUP BY\n      _base.\"monthly.region\"\n  ) AS _stage_inner\n)\nSELECT\n  DATE_TRUNC('MONTH', monthly.ordered_at) AS \"monthly.ordered_at\",\n  SUM(\n    monthly.amount * _cm_min_amount_sum_partition_by_ordered_at_region.\"min_amount_sum_partition_by_ordered_at_region\"\n  ) AS \"monthly.m\"\nFROM monthly AS monthly\nLEFT JOIN _cm_min_amount_sum_partition_by_ordered_at_region\n  ON monthly.region IS NOT DISTINCT FROM _cm_min_amount_sum_partition_by_ordered_at_region.\"region\"\nGROUP BY\n  DATE_TRUNC('MONTH', monthly.ordered_at)",
  "lifted/mixed_reagg::sqlite": "WITH _cm_amount_sum_partition_by_ordered_at_region AS (\n  SELECT\n    _stage_inner.\"monthly.region\" AS \"region\",\n    _stage_inner.\"monthly.ordered_at_month\" AS \"ordered_at_month\",\n    _stage_inner.\"monthly.amount_sum_partition_by_ordered_at_region\" AS \"amount_sum_partition_by_ordered_at_region\"\n  FROM (\n    SELECT\n      monthly.region AS \"monthly.region\",\n      STRFTIME('%Y-%m-01', monthly.ordered_at) AS \"monthly.ordered_at_month\",\n      CAST(SUM(monthly.amount) AS REAL) AS \"monthly.amount_sum_partition_by_ordered_at_region\"\n    FROM monthly AS monthly\n    GROUP BY\n      monthly.region,\n      STRFTIME('%Y-%m-01', monthly.ordered_at)\n  ) AS _stage_inner\n), _cm_min_amount_sum_partition_by_ordered_at_region AS (\n  SELECT\n    _stage_inner.\"monthly.region\" AS \"region\",\n    _stage_inner.\"monthly.min_amount_sum_partition_by_ordered_at_region\" AS \"min_amount_sum_partition_by_ordered_at_region\"\n  FROM (\n    SELECT\n      _base.\"monthly.region\" AS \"monthly.region\",\n      MIN(_base._v) AS \"monthly.min_amount_sum_partition_by_ordered_at_region\"\n    FROM (\n      SELECT\n        monthly.region AS \"monthly.region\",\n        monthly.region AS _ek0,\n        STRFTIME('%Y-%m-01', monthly.ordered_at) AS _ek1,\n        MAX(\n          _cm_amount_sum_partition_by_ordered_at_region.\"amount_sum_partition_by_ordered_at_region\"\n        ) AS _v\n      FROM monthly AS monthly\n      LEFT JOIN _cm_amount_sum_partition_by_ordered_at_region\n        ON monthly.region IS _cm_amount_sum_partition_by_ordered_at_region.\"region\"\n        AND STRFTIME('%Y-%m-01', monthly.ordered_at) IS _cm_amount_sum_partition_by_ordered_at_region.\"ordered_at_month\"\n      GROUP BY\n        monthly.region,\n        monthly.region,\n        STRFTIME('%Y-%m-01', monthly.ordered_at)\n    ) AS _base\n    GROUP BY\n      _base.\"monthly.region\"\n  ) AS _stage_inner\n)\nSELECT\n  STRFTIME('%Y-%m-01', monthly.ordered_at) AS \"monthly.ordered_at\",\n  SUM(\n    monthly.amount * _cm_min_amount_sum_partition_by_ordered_at_region.\"min_amount_sum_partition_by_ordered_at_region\"\n  ) AS \"monthly.m\"\nFROM monthly AS monthly\nLEFT JOIN _cm_min_amount_sum_partition_by_ordered_at_region\n  ON monthly.region IS _cm_min_amount_sum_partition_by_ordered_at_region.\"region\"\nGROUP BY\n  STRFTIME('%Y-%m-01', monthly.ordered_at)",
  "lifted/mixed_reagg::tsql": "WITH _cm_amount_sum_partition_by_ordered_at_region AS (\n  SELECT\n    _stage_inner.[monthly___region] AS [region],\n    _stage_inner.[monthly___ordered_at_month] AS [ordered_at_month],\n    _stage_inner.[monthly___amount_sum_partition_by_ordered_at_region] AS [amount_sum_partition_by_ordered_at_region]\n  FROM (\n    SELECT\n      monthly.region AS [monthly___region],\n      DATETRUNC(MONTH, monthly.ordered_at) AS [monthly___ordered_at_month],\n      CAST(SUM(monthly.amount) AS FLOAT) AS [monthly___amount_sum_partition_by_ordered_at_region]\n    FROM monthly AS monthly\n    GROUP BY\n      monthly.region,\n      DATETRUNC(MONTH, monthly.ordered_at)\n  ) AS _stage_inner\n), _cm_min_amount_sum_partition_by_ordered_at_region AS (\n  SELECT\n    _stage_inner.[monthly___region] AS [region],\n    _stage_inner.[monthly___min_amount_sum_partition_by_ordered_at_region] AS [min_amount_sum_partition_by_ordered_at_region]\n  FROM (\n    SELECT\n      _base.[monthly___region] AS [monthly___region],\n      MIN(_base._v) AS [monthly___min_amount_sum_partition_by_ordered_at_region]\n    FROM (\n      SELECT\n        monthly.region AS [monthly___region],\n        monthly.region AS _ek0,\n        DATETRUNC(MONTH, monthly.ordered_at) AS _ek1,\n        MAX(\n          _cm_amount_sum_partition_by_ordered_at_region.[amount_sum_partition_by_ordered_at_region]\n        ) AS _v\n      FROM monthly AS monthly\n      LEFT JOIN _cm_amount_sum_partition_by_ordered_at_region\n        ON (\n          monthly.region = _cm_amount_sum_partition_by_ordered_at_region.[region]\n          OR (\n            monthly.region IS NULL\n            AND _cm_amount_sum_partition_by_ordered_at_region.[region] IS NULL\n          )\n        )\n        AND (\n          DATETRUNC(MONTH, monthly.ordered_at) = _cm_amount_sum_partition_by_ordered_at_region.[ordered_at_month]\n          OR (\n            DATETRUNC(MONTH, monthly.ordered_at) IS NULL\n            AND _cm_amount_sum_partition_by_ordered_at_region.[ordered_at_month] IS NULL\n          )\n        )\n      GROUP BY\n        monthly.region,\n        monthly.region,\n        DATETRUNC(MONTH, monthly.ordered_at)\n    ) AS _base\n    GROUP BY\n      _base.[monthly___region]\n  ) AS _stage_inner\n)\nSELECT\n  DATETRUNC(month, monthly.ordered_at) AS [monthly___ordered_at],\n  SUM(\n    monthly.amount * _cm_min_amount_sum_partition_by_ordered_at_region.[min_amount_sum_partition_by_ordered_at_region]\n  ) AS [monthly___m]\nFROM monthly AS monthly\nLEFT JOIN _cm_min_amount_sum_partition_by_ordered_at_region\n  ON (\n    monthly.region = _cm_min_amount_sum_partition_by_ordered_at_region.[region]\n    OR (\n      monthly.region IS NULL\n      AND _cm_min_amount_sum_partition_by_ordered_at_region.[region] IS NULL\n    )\n  )\nGROUP BY\n  DATETRUNC(month, monthly.ordered_at)",
  "lifted/mixed_transform::bigquery": "WITH base_2 AS (\n  SELECT\n    sales.product AS `sales___product`,\n    AVG(sales.unit_price) AS `sales___unit_price_avg_partition_by_product`\n  FROM sales AS sales\n  GROUP BY\n    sales.product\n), step1 AS (\n  SELECT\n    `sales___product`,\n    `sales___unit_price_avg_partition_by_product`,\n    RANK() OVER (ORDER BY `sales___unit_price_avg_partition_by_product` DESC) AS `sales___rank`\n  FROM base_2\n), _cm_rank AS (\n  SELECT\n    _stage_inner.`sales___product` AS `product`,\n    _stage_inner.`sales___rank` AS `rank`\n  FROM (\n    SELECT\n      `sales___product`,\n      `sales___rank`\n    FROM (\n      SELECT\n        `sales___product`,\n        `sales___unit_price_avg_partition_by_product`,\n        `sales___rank`\n      FROM step1\n    ) AS _outer\n  ) AS _stage_inner\n)\nSELECT\n  sales.region AS `sales___region`,\n  SUM(sales.quantity * _cm_rank.`rank`) AS `sales___m`\nFROM sales AS sales\nLEFT JOIN _cm_rank\n  ON sales.product IS NOT DISTINCT FROM _cm_rank.`product`\nGROUP BY\n  sales.region",
  "lifted/mixed_transform::duckdb": "WITH base_2 AS (\n  SELECT\n    sales.product AS \"sales.product\",\n    AVG(sales.unit_price) AS \"sales.unit_price_avg_partition_by_product\"\n  FROM sales AS sales\n  GROUP BY\n    sales.product\n), step1 AS (\n  SELECT\n    \"sales.product\",\n    \"sales.unit_price_avg_partition_by_product\",\n    RANK() OVER (ORDER BY \"sales.unit_price_avg_partition_by_product\" DESC) AS \"sales.rank\"\n  FROM base_2\n), _cm_rank AS (\n  SELECT\n    _stage_inner.\"sales.product\" AS \"product\",\n    _stage_inner.\"sales.rank\" AS \"rank\"\n  FROM (\n    SELECT\n      \"sales.product\",\n      \"sales.rank\"\n    FROM (\n      SELECT\n        \"sales.product\",\n        \"sales.unit_price_avg_partition_by_product\",\n        \"sales.rank\"\n      FROM step1\n    ) AS _outer\n  ) AS _stage_inner\n)\nSELECT\n  sales.region AS \"sales.region\",\n  SUM(sales.quantity * _cm_rank.\"rank\") AS \"sales.m\"\nFROM sales AS sales\nLEFT JOIN _cm_rank\n  ON sales.product IS NOT DISTINCT FROM _cm_rank.\"product\"\nGROUP BY\n  sales.region",
  "lifted/mixed_transform::mysql": "WITH base_2 AS (\n  SELECT\n    sales.product AS `sales.product`,\n    AVG(sales.unit_price) AS `sales.unit_price_avg_partition_by_product`\n  FROM sales AS sales\n  GROUP BY\n    sales.product\n), step1 AS (\n  SELECT\n    `sales.product`,\n    `sales.unit_price_avg_partition_by_product`,\n    RANK() OVER (ORDER BY `sales.unit_price_avg_partition_by_product` DESC) AS `sales.rank`\n  FROM base_2\n), _cm_rank AS (\n  SELECT\n    _stage_inner.`sales.product` AS `product`,\n    _stage_inner.`sales.rank` AS `rank`\n  FROM (\n    SELECT\n      `sales.product`,\n      `sales.rank`\n    FROM (\n      SELECT\n        `sales.product`,\n        `sales.unit_price_avg_partition_by_product`,\n        `sales.rank`\n      FROM step1\n    ) AS _outer\n  ) AS _stage_inner\n)\nSELECT\n  sales.region AS `sales.region`,\n  SUM(sales.quantity * _cm_rank.`rank`) AS `sales.m`\nFROM sales AS sales\nLEFT JOIN _cm_rank\n  ON sales.product <=> _cm_rank.`product`\nGROUP BY\n  sales.region",
  "lifted/mixed_transform::postgres": "WITH base_2 AS (\n  SELECT\n    sales.product AS \"sales.product\",\n    AVG(sales.unit_price) AS \"sales.unit_price_avg_partition_by_product\"\n  FROM sales AS sales\n  GROUP BY\n    sales.product\n), step1 AS (\n  SELECT\n    \"sales.product\",\n    \"sales.unit_price_avg_partition_by_product\",\n    RANK() OVER (ORDER BY \"sales.unit_price_avg_partition_by_product\" DESC NULLS LAST) AS \"sales.rank\"\n  FROM base_2\n), _cm_rank AS (\n  SELECT\n    _stage_inner.\"sales.product\" AS \"product\",\n    _stage_inner.\"sales.rank\" AS \"rank\"\n  FROM (\n    SELECT\n      \"sales.product\",\n      \"sales.rank\"\n    FROM (\n      SELECT\n        \"sales.product\",\n        \"sales.unit_price_avg_partition_by_product\",\n        \"sales.rank\"\n      FROM step1\n    ) AS _outer\n  ) AS _stage_inner\n)\nSELECT\n  sales.region AS \"sales.region\",\n  SUM(sales.quantity * _cm_rank.\"rank\") AS \"sales.m\"\nFROM sales AS sales\nLEFT JOIN _cm_rank\n  ON sales.product IS NOT DISTINCT FROM _cm_rank.\"product\"\nGROUP BY\n  sales.region",
  "lifted/mixed_transform::snowflake": "WITH base_2 AS (\n  SELECT\n    sales.product AS \"sales.product\",\n    AVG(sales.unit_price) AS \"sales.unit_price_avg_partition_by_product\"\n  FROM sales AS sales\n  GROUP BY\n    sales.product\n), step1 AS (\n  SELECT\n    \"sales.product\",\n    \"sales.unit_price_avg_partition_by_product\",\n    RANK() OVER (ORDER BY \"sales.unit_price_avg_partition_by_product\" DESC NULLS LAST) AS \"sales.rank\"\n  FROM base_2\n), _cm_rank AS (\n  SELECT\n    _stage_inner.\"sales.product\" AS \"product\",\n    _stage_inner.\"sales.rank\" AS \"rank\"\n  FROM (\n    SELECT\n      \"sales.product\",\n      \"sales.rank\"\n    FROM (\n      SELECT\n        \"sales.product\",\n        \"sales.unit_price_avg_partition_by_product\",\n        \"sales.rank\"\n      FROM step1\n    ) AS _outer\n  ) AS _stage_inner\n)\nSELECT\n  sales.region AS \"sales.region\",\n  SUM(sales.quantity * _cm_rank.\"rank\") AS \"sales.m\"\nFROM sales AS sales\nLEFT JOIN _cm_rank\n  ON sales.product IS NOT DISTINCT FROM _cm_rank.\"product\"\nGROUP BY\n  sales.region",
  "lifted/mixed_transform::sqlite": "WITH base_2 AS (\n  SELECT\n    sales.product AS \"sales.product\",\n    AVG(sales.unit_price) AS \"sales.unit_price_avg_partition_by_product\"\n  FROM sales AS sales\n  GROUP BY\n    sales.product\n), step1 AS (\n  SELECT\n    \"sales.product\",\n    \"sales.unit_price_avg_partition_by_product\",\n    RANK() OVER (ORDER BY \"sales.unit_price_avg_partition_by_product\" DESC) AS \"sales.rank\"\n  FROM base_2\n), _cm_rank AS (\n  SELECT\n    _stage_inner.\"sales.product\" AS \"product\",\n    _stage_inner.\"sales.rank\" AS \"rank\"\n  FROM (\n    SELECT\n      \"sales.product\",\n      \"sales.rank\"\n    FROM (\n      SELECT\n        \"sales.product\",\n        \"sales.unit_price_avg_partition_by_product\",\n        \"sales.rank\"\n      FROM step1\n    ) AS _outer\n  ) AS _stage_inner\n)\nSELECT\n  sales.region AS \"sales.region\",\n  SUM(sales.quantity * _cm_rank.\"rank\") AS \"sales.m\"\nFROM sales AS sales\nLEFT JOIN _cm_rank\n  ON sales.product IS _cm_rank.\"product\"\nGROUP BY\n  sales.region",
  "lifted/mixed_transform::tsql": "WITH base_2 AS (\n  SELECT\n    sales.product AS [sales___product],\n    AVG(sales.unit_price) AS [sales___unit_price_avg_partition_by_product]\n  FROM sales AS sales\n  GROUP BY\n    sales.product\n), step1 AS (\n  SELECT\n    [sales___product] AS [sales___product],\n    [sales___unit_price_avg_partition_by_product] AS [sales___unit_price_avg_partition_by_product],\n    RANK() OVER (ORDER BY [sales___unit_price_avg_partition_by_product] DESC) AS [sales___rank]\n  FROM base_2\n), _cm_rank AS (\n  SELECT\n    _stage_inner.[sales___product] AS [product],\n    _stage_inner.[sales___rank] AS [rank]\n  FROM (\n    SELECT\n      [sales___product] AS [sales___product],\n      [sales___rank] AS [sales___rank]\n    FROM (\n      SELECT\n        [sales___product] AS [sales___product],\n        [sales___unit_price_avg_partition_by_product] AS [sales___unit_price_avg_partition_by_product],\n        [sales___rank] AS [sales___rank]\n      FROM step1\n    ) AS _outer\n  ) AS _stage_inner\n)\nSELECT\n  sales.region AS [sales___region],\n  SUM(sales.quantity * _cm_rank.[rank]) AS [sales___m]\nFROM sales AS sales\nLEFT JOIN _cm_rank\n  ON (\n    sales.product = _cm_rank.[product]\n    OR (\n      sales.product IS NULL AND _cm_rank.[product] IS NULL\n    )\n  )\nGROUP BY\n  sales.region",
  "lifted/target_homed::bigquery": "WITH _base AS (\n  SELECT\n    customers.tier AS `orders___customers___tier`\n  FROM orders AS orders\n  LEFT JOIN customers AS customers\n    ON orders.customer_id = customers.id\n  GROUP BY\n    customers.tier\n), _cm_orders__customers__customers_spend_customers_regions_pop_sum AS (\n  SELECT\n    customers.tier AS `customers___tier`,\n    SUM(customers.spend - regions.pop) AS `customers___spend_regions_pop_sum`\n  FROM customers AS customers\n  LEFT JOIN regions AS regions\n    ON customers.region_id = regions.id\n  GROUP BY\n    customers.tier\n)\nSELECT\n  _base.`orders___customers___tier`,\n  _cm_orders__customers__customers_spend_customers_regions_pop_sum.`customers___spend_regions_pop_sum` AS `orders___m`\nFROM _base\nLEFT JOIN _cm_orders__customers__customers_spend_customers_regions_pop_sum\n  ON _base.`orders___customers___tier` IS NOT DISTINCT FROM _cm_orders__customers__customers_spend_customers_regions_pop_sum.`customers___tier`",
  "lifted/target_homed::duckdb": "WITH _base AS (\n  SELECT\n    customers.tier AS \"orders.customers.tier\"\n  FROM orders AS orders\n  LEFT JOIN customers AS customers\n    ON orders.customer_id = customers.id\n  GROUP BY\n    customers.tier\n), _cm_orders__customers__customers_spend_customers_regions_pop_sum AS (\n  SELECT\n    customers.tier AS \"customers.tier\",\n    SUM(customers.spend - regions.pop) AS \"customers.spend_regions_pop_sum\"\n  FROM customers AS customers\n  LEFT JOIN regions AS regions\n    ON customers.region_id = regions.id\n  GROUP BY\n    customers.tier\n)\nSELECT\n  _base.\"orders.customers.tier\",\n  _cm_orders__customers__customers_spend_customers_regions_pop_sum.\"customers.spend_regions_pop_sum\" AS \"orders.m\"\nFROM _base\nLEFT JOIN _cm_orders__customers__customers_spend_customers_regions_pop_sum\n  ON _base.\"orders.customers.tier\" IS NOT DISTINCT FROM _cm_orders__customers__customers_spend_customers_regions_pop_sum.\"customers.tier\"",
  "lifted/target_homed::mysql": "WITH _base AS (\n  SELECT\n    customers.tier AS `orders.customers.tier`\n  FROM orders AS orders\n  LEFT JOIN customers AS customers\n    ON orders.customer_id = customers.id\n  GROUP BY\n    customers.tier\n), _cm_orders__customers__customers_spend_customers_regions_pop_sum AS (\n  SELECT\n    customers.tier AS `customers.tier`,\n    SUM(customers.spend - regions.pop) AS `customers.spend_regions_pop_sum`\n  FROM customers AS customers\n  LEFT JOIN regions AS regions\n    ON customers.region_id = regions.id\n  GROUP BY\n    customers.tier\n)\nSELECT\n  _base.`orders.customers.tier`,\n  _cm_orders__customers__customers_spend_customers_regions_pop_sum.`customers.spend_regions_pop_sum` AS `orders.m`\nFROM _base\nLEFT JOIN _cm_orders__customers__customers_spend_customers_regions_pop_sum\n  ON _base.`orders.customers.tier` <=> _cm_orders__customers__customers_spend_customers_regions_pop_sum.`customers.tier`",
  "lifted/target_homed::postgres": "WITH _base AS (\n  SELECT\n    customers.tier AS \"orders.customers.tier\"\n  FROM orders AS orders\n  LEFT JOIN customers AS customers\n    ON orders.customer_id = customers.id\n  GROUP BY\n    customers.tier\n), _cm_orders__customers__cust_03342ef8_customers_regions_pop_sum AS (\n  SELECT\n    customers.tier AS \"customers.tier\",\n    SUM(customers.spend - regions.pop) AS \"customers.spend_regions_pop_sum\"\n  FROM customers AS customers\n  LEFT JOIN regions AS regions\n    ON customers.region_id = regions.id\n  GROUP BY\n    customers.tier\n)\nSELECT\n  _base.\"orders.customers.tier\",\n  _cm_orders__customers__cust_03342ef8_customers_regions_pop_sum.\"customers.spend_regions_pop_sum\" AS \"orders.m\"\nFROM _base\nLEFT JOIN _cm_orders__customers__cust_03342ef8_customers_regions_pop_sum\n  ON _base.\"orders.customers.tier\" IS NOT DISTINCT FROM _cm_orders__customers__cust_03342ef8_customers_regions_pop_sum.\"customers.tier\"",
  "lifted/target_homed::snowflake": "WITH _base AS (\n  SELECT\n    customers.tier AS \"orders.customers.tier\"\n  FROM orders AS orders\n  LEFT JOIN customers AS customers\n    ON orders.customer_id = customers.id\n  GROUP BY\n    customers.tier\n), _cm_orders__customers__customers_spend_customers_regions_pop_sum AS (\n  SELECT\n    customers.tier AS \"customers.tier\",\n    SUM(customers.spend - regions.pop) AS \"customers.spend_regions_pop_sum\"\n  FROM customers AS customers\n  LEFT JOIN regions AS regions\n    ON customers.region_id = regions.id\n  GROUP BY\n    customers.tier\n)\nSELECT\n  _base.\"orders.customers.tier\",\n  _cm_orders__customers__customers_spend_customers_regions_pop_sum.\"customers.spend_regions_pop_sum\" AS \"orders.m\"\nFROM _base\nLEFT JOIN _cm_orders__customers__customers_spend_customers_regions_pop_sum\n  ON _base.\"orders.customers.tier\" IS NOT DISTINCT FROM _cm_orders__customers__customers_spend_customers_regions_pop_sum.\"customers.tier\"",
  "lifted/target_homed::sqlite": "WITH _base AS (\n  SELECT\n    customers.tier AS \"orders.customers.tier\"\n  FROM orders AS orders\n  LEFT JOIN customers AS customers\n    ON orders.customer_id = customers.id\n  GROUP BY\n    customers.tier\n), _cm_orders__customers__customers_spend_customers_regions_pop_sum AS (\n  SELECT\n    customers.tier AS \"customers.tier\",\n    SUM(customers.spend - regions.pop) AS \"customers.spend_regions_pop_sum\"\n  FROM customers AS customers\n  LEFT JOIN regions AS regions\n    ON customers.region_id = regions.id\n  GROUP BY\n    customers.tier\n)\nSELECT\n  _base.\"orders.customers.tier\",\n  _cm_orders__customers__customers_spend_customers_regions_pop_sum.\"customers.spend_regions_pop_sum\" AS \"orders.m\"\nFROM _base\nLEFT JOIN _cm_orders__customers__customers_spend_customers_regions_pop_sum\n  ON _base.\"orders.customers.tier\" IS _cm_orders__customers__customers_spend_customers_regions_pop_sum.\"customers.tier\"",
  "lifted/target_homed::tsql": "WITH _base AS (\n  SELECT\n    customers.tier AS [orders___customers___tier]\n  FROM orders AS orders\n  LEFT JOIN customers AS customers\n    ON orders.customer_id = customers.id\n  GROUP BY\n    customers.tier\n), _cm_orders__customers__customers_spend_customers_regions_pop_sum AS (\n  SELECT\n    customers.tier AS [customers___tier],\n    SUM(customers.spend - regions.pop) AS [customers___spend_regions_pop_sum]\n  FROM customers AS customers\n  LEFT JOIN regions AS regions\n    ON customers.region_id = regions.id\n  GROUP BY\n    customers.tier\n)\nSELECT\n  _base.[orders___customers___tier],\n  _cm_orders__customers__customers_spend_customers_regions_pop_sum.[customers___spend_regions_pop_sum] AS [orders___m]\nFROM _base\nLEFT JOIN _cm_orders__customers__customers_spend_customers_regions_pop_sum\n  ON (\n    _base.[orders___customers___tier] = _cm_orders__customers__customers_spend_customers_regions_pop_sum.[customers___tier]\n    OR (\n      _base.[orders___customers___tier] IS NULL\n      AND _cm_orders__customers__customers_spend_customers_regions_pop_sum.[customers___tier] IS NULL\n    )\n  )",
  "lifted/transform_constituent::bigquery": "WITH _base AS (\n  SELECT\n    DATE_TRUNC(monthly.ordered_at, MONTH) AS `monthly___ordered_at`\n  FROM monthly AS monthly\n  GROUP BY\n    DATE_TRUNC(monthly.ordered_at, MONTH)\n), base_2 AS (\n  SELECT\n    monthly.region AS `monthly___region`,\n    DATE_TRUNC(monthly.ordered_at, MONTH) AS `monthly___ordered_at_month`,\n    SUM(monthly.amount) AS `monthly___amount_sum_partition_by_ordered_at_region`\n  FROM monthly AS monthly\n  GROUP BY\n    monthly.region,\n    DATE_TRUNC(monthly.ordered_at, MONTH)\n), step1 AS (\n  SELECT\n    `monthly___region`,\n    `monthly___ordered_at_month`,\n    `monthly___amount_sum_partition_by_ordered_at_region`,\n    SUM(`monthly___amount_sum_partition_by_ordered_at_region`) OVER (PARTITION BY `monthly___region` ORDER BY `monthly___ordered_at_month`) AS `monthly___cumsum`\n  FROM base_2\n), _cm_cumsum AS (\n  SELECT\n    `_stage_inner`.`monthly___region` AS `region`,\n    `_stage_inner`.`monthly___ordered_at_month` AS `ordered_at_month`,\n    `_stage_inner`.`monthly___cumsum` AS `cumsum`\n  FROM (\n    SELECT\n      `monthly___region`,\n      `monthly___ordered_at_month`,\n      `monthly___cumsum`\n    FROM (\n      SELECT\n        `monthly___region`,\n        `monthly___ordered_at_month`,\n        `monthly___amount_sum_partition_by_ordered_at_region`,\n        `monthly___cumsum`\n      FROM step1\n    ) AS _outer\n  ) AS _stage_inner\n), _cm_monthly__op_cumsum_input_aggregatekey_9a06f4b9_month_1_sum AS (\n  SELECT\n    `_base`.`monthly___ordered_at_month` AS `monthly___ordered_at_month`,\n    SUM(_base._v) AS `monthly___m`\n  FROM (\n    SELECT\n      DATE_TRUNC(monthly.ordered_at, MONTH) AS `monthly___ordered_at_month`,\n      monthly.region AS _ek0,\n      DATE_TRUNC(monthly.ordered_at, MONTH) AS _ek1,\n      MAX(_cm_cumsum.`cumsum` - 1) AS _v\n    FROM monthly AS monthly\n    LEFT JOIN _cm_cumsum\n      ON monthly.region IS NOT DISTINCT FROM _cm_cumsum.`region`\n      AND DATE_TRUNC(monthly.ordered_at, MONTH) IS NOT DISTINCT FROM _cm_cumsum.`ordered_at_month`\n    GROUP BY\n      DATE_TRUNC(monthly.ordered_at, MONTH),\n      monthly.region,\n      DATE_TRUNC(monthly.ordered_at, MONTH)\n  ) AS _base\n  GROUP BY\n    `_base`.`monthly___ordered_at_month`\n)\nSELECT\n  _base.`monthly___ordered_at`,\n  _cm_monthly__op_cumsum_input_aggregatekey_9a06f4b9_month_1_sum.`monthly___m`\nFROM _base\nLEFT JOIN _cm_monthly__op_cumsum_input_aggregatekey_9a06f4b9_month_1_sum\n  ON _base.`monthly___ordered_at` IS NOT DISTINCT FROM _cm_monthly__op_cumsum_input_aggregatekey_9a06f4b9_month_1_sum.`monthly___ordered_at_month`",
  "lifted/transform_constituent::duckdb": "WITH _base AS (\n  SELECT\n    DATE_TRUNC('MONTH', monthly.ordered_at) AS \"monthly.ordered_at\"\n  FROM monthly AS monthly\n  GROUP BY\n    DATE_TRUNC('MONTH', monthly.ordered_at)\n), base_2 AS (\n  SELECT\n    monthly.region AS \"monthly.region\",\n    DATE_TRUNC('MONTH', monthly.ordered_at) AS \"monthly.ordered_at_month\",\n    SUM(monthly.amount) AS \"monthly.amount_sum_partition_by_ordered_at_region\"\n  FROM monthly AS monthly\n  GROUP BY\n    monthly.region,\n    DATE_TRUNC('MONTH', monthly.ordered_at)\n), step1 AS (\n  SELECT\n    \"monthly.region\",\n    \"monthly.ordered_at_month\",\n    \"monthly.amount_sum_partition_by_ordered_at_region\",\n    SUM(\"monthly.amount_sum_partition_by_ordered_at_region\") OVER (PARTITION BY \"monthly.region\" ORDER BY \"monthly.ordered_at_month\") AS \"monthly.cumsum\"\n  FROM base_2\n), _cm_cumsum AS (\n  SELECT\n    _stage_inner.\"monthly.region\" AS \"region\",\n    _stage_inner.\"monthly.ordered_at_month\" AS \"ordered_at_month\",\n    _stage_inner.\"monthly.cumsum\" AS \"cumsum\"\n  FROM (\n    SELECT\n      \"monthly.region\",\n      \"monthly.ordered_at_month\",\n      \"monthly.cumsum\"\n    FROM (\n      SELECT\n        \"monthly.region\",\n        \"monthly.ordered_at_month\",\n        \"monthly.amount_sum_partition_by_ordered_at_region\",\n        \"monthly.cumsum\"\n      FROM step1\n    ) AS _outer\n  ) AS _stage_inner\n), _cm_monthly__op_cumsum_input_aggregatekey_9a06f4b9_month_1_sum AS (\n  SELECT\n    _base.\"monthly.ordered_at_month\" AS \"monthly.ordered_at_month\",\n    SUM(_base._v) AS \"monthly.m\"\n  FROM (\n    SELECT\n      DATE_TRUNC('MONTH', monthly.ordered_at) AS \"monthly.ordered_at_month\",\n      monthly.region AS _ek0,\n      DATE_TRUNC('MONTH', monthly.ordered_at) AS _ek1,\n      MAX(_cm_cumsum.\"cumsum\" - 1) AS _v\n    FROM monthly AS monthly\n    LEFT JOIN _cm_cumsum\n      ON monthly.region IS NOT DISTINCT FROM _cm_cumsum.\"region\"\n      AND DATE_TRUNC('MONTH', monthly.ordered_at) IS NOT DISTINCT FROM _cm_cumsum.\"ordered_at_month\"\n    GROUP BY\n      DATE_TRUNC('MONTH', monthly.ordered_at),\n      monthly.region,\n      DATE_TRUNC('MONTH', monthly.ordered_at)\n  ) AS _base\n  GROUP BY\n    _base.\"monthly.ordered_at_month\"\n)\nSELECT\n  _base.\"monthly.ordered_at\",\n  _cm_monthly__op_cumsum_input_aggregatekey_9a06f4b9_month_1_sum.\"monthly.m\"\nFROM _base\nLEFT JOIN _cm_monthly__op_cumsum_input_aggregatekey_9a06f4b9_month_1_sum\n  ON _base.\"monthly.ordered_at\" IS NOT DISTINCT FROM _cm_monthly__op_cumsum_input_aggregatekey_9a06f4b9_month_1_sum.\"monthly.ordered_at_month\"",
  "lifted/transform_constituent::mysql": "WITH _base AS (\n  SELECT\n    STR_TO_DATE(CONCAT(YEAR(monthly.ordered_at), ' ', MONTH(monthly.ordered_at), ' 1'), '%Y %c %e') AS `monthly.ordered_at`\n  FROM monthly AS monthly\n  GROUP BY\n    STR_TO_DATE(CONCAT(YEAR(monthly.ordered_at), ' ', MONTH(monthly.ordered_at), ' 1'), '%Y %c %e')\n), base_2 AS (\n  SELECT\n    monthly.region AS `monthly.region`,\n    STR_TO_DATE(CONCAT(YEAR(monthly.ordered_at), ' ', MONTH(monthly.ordered_at), ' 1'), '%Y %c %e') AS `monthly.ordered_at_month`,\n    SUM(monthly.amount) AS `monthly.amount_sum_partition_by_ordered_at_region`\n  FROM monthly AS monthly\n  GROUP BY\n    monthly.region,\n    STR_TO_DATE(CONCAT(YEAR(monthly.ordered_at), ' ', MONTH(monthly.ordered_at), ' 1'), '%Y %c %e')\n), step1 AS (\n  SELECT\n    `monthly.region`,\n    `monthly.ordered_at_month`,\n    `monthly.amount_sum_partition_by_ordered_at_region`,\n    SUM(`monthly.amount_sum_partition_by_ordered_at_region`) OVER (PARTITION BY `monthly.region` ORDER BY `monthly.ordered_at_month`) AS `monthly.cumsum`\n  FROM base_2\n), _cm_cumsum AS (\n  SELECT\n    _stage_inner.`monthly.region` AS `region`,\n    _stage_inner.`monthly.ordered_at_month` AS `ordered_at_month`,\n    _stage_inner.`monthly.cumsum` AS `cumsum`\n  FROM (\n    SELECT\n      `monthly.region`,\n      `monthly.ordered_at_month`,\n      `monthly.cumsum`\n    FROM (\n      SELECT\n        `monthly.region`,\n        `monthly.ordered_at_month`,\n        `monthly.amount_sum_partition_by_ordered_at_region`,\n        `monthly.cumsum`\n      FROM step1\n    ) AS _outer\n  ) AS _stage_inner\n), _cm_monthly__op_cumsum_input_aggregatekey_9a06f4b9_month_1_sum AS (\n  SELECT\n    _base.`monthly.ordered_at_month` AS `monthly.ordered_at_month`,\n    SUM(_base._v) AS `monthly.m`\n  FROM (\n    SELECT\n      STR_TO_DATE(CONCAT(YEAR(monthly.ordered_at), ' ', MONTH(monthly.ordered_at), ' 1'), '%Y %c %e') AS `monthly.ordered_at_month`,\n      monthly.region AS _ek0,\n      STR_TO_DATE(CONCAT(YEAR(monthly.ordered_at), ' ', MONTH(monthly.ordered_at), ' 1'), '%Y %c %e') AS _ek1,\n      MAX(_cm_cumsum.`cumsum` - 1) AS _v\n    FROM monthly AS monthly\n    LEFT JOIN _cm_cumsum\n      ON monthly.region <=> _cm_cumsum.`region`\n      AND STR_TO_DATE(CONCAT(YEAR(monthly.ordered_at), ' ', MONTH(monthly.ordered_at), ' 1'), '%Y %c %e') <=> _cm_cumsum.`ordered_at_month`\n    GROUP BY\n      STR_TO_DATE(CONCAT(YEAR(monthly.ordered_at), ' ', MONTH(monthly.ordered_at), ' 1'), '%Y %c %e'),\n      monthly.region,\n      STR_TO_DATE(CONCAT(YEAR(monthly.ordered_at), ' ', MONTH(monthly.ordered_at), ' 1'), '%Y %c %e')\n  ) AS _base\n  GROUP BY\n    _base.`monthly.ordered_at_month`\n)\nSELECT\n  _base.`monthly.ordered_at`,\n  _cm_monthly__op_cumsum_input_aggregatekey_9a06f4b9_month_1_sum.`monthly.m`\nFROM _base\nLEFT JOIN _cm_monthly__op_cumsum_input_aggregatekey_9a06f4b9_month_1_sum\n  ON _base.`monthly.ordered_at` <=> _cm_monthly__op_cumsum_input_aggregatekey_9a06f4b9_month_1_sum.`monthly.ordered_at_month`",
  "lifted/transform_constituent::postgres": "WITH _base AS (\n  SELECT\n    DATE_TRUNC('MONTH', monthly.ordered_at) AS \"monthly.ordered_at\"\n  FROM monthly AS monthly\n  GROUP BY\n    DATE_TRUNC('MONTH', monthly.ordered_at)\n), base_2 AS (\n  SELECT\n    monthly.region AS \"monthly.region\",\n    DATE_TRUNC('MONTH', monthly.ordered_at) AS \"monthly.ordered_at_month\",\n    SUM(monthly.amount) AS \"monthly.amount_sum_partition_by_ordered_at_region\"\n  FROM monthly AS monthly\n  GROUP BY\n    monthly.region,\n    DATE_TRUNC('MONTH', monthly.ordered_at)\n), step1 AS (\n  SELECT\n    \"monthly.region\",\n    \"monthly.ordered_at_month\",\n    \"monthly.amount_sum_partition_by_ordered_at_region\",\n    SUM(\"monthly.amount_sum_partition_by_ordered_at_region\") OVER (PARTITION BY \"monthly.region\" ORDER BY \"monthly.ordered_at_month\") AS \"monthly.cumsum\"\n  FROM base_2\n), _cm_cumsum AS (\n  SELECT\n    _stage_inner.\"monthly.region\" AS \"region\",\n    _stage_inner.\"monthly.ordered_at_month\" AS \"ordered_at_month\",\n    _stage_inner.\"monthly.cumsum\" AS \"cumsum\"\n  FROM (\n    SELECT\n      \"monthly.region\",\n      \"monthly.ordered_at_month\",\n      \"monthly.cumsum\"\n    FROM (\n      SELECT\n        \"monthly.region\",\n        \"monthly.ordered_at_month\",\n        \"monthly.amount_sum_partition_by_ordered_at_region\",\n        \"monthly.cumsum\"\n      FROM step1\n    ) AS _outer\n  ) AS _stage_inner\n), _cm_monthly__op_cumsum_input_aggregatekey_9a06f4b9_month_1_sum AS (\n  SELECT\n    _base.\"monthly.ordered_at_month\" AS \"monthly.ordered_at_month\",\n    SUM(_base._v) AS \"monthly.m\"\n  FROM (\n    SELECT\n      DATE_TRUNC('MONTH', monthly.ordered_at) AS \"monthly.ordered_at_month\",\n      monthly.region AS _ek0,\n      DATE_TRUNC('MONTH', monthly.ordered_at) AS _ek1,\n      MAX(_cm_cumsum.\"cumsum\" - 1) AS _v\n    FROM monthly AS monthly\n    LEFT JOIN _cm_cumsum\n      ON monthly.region IS NOT DISTINCT FROM _cm_cumsum.\"region\"\n      AND DATE_TRUNC('MONTH', monthly.ordered_at) IS NOT DISTINCT FROM _cm_cumsum.\"ordered_at_month\"\n    GROUP BY\n      DATE_TRUNC('MONTH', monthly.ordered_at),\n      monthly.region,\n      DATE_TRUNC('MONTH', monthly.ordered_at)\n  ) AS _base\n  GROUP BY\n    _base.\"monthly.ordered_at_month\"\n)\nSELECT\n  _base.\"monthly.ordered_at\",\n  _cm_monthly__op_cumsum_input_aggregatekey_9a06f4b9_month_1_sum.\"monthly.m\"\nFROM _base\nLEFT JOIN _cm_monthly__op_cumsum_input_aggregatekey_9a06f4b9_month_1_sum\n  ON _base.\"monthly.ordered_at\" IS NOT DISTINCT FROM _cm_monthly__op_cumsum_input_aggregatekey_9a06f4b9_month_1_sum.\"monthly.ordered_at_month\"",
  "lifted/transform_constituent::snowflake": "WITH _base AS (\n  SELECT\n    DATE_TRUNC('MONTH', monthly.ordered_at) AS \"monthly.ordered_at\"\n  FROM monthly AS monthly\n  GROUP BY\n    DATE_TRUNC('MONTH', monthly.ordered_at)\n), base_2 AS (\n  SELECT\n    monthly.region AS \"monthly.region\",\n    DATE_TRUNC('MONTH', monthly.ordered_at) AS \"monthly.ordered_at_month\",\n    SUM(monthly.amount) AS \"monthly.amount_sum_partition_by_ordered_at_region\"\n  FROM monthly AS monthly\n  GROUP BY\n    monthly.region,\n    DATE_TRUNC('MONTH', monthly.ordered_at)\n), step1 AS (\n  SELECT\n    \"monthly.region\",\n    \"monthly.ordered_at_month\",\n    \"monthly.amount_sum_partition_by_ordered_at_region\",\n    SUM(\"monthly.amount_sum_partition_by_ordered_at_region\") OVER (PARTITION BY \"monthly.region\" ORDER BY \"monthly.ordered_at_month\") AS \"monthly.cumsum\"\n  FROM base_2\n), _cm_cumsum AS (\n  SELECT\n    _stage_inner.\"monthly.region\" AS \"region\",\n    _stage_inner.\"monthly.ordered_at_month\" AS \"ordered_at_month\",\n    _stage_inner.\"monthly.cumsum\" AS \"cumsum\"\n  FROM (\n    SELECT\n      \"monthly.region\",\n      \"monthly.ordered_at_month\",\n      \"monthly.cumsum\"\n    FROM (\n      SELECT\n        \"monthly.region\",\n        \"monthly.ordered_at_month\",\n        \"monthly.amount_sum_partition_by_ordered_at_region\",\n        \"monthly.cumsum\"\n      FROM step1\n    ) AS _outer\n  ) AS _stage_inner\n), _cm_monthly__op_cumsum_input_aggregatekey_9a06f4b9_month_1_sum AS (\n  SELECT\n    _base.\"monthly.ordered_at_month\" AS \"monthly.ordered_at_month\",\n    SUM(_base._v) AS \"monthly.m\"\n  FROM (\n    SELECT\n      DATE_TRUNC('MONTH', monthly.ordered_at) AS \"monthly.ordered_at_month\",\n      monthly.region AS _ek0,\n      DATE_TRUNC('MONTH', monthly.ordered_at) AS _ek1,\n      MAX(_cm_cumsum.\"cumsum\" - 1) AS _v\n    FROM monthly AS monthly\n    LEFT JOIN _cm_cumsum\n      ON monthly.region IS NOT DISTINCT FROM _cm_cumsum.\"region\"\n      AND DATE_TRUNC('MONTH', monthly.ordered_at) IS NOT DISTINCT FROM _cm_cumsum.\"ordered_at_month\"\n    GROUP BY\n      DATE_TRUNC('MONTH', monthly.ordered_at),\n      monthly.region,\n      DATE_TRUNC('MONTH', monthly.ordered_at)\n  ) AS _base\n  GROUP BY\n    _base.\"monthly.ordered_at_month\"\n)\nSELECT\n  _base.\"monthly.ordered_at\",\n  _cm_monthly__op_cumsum_input_aggregatekey_9a06f4b9_month_1_sum.\"monthly.m\"\nFROM _base\nLEFT JOIN _cm_monthly__op_cumsum_input_aggregatekey_9a06f4b9_month_1_sum\n  ON _base.\"monthly.ordered_at\" IS NOT DISTINCT FROM _cm_monthly__op_cumsum_input_aggregatekey_9a06f4b9_month_1_sum.\"monthly.ordered_at_month\"",
  "lifted/transform_constituent::sqlite": "WITH _base AS (\n  SELECT\n    STRFTIME('%Y-%m-01', monthly.ordered_at) AS \"monthly.ordered_at\"\n  FROM monthly AS monthly\n  GROUP BY\n    STRFTIME('%Y-%m-01', monthly.ordered_at)\n), base_2 AS (\n  SELECT\n    monthly.region AS \"monthly.region\",\n    STRFTIME('%Y-%m-01', monthly.ordered_at) AS \"monthly.ordered_at_month\",\n    SUM(monthly.amount) AS \"monthly.amount_sum_partition_by_ordered_at_region\"\n  FROM monthly AS monthly\n  GROUP BY\n    monthly.region,\n    STRFTIME('%Y-%m-01', monthly.ordered_at)\n), step1 AS (\n  SELECT\n    \"monthly.region\",\n    \"monthly.ordered_at_month\",\n    \"monthly.amount_sum_partition_by_ordered_at_region\",\n    SUM(\"monthly.amount_sum_partition_by_ordered_at_region\") OVER (PARTITION BY \"monthly.region\" ORDER BY \"monthly.ordered_at_month\") AS \"monthly.cumsum\"\n  FROM base_2\n), _cm_cumsum AS (\n  SELECT\n    _stage_inner.\"monthly.region\" AS \"region\",\n    _stage_inner.\"monthly.ordered_at_month\" AS \"ordered_at_month\",\n    _stage_inner.\"monthly.cumsum\" AS \"cumsum\"\n  FROM (\n    SELECT\n      \"monthly.region\",\n      \"monthly.ordered_at_month\",\n      \"monthly.cumsum\"\n    FROM (\n      SELECT\n        \"monthly.region\",\n        \"monthly.ordered_at_month\",\n        \"monthly.amount_sum_partition_by_ordered_at_region\",\n        \"monthly.cumsum\"\n      FROM step1\n    ) AS _outer\n  ) AS _stage_inner\n), _cm_monthly__op_cumsum_input_aggregatekey_9a06f4b9_month_1_sum AS (\n  SELECT\n    _base.\"monthly.ordered_at_month\" AS \"monthly.ordered_at_month\",\n    SUM(_base._v) AS \"monthly.m\"\n  FROM (\n    SELECT\n      STRFTIME('%Y-%m-01', monthly.ordered_at) AS \"monthly.ordered_at_month\",\n      monthly.region AS _ek0,\n      STRFTIME('%Y-%m-01', monthly.ordered_at) AS _ek1,\n      MAX(_cm_cumsum.\"cumsum\" - 1) AS _v\n    FROM monthly AS monthly\n    LEFT JOIN _cm_cumsum\n      ON monthly.region IS _cm_cumsum.\"region\"\n      AND STRFTIME('%Y-%m-01', monthly.ordered_at) IS _cm_cumsum.\"ordered_at_month\"\n    GROUP BY\n      STRFTIME('%Y-%m-01', monthly.ordered_at),\n      monthly.region,\n      STRFTIME('%Y-%m-01', monthly.ordered_at)\n  ) AS _base\n  GROUP BY\n    _base.\"monthly.ordered_at_month\"\n)\nSELECT\n  _base.\"monthly.ordered_at\",\n  _cm_monthly__op_cumsum_input_aggregatekey_9a06f4b9_month_1_sum.\"monthly.m\"\nFROM _base\nLEFT JOIN _cm_monthly__op_cumsum_input_aggregatekey_9a06f4b9_month_1_sum\n  ON _base.\"monthly.ordered_at\" IS _cm_monthly__op_cumsum_input_aggregatekey_9a06f4b9_month_1_sum.\"monthly.ordered_at_month\"",
  "lifted/transform_constituent::tsql": "WITH _base AS (\n  SELECT\n    DATETRUNC(month, monthly.ordered_at) AS [monthly___ordered_at]\n  FROM monthly AS monthly\n  GROUP BY\n    DATETRUNC(month, monthly.ordered_at)\n), base_2 AS (\n  SELECT\n    monthly.region AS [monthly___region],\n    DATETRUNC(MONTH, monthly.ordered_at) AS [monthly___ordered_at_month],\n    SUM(monthly.amount) AS [monthly___amount_sum_partition_by_ordered_at_region]\n  FROM monthly AS monthly\n  GROUP BY\n    monthly.region,\n    DATETRUNC(MONTH, monthly.ordered_at)\n), step1 AS (\n  SELECT\n    [monthly___region] AS [monthly___region],\n    [monthly___ordered_at_month] AS [monthly___ordered_at_month],\n    [monthly___amount_sum_partition_by_ordered_at_region] AS [monthly___amount_sum_partition_by_ordered_at_region],\n    SUM([monthly___amount_sum_partition_by_ordered_at_region]) OVER (PARTITION BY [monthly___region] ORDER BY [monthly___ordered_at_month]) AS [monthly___cumsum]\n  FROM base_2\n), _cm_cumsum AS (\n  SELECT\n    _stage_inner.[monthly___region] AS [region],\n    _stage_inner.[monthly___ordered_at_month] AS [ordered_at_month],\n    _stage_inner.[monthly___cumsum] AS [cumsum]\n  FROM (\n    SELECT\n      [monthly___region] AS [monthly___region],\n      [monthly___ordered_at_month] AS [monthly___ordered_at_month],\n      [monthly___cumsum] AS [monthly___cumsum]\n    FROM (\n      SELECT\n        [monthly___region] AS [monthly___region],\n        [monthly___ordered_at_month] AS [monthly___ordered_at_month],\n        [monthly___amount_sum_partition_by_ordered_at_region] AS [monthly___amount_sum_partition_by_ordered_at_region],\n        [monthly___cumsum] AS [monthly___cumsum]\n      FROM step1\n    ) AS _outer\n  ) AS _stage_inner\n), _cm_monthly__op_cumsum_input_aggregatekey_9a06f4b9_month_1_sum AS (\n  SELECT\n    _base.[monthly___ordered_at_month] AS [monthly___ordered_at_month],\n    SUM(_base._v) AS [monthly___m]\n  FROM (\n    SELECT\n      DATETRUNC(MONTH, monthly.ordered_at) AS [monthly___ordered_at_month],\n      monthly.region AS _ek0,\n      DATETRUNC(MONTH, monthly.ordered_at) AS _ek1,\n      MAX(_cm_cumsum.[cumsum] - 1) AS _v\n    FROM monthly AS monthly\n    LEFT JOIN _cm_cumsum\n      ON (\n        monthly.region = _cm_cumsum.[region]\n        OR (\n          monthly.region IS NULL AND _cm_cumsum.[region] IS NULL\n        )\n      )\n      AND (\n        DATETRUNC(MONTH, monthly.ordered_at) = _cm_cumsum.[ordered_at_month]\n        OR (\n          DATETRUNC(MONTH, monthly.ordered_at) IS NULL\n          AND _cm_cumsum.[ordered_at_month] IS NULL\n        )\n      )\n    GROUP BY\n      DATETRUNC(MONTH, monthly.ordered_at),\n      monthly.region,\n      DATETRUNC(MONTH, monthly.ordered_at)\n  ) AS _base\n  GROUP BY\n    _base.[monthly___ordered_at_month]\n)\nSELECT\n  _base.[monthly___ordered_at],\n  _cm_monthly__op_cumsum_input_aggregatekey_9a06f4b9_month_1_sum.[monthly___m]\nFROM _base\nLEFT JOIN _cm_monthly__op_cumsum_input_aggregatekey_9a06f4b9_month_1_sum\n  ON (\n    _base.[monthly___ordered_at] = _cm_monthly__op_cumsum_input_aggregatekey_9a06f4b9_month_1_sum.[monthly___ordered_at_month]\n    OR (\n      _base.[monthly___ordered_at] IS NULL\n      AND _cm_monthly__op_cumsum_input_aggregatekey_9a06f4b9_month_1_sum.[monthly___ordered_at_month] IS NULL\n    )\n  )",
  "lifted/two_branch::bigquery": "WITH _base AS (\n  SELECT\n    orders.status AS `orders___status`\n  FROM orders AS orders\n  GROUP BY\n    orders.status\n), _cm_orders__customers_spend_stores_rent_sum AS (\n  SELECT\n    orders.status AS `orders___status`,\n    SUM(customers.spend - stores.rent) AS `orders___m`\n  FROM orders AS orders\n  LEFT JOIN customers AS customers\n    ON orders.customer_id = customers.id\n  LEFT JOIN stores AS stores\n    ON orders.store_co = stores.co AND orders.store_no = stores.`no`\n  GROUP BY\n    orders.status\n)\nSELECT\n  _base.`orders___status`,\n  _cm_orders__customers_spend_stores_rent_sum.`orders___m`\nFROM _base\nLEFT JOIN _cm_orders__customers_spend_stores_rent_sum\n  ON _base.`orders___status` IS NOT DISTINCT FROM _cm_orders__customers_spend_stores_rent_sum.`orders___status`",
  "lifted/two_branch::duckdb": "WITH _base AS (\n  SELECT\n    orders.status AS \"orders.status\"\n  FROM orders AS orders\n  GROUP BY\n    orders.status\n), _cm_orders__customers_spend_stores_rent_sum AS (\n  SELECT\n    orders.status AS \"orders.status\",\n    SUM(customers.spend - stores.rent) AS \"orders.m\"\n  FROM orders AS orders\n  LEFT JOIN customers AS customers\n    ON orders.customer_id = customers.id\n  LEFT JOIN stores AS stores\n    ON orders.store_co = stores.co AND orders.store_no = stores.no\n  GROUP BY\n    orders.status\n)\nSELECT\n  _base.\"orders.status\",\n  _cm_orders__customers_spend_stores_rent_sum.\"orders.m\"\nFROM _base\nLEFT JOIN _cm_orders__customers_spend_stores_rent_sum\n  ON _base.\"orders.status\" IS NOT DISTINCT FROM _cm_orders__customers_spend_stores_rent_sum.\"orders.status\"",
  "lifted/two_branch::mysql": "WITH _base AS (\n  SELECT\n    orders.status AS `orders.status`\n  FROM orders AS orders\n  GROUP BY\n    orders.status\n), _cm_orders__customers_spend_stores_rent_sum AS (\n  SELECT\n    orders.status AS `orders.status`,\n    SUM(customers.spend - stores.rent) AS `orders.m`\n  FROM orders AS orders\n  LEFT JOIN customers AS customers\n    ON orders.customer_id = customers.id\n  LEFT JOIN stores AS stores\n    ON orders.store_co = stores.co AND orders.store_no = stores.no\n  GROUP BY\n    orders.status\n)\nSELECT\n  _base.`orders.status`,\n  _cm_orders__customers_spend_stores_rent_sum.`orders.m`\nFROM _base\nLEFT JOIN _cm_orders__customers_spend_stores_rent_sum\n  ON _base.`orders.status` <=> _cm_orders__customers_spend_stores_rent_sum.`orders.status`",
  "lifted/two_branch::postgres": "WITH _base AS (\n  SELECT\n    orders.status AS \"orders.status\"\n  FROM orders AS orders\n  GROUP BY\n    orders.status\n), _cm_orders__customers_spend_stores_rent_sum AS (\n  SELECT\n    orders.status AS \"orders.status\",\n    SUM(customers.spend - stores.rent) AS \"orders.m\"\n  FROM orders AS orders\n  LEFT JOIN customers AS customers\n    ON orders.customer_id = customers.id\n  LEFT JOIN stores AS stores\n    ON orders.store_co = stores.co AND orders.store_no = stores.no\n  GROUP BY\n    orders.status\n)\nSELECT\n  _base.\"orders.status\",\n  _cm_orders__customers_spend_stores_rent_sum.\"orders.m\"\nFROM _base\nLEFT JOIN _cm_orders__customers_spend_stores_rent_sum\n  ON _base.\"orders.status\" IS NOT DISTINCT FROM _cm_orders__customers_spend_stores_rent_sum.\"orders.status\"",
  "lifted/two_branch::snowflake": "WITH _base AS (\n  SELECT\n    orders.status AS \"orders.status\"\n  FROM orders AS orders\n  GROUP BY\n    orders.status\n), _cm_orders__customers_spend_stores_rent_sum AS (\n  SELECT\n    orders.status AS \"orders.status\",\n    SUM(customers.spend - stores.rent) AS \"orders.m\"\n  FROM orders AS orders\n  LEFT JOIN customers AS customers\n    ON orders.customer_id = customers.id\n  LEFT JOIN stores AS stores\n    ON orders.store_co = stores.co AND orders.store_no = stores.no\n  GROUP BY\n    orders.status\n)\nSELECT\n  _base.\"orders.status\",\n  _cm_orders__customers_spend_stores_rent_sum.\"orders.m\"\nFROM _base\nLEFT JOIN _cm_orders__customers_spend_stores_rent_sum\n  ON _base.\"orders.status\" IS NOT DISTINCT FROM _cm_orders__customers_spend_stores_rent_sum.\"orders.status\"",
  "lifted/two_branch::sqlite": "WITH _base AS (\n  SELECT\n    orders.status AS \"orders.status\"\n  FROM orders AS orders\n  GROUP BY\n    orders.status\n), _cm_orders__customers_spend_stores_rent_sum AS (\n  SELECT\n    orders.status AS \"orders.status\",\n    SUM(customers.spend - stores.rent) AS \"orders.m\"\n  FROM orders AS orders\n  LEFT JOIN customers AS customers\n    ON orders.customer_id = customers.id\n  LEFT JOIN stores AS stores\n    ON orders.store_co = stores.co AND orders.store_no = stores.no\n  GROUP BY\n    orders.status\n)\nSELECT\n  _base.\"orders.status\",\n  _cm_orders__customers_spend_stores_rent_sum.\"orders.m\"\nFROM _base\nLEFT JOIN _cm_orders__customers_spend_stores_rent_sum\n  ON _base.\"orders.status\" IS _cm_orders__customers_spend_stores_rent_sum.\"orders.status\"",
  "lifted/two_branch::tsql": "WITH _base AS (\n  SELECT\n    orders.status AS [orders___status]\n  FROM orders AS orders\n  GROUP BY\n    orders.status\n), _cm_orders__customers_spend_stores_rent_sum AS (\n  SELECT\n    orders.status AS [orders___status],\n    SUM(customers.spend - stores.rent) AS [orders___m]\n  FROM orders AS orders\n  LEFT JOIN customers AS customers\n    ON orders.customer_id = customers.id\n  LEFT JOIN stores AS stores\n    ON orders.store_co = stores.co AND orders.store_no = stores.no\n  GROUP BY\n    orders.status\n)\nSELECT\n  _base.[orders___status],\n  _cm_orders__customers_spend_stores_rent_sum.[orders___m]\nFROM _base\nLEFT JOIN _cm_orders__customers_spend_stores_rent_sum\n  ON (\n    _base.[orders___status] = _cm_orders__customers_spend_stores_rent_sum.[orders___status]\n    OR (\n      _base.[orders___status] IS NULL\n      AND _cm_orders__customers_spend_stores_rent_sum.[orders___status] IS NULL\n    )\n  )",
  "lifted/windowed_inner::bigquery": "WITH _base AS (\n  SELECT\n    DATE_TRUNC(monthly.ordered_at, MONTH) AS `monthly___ordered_at`\n  FROM monthly AS monthly\n  GROUP BY\n    DATE_TRUNC(monthly.ordered_at, MONTH)\n), _base_2 AS (\n  SELECT\n    monthly.region AS `monthly___region`,\n    DATE_TRUNC(monthly.ordered_at, MONTH) AS `monthly___ordered_at_month`\n  FROM monthly AS monthly\n  GROUP BY\n    monthly.region,\n    DATE_TRUNC(monthly.ordered_at, MONTH)\n), _cm_monthly__amount_sum_window_90d_partition_by_region AS (\n  SELECT\n    `_base`.`monthly___region`,\n    `_base`.`monthly___ordered_at_month`,\n    CAST(SUM(_src._w_value) AS FLOAT64) AS `monthly___amount_sum_window_90d_partition_by_region`\n  FROM (\n    SELECT\n      monthly.region AS `monthly___region`,\n      DATE_TRUNC(monthly.ordered_at, MONTH) AS `monthly___ordered_at_month`\n    FROM monthly AS monthly\n    GROUP BY\n      monthly.region,\n      DATE_TRUNC(monthly.ordered_at, MONTH)\n  ) AS _base\n  LEFT JOIN (\n    SELECT\n      monthly.region AS _w_dim_0,\n      monthly.ordered_at AS _w_time,\n      monthly.amount AS _w_value\n    FROM monthly AS monthly\n  ) AS _src\n    ON _src._w_dim_0 IS NOT DISTINCT FROM `_base`.`monthly___region`\n    AND _src._w_time >= `_base`.`monthly___ordered_at_month` + INTERVAL '1' MONTH - INTERVAL '90' DAY\n    AND _src._w_time < `_base`.`monthly___ordered_at_month` + INTERVAL '1' MONTH\n  GROUP BY\n    `_base`.`monthly___region`,\n    `_base`.`monthly___ordered_at_month`\n), base_2 AS (\n  SELECT\n    `_base_2`.`monthly___region`,\n    `_base_2`.`monthly___ordered_at_month`,\n    `_cm_monthly__amount_sum_window_90d_partition_by_region`.`monthly___amount_sum_window_90d_partition_by_region` AS `monthly___amount_sum_window_90d_partition_by_region`\n  FROM _base_2\n  LEFT JOIN _cm_monthly__amount_sum_window_90d_partition_by_region\n    ON `_base_2`.`monthly___region` IS NOT DISTINCT FROM `_cm_monthly__amount_sum_window_90d_partition_by_region`.`monthly___region`\n    AND `_base_2`.`monthly___ordered_at_month` IS NOT DISTINCT FROM `_cm_monthly__amount_sum_window_90d_partition_by_region`.`monthly___ordered_at_month`\n), step1 AS (\n  SELECT\n    `monthly___region`,\n    `monthly___ordered_at_month`,\n    `monthly___amount_sum_window_90d_partition_by_region`,\n    RANK() OVER (ORDER BY `monthly___amount_sum_window_90d_partition_by_region` DESC) AS `monthly___rank`\n  FROM base_2\n), _cm_rank AS (\n  SELECT\n    `_stage_inner`.`monthly___region` AS `region`,\n    `_stage_inner`.`monthly___ordered_at_month` AS `ordered_at_month`,\n    `_stage_inner`.`monthly___rank` AS `rank`\n  FROM (\n    SELECT\n      `monthly___region`,\n      `monthly___ordered_at_month`,\n      `monthly___rank`\n    FROM (\n      SELECT\n        `monthly___region`,\n        `monthly___ordered_at_month`,\n        `monthly___amount_sum_window_90d_partition_by_region`,\n        `monthly___rank`\n      FROM step1\n    ) AS _outer\n  ) AS _stage_inner\n), _cm_monthly___sum AS (\n  SELECT\n    `_base`.`monthly___ordered_at_month` AS `monthly___ordered_at_month`,\n    SUM(_base._v) AS `monthly___m`\n  FROM (\n    SELECT\n      DATE_TRUNC(monthly.ordered_at, MONTH) AS `monthly___ordered_at_month`,\n      monthly.region AS _ek0,\n      DATE_TRUNC(monthly.ordered_at, MONTH) AS _ek1,\n      MAX(_cm_rank.`rank`) AS _v\n    FROM monthly AS monthly\n    LEFT JOIN _cm_rank\n      ON monthly.region IS NOT DISTINCT FROM _cm_rank.`region`\n      AND DATE_TRUNC(monthly.ordered_at, MONTH) IS NOT DISTINCT FROM _cm_rank.`ordered_at_month`\n    GROUP BY\n      DATE_TRUNC(monthly.ordered_at, MONTH),\n      monthly.region,\n      DATE_TRUNC(monthly.ordered_at, MONTH)\n  ) AS _base\n  GROUP BY\n    `_base`.`monthly___ordered_at_month`\n)\nSELECT\n  _base.`monthly___ordered_at`,\n  _cm_monthly___sum.`monthly___m`\nFROM _base\nLEFT JOIN _cm_monthly___sum\n  ON _base.`monthly___ordered_at` IS NOT DISTINCT FROM _cm_monthly___sum.`monthly___ordered_at_month`",
  "lifted/windowed_inner::duckdb": "WITH _base AS (\n  SELECT\n    DATE_TRUNC('MONTH', monthly.ordered_at) AS \"monthly.ordered_at\"\n  FROM monthly AS monthly\n  GROUP BY\n    DATE_TRUNC('MONTH', monthly.ordered_at)\n), _base_2 AS (\n  SELECT\n    monthly.region AS \"monthly.region\",\n    DATE_TRUNC('MONTH', monthly.ordered_at) AS \"monthly.ordered_at_month\"\n  FROM monthly AS monthly\n  GROUP BY\n    monthly.region,\n    DATE_TRUNC('MONTH', monthly.ordered_at)\n), _cm_monthly__amount_sum_window_90d_partition_by_region AS (\n  SELECT\n    _base.\"monthly.region\",\n    _base.\"monthly.ordered_at_month\",\n    CAST(SUM(_src._w_value) AS DOUBLE) AS \"monthly.amount_sum_window_90d_partition_by_region\"\n  FROM (\n    SELECT\n      monthly.region AS \"monthly.region\",\n      DATE_TRUNC('MONTH', monthly.ordered_at) AS \"monthly.ordered_at_month\"\n    FROM monthly AS monthly\n    GROUP BY\n      monthly.region,\n      DATE_TRUNC('MONTH', monthly.ordered_at)\n  ) AS _base\n  LEFT JOIN (\n    SELECT\n      monthly.region AS _w_dim_0,\n      monthly.ordered_at AS _w_time,\n      monthly.amount AS _w_value\n    FROM monthly AS monthly\n  ) AS _src\n    ON _src._w_dim_0 IS NOT DISTINCT FROM _base.\"monthly.region\"\n    AND _src._w_time >= _base.\"monthly.ordered_at_month\" + INTERVAL '1' MONTH - INTERVAL '90' DAY\n    AND _src._w_time < _base.\"monthly.ordered_at_month\" + INTERVAL '1' MONTH\n  GROUP BY\n    _base.\"monthly.region\",\n    _base.\"monthly.ordered_at_month\"\n), base_2 AS (\n  SELECT\n    _base_2.\"monthly.region\",\n    _base_2.\"monthly.ordered_at_month\",\n    _cm_monthly__amount_sum_window_90d_partition_by_region.\"monthly.amount_sum_window_90d_partition_by_region\" AS \"monthly.amount_sum_window_90d_partition_by_region\"\n  FROM _base_2\n  LEFT JOIN _cm_monthly__amount_sum_window_90d_partition_by_region\n    ON _base_2.\"monthly.region\" IS NOT DISTINCT FROM _cm_monthly__amount_sum_window_90d_partition_by_region.\"monthly.region\"\n    AND _base_2.\"monthly.ordered_at_month\" IS NOT DISTINCT FROM _cm_monthly__amount_sum_window_90d_partition_by_region.\"monthly.ordered_at_month\"\n), step1 AS (\n  SELECT\n    \"monthly.region\",\n    \"monthly.ordered_at_month\",\n    \"monthly.amount_sum_window_90d_partition_by_region\",\n    RANK() OVER (ORDER BY \"monthly.amount_sum_window_90d_partition_by_region\" DESC) AS \"monthly.rank\"\n  FROM base_2\n), _cm_rank AS (\n  SELECT\n    _stage_inner.\"monthly.region\" AS \"region\",\n    _stage_inner.\"monthly.ordered_at_month\" AS \"ordered_at_month\",\n    _stage_inner.\"monthly.rank\" AS \"rank\"\n  FROM (\n    SELECT\n      \"monthly.region\",\n      \"monthly.ordered_at_month\",\n      \"monthly.rank\"\n    FROM (\n      SELECT\n        \"monthly.region\",\n        \"monthly.ordered_at_month\",\n        \"monthly.amount_sum_window_90d_partition_by_region\",\n        \"monthly.rank\"\n      FROM step1\n    ) AS _outer\n  ) AS _stage_inner\n), _cm_monthly___sum AS (\n  SELECT\n    _base.\"monthly.ordered_at_month\" AS \"monthly.ordered_at_month\",\n    SUM(_base._v) AS \"monthly.m\"\n  FROM (\n    SELECT\n      DATE_TRUNC('MONTH', monthly.ordered_at) AS \"monthly.ordered_at_month\",\n      monthly.region AS _ek0,\n      DATE_TRUNC('MONTH', monthly.ordered_at) AS _ek1,\n      MAX(_cm_rank.\"rank\") AS _v\n    FROM monthly AS monthly\n    LEFT JOIN _cm_rank\n      ON monthly.region IS NOT DISTINCT FROM _cm_rank.\"region\"\n      AND DATE_TRUNC('MONTH', monthly.ordered_at) IS NOT DISTINCT FROM _cm_rank.\"ordered_at_month\"\n    GROUP BY\n      DATE_TRUNC('MONTH', monthly.ordered_at),\n      monthly.region,\n      DATE_TRUNC('MONTH', monthly.ordered_at)\n  ) AS _base\n  GROUP BY\n    _base.\"monthly.ordered_at_month\"\n)\nSELECT\n  _base.\"monthly.ordered_at\",\n  _cm_monthly___sum.\"monthly.m\"\nFROM _base\nLEFT JOIN _cm_monthly___sum\n  ON _base.\"monthly.ordered_at\" IS NOT DISTINCT FROM _cm_monthly___sum.\"monthly.ordered_at_month\"",
  "lifted/windowed_inner::mysql": "WITH _base AS (\n  SELECT\n    STR_TO_DATE(CONCAT(YEAR(monthly.ordered_at), ' ', MONTH(monthly.ordered_at), ' 1'), '%Y %c %e') AS `monthly.ordered_at`\n  FROM monthly AS monthly\n  GROUP BY\n    STR_TO_DATE(CONCAT(YEAR(monthly.ordered_at), ' ', MONTH(monthly.ordered_at), ' 1'), '%Y %c %e')\n), _base_2 AS (\n  SELECT\n    monthly.region AS `monthly.region`,\n    STR_TO_DATE(CONCAT(YEAR(monthly.ordered_at), ' ', MONTH(monthly.ordered_at), ' 1'), '%Y %c %e') AS `monthly.ordered_at_month`\n  FROM monthly AS monthly\n  GROUP BY\n    monthly.region,\n    STR_TO_DATE(CONCAT(YEAR(monthly.ordered_at), ' ', MONTH(monthly.ordered_at), ' 1'), '%Y %c %e')\n), _cm_monthly__amount_sum_window_90d_partition_by_region AS (\n  SELECT\n    _base.`monthly.region`,\n    _base.`monthly.ordered_at_month`,\n    CAST(SUM(_src._w_value) AS DOUBLE) AS `monthly.amount_sum_window_90d_partition_by_region`\n  FROM (\n    SELECT\n      monthly.region AS `monthly.region`,\n      STR_TO_DATE(CONCAT(YEAR(monthly.ordered_at), ' ', MONTH(monthly.ordered_at), ' 1'), '%Y %c %e') AS `monthly.ordered_at_month`\n    FROM monthly AS monthly\n    GROUP BY\n      monthly.region,\n      STR_TO_DATE(CONCAT(YEAR(monthly.ordered_at), ' ', MONTH(monthly.ordered_at), ' 1'), '%Y %c %e')\n  ) AS _base\n  LEFT JOIN (\n    SELECT\n      monthly.region AS _w_dim_0,\n      monthly.ordered_at AS _w_time,\n      monthly.amount AS _w_value\n    FROM monthly AS monthly\n  ) AS _src\n    ON _src._w_dim_0 <=> _base.`monthly.region`\n    AND _src._w_time >= _base.`monthly.ordered_at_month` + INTERVAL '1' MONTH - INTERVAL '90' DAY\n    AND _src._w_time < _base.`monthly.ordered_at_month` + INTERVAL '1' MONTH\n  GROUP BY\n    _base.`monthly.region`,\n    _base.`monthly.ordered_at_month`\n), base_2 AS (\n  SELECT\n    _base_2.`monthly.region`,\n    _base_2.`monthly.ordered_at_month`,\n    _cm_monthly__amount_sum_window_90d_partition_by_region.`monthly.amount_sum_window_90d_partition_by_region` AS `monthly.amount_sum_window_90d_partition_by_region`\n  FROM _base_2\n  LEFT JOIN _cm_monthly__amount_sum_window_90d_partition_by_region\n    ON _base_2.`monthly.region` <=> _cm_monthly__amount_sum_window_90d_partition_by_region.`monthly.region`\n    AND _base_2.`monthly.ordered_at_month` <=> _cm_monthly__amount_sum_window_90d_partition_by_region.`monthly.ordered_at_month`\n), step1 AS (\n  SELECT\n    `monthly.region`,\n    `monthly.ordered_at_month`,\n    `monthly.amount_sum_window_90d_partition_by_region`,\n    RANK() OVER (ORDER BY `monthly.amount_sum_window_90d_partition_by_region` DESC) AS `monthly.rank`\n  FROM base_2\n), _cm_rank AS (\n  SELECT\n    _stage_inner.`monthly.region` AS `region`,\n    _stage_inner.`monthly.ordered_at_month` AS `ordered_at_month`,\n    _stage_inner.`monthly.rank` AS `rank`\n  FROM (\n    SELECT\n      `monthly.region`,\n      `monthly.ordered_at_month`,\n      `monthly.rank`\n    FROM (\n      SELECT\n        `monthly.region`,\n        `monthly.ordered_at_month`,\n        `monthly.amount_sum_window_90d_partition_by_region`,\n        `monthly.rank`\n      FROM step1\n    ) AS _outer\n  ) AS _stage_inner\n), _cm_monthly___sum AS (\n  SELECT\n    _base.`monthly.ordered_at_month` AS `monthly.ordered_at_month`,\n    SUM(_base._v) AS `monthly.m`\n  FROM (\n    SELECT\n      STR_TO_DATE(CONCAT(YEAR(monthly.ordered_at), ' ', MONTH(monthly.ordered_at), ' 1'), '%Y %c %e') AS `monthly.ordered_at_month`,\n      monthly.region AS _ek0,\n      STR_TO_DATE(CONCAT(YEAR(monthly.ordered_at), ' ', MONTH(monthly.ordered_at), ' 1'), '%Y %c %e') AS _ek1,\n      MAX(_cm_rank.`rank`) AS _v\n    FROM monthly AS monthly\n    LEFT JOIN _cm_rank\n      ON monthly.region <=> _cm_rank.`region`\n      AND STR_TO_DATE(CONCAT(YEAR(monthly.ordered_at), ' ', MONTH(monthly.ordered_at), ' 1'), '%Y %c %e') <=> _cm_rank.`ordered_at_month`\n    GROUP BY\n      STR_TO_DATE(CONCAT(YEAR(monthly.ordered_at), ' ', MONTH(monthly.ordered_at), ' 1'), '%Y %c %e'),\n      monthly.region,\n      STR_TO_DATE(CONCAT(YEAR(monthly.ordered_at), ' ', MONTH(monthly.ordered_at), ' 1'), '%Y %c %e')\n  ) AS _base\n  GROUP BY\n    _base.`monthly.ordered_at_month`\n)\nSELECT\n  _base.`monthly.ordered_at`,\n  _cm_monthly___sum.`monthly.m`\nFROM _base\nLEFT JOIN _cm_monthly___sum\n  ON _base.`monthly.ordered_at` <=> _cm_monthly___sum.`monthly.ordered_at_month`",
  "lifted/windowed_inner::postgres": "WITH _base AS (\n  SELECT\n    DATE_TRUNC('MONTH', monthly.ordered_at) AS \"monthly.ordered_at\"\n  FROM monthly AS monthly\n  GROUP BY\n    DATE_TRUNC('MONTH', monthly.ordered_at)\n), _base_2 AS (\n  SELECT\n    monthly.region AS \"monthly.region\",\n    DATE_TRUNC('MONTH', monthly.ordered_at) AS \"monthly.ordered_at_month\"\n  FROM monthly AS monthly\n  GROUP BY\n    monthly.region,\n    DATE_TRUNC('MONTH', monthly.ordered_at)\n), _cm_monthly__amount_sum_window_90d_partition_by_region AS (\n  SELECT\n    _base.\"monthly.region\",\n    _base.\"monthly.ordered_at_month\",\n    CAST(SUM(_src._w_value) AS DOUBLE PRECISION) AS \"monthly.amount_sum_window_90d_partition_by_region\"\n  FROM (\n    SELECT\n      monthly.region AS \"monthly.region\",\n      DATE_TRUNC('MONTH', monthly.ordered_at) AS \"monthly.ordered_at_month\"\n    FROM monthly AS monthly\n    GROUP BY\n      monthly.region,\n      DATE_TRUNC('MONTH', monthly.ordered_at)\n  ) AS _base\n  LEFT JOIN (\n    SELECT\n      monthly.region AS _w_dim_0,\n      monthly.ordered_at AS _w_time,\n      monthly.amount AS _w_value\n    FROM monthly AS monthly\n  ) AS _src\n    ON _src._w_dim_0 IS NOT DISTINCT FROM _base.\"monthly.region\"\n    AND _src._w_time >= _base.\"monthly.ordered_at_month\" + INTERVAL '1 MONTH' - INTERVAL '90 DAY'\n    AND _src._w_time < _base.\"monthly.ordered_at_month\" + INTERVAL '1 MONTH'\n  GROUP BY\n    _base.\"monthly.region\",\n    _base.\"monthly.ordered_at_month\"\n), base_2 AS (\n  SELECT\n    _base_2.\"monthly.region\",\n    _base_2.\"monthly.ordered_at_month\",\n    _cm_monthly__amount_sum_window_90d_partition_by_region.\"monthly.amount_sum_window_90d_partition_by_region\" AS \"monthly.amount_sum_window_90d_partition_by_region\"\n  FROM _base_2\n  LEFT JOIN _cm_monthly__amount_sum_window_90d_partition_by_region\n    ON _base_2.\"monthly.region\" IS NOT DISTINCT FROM _cm_monthly__amount_sum_window_90d_partition_by_region.\"monthly.region\"\n    AND _base_2.\"monthly.ordered_at_month\" IS NOT DISTINCT FROM _cm_monthly__amount_sum_window_90d_partition_by_region.\"monthly.ordered_at_month\"\n), step1 AS (\n  SELECT\n    \"monthly.region\",\n    \"monthly.ordered_at_month\",\n    \"monthly.amount_sum_window_90d_partition_by_region\",\n    RANK() OVER (ORDER BY \"monthly.amount_sum_window_90d_partition_by_region\" DESC NULLS LAST) AS \"monthly.rank\"\n  FROM base_2\n), _cm_rank AS (\n  SELECT\n    _stage_inner.\"monthly.region\" AS \"region\",\n    _stage_inner.\"monthly.ordered_at_month\" AS \"ordered_at_month\",\n    _stage_inner.\"monthly.rank\" AS \"rank\"\n  FROM (\n    SELECT\n      \"monthly.region\",\n      \"monthly.ordered_at_month\",\n      \"monthly.rank\"\n    FROM (\n      SELECT\n        \"monthly.region\",\n        \"monthly.ordered_at_month\",\n        \"monthly.amount_sum_window_90d_partition_by_region\",\n        \"monthly.rank\"\n      FROM step1\n    ) AS _outer\n  ) AS _stage_inner\n), _cm_monthly___sum AS (\n  SELECT\n    _base.\"monthly.ordered_at_month\" AS \"monthly.ordered_at_month\",\n    SUM(_base._v) AS \"monthly.m\"\n  FROM (\n    SELECT\n      DATE_TRUNC('MONTH', monthly.ordered_at) AS \"monthly.ordered_at_month\",\n      monthly.region AS _ek0,\n      DATE_TRUNC('MONTH', monthly.ordered_at) AS _ek1,\n      MAX(_cm_rank.\"rank\") AS _v\n    FROM monthly AS monthly\n    LEFT JOIN _cm_rank\n      ON monthly.region IS NOT DISTINCT FROM _cm_rank.\"region\"\n      AND DATE_TRUNC('MONTH', monthly.ordered_at) IS NOT DISTINCT FROM _cm_rank.\"ordered_at_month\"\n    GROUP BY\n      DATE_TRUNC('MONTH', monthly.ordered_at),\n      monthly.region,\n      DATE_TRUNC('MONTH', monthly.ordered_at)\n  ) AS _base\n  GROUP BY\n    _base.\"monthly.ordered_at_month\"\n)\nSELECT\n  _base.\"monthly.ordered_at\",\n  _cm_monthly___sum.\"monthly.m\"\nFROM _base\nLEFT JOIN _cm_monthly___sum\n  ON _base.\"monthly.ordered_at\" IS NOT DISTINCT FROM _cm_monthly___sum.\"monthly.ordered_at_month\"",
  "lifted/windowed_inner::snowflake": "WITH _base AS (\n  SELECT\n    DATE_TRUNC('MONTH', monthly.ordered_at) AS \"monthly.ordered_at\"\n  FROM monthly AS monthly\n  GROUP BY\n    DATE_TRUNC('MONTH', monthly.ordered_at)\n), _base_2 AS (\n  SELECT\n    monthly.region AS \"monthly.region\",\n    DATE_TRUNC('MONTH', monthly.ordered_at) AS \"monthly.ordered_at_month\"\n  FROM monthly AS monthly\n  GROUP BY\n    monthly.region,\n    DATE_TRUNC('MONTH', monthly.ordered_at)\n), _cm_monthly__amount_sum_window_90d_partition_by_region AS (\n  SELECT\n    _base.\"monthly.region\",\n    _base.\"monthly.ordered_at_month\",\n    CAST(SUM(_src._w_value) AS DOUBLE) AS \"monthly.amount_sum_window_90d_partition_by_region\"\n  FROM (\n    SELECT\n      monthly.region AS \"monthly.region\",\n      DATE_TRUNC('MONTH', monthly.ordered_at) AS \"monthly.ordered_at_month\"\n    FROM monthly AS monthly\n    GROUP BY\n      monthly.region,\n      DATE_TRUNC('MONTH', monthly.ordered_at)\n  ) AS _base\n  LEFT JOIN (\n    SELECT\n      monthly.region AS _w_dim_0,\n      monthly.ordered_at AS _w_time,\n      monthly.amount AS _w_value\n    FROM monthly AS monthly\n  ) AS _src\n    ON _src._w_dim_0 IS NOT DISTINCT FROM _base.\"monthly.region\"\n    AND _src._w_time >= _base.\"monthly.ordered_at_month\" + INTERVAL '1 MONTH' - INTERVAL '90 DAY'\n    AND _src._w_time < _base.\"monthly.ordered_at_month\" + INTERVAL '1 MONTH'\n  GROUP BY\n    _base.\"monthly.region\",\n    _base.\"monthly.ordered_at_month\"\n), base_2 AS (\n  SELECT\n    _base_2.\"monthly.region\",\n    _base_2.\"monthly.ordered_at_month\",\n    _cm_monthly__amount_sum_window_90d_partition_by_region.\"monthly.amount_sum_window_90d_partition_by_region\" AS \"monthly.amount_sum_window_90d_partition_by_region\"\n  FROM _base_2\n  LEFT JOIN _cm_monthly__amount_sum_window_90d_partition_by_region\n    ON _base_2.\"monthly.region\" IS NOT DISTINCT FROM _cm_monthly__amount_sum_window_90d_partition_by_region.\"monthly.region\"\n    AND _base_2.\"monthly.ordered_at_month\" IS NOT DISTINCT FROM _cm_monthly__amount_sum_window_90d_partition_by_region.\"monthly.ordered_at_month\"\n), step1 AS (\n  SELECT\n    \"monthly.region\",\n    \"monthly.ordered_at_month\",\n    \"monthly.amount_sum_window_90d_partition_by_region\",\n    RANK() OVER (ORDER BY \"monthly.amount_sum_window_90d_partition_by_region\" DESC NULLS LAST) AS \"monthly.rank\"\n  FROM base_2\n), _cm_rank AS (\n  SELECT\n    _stage_inner.\"monthly.region\" AS \"region\",\n    _stage_inner.\"monthly.ordered_at_month\" AS \"ordered_at_month\",\n    _stage_inner.\"monthly.rank\" AS \"rank\"\n  FROM (\n    SELECT\n      \"monthly.region\",\n      \"monthly.ordered_at_month\",\n      \"monthly.rank\"\n    FROM (\n      SELECT\n        \"monthly.region\",\n        \"monthly.ordered_at_month\",\n        \"monthly.amount_sum_window_90d_partition_by_region\",\n        \"monthly.rank\"\n      FROM step1\n    ) AS _outer\n  ) AS _stage_inner\n), _cm_monthly___sum AS (\n  SELECT\n    _base.\"monthly.ordered_at_month\" AS \"monthly.ordered_at_month\",\n    SUM(_base._v) AS \"monthly.m\"\n  FROM (\n    SELECT\n      DATE_TRUNC('MONTH', monthly.ordered_at) AS \"monthly.ordered_at_month\",\n      monthly.region AS _ek0,\n      DATE_TRUNC('MONTH', monthly.ordered_at) AS _ek1,\n      MAX(_cm_rank.\"rank\") AS _v\n    FROM monthly AS monthly\n    LEFT JOIN _cm_rank\n      ON monthly.region IS NOT DISTINCT FROM _cm_rank.\"region\"\n      AND DATE_TRUNC('MONTH', monthly.ordered_at) IS NOT DISTINCT FROM _cm_rank.\"ordered_at_month\"\n    GROUP BY\n      DATE_TRUNC('MONTH', monthly.ordered_at),\n      monthly.region,\n      DATE_TRUNC('MONTH', monthly.ordered_at)\n  ) AS _base\n  GROUP BY\n    _base.\"monthly.ordered_at_month\"\n)\nSELECT\n  _base.\"monthly.ordered_at\",\n  _cm_monthly___sum.\"monthly.m\"\nFROM _base\nLEFT JOIN _cm_monthly___sum\n  ON _base.\"monthly.ordered_at\" IS NOT DISTINCT FROM _cm_monthly___sum.\"monthly.ordered_at_month\"",
  "lifted/windowed_inner::sqlite": "WITH _base AS (\n  SELECT\n    STRFTIME('%Y-%m-01', monthly.ordered_at) AS \"monthly.ordered_at\"\n  FROM monthly AS monthly\n  GROUP BY\n    STRFTIME('%Y-%m-01', monthly.ordered_at)\n), _base_2 AS (\n  SELECT\n    monthly.region AS \"monthly.region\",\n    STRFTIME('%Y-%m-01', monthly.ordered_at) AS \"monthly.ordered_at_month\"\n  FROM monthly AS monthly\n  GROUP BY\n    monthly.region,\n    STRFTIME('%Y-%m-01', monthly.ordered_at)\n), _cm_monthly__amount_sum_window_90d_partition_by_region AS (\n  SELECT\n    _base.\"monthly.region\",\n    _base.\"monthly.ordered_at_month\",\n    CAST(SUM(_src._w_value) AS REAL) AS \"monthly.amount_sum_window_90d_partition_by_region\"\n  FROM (\n    SELECT\n      monthly.region AS \"monthly.region\",\n      STRFTIME('%Y-%m-01', monthly.ordered_at) AS \"monthly.ordered_at_month\"\n    FROM monthly AS monthly\n    GROUP BY\n      monthly.region,\n      STRFTIME('%Y-%m-01', monthly.ordered_at)\n  ) AS _base\n  LEFT JOIN (\n    SELECT\n      monthly.region AS _w_dim_0,\n      monthly.ordered_at AS _w_time,\n      monthly.amount AS _w_value\n    FROM monthly AS monthly\n  ) AS _src\n    ON _src._w_dim_0 IS _base.\"monthly.region\"\n    AND _src._w_time >= DATETIME(DATETIME(_base.\"monthly.ordered_at_month\", '+1 months'), '-90 days')\n    AND _src._w_time < DATETIME(_base.\"monthly.ordered_at_month\", '+1 months')\n  GROUP BY\n    _base.\"monthly.region\",\n    _base.\"monthly.ordered_at_month\"\n), base_2 AS (\n  SELECT\n    _base_2.\"monthly.region\",\n    _base_2.\"monthly.ordered_at_month\",\n    _cm_monthly__amount_sum_window_90d_partition_by_region.\"monthly.amount_sum_window_90d_partition_by_region\" AS \"monthly.amount_sum_window_90d_partition_by_region\"\n  FROM _base_2\n  LEFT JOIN _cm_monthly__amount_sum_window_90d_partition_by_region\n    ON _base_2.\"monthly.region\" IS _cm_monthly__amount_sum_window_90d_partition_by_region.\"monthly.region\"\n    AND _base_2.\"monthly.ordered_at_month\" IS _cm_monthly__amount_sum_window_90d_partition_by_region.\"monthly.ordered_at_month\"\n), step1 AS (\n  SELECT\n    \"monthly.region\",\n    \"monthly.ordered_at_month\",\n    \"monthly.amount_sum_window_90d_partition_by_region\",\n    RANK() OVER (ORDER BY \"monthly.amount_sum_window_90d_partition_by_region\" DESC) AS \"monthly.rank\"\n  FROM base_2\n), _cm_rank AS (\n  SELECT\n    _stage_inner.\"monthly.region\" AS \"region\",\n    _stage_inner.\"monthly.ordered_at_month\" AS \"ordered_at_month\",\n    _stage_inner.\"monthly.rank\" AS \"rank\"\n  FROM (\n    SELECT\n      \"monthly.region\",\n      \"monthly.ordered_at_month\",\n      \"monthly.rank\"\n    FROM (\n      SELECT\n        \"monthly.region\",\n        \"monthly.ordered_at_month\",\n        \"monthly.amount_sum_window_90d_partition_by_region\",\n        \"monthly.rank\"\n      FROM step1\n    ) AS _outer\n  ) AS _stage_inner\n), _cm_monthly___sum AS (\n  SELECT\n    _base.\"monthly.ordered_at_month\" AS \"monthly.ordered_at_month\",\n    SUM(_base._v) AS \"monthly.m\"\n  FROM (\n    SELECT\n      STRFTIME('%Y-%m-01', monthly.ordered_at) AS \"monthly.ordered_at_month\",\n      monthly.region AS _ek0,\n      STRFTIME('%Y-%m-01', monthly.ordered_at) AS _ek1,\n      MAX(_cm_rank.\"rank\") AS _v\n    FROM monthly AS monthly\n    LEFT JOIN _cm_rank\n      ON monthly.region IS _cm_rank.\"region\"\n      AND STRFTIME('%Y-%m-01', monthly.ordered_at) IS _cm_rank.\"ordered_at_month\"\n    GROUP BY\n      STRFTIME('%Y-%m-01', monthly.ordered_at),\n      monthly.region,\n      STRFTIME('%Y-%m-01', monthly.ordered_at)\n  ) AS _base\n  GROUP BY\n    _base.\"monthly.ordered_at_month\"\n)\nSELECT\n  _base.\"monthly.ordered_at\",\n  _cm_monthly___sum.\"monthly.m\"\nFROM _base\nLEFT JOIN _cm_monthly___sum\n  ON _base.\"monthly.ordered_at\" IS _cm_monthly___sum.\"monthly.ordered_at_month\"",
  "lifted/windowed_inner::tsql": "WITH _base AS (\n  SELECT\n    DATETRUNC(month, monthly.ordered_at) AS [monthly___ordered_at]\n  FROM monthly AS monthly\n  GROUP BY\n    DATETRUNC(month, monthly.ordered_at)\n), _base_2 AS (\n  SELECT\n    monthly.region AS [monthly___region],\n    DATETRUNC(MONTH, monthly.ordered_at) AS [monthly___ordered_at_month]\n  FROM monthly AS monthly\n  GROUP BY\n    monthly.region,\n    DATETRUNC(MONTH, monthly.ordered_at)\n), _cm_monthly__amount_sum_window_90d_partition_by_region AS (\n  SELECT\n    _base.[monthly___region] AS [monthly___region],\n    _base.[monthly___ordered_at_month] AS [monthly___ordered_at_month],\n    CAST(SUM(_src._w_value) AS FLOAT) AS [monthly___amount_sum_window_90d_partition_by_region]\n  FROM (\n    SELECT\n      monthly.region AS [monthly___region],\n      DATETRUNC(MONTH, monthly.ordered_at) AS [monthly___ordered_at_month]\n    FROM monthly AS monthly\n    GROUP BY\n      monthly.region,\n      DATETRUNC(MONTH, monthly.ordered_at)\n  ) AS _base\n  LEFT JOIN (\n    SELECT\n      monthly.region AS _w_dim_0,\n      monthly.ordered_at AS _w_time,\n      monthly.amount AS _w_value\n    FROM monthly AS monthly\n  ) AS _src\n    ON (\n      _src._w_dim_0 = _base.[monthly___region]\n      OR (\n        _src._w_dim_0 IS NULL AND _base.[monthly___region] IS NULL\n      )\n    )\n    AND _src._w_time >= DATEADD(DAY, -90, DATEADD(MONTH, 1, _base.[monthly___ordered_at_month]))\n    AND _src._w_time < DATEADD(MONTH, 1, _base.[monthly___ordered_at_month])\n  GROUP BY\n    _base.[monthly___region],\n    _base.[monthly___ordered_at_month]\n), base_2 AS (\n  SELECT\n    _base_2.[monthly___region] AS [monthly___region],\n    _base_2.[monthly___ordered_at_month] AS [monthly___ordered_at_month],\n    _cm_monthly__amount_sum_window_90d_partition_by_region.[monthly___amount_sum_window_90d_partition_by_region] AS [monthly___amount_sum_window_90d_partition_by_region]\n  FROM _base_2\n  LEFT JOIN _cm_monthly__amount_sum_window_90d_partition_by_region\n    ON (\n      _base_2.[monthly___region] = _cm_monthly__amount_sum_window_90d_partition_by_region.[monthly___region]\n      OR (\n        _base_2.[monthly___region] IS NULL\n        AND _cm_monthly__amount_sum_window_90d_partition_by_region.[monthly___region] IS NULL\n      )\n    )\n    AND (\n      _base_2.[monthly___ordered_at_month] = _cm_monthly__amount_sum_window_90d_partition_by_region.[monthly___ordered_at_month]\n      OR (\n        _base_2.[monthly___ordered_at_month] IS NULL\n        AND _cm_monthly__amount_sum_window_90d_partition_by_region.[monthly___ordered_at_month] IS NULL\n      )\n    )\n), step1 AS (\n  SELECT\n    [monthly___region] AS [monthly___region],\n    [monthly___ordered_at_month] AS [monthly___ordered_at_month],\n    [monthly___amount_sum_window_90d_partition_by_region] AS [monthly___amount_sum_window_90d_partition_by_region],\n    RANK() OVER (ORDER BY [monthly___amount_sum_window_90d_partition_by_region] DESC) AS [monthly___rank]\n  FROM base_2\n), _cm_rank AS (\n  SELECT\n    _stage_inner.[monthly___region] AS [region],\n    _stage_inner.[monthly___ordered_at_month] AS [ordered_at_month],\n    _stage_inner.[monthly___rank] AS [rank]\n  FROM (\n    SELECT\n      [monthly___region] AS [monthly___region],\n      [monthly___ordered_at_month] AS [monthly___ordered_at_month],\n      [monthly___rank] AS [monthly___rank]\n    FROM (\n      SELECT\n        [monthly___region] AS [monthly___region],\n        [monthly___ordered_at_month] AS [monthly___ordered_at_month],\n        [monthly___amount_sum_window_90d_partition_by_region] AS [monthly___amount_sum_window_90d_partition_by_region],\n        [monthly___rank] AS [monthly___rank]\n      FROM step1\n    ) AS _outer\n  ) AS _stage_inner\n), _cm_monthly___sum AS (\n  SELECT\n    _base.[monthly___ordered_at_month] AS [monthly___ordered_at_month],\n    SUM(_base._v) AS [monthly___m]\n  FROM (\n    SELECT\n      DATETRUNC(MONTH, monthly.ordered_at) AS [monthly___ordered_at_month],\n      monthly.region AS _ek0,\n      DATETRUNC(MONTH, monthly.ordered_at) AS _ek1,\n      MAX(_cm_rank.[rank]) AS _v\n    FROM monthly AS monthly\n    LEFT JOIN _cm_rank\n      ON (\n        monthly.region = _cm_rank.[region]\n        OR (\n          monthly.region IS NULL AND _cm_rank.[region] IS NULL\n        )\n      )\n      AND (\n        DATETRUNC(MONTH, monthly.ordered_at) = _cm_rank.[ordered_at_month]\n        OR (\n          DATETRUNC(MONTH, monthly.ordered_at) IS NULL\n          AND _cm_rank.[ordered_at_month] IS NULL\n        )\n      )\n    GROUP BY\n      DATETRUNC(MONTH, monthly.ordered_at),\n      monthly.region,\n      DATETRUNC(MONTH, monthly.ordered_at)\n  ) AS _base\n  GROUP BY\n    _base.[monthly___ordered_at_month]\n)\nSELECT\n  _base.[monthly___ordered_at],\n  _cm_monthly___sum.[monthly___m]\nFROM _base\nLEFT JOIN _cm_monthly___sum\n  ON (\n    _base.[monthly___ordered_at] = _cm_monthly___sum.[monthly___ordered_at_month]\n    OR (\n      _base.[monthly___ordered_at] IS NULL\n      AND _cm_monthly___sum.[monthly___ordered_at_month] IS NULL\n    )\n  )",
  "positive/mixed_row_attached::bigquery": "WITH _cm_unit_price_avg_partition_by_product AS (\n  SELECT\n    _stage_inner.`sales___product` AS `product`,\n    _stage_inner.`sales___unit_price_avg_partition_by_product` AS `unit_price_avg_partition_by_product`\n  FROM (\n    SELECT\n      sales.product AS `sales___product`,\n      CAST(AVG(sales.unit_price) AS FLOAT64) AS `sales___unit_price_avg_partition_by_product`\n    FROM sales AS sales\n    GROUP BY\n      sales.product\n  ) AS _stage_inner\n)\nSELECT\n  sales.region AS `sales___region`,\n  SUM(\n    sales.quantity * _cm_unit_price_avg_partition_by_product.`unit_price_avg_partition_by_product`\n  ) AS `sales___m`\nFROM sales AS sales\nLEFT JOIN _cm_unit_price_avg_partition_by_product\n  ON sales.product IS NOT DISTINCT FROM _cm_unit_price_avg_partition_by_product.`product`\nGROUP BY\n  sales.region",
  "positive/mixed_row_attached::duckdb": "WITH _cm_unit_price_avg_partition_by_product AS (\n  SELECT\n    _stage_inner.\"sales.product\" AS \"product\",\n    _stage_inner.\"sales.unit_price_avg_partition_by_product\" AS \"unit_price_avg_partition_by_product\"\n  FROM (\n    SELECT\n      sales.product AS \"sales.product\",\n      CAST(AVG(sales.unit_price) AS DOUBLE) AS \"sales.unit_price_avg_partition_by_product\"\n    FROM sales AS sales\n    GROUP BY\n      sales.product\n  ) AS _stage_inner\n)\nSELECT\n  sales.region AS \"sales.region\",\n  SUM(\n    sales.quantity * _cm_unit_price_avg_partition_by_product.\"unit_price_avg_partition_by_product\"\n  ) AS \"sales.m\"\nFROM sales AS sales\nLEFT JOIN _cm_unit_price_avg_partition_by_product\n  ON sales.product IS NOT DISTINCT FROM _cm_unit_price_avg_partition_by_product.\"product\"\nGROUP BY\n  sales.region",
  "positive/mixed_row_attached::mysql": "WITH _cm_unit_price_avg_partition_by_product AS (\n  SELECT\n    _stage_inner.`sales.product` AS `product`,\n    _stage_inner.`sales.unit_price_avg_partition_by_product` AS `unit_price_avg_partition_by_product`\n  FROM (\n    SELECT\n      sales.product AS `sales.product`,\n      CAST(AVG(sales.unit_price) AS DOUBLE) AS `sales.unit_price_avg_partition_by_product`\n    FROM sales AS sales\n    GROUP BY\n      sales.product\n  ) AS _stage_inner\n)\nSELECT\n  sales.region AS `sales.region`,\n  SUM(\n    sales.quantity * _cm_unit_price_avg_partition_by_product.`unit_price_avg_partition_by_product`\n  ) AS `sales.m`\nFROM sales AS sales\nLEFT JOIN _cm_unit_price_avg_partition_by_product\n  ON sales.product <=> _cm_unit_price_avg_partition_by_product.`product`\nGROUP BY\n  sales.region",
  "positive/mixed_row_attached::postgres": "WITH _cm_unit_price_avg_partition_by_product AS (\n  SELECT\n    _stage_inner.\"sales.product\" AS \"product\",\n    _stage_inner.\"sales.unit_price_avg_partition_by_product\" AS \"unit_price_avg_partition_by_product\"\n  FROM (\n    SELECT\n      sales.product AS \"sales.product\",\n      CAST(AVG(sales.unit_price) AS DOUBLE PRECISION) AS \"sales.unit_price_avg_partition_by_product\"\n    FROM sales AS sales\n    GROUP BY\n      sales.product\n  ) AS _stage_inner\n)\nSELECT\n  sales.region AS \"sales.region\",\n  SUM(\n    sales.quantity * _cm_unit_price_avg_partition_by_product.\"unit_price_avg_partition_by_product\"\n  ) AS \"sales.m\"\nFROM sales AS sales\nLEFT JOIN _cm_unit_price_avg_partition_by_product\n  ON sales.product IS NOT DISTINCT FROM _cm_unit_price_avg_partition_by_product.\"product\"\nGROUP BY\n  sales.region",
  "positive/mixed_row_attached::snowflake": "WITH _cm_unit_price_avg_partition_by_product AS (\n  SELECT\n    _stage_inner.\"sales.product\" AS \"product\",\n    _stage_inner.\"sales.unit_price_avg_partition_by_product\" AS \"unit_price_avg_partition_by_product\"\n  FROM (\n    SELECT\n      sales.product AS \"sales.product\",\n      CAST(AVG(sales.unit_price) AS DOUBLE) AS \"sales.unit_price_avg_partition_by_product\"\n    FROM sales AS sales\n    GROUP BY\n      sales.product\n  ) AS _stage_inner\n)\nSELECT\n  sales.region AS \"sales.region\",\n  SUM(\n    sales.quantity * _cm_unit_price_avg_partition_by_product.\"unit_price_avg_partition_by_product\"\n  ) AS \"sales.m\"\nFROM sales AS sales\nLEFT JOIN _cm_unit_price_avg_partition_by_product\n  ON sales.product IS NOT DISTINCT FROM _cm_unit_price_avg_partition_by_product.\"product\"\nGROUP BY\n  sales.region",
  "positive/mixed_row_attached::sqlite": "WITH _cm_unit_price_avg_partition_by_product AS (\n  SELECT\n    _stage_inner.\"sales.product\" AS \"product\",\n    _stage_inner.\"sales.unit_price_avg_partition_by_product\" AS \"unit_price_avg_partition_by_product\"\n  FROM (\n    SELECT\n      sales.product AS \"sales.product\",\n      CAST(AVG(sales.unit_price) AS REAL) AS \"sales.unit_price_avg_partition_by_product\"\n    FROM sales AS sales\n    GROUP BY\n      sales.product\n  ) AS _stage_inner\n)\nSELECT\n  sales.region AS \"sales.region\",\n  SUM(\n    sales.quantity * _cm_unit_price_avg_partition_by_product.\"unit_price_avg_partition_by_product\"\n  ) AS \"sales.m\"\nFROM sales AS sales\nLEFT JOIN _cm_unit_price_avg_partition_by_product\n  ON sales.product IS _cm_unit_price_avg_partition_by_product.\"product\"\nGROUP BY\n  sales.region",
  "positive/mixed_row_attached::tsql": "WITH _cm_unit_price_avg_partition_by_product AS (\n  SELECT\n    _stage_inner.[sales___product] AS [product],\n    _stage_inner.[sales___unit_price_avg_partition_by_product] AS [unit_price_avg_partition_by_product]\n  FROM (\n    SELECT\n      sales.product AS [sales___product],\n      CAST(AVG(sales.unit_price) AS FLOAT) AS [sales___unit_price_avg_partition_by_product]\n    FROM sales AS sales\n    GROUP BY\n      sales.product\n  ) AS _stage_inner\n)\nSELECT\n  sales.region AS [sales___region],\n  SUM(\n    sales.quantity * _cm_unit_price_avg_partition_by_product.[unit_price_avg_partition_by_product]\n  ) AS [sales___m]\nFROM sales AS sales\nLEFT JOIN _cm_unit_price_avg_partition_by_product\n  ON (\n    sales.product = _cm_unit_price_avg_partition_by_product.[product]\n    OR (\n      sales.product IS NULL\n      AND _cm_unit_price_avg_partition_by_product.[product] IS NULL\n    )\n  )\nGROUP BY\n  sales.region",
  "positive/reaggregation::bigquery": "WITH _base AS (\n  SELECT\n    sales.region AS `sales___region`\n  FROM sales AS sales\n  GROUP BY\n    sales.region\n), _cm_amount_sum_partition_by_city_region AS (\n  SELECT\n    `_stage_inner`.`sales___city` AS `city`,\n    `_stage_inner`.`sales___region` AS `region`,\n    `_stage_inner`.`sales___amount_sum_partition_by_city_region` AS `amount_sum_partition_by_city_region`\n  FROM (\n    SELECT\n      sales.city AS `sales___city`,\n      sales.region AS `sales___region`,\n      CAST(SUM(sales.amount) AS FLOAT64) AS `sales___amount_sum_partition_by_city_region`\n    FROM sales AS sales\n    GROUP BY\n      sales.city,\n      sales.region\n  ) AS _stage_inner\n), _cm_sales___avg AS (\n  SELECT\n    `_base`.`sales___region` AS `sales___region`,\n    CAST(AVG(_base._v) AS FLOAT64) AS `sales___m`\n  FROM (\n    SELECT\n      sales.region AS `sales___region`,\n      sales.city AS _ek0,\n      sales.region AS _ek1,\n      MAX(_cm_amount_sum_partition_by_city_region.`amount_sum_partition_by_city_region`) AS _v\n    FROM sales AS sales\n    LEFT JOIN _cm_amount_sum_partition_by_city_region\n      ON sales.city IS NOT DISTINCT FROM _cm_amount_sum_partition_by_city_region.`city`\n      AND sales.region IS NOT DISTINCT FROM _cm_amount_sum_partition_by_city_region.`region`\n    GROUP BY\n      sales.region,\n      sales.city,\n      sales.region\n  ) AS _base\n  GROUP BY\n    `_base`.`sales___region`\n)\nSELECT\n  _base.`sales___region`,\n  _cm_sales___avg.`sales___m`\nFROM _base\nLEFT JOIN _cm_sales___avg\n  ON _base.`sales___region` IS NOT DISTINCT FROM _cm_sales___avg.`sales___region`",
  "positive/reaggregation::duckdb": "WITH _base AS (\n  SELECT\n    sales.region AS \"sales.region\"\n  FROM sales AS sales\n  GROUP BY\n    sales.region\n), _cm_amount_sum_partition_by_city_region AS (\n  SELECT\n    _stage_inner.\"sales.city\" AS \"city\",\n    _stage_inner.\"sales.region\" AS \"region\",\n    _stage_inner.\"sales.amount_sum_partition_by_city_region\" AS \"amount_sum_partition_by_city_region\"\n  FROM (\n    SELECT\n      sales.city AS \"sales.city\",\n      sales.region AS \"sales.region\",\n      CAST(SUM(sales.amount) AS DOUBLE) AS \"sales.amount_sum_partition_by_city_region\"\n    FROM sales AS sales\n    GROUP BY\n      sales.city,\n      sales.region\n  ) AS _stage_inner\n), _cm_sales___avg AS (\n  SELECT\n    _base.\"sales.region\" AS \"sales.region\",\n    CAST(AVG(_base._v) AS DOUBLE) AS \"sales.m\"\n  FROM (\n    SELECT\n      sales.region AS \"sales.region\",\n      sales.city AS _ek0,\n      sales.region AS _ek1,\n      MAX(_cm_amount_sum_partition_by_city_region.\"amount_sum_partition_by_city_region\") AS _v\n    FROM sales AS sales\n    LEFT JOIN _cm_amount_sum_partition_by_city_region\n      ON sales.city IS NOT DISTINCT FROM _cm_amount_sum_partition_by_city_region.\"city\"\n      AND sales.region IS NOT DISTINCT FROM _cm_amount_sum_partition_by_city_region.\"region\"\n    GROUP BY\n      sales.region,\n      sales.city,\n      sales.region\n  ) AS _base\n  GROUP BY\n    _base.\"sales.region\"\n)\nSELECT\n  _base.\"sales.region\",\n  _cm_sales___avg.\"sales.m\"\nFROM _base\nLEFT JOIN _cm_sales___avg\n  ON _base.\"sales.region\" IS NOT DISTINCT FROM _cm_sales___avg.\"sales.region\"",
  "positive/reaggregation::mysql": "WITH _base AS (\n  SELECT\n    sales.region AS `sales.region`\n  FROM sales AS sales\n  GROUP BY\n    sales.region\n), _cm_amount_sum_partition_by_city_region AS (\n  SELECT\n    _stage_inner.`sales.city` AS `city`,\n    _stage_inner.`sales.region` AS `region`,\n    _stage_inner.`sales.amount_sum_partition_by_city_region` AS `amount_sum_partition_by_city_region`\n  FROM (\n    SELECT\n      sales.city AS `sales.city`,\n      sales.region AS `sales.region`,\n      CAST(SUM(sales.amount) AS DOUBLE) AS `sales.amount_sum_partition_by_city_region`\n    FROM sales AS sales\n    GROUP BY\n      sales.city,\n      sales.region\n  ) AS _stage_inner\n), _cm_sales___avg AS (\n  SELECT\n    _base.`sales.region` AS `sales.region`,\n    CAST(AVG(_base._v) AS DOUBLE) AS `sales.m`\n  FROM (\n    SELECT\n      sales.region AS `sales.region`,\n      sales.city AS _ek0,\n      sales.region AS _ek1,\n      MAX(_cm_amount_sum_partition_by_city_region.`amount_sum_partition_by_city_region`) AS _v\n    FROM sales AS sales\n    LEFT JOIN _cm_amount_sum_partition_by_city_region\n      ON sales.city <=> _cm_amount_sum_partition_by_city_region.`city`\n      AND sales.region <=> _cm_amount_sum_partition_by_city_region.`region`\n    GROUP BY\n      sales.region,\n      sales.city,\n      sales.region\n  ) AS _base\n  GROUP BY\n    _base.`sales.region`\n)\nSELECT\n  _base.`sales.region`,\n  _cm_sales___avg.`sales.m`\nFROM _base\nLEFT JOIN _cm_sales___avg\n  ON _base.`sales.region` <=> _cm_sales___avg.`sales.region`",
  "positive/reaggregation::postgres": "WITH _base AS (\n  SELECT\n    sales.region AS \"sales.region\"\n  FROM sales AS sales\n  GROUP BY\n    sales.region\n), _cm_amount_sum_partition_by_city_region AS (\n  SELECT\n    _stage_inner.\"sales.city\" AS \"city\",\n    _stage_inner.\"sales.region\" AS \"region\",\n    _stage_inner.\"sales.amount_sum_partition_by_city_region\" AS \"amount_sum_partition_by_city_region\"\n  FROM (\n    SELECT\n      sales.city AS \"sales.city\",\n      sales.region AS \"sales.region\",\n      CAST(SUM(sales.amount) AS DOUBLE PRECISION) AS \"sales.amount_sum_partition_by_city_region\"\n    FROM sales AS sales\n    GROUP BY\n      sales.city,\n      sales.region\n  ) AS _stage_inner\n), _cm_sales___avg AS (\n  SELECT\n    _base.\"sales.region\" AS \"sales.region\",\n    CAST(AVG(_base._v) AS DOUBLE PRECISION) AS \"sales.m\"\n  FROM (\n    SELECT\n      sales.region AS \"sales.region\",\n      sales.city AS _ek0,\n      sales.region AS _ek1,\n      MAX(_cm_amount_sum_partition_by_city_region.\"amount_sum_partition_by_city_region\") AS _v\n    FROM sales AS sales\n    LEFT JOIN _cm_amount_sum_partition_by_city_region\n      ON sales.city IS NOT DISTINCT FROM _cm_amount_sum_partition_by_city_region.\"city\"\n      AND sales.region IS NOT DISTINCT FROM _cm_amount_sum_partition_by_city_region.\"region\"\n    GROUP BY\n      sales.region,\n      sales.city,\n      sales.region\n  ) AS _base\n  GROUP BY\n    _base.\"sales.region\"\n)\nSELECT\n  _base.\"sales.region\",\n  _cm_sales___avg.\"sales.m\"\nFROM _base\nLEFT JOIN _cm_sales___avg\n  ON _base.\"sales.region\" IS NOT DISTINCT FROM _cm_sales___avg.\"sales.region\"",
  "positive/reaggregation::snowflake": "WITH _base AS (\n  SELECT\n    sales.region AS \"sales.region\"\n  FROM sales AS sales\n  GROUP BY\n    sales.region\n), _cm_amount_sum_partition_by_city_region AS (\n  SELECT\n    _stage_inner.\"sales.city\" AS \"city\",\n    _stage_inner.\"sales.region\" AS \"region\",\n    _stage_inner.\"sales.amount_sum_partition_by_city_region\" AS \"amount_sum_partition_by_city_region\"\n  FROM (\n    SELECT\n      sales.city AS \"sales.city\",\n      sales.region AS \"sales.region\",\n      CAST(SUM(sales.amount) AS DOUBLE) AS \"sales.amount_sum_partition_by_city_region\"\n    FROM sales AS sales\n    GROUP BY\n      sales.city,\n      sales.region\n  ) AS _stage_inner\n), _cm_sales___avg AS (\n  SELECT\n    _base.\"sales.region\" AS \"sales.region\",\n    CAST(AVG(_base._v) AS DOUBLE) AS \"sales.m\"\n  FROM (\n    SELECT\n      sales.region AS \"sales.region\",\n      sales.city AS _ek0,\n      sales.region AS _ek1,\n      MAX(_cm_amount_sum_partition_by_city_region.\"amount_sum_partition_by_city_region\") AS _v\n    FROM sales AS sales\n    LEFT JOIN _cm_amount_sum_partition_by_city_region\n      ON sales.city IS NOT DISTINCT FROM _cm_amount_sum_partition_by_city_region.\"city\"\n      AND sales.region IS NOT DISTINCT FROM _cm_amount_sum_partition_by_city_region.\"region\"\n    GROUP BY\n      sales.region,\n      sales.city,\n      sales.region\n  ) AS _base\n  GROUP BY\n    _base.\"sales.region\"\n)\nSELECT\n  _base.\"sales.region\",\n  _cm_sales___avg.\"sales.m\"\nFROM _base\nLEFT JOIN _cm_sales___avg\n  ON _base.\"sales.region\" IS NOT DISTINCT FROM _cm_sales___avg.\"sales.region\"",
  "positive/reaggregation::sqlite": "WITH _base AS (\n  SELECT\n    sales.region AS \"sales.region\"\n  FROM sales AS sales\n  GROUP BY\n    sales.region\n), _cm_amount_sum_partition_by_city_region AS (\n  SELECT\n    _stage_inner.\"sales.city\" AS \"city\",\n    _stage_inner.\"sales.region\" AS \"region\",\n    _stage_inner.\"sales.amount_sum_partition_by_city_region\" AS \"amount_sum_partition_by_city_region\"\n  FROM (\n    SELECT\n      sales.city AS \"sales.city\",\n      sales.region AS \"sales.region\",\n      CAST(SUM(sales.amount) AS REAL) AS \"sales.amount_sum_partition_by_city_region\"\n    FROM sales AS sales\n    GROUP BY\n      sales.city,\n      sales.region\n  ) AS _stage_inner\n), _cm_sales___avg AS (\n  SELECT\n    _base.\"sales.region\" AS \"sales.region\",\n    CAST(AVG(_base._v) AS REAL) AS \"sales.m\"\n  FROM (\n    SELECT\n      sales.region AS \"sales.region\",\n      sales.city AS _ek0,\n      sales.region AS _ek1,\n      MAX(_cm_amount_sum_partition_by_city_region.\"amount_sum_partition_by_city_region\") AS _v\n    FROM sales AS sales\n    LEFT JOIN _cm_amount_sum_partition_by_city_region\n      ON sales.city IS _cm_amount_sum_partition_by_city_region.\"city\"\n      AND sales.region IS _cm_amount_sum_partition_by_city_region.\"region\"\n    GROUP BY\n      sales.region,\n      sales.city,\n      sales.region\n  ) AS _base\n  GROUP BY\n    _base.\"sales.region\"\n)\nSELECT\n  _base.\"sales.region\",\n  _cm_sales___avg.\"sales.m\"\nFROM _base\nLEFT JOIN _cm_sales___avg\n  ON _base.\"sales.region\" IS _cm_sales___avg.\"sales.region\"",
  "positive/reaggregation::tsql": "WITH _base AS (\n  SELECT\n    sales.region AS [sales___region]\n  FROM sales AS sales\n  GROUP BY\n    sales.region\n), _cm_amount_sum_partition_by_city_region AS (\n  SELECT\n    _stage_inner.[sales___city] AS [city],\n    _stage_inner.[sales___region] AS [region],\n    _stage_inner.[sales___amount_sum_partition_by_city_region] AS [amount_sum_partition_by_city_region]\n  FROM (\n    SELECT\n      sales.city AS [sales___city],\n      sales.region AS [sales___region],\n      CAST(SUM(sales.amount) AS FLOAT) AS [sales___amount_sum_partition_by_city_region]\n    FROM sales AS sales\n    GROUP BY\n      sales.city,\n      sales.region\n  ) AS _stage_inner\n), _cm_sales___avg AS (\n  SELECT\n    _base.[sales___region] AS [sales___region],\n    CAST(AVG(_base._v) AS FLOAT) AS [sales___m]\n  FROM (\n    SELECT\n      sales.region AS [sales___region],\n      sales.city AS _ek0,\n      sales.region AS _ek1,\n      MAX(_cm_amount_sum_partition_by_city_region.[amount_sum_partition_by_city_region]) AS _v\n    FROM sales AS sales\n    LEFT JOIN _cm_amount_sum_partition_by_city_region\n      ON (\n        sales.city = _cm_amount_sum_partition_by_city_region.[city]\n        OR (\n          sales.city IS NULL AND _cm_amount_sum_partition_by_city_region.[city] IS NULL\n        )\n      )\n      AND (\n        sales.region = _cm_amount_sum_partition_by_city_region.[region]\n        OR (\n          sales.region IS NULL AND _cm_amount_sum_partition_by_city_region.[region] IS NULL\n        )\n      )\n    GROUP BY\n      sales.region,\n      sales.city,\n      sales.region\n  ) AS _base\n  GROUP BY\n    _base.[sales___region]\n)\nSELECT\n  _base.[sales___region],\n  _cm_sales___avg.[sales___m]\nFROM _base\nLEFT JOIN _cm_sales___avg\n  ON (\n    _base.[sales___region] = _cm_sales___avg.[sales___region]\n    OR (\n      _base.[sales___region] IS NULL AND _cm_sales___avg.[sales___region] IS NULL\n    )\n  )",
  "positive/same_model_arith::bigquery": "SELECT\n  orders.status AS `orders___status`,\n  SUM(orders.amount - orders.cost) AS `orders___m`\nFROM orders AS orders\nGROUP BY\n  orders.status",
  "positive/same_model_arith::duckdb": "SELECT\n  orders.status AS \"orders.status\",\n  SUM(orders.amount - orders.cost) AS \"orders.m\"\nFROM orders AS orders\nGROUP BY\n  orders.status",
  "positive/same_model_arith::mysql": "SELECT\n  orders.status AS `orders.status`,\n  SUM(orders.amount - orders.cost) AS `orders.m`\nFROM orders AS orders\nGROUP BY\n  orders.status",
  "positive/same_model_arith::postgres": "SELECT\n  orders.status AS \"orders.status\",\n  SUM(orders.amount - orders.cost) AS \"orders.m\"\nFROM orders AS orders\nGROUP BY\n  orders.status",
  "positive/same_model_arith::snowflake": "SELECT\n  orders.status AS \"orders.status\",\n  SUM(orders.amount - orders.cost) AS \"orders.m\"\nFROM orders AS orders\nGROUP BY\n  orders.status",
  "positive/same_model_arith::sqlite": "SELECT\n  orders.status AS \"orders.status\",\n  SUM(orders.amount - orders.cost) AS \"orders.m\"\nFROM orders AS orders\nGROUP BY\n  orders.status",
  "positive/same_model_arith::tsql": "SELECT\n  orders.status AS [orders___status],\n  SUM(orders.amount - orders.cost) AS [orders___m]\nFROM orders AS orders\nGROUP BY\n  orders.status"
}
