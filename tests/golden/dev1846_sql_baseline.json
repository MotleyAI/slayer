{
  "cp/and_simple::bigquery": "SELECT\n    `sales___ordered_at`,\n    `sales___streak`\nFROM (\nWITH base AS (\n  SELECT\n    DATE_TRUNC(sales.ordered_at, MONTH) AS `sales___ordered_at`,\n    SUM(sales.revenue) AS `sales___revenue_sum`,\n    SUM(sales.cost) AS `sales___cost_sum`\n  FROM sales AS sales\n  GROUP BY\n    DATE_TRUNC(sales.ordered_at, MONTH)\n), cp_reset_streak AS (\n  SELECT\n    `sales___ordered_at`,\n    `sales___revenue_sum`,\n    `sales___cost_sum`,\n    SUM(\n      CASE\n        WHEN COALESCE(`sales___revenue_sum` > 40 AND `sales___cost_sum` > 10, FALSE)\n        THEN 0\n        ELSE 1\n      END\n    ) OVER (ORDER BY `sales___ordered_at` ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW) AS `_cp_reset_sales___streak`\n  FROM base\n), cp_value_streak AS (\n  SELECT\n    `sales___ordered_at`,\n    `sales___revenue_sum`,\n    `sales___cost_sum`,\n    CASE\n      WHEN COALESCE(`sales___revenue_sum` > 40 AND `sales___cost_sum` > 10, FALSE)\n      THEN SUM(\n        CASE\n          WHEN COALESCE(`sales___revenue_sum` > 40 AND `sales___cost_sum` > 10, FALSE)\n          THEN 1\n          ELSE 0\n        END\n      ) OVER (\n        PARTITION BY `_cp_reset_sales___streak`\n        ORDER BY `sales___ordered_at`\n        ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n      )\n      ELSE 0\n    END AS `sales___streak`\n  FROM cp_reset_streak\n)\nSELECT\n  `sales___ordered_at`,\n  `sales___revenue_sum`,\n  `sales___cost_sum`,\n  `sales___streak`\nFROM cp_value_streak\n) AS _outer",
  "cp/and_simple::duckdb": "SELECT\n    \"sales.ordered_at\",\n    \"sales.streak\"\nFROM (\nWITH base AS (\n  SELECT\n    DATE_TRUNC('MONTH', sales.ordered_at) AS \"sales.ordered_at\",\n    SUM(sales.revenue) AS \"sales.revenue_sum\",\n    SUM(sales.cost) AS \"sales.cost_sum\"\n  FROM sales AS sales\n  GROUP BY\n    DATE_TRUNC('MONTH', sales.ordered_at)\n), cp_reset_streak AS (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    \"sales.cost_sum\",\n    SUM(\n      CASE\n        WHEN COALESCE(\"sales.revenue_sum\" > 40 AND \"sales.cost_sum\" > 10, FALSE)\n        THEN 0\n        ELSE 1\n      END\n    ) OVER (ORDER BY \"sales.ordered_at\" ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW) AS \"_cp_reset_sales.streak\"\n  FROM base\n), cp_value_streak AS (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    \"sales.cost_sum\",\n    CASE\n      WHEN COALESCE(\"sales.revenue_sum\" > 40 AND \"sales.cost_sum\" > 10, FALSE)\n      THEN SUM(\n        CASE\n          WHEN COALESCE(\"sales.revenue_sum\" > 40 AND \"sales.cost_sum\" > 10, FALSE)\n          THEN 1\n          ELSE 0\n        END\n      ) OVER (\n        PARTITION BY \"_cp_reset_sales.streak\"\n        ORDER BY \"sales.ordered_at\"\n        ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n      )\n      ELSE 0\n    END AS \"sales.streak\"\n  FROM cp_reset_streak\n)\nSELECT\n  \"sales.ordered_at\",\n  \"sales.revenue_sum\",\n  \"sales.cost_sum\",\n  \"sales.streak\"\nFROM cp_value_streak\n) AS _outer",
  "cp/and_simple::postgres": "SELECT\n    \"sales.ordered_at\",\n    \"sales.streak\"\nFROM (\nWITH base AS (\n  SELECT\n    DATE_TRUNC('MONTH', sales.ordered_at) AS \"sales.ordered_at\",\n    SUM(sales.revenue) AS \"sales.revenue_sum\",\n    SUM(sales.cost) AS \"sales.cost_sum\"\n  FROM sales AS sales\n  GROUP BY\n    DATE_TRUNC('MONTH', sales.ordered_at)\n), cp_reset_streak AS (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    \"sales.cost_sum\",\n    SUM(\n      CASE\n        WHEN COALESCE(\"sales.revenue_sum\" > 40 AND \"sales.cost_sum\" > 10, FALSE)\n        THEN 0\n        ELSE 1\n      END\n    ) OVER (ORDER BY \"sales.ordered_at\" ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW) AS \"_cp_reset_sales.streak\"\n  FROM base\n), cp_value_streak AS (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    \"sales.cost_sum\",\n    CASE\n      WHEN COALESCE(\"sales.revenue_sum\" > 40 AND \"sales.cost_sum\" > 10, FALSE)\n      THEN SUM(\n        CASE\n          WHEN COALESCE(\"sales.revenue_sum\" > 40 AND \"sales.cost_sum\" > 10, FALSE)\n          THEN 1\n          ELSE 0\n        END\n      ) OVER (\n        PARTITION BY \"_cp_reset_sales.streak\"\n        ORDER BY \"sales.ordered_at\"\n        ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n      )\n      ELSE 0\n    END AS \"sales.streak\"\n  FROM cp_reset_streak\n)\nSELECT\n  \"sales.ordered_at\",\n  \"sales.revenue_sum\",\n  \"sales.cost_sum\",\n  \"sales.streak\"\nFROM cp_value_streak\n) AS _outer",
  "cp/and_simple::sqlite": "SELECT\n    \"sales.ordered_at\",\n    \"sales.streak\"\nFROM (\nWITH base AS (\n  SELECT\n    STRFTIME('%Y-%m-01', sales.ordered_at) AS \"sales.ordered_at\",\n    SUM(sales.revenue) AS \"sales.revenue_sum\",\n    SUM(sales.cost) AS \"sales.cost_sum\"\n  FROM sales AS sales\n  GROUP BY\n    STRFTIME('%Y-%m-01', sales.ordered_at)\n), cp_reset_streak AS (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    \"sales.cost_sum\",\n    SUM(\n      CASE\n        WHEN COALESCE(\"sales.revenue_sum\" > 40 AND \"sales.cost_sum\" > 10, FALSE)\n        THEN 0\n        ELSE 1\n      END\n    ) OVER (ORDER BY \"sales.ordered_at\" ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW) AS \"_cp_reset_sales.streak\"\n  FROM base\n), cp_value_streak AS (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    \"sales.cost_sum\",\n    CASE\n      WHEN COALESCE(\"sales.revenue_sum\" > 40 AND \"sales.cost_sum\" > 10, FALSE)\n      THEN SUM(\n        CASE\n          WHEN COALESCE(\"sales.revenue_sum\" > 40 AND \"sales.cost_sum\" > 10, FALSE)\n          THEN 1\n          ELSE 0\n        END\n      ) OVER (\n        PARTITION BY \"_cp_reset_sales.streak\"\n        ORDER BY \"sales.ordered_at\"\n        ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n      )\n      ELSE 0\n    END AS \"sales.streak\"\n  FROM cp_reset_streak\n)\nSELECT\n  \"sales.ordered_at\",\n  \"sales.revenue_sum\",\n  \"sales.cost_sum\",\n  \"sales.streak\"\nFROM cp_value_streak\n) AS _outer",
  "cp/and_simple::tsql": "WITH base AS (\n  SELECT\n    DATETRUNC(MONTH, sales.ordered_at) AS [sales___ordered_at],\n    SUM(sales.revenue) AS [sales___revenue_sum],\n    SUM(sales.cost) AS [sales___cost_sum]\n  FROM sales AS sales\n  GROUP BY\n    DATETRUNC(MONTH, sales.ordered_at)\n), cp_reset_streak AS (\n  SELECT\n    [sales___ordered_at] AS [sales___ordered_at],\n    [sales___revenue_sum] AS [sales___revenue_sum],\n    [sales___cost_sum] AS [sales___cost_sum],\n    SUM(\n      CASE\n        WHEN COALESCE([sales___revenue_sum] > 40 AND [sales___cost_sum] > 10, (\n          1 = 0\n        ))\n        THEN 0\n        ELSE 1\n      END\n    ) OVER (ORDER BY [sales___ordered_at] ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW) AS [_cp_reset_sales___streak]\n  FROM base\n), cp_value_streak AS (\n  SELECT\n    [sales___ordered_at] AS [sales___ordered_at],\n    [sales___revenue_sum] AS [sales___revenue_sum],\n    [sales___cost_sum] AS [sales___cost_sum],\n    CASE\n      WHEN COALESCE([sales___revenue_sum] > 40 AND [sales___cost_sum] > 10, (\n        1 = 0\n      ))\n      THEN SUM(\n        CASE\n          WHEN COALESCE([sales___revenue_sum] > 40 AND [sales___cost_sum] > 10, (\n            1 = 0\n          ))\n          THEN 1\n          ELSE 0\n        END\n      ) OVER (\n        PARTITION BY [_cp_reset_sales___streak]\n        ORDER BY [sales___ordered_at]\n        ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n      )\n      ELSE 0\n    END AS [sales___streak]\n  FROM cp_reset_streak\n)\nSELECT\n  [sales___ordered_at],\n  [sales___streak]\nFROM (\n  SELECT\n    [sales___ordered_at] AS [sales___ordered_at],\n    [sales___revenue_sum] AS [sales___revenue_sum],\n    [sales___cost_sum] AS [sales___cost_sum],\n    [sales___streak] AS [sales___streak]\n  FROM cp_value_streak\n) AS _outer",
  "cp/bare_cumsum::bigquery": "SELECT\n    `sales___ordered_at`,\n    `sales___streak`\nFROM (\nWITH base AS (\n  SELECT\n    DATE_TRUNC(sales.ordered_at, MONTH) AS `sales___ordered_at`,\n    SUM(sales.revenue) AS `sales___revenue_sum`\n  FROM sales AS sales\n  GROUP BY\n    DATE_TRUNC(sales.ordered_at, MONTH)\n), step1 AS (\n  SELECT\n    `sales___ordered_at`,\n    `sales___revenue_sum`,\n    SUM(`sales___revenue_sum`) OVER (ORDER BY `sales___ordered_at`) AS `sales____cumsum_inner`\n  FROM base\n), cp_reset_streak AS (\n  SELECT\n    `sales___ordered_at`,\n    `sales___revenue_sum`,\n    `sales____cumsum_inner`,\n    SUM(\n      CASE\n        WHEN NOT `sales____cumsum_inner` IS NULL AND `sales____cumsum_inner` <> 0\n        THEN 0\n        ELSE 1\n      END\n    ) OVER (ORDER BY `sales___ordered_at` ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW) AS `_cp_reset_sales___streak`\n  FROM step1\n), cp_value_streak AS (\n  SELECT\n    `sales___ordered_at`,\n    `sales___revenue_sum`,\n    `sales____cumsum_inner`,\n    CASE\n      WHEN NOT `sales____cumsum_inner` IS NULL AND `sales____cumsum_inner` <> 0\n      THEN SUM(\n        CASE\n          WHEN NOT `sales____cumsum_inner` IS NULL AND `sales____cumsum_inner` <> 0\n          THEN 1\n          ELSE 0\n        END\n      ) OVER (\n        PARTITION BY `_cp_reset_sales___streak`\n        ORDER BY `sales___ordered_at`\n        ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n      )\n      ELSE 0\n    END AS `sales___streak`\n  FROM cp_reset_streak\n)\nSELECT\n  `sales___ordered_at`,\n  `sales___revenue_sum`,\n  `sales____cumsum_inner`,\n  `sales___streak`\nFROM cp_value_streak\n) AS _outer",
  "cp/bare_cumsum::duckdb": "SELECT\n    \"sales.ordered_at\",\n    \"sales.streak\"\nFROM (\nWITH base AS (\n  SELECT\n    DATE_TRUNC('MONTH', sales.ordered_at) AS \"sales.ordered_at\",\n    SUM(sales.revenue) AS \"sales.revenue_sum\"\n  FROM sales AS sales\n  GROUP BY\n    DATE_TRUNC('MONTH', sales.ordered_at)\n), step1 AS (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    SUM(\"sales.revenue_sum\") OVER (ORDER BY \"sales.ordered_at\") AS \"sales._cumsum_inner\"\n  FROM base\n), cp_reset_streak AS (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    \"sales._cumsum_inner\",\n    SUM(\n      CASE\n        WHEN NOT \"sales._cumsum_inner\" IS NULL AND \"sales._cumsum_inner\" <> 0\n        THEN 0\n        ELSE 1\n      END\n    ) OVER (ORDER BY \"sales.ordered_at\" ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW) AS \"_cp_reset_sales.streak\"\n  FROM step1\n), cp_value_streak AS (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    \"sales._cumsum_inner\",\n    CASE\n      WHEN NOT \"sales._cumsum_inner\" IS NULL AND \"sales._cumsum_inner\" <> 0\n      THEN SUM(\n        CASE\n          WHEN NOT \"sales._cumsum_inner\" IS NULL AND \"sales._cumsum_inner\" <> 0\n          THEN 1\n          ELSE 0\n        END\n      ) OVER (\n        PARTITION BY \"_cp_reset_sales.streak\"\n        ORDER BY \"sales.ordered_at\"\n        ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n      )\n      ELSE 0\n    END AS \"sales.streak\"\n  FROM cp_reset_streak\n)\nSELECT\n  \"sales.ordered_at\",\n  \"sales.revenue_sum\",\n  \"sales._cumsum_inner\",\n  \"sales.streak\"\nFROM cp_value_streak\n) AS _outer",
  "cp/bare_cumsum::postgres": "SELECT\n    \"sales.ordered_at\",\n    \"sales.streak\"\nFROM (\nWITH base AS (\n  SELECT\n    DATE_TRUNC('MONTH', sales.ordered_at) AS \"sales.ordered_at\",\n    SUM(sales.revenue) AS \"sales.revenue_sum\"\n  FROM sales AS sales\n  GROUP BY\n    DATE_TRUNC('MONTH', sales.ordered_at)\n), step1 AS (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    SUM(\"sales.revenue_sum\") OVER (ORDER BY \"sales.ordered_at\") AS \"sales._cumsum_inner\"\n  FROM base\n), cp_reset_streak AS (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    \"sales._cumsum_inner\",\n    SUM(\n      CASE\n        WHEN NOT \"sales._cumsum_inner\" IS NULL AND \"sales._cumsum_inner\" <> 0\n        THEN 0\n        ELSE 1\n      END\n    ) OVER (ORDER BY \"sales.ordered_at\" ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW) AS \"_cp_reset_sales.streak\"\n  FROM step1\n), cp_value_streak AS (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    \"sales._cumsum_inner\",\n    CASE\n      WHEN NOT \"sales._cumsum_inner\" IS NULL AND \"sales._cumsum_inner\" <> 0\n      THEN SUM(\n        CASE\n          WHEN NOT \"sales._cumsum_inner\" IS NULL AND \"sales._cumsum_inner\" <> 0\n          THEN 1\n          ELSE 0\n        END\n      ) OVER (\n        PARTITION BY \"_cp_reset_sales.streak\"\n        ORDER BY \"sales.ordered_at\"\n        ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n      )\n      ELSE 0\n    END AS \"sales.streak\"\n  FROM cp_reset_streak\n)\nSELECT\n  \"sales.ordered_at\",\n  \"sales.revenue_sum\",\n  \"sales._cumsum_inner\",\n  \"sales.streak\"\nFROM cp_value_streak\n) AS _outer",
  "cp/bare_cumsum::sqlite": "SELECT\n    \"sales.ordered_at\",\n    \"sales.streak\"\nFROM (\nWITH base AS (\n  SELECT\n    STRFTIME('%Y-%m-01', sales.ordered_at) AS \"sales.ordered_at\",\n    SUM(sales.revenue) AS \"sales.revenue_sum\"\n  FROM sales AS sales\n  GROUP BY\n    STRFTIME('%Y-%m-01', sales.ordered_at)\n), step1 AS (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    SUM(\"sales.revenue_sum\") OVER (ORDER BY \"sales.ordered_at\") AS \"sales._cumsum_inner\"\n  FROM base\n), cp_reset_streak AS (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    \"sales._cumsum_inner\",\n    SUM(\n      CASE\n        WHEN NOT \"sales._cumsum_inner\" IS NULL AND \"sales._cumsum_inner\" <> 0\n        THEN 0\n        ELSE 1\n      END\n    ) OVER (ORDER BY \"sales.ordered_at\" ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW) AS \"_cp_reset_sales.streak\"\n  FROM step1\n), cp_value_streak AS (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    \"sales._cumsum_inner\",\n    CASE\n      WHEN NOT \"sales._cumsum_inner\" IS NULL AND \"sales._cumsum_inner\" <> 0\n      THEN SUM(\n        CASE\n          WHEN NOT \"sales._cumsum_inner\" IS NULL AND \"sales._cumsum_inner\" <> 0\n          THEN 1\n          ELSE 0\n        END\n      ) OVER (\n        PARTITION BY \"_cp_reset_sales.streak\"\n        ORDER BY \"sales.ordered_at\"\n        ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n      )\n      ELSE 0\n    END AS \"sales.streak\"\n  FROM cp_reset_streak\n)\nSELECT\n  \"sales.ordered_at\",\n  \"sales.revenue_sum\",\n  \"sales._cumsum_inner\",\n  \"sales.streak\"\nFROM cp_value_streak\n) AS _outer",
  "cp/bare_cumsum::tsql": "WITH base AS (\n  SELECT\n    DATETRUNC(MONTH, sales.ordered_at) AS [sales___ordered_at],\n    SUM(sales.revenue) AS [sales___revenue_sum]\n  FROM sales AS sales\n  GROUP BY\n    DATETRUNC(MONTH, sales.ordered_at)\n), step1 AS (\n  SELECT\n    [sales___ordered_at] AS [sales___ordered_at],\n    [sales___revenue_sum] AS [sales___revenue_sum],\n    SUM([sales___revenue_sum]) OVER (ORDER BY [sales___ordered_at]) AS [sales____cumsum_inner]\n  FROM base\n), cp_reset_streak AS (\n  SELECT\n    [sales___ordered_at] AS [sales___ordered_at],\n    [sales___revenue_sum] AS [sales___revenue_sum],\n    [sales____cumsum_inner] AS [sales____cumsum_inner],\n    SUM(\n      CASE\n        WHEN NOT [sales____cumsum_inner] IS NULL AND [sales____cumsum_inner] <> 0\n        THEN 0\n        ELSE 1\n      END\n    ) OVER (ORDER BY [sales___ordered_at] ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW) AS [_cp_reset_sales___streak]\n  FROM step1\n), cp_value_streak AS (\n  SELECT\n    [sales___ordered_at] AS [sales___ordered_at],\n    [sales___revenue_sum] AS [sales___revenue_sum],\n    [sales____cumsum_inner] AS [sales____cumsum_inner],\n    CASE\n      WHEN NOT [sales____cumsum_inner] IS NULL AND [sales____cumsum_inner] <> 0\n      THEN SUM(\n        CASE\n          WHEN NOT [sales____cumsum_inner] IS NULL AND [sales____cumsum_inner] <> 0\n          THEN 1\n          ELSE 0\n        END\n      ) OVER (\n        PARTITION BY [_cp_reset_sales___streak]\n        ORDER BY [sales___ordered_at]\n        ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n      )\n      ELSE 0\n    END AS [sales___streak]\n  FROM cp_reset_streak\n)\nSELECT\n  [sales___ordered_at],\n  [sales___streak]\nFROM (\n  SELECT\n    [sales___ordered_at] AS [sales___ordered_at],\n    [sales___revenue_sum] AS [sales___revenue_sum],\n    [sales____cumsum_inner] AS [sales____cumsum_inner],\n    [sales___streak] AS [sales___streak]\n  FROM cp_value_streak\n) AS _outer",
  "cp/change_gt_anchor::bigquery": "SELECT\n    `sales___ordered_at`,\n    `sales___streak`\nFROM (\nWITH base AS (\n  SELECT\n    DATE_TRUNC(sales.ordered_at, MONTH) AS `sales___ordered_at`,\n    SUM(sales.revenue) AS `sales___revenue_sum`\n  FROM sales AS sales\n  GROUP BY\n    DATE_TRUNC(sales.ordered_at, MONTH)\n), shifted__time_shift_inner AS (\n  SELECT\n    DATE_TRUNC(sales.ordered_at, MONTH) + INTERVAL 1 MONTH AS `sales___ordered_at`,\n    SUM(sales.revenue) AS `sales___revenue_sum`\n  FROM sales AS sales\n  GROUP BY\n    DATE_TRUNC(sales.ordered_at, MONTH) + INTERVAL 1 MONTH\n), sjoin__time_shift_inner AS (\n  SELECT\n    base.`sales___ordered_at`,\n    base.`sales___revenue_sum`,\n    shifted__time_shift_inner.`sales___revenue_sum` AS `sales____time_shift_inner`\n  FROM base\n  LEFT JOIN shifted__time_shift_inner\n    ON base.`sales___ordered_at` IS NOT DISTINCT FROM shifted__time_shift_inner.`sales___ordered_at`\n), cp_reset_streak AS (\n  SELECT\n    `sales___ordered_at`,\n    `sales___revenue_sum`,\n    `sales____time_shift_inner`,\n    SUM(\n      CASE\n        WHEN COALESCE(`sales___revenue_sum` - `sales____time_shift_inner` > 0, FALSE)\n        THEN 0\n        ELSE 1\n      END\n    ) OVER (ORDER BY `sales___ordered_at` ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW) AS `_cp_reset_sales___streak`\n  FROM sjoin__time_shift_inner\n), cp_value_streak AS (\n  SELECT\n    `sales___ordered_at`,\n    `sales___revenue_sum`,\n    `sales____time_shift_inner`,\n    CASE\n      WHEN COALESCE(`sales___revenue_sum` - `sales____time_shift_inner` > 0, FALSE)\n      THEN SUM(\n        CASE\n          WHEN COALESCE(`sales___revenue_sum` - `sales____time_shift_inner` > 0, FALSE)\n          THEN 1\n          ELSE 0\n        END\n      ) OVER (\n        PARTITION BY `_cp_reset_sales___streak`\n        ORDER BY `sales___ordered_at`\n        ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n      )\n      ELSE 0\n    END AS `sales___streak`\n  FROM cp_reset_streak\n)\nSELECT\n  `sales___ordered_at`,\n  `sales___revenue_sum`,\n  `sales____time_shift_inner`,\n  `sales___streak`\nFROM cp_value_streak\n) AS _outer",
  "cp/change_gt_anchor::duckdb": "SELECT\n    \"sales.ordered_at\",\n    \"sales.streak\"\nFROM (\nWITH base AS (\n  SELECT\n    DATE_TRUNC('MONTH', sales.ordered_at) AS \"sales.ordered_at\",\n    SUM(sales.revenue) AS \"sales.revenue_sum\"\n  FROM sales AS sales\n  GROUP BY\n    DATE_TRUNC('MONTH', sales.ordered_at)\n), shifted__time_shift_inner AS (\n  SELECT\n    DATE_TRUNC('MONTH', sales.ordered_at) + INTERVAL 1 MONTH AS \"sales.ordered_at\",\n    SUM(sales.revenue) AS \"sales.revenue_sum\"\n  FROM sales AS sales\n  GROUP BY\n    DATE_TRUNC('MONTH', sales.ordered_at) + INTERVAL 1 MONTH\n), sjoin__time_shift_inner AS (\n  SELECT\n    base.\"sales.ordered_at\",\n    base.\"sales.revenue_sum\",\n    shifted__time_shift_inner.\"sales.revenue_sum\" AS \"sales._time_shift_inner\"\n  FROM base\n  LEFT JOIN shifted__time_shift_inner\n    ON base.\"sales.ordered_at\" IS NOT DISTINCT FROM shifted__time_shift_inner.\"sales.ordered_at\"\n), cp_reset_streak AS (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    \"sales._time_shift_inner\",\n    SUM(\n      CASE\n        WHEN COALESCE(\"sales.revenue_sum\" - \"sales._time_shift_inner\" > 0, FALSE)\n        THEN 0\n        ELSE 1\n      END\n    ) OVER (ORDER BY \"sales.ordered_at\" ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW) AS \"_cp_reset_sales.streak\"\n  FROM sjoin__time_shift_inner\n), cp_value_streak AS (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    \"sales._time_shift_inner\",\n    CASE\n      WHEN COALESCE(\"sales.revenue_sum\" - \"sales._time_shift_inner\" > 0, FALSE)\n      THEN SUM(\n        CASE\n          WHEN COALESCE(\"sales.revenue_sum\" - \"sales._time_shift_inner\" > 0, FALSE)\n          THEN 1\n          ELSE 0\n        END\n      ) OVER (\n        PARTITION BY \"_cp_reset_sales.streak\"\n        ORDER BY \"sales.ordered_at\"\n        ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n      )\n      ELSE 0\n    END AS \"sales.streak\"\n  FROM cp_reset_streak\n)\nSELECT\n  \"sales.ordered_at\",\n  \"sales.revenue_sum\",\n  \"sales._time_shift_inner\",\n  \"sales.streak\"\nFROM cp_value_streak\n) AS _outer",
  "cp/change_gt_anchor::postgres": "SELECT\n    \"sales.ordered_at\",\n    \"sales.streak\"\nFROM (\nWITH base AS (\n  SELECT\n    DATE_TRUNC('MONTH', sales.ordered_at) AS \"sales.ordered_at\",\n    SUM(sales.revenue) AS \"sales.revenue_sum\"\n  FROM sales AS sales\n  GROUP BY\n    DATE_TRUNC('MONTH', sales.ordered_at)\n), shifted__time_shift_inner AS (\n  SELECT\n    DATE_TRUNC('MONTH', sales.ordered_at) + INTERVAL '1 MONTH' AS \"sales.ordered_at\",\n    SUM(sales.revenue) AS \"sales.revenue_sum\"\n  FROM sales AS sales\n  GROUP BY\n    DATE_TRUNC('MONTH', sales.ordered_at) + INTERVAL '1 MONTH'\n), sjoin__time_shift_inner AS (\n  SELECT\n    base.\"sales.ordered_at\",\n    base.\"sales.revenue_sum\",\n    shifted__time_shift_inner.\"sales.revenue_sum\" AS \"sales._time_shift_inner\"\n  FROM base\n  LEFT JOIN shifted__time_shift_inner\n    ON base.\"sales.ordered_at\" IS NOT DISTINCT FROM shifted__time_shift_inner.\"sales.ordered_at\"\n), cp_reset_streak AS (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    \"sales._time_shift_inner\",\n    SUM(\n      CASE\n        WHEN COALESCE(\"sales.revenue_sum\" - \"sales._time_shift_inner\" > 0, FALSE)\n        THEN 0\n        ELSE 1\n      END\n    ) OVER (ORDER BY \"sales.ordered_at\" ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW) AS \"_cp_reset_sales.streak\"\n  FROM sjoin__time_shift_inner\n), cp_value_streak AS (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    \"sales._time_shift_inner\",\n    CASE\n      WHEN COALESCE(\"sales.revenue_sum\" - \"sales._time_shift_inner\" > 0, FALSE)\n      THEN SUM(\n        CASE\n          WHEN COALESCE(\"sales.revenue_sum\" - \"sales._time_shift_inner\" > 0, FALSE)\n          THEN 1\n          ELSE 0\n        END\n      ) OVER (\n        PARTITION BY \"_cp_reset_sales.streak\"\n        ORDER BY \"sales.ordered_at\"\n        ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n      )\n      ELSE 0\n    END AS \"sales.streak\"\n  FROM cp_reset_streak\n)\nSELECT\n  \"sales.ordered_at\",\n  \"sales.revenue_sum\",\n  \"sales._time_shift_inner\",\n  \"sales.streak\"\nFROM cp_value_streak\n) AS _outer",
  "cp/change_gt_anchor::sqlite": "SELECT\n    \"sales.ordered_at\",\n    \"sales.streak\"\nFROM (\nWITH base AS (\n  SELECT\n    STRFTIME('%Y-%m-01', sales.ordered_at) AS \"sales.ordered_at\",\n    SUM(sales.revenue) AS \"sales.revenue_sum\"\n  FROM sales AS sales\n  GROUP BY\n    STRFTIME('%Y-%m-01', sales.ordered_at)\n), shifted__time_shift_inner AS (\n  SELECT\n    DATE(STRFTIME('%Y-%m-01', sales.ordered_at), '1 months') AS \"sales.ordered_at\",\n    SUM(sales.revenue) AS \"sales.revenue_sum\"\n  FROM sales AS sales\n  GROUP BY\n    DATE(STRFTIME('%Y-%m-01', sales.ordered_at), '1 months')\n), sjoin__time_shift_inner AS (\n  SELECT\n    base.\"sales.ordered_at\",\n    base.\"sales.revenue_sum\",\n    shifted__time_shift_inner.\"sales.revenue_sum\" AS \"sales._time_shift_inner\"\n  FROM base\n  LEFT JOIN shifted__time_shift_inner\n    ON base.\"sales.ordered_at\" IS shifted__time_shift_inner.\"sales.ordered_at\"\n), cp_reset_streak AS (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    \"sales._time_shift_inner\",\n    SUM(\n      CASE\n        WHEN COALESCE(\"sales.revenue_sum\" - \"sales._time_shift_inner\" > 0, FALSE)\n        THEN 0\n        ELSE 1\n      END\n    ) OVER (ORDER BY \"sales.ordered_at\" ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW) AS \"_cp_reset_sales.streak\"\n  FROM sjoin__time_shift_inner\n), cp_value_streak AS (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    \"sales._time_shift_inner\",\n    CASE\n      WHEN COALESCE(\"sales.revenue_sum\" - \"sales._time_shift_inner\" > 0, FALSE)\n      THEN SUM(\n        CASE\n          WHEN COALESCE(\"sales.revenue_sum\" - \"sales._time_shift_inner\" > 0, FALSE)\n          THEN 1\n          ELSE 0\n        END\n      ) OVER (\n        PARTITION BY \"_cp_reset_sales.streak\"\n        ORDER BY \"sales.ordered_at\"\n        ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n      )\n      ELSE 0\n    END AS \"sales.streak\"\n  FROM cp_reset_streak\n)\nSELECT\n  \"sales.ordered_at\",\n  \"sales.revenue_sum\",\n  \"sales._time_shift_inner\",\n  \"sales.streak\"\nFROM cp_value_streak\n) AS _outer",
  "cp/change_gt_anchor::tsql": "WITH base AS (\n  SELECT\n    DATETRUNC(MONTH, sales.ordered_at) AS [sales___ordered_at],\n    SUM(sales.revenue) AS [sales___revenue_sum]\n  FROM sales AS sales\n  GROUP BY\n    DATETRUNC(MONTH, sales.ordered_at)\n), shifted__time_shift_inner AS (\n  SELECT\n    DATEADD(MONTH, 1, DATETRUNC(MONTH, sales.ordered_at)) AS [sales___ordered_at],\n    SUM(sales.revenue) AS [sales___revenue_sum]\n  FROM sales AS sales\n  GROUP BY\n    DATEADD(MONTH, 1, DATETRUNC(MONTH, sales.ordered_at))\n), sjoin__time_shift_inner AS (\n  SELECT\n    base.[sales___ordered_at] AS [sales___ordered_at],\n    base.[sales___revenue_sum] AS [sales___revenue_sum],\n    shifted__time_shift_inner.[sales___revenue_sum] AS [sales____time_shift_inner]\n  FROM base\n  LEFT JOIN shifted__time_shift_inner\n    ON (\n      base.[sales___ordered_at] = shifted__time_shift_inner.[sales___ordered_at]\n      OR (\n        base.[sales___ordered_at] IS NULL\n        AND shifted__time_shift_inner.[sales___ordered_at] IS NULL\n      )\n    )\n), cp_reset_streak AS (\n  SELECT\n    [sales___ordered_at] AS [sales___ordered_at],\n    [sales___revenue_sum] AS [sales___revenue_sum],\n    [sales____time_shift_inner] AS [sales____time_shift_inner],\n    SUM(\n      CASE\n        WHEN COALESCE([sales___revenue_sum] - [sales____time_shift_inner] > 0, (\n          1 = 0\n        ))\n        THEN 0\n        ELSE 1\n      END\n    ) OVER (ORDER BY [sales___ordered_at] ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW) AS [_cp_reset_sales___streak]\n  FROM sjoin__time_shift_inner\n), cp_value_streak AS (\n  SELECT\n    [sales___ordered_at] AS [sales___ordered_at],\n    [sales___revenue_sum] AS [sales___revenue_sum],\n    [sales____time_shift_inner] AS [sales____time_shift_inner],\n    CASE\n      WHEN COALESCE([sales___revenue_sum] - [sales____time_shift_inner] > 0, (\n        1 = 0\n      ))\n      THEN SUM(\n        CASE\n          WHEN COALESCE([sales___revenue_sum] - [sales____time_shift_inner] > 0, (\n            1 = 0\n          ))\n          THEN 1\n          ELSE 0\n        END\n      ) OVER (\n        PARTITION BY [_cp_reset_sales___streak]\n        ORDER BY [sales___ordered_at]\n        ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n      )\n      ELSE 0\n    END AS [sales___streak]\n  FROM cp_reset_streak\n)\nSELECT\n  [sales___ordered_at],\n  [sales___streak]\nFROM (\n  SELECT\n    [sales___ordered_at] AS [sales___ordered_at],\n    [sales___revenue_sum] AS [sales___revenue_sum],\n    [sales____time_shift_inner] AS [sales____time_shift_inner],\n    [sales___streak] AS [sales___streak]\n  FROM cp_value_streak\n) AS _outer",
  "cp/iif::bigquery": "SELECT\n    `sales___ordered_at`,\n    `sales___streak`\nFROM (\nWITH base AS (\n  SELECT\n    DATE_TRUNC(sales.ordered_at, MONTH) AS `sales___ordered_at`,\n    SUM(sales.revenue) AS `sales___revenue_sum`\n  FROM sales AS sales\n  GROUP BY\n    DATE_TRUNC(sales.ordered_at, MONTH)\n), cp_reset_streak AS (\n  SELECT\n    `sales___ordered_at`,\n    `sales___revenue_sum`,\n    SUM(\n      CASE\n        WHEN NOT CASE WHEN `sales___revenue_sum` > 0 THEN 1 ELSE 0 END IS NULL\n        AND CASE WHEN `sales___revenue_sum` > 0 THEN 1 ELSE 0 END <> 0\n        THEN 0\n        ELSE 1\n      END\n    ) OVER (ORDER BY `sales___ordered_at` ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW) AS `_cp_reset_sales___streak`\n  FROM base\n), cp_value_streak AS (\n  SELECT\n    `sales___ordered_at`,\n    `sales___revenue_sum`,\n    CASE\n      WHEN NOT CASE WHEN `sales___revenue_sum` > 0 THEN 1 ELSE 0 END IS NULL\n      AND CASE WHEN `sales___revenue_sum` > 0 THEN 1 ELSE 0 END <> 0\n      THEN SUM(\n        CASE\n          WHEN NOT CASE WHEN `sales___revenue_sum` > 0 THEN 1 ELSE 0 END IS NULL\n          AND CASE WHEN `sales___revenue_sum` > 0 THEN 1 ELSE 0 END <> 0\n          THEN 1\n          ELSE 0\n        END\n      ) OVER (\n        PARTITION BY `_cp_reset_sales___streak`\n        ORDER BY `sales___ordered_at`\n        ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n      )\n      ELSE 0\n    END AS `sales___streak`\n  FROM cp_reset_streak\n)\nSELECT\n  `sales___ordered_at`,\n  `sales___revenue_sum`,\n  `sales___streak`\nFROM cp_value_streak\n) AS _outer",
  "cp/iif::duckdb": "SELECT\n    \"sales.ordered_at\",\n    \"sales.streak\"\nFROM (\nWITH base AS (\n  SELECT\n    DATE_TRUNC('MONTH', sales.ordered_at) AS \"sales.ordered_at\",\n    SUM(sales.revenue) AS \"sales.revenue_sum\"\n  FROM sales AS sales\n  GROUP BY\n    DATE_TRUNC('MONTH', sales.ordered_at)\n), cp_reset_streak AS (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    SUM(\n      CASE\n        WHEN NOT CASE WHEN \"sales.revenue_sum\" > 0 THEN 1 ELSE 0 END IS NULL\n        AND CASE WHEN \"sales.revenue_sum\" > 0 THEN 1 ELSE 0 END <> 0\n        THEN 0\n        ELSE 1\n      END\n    ) OVER (ORDER BY \"sales.ordered_at\" ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW) AS \"_cp_reset_sales.streak\"\n  FROM base\n), cp_value_streak AS (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    CASE\n      WHEN NOT CASE WHEN \"sales.revenue_sum\" > 0 THEN 1 ELSE 0 END IS NULL\n      AND CASE WHEN \"sales.revenue_sum\" > 0 THEN 1 ELSE 0 END <> 0\n      THEN SUM(\n        CASE\n          WHEN NOT CASE WHEN \"sales.revenue_sum\" > 0 THEN 1 ELSE 0 END IS NULL\n          AND CASE WHEN \"sales.revenue_sum\" > 0 THEN 1 ELSE 0 END <> 0\n          THEN 1\n          ELSE 0\n        END\n      ) OVER (\n        PARTITION BY \"_cp_reset_sales.streak\"\n        ORDER BY \"sales.ordered_at\"\n        ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n      )\n      ELSE 0\n    END AS \"sales.streak\"\n  FROM cp_reset_streak\n)\nSELECT\n  \"sales.ordered_at\",\n  \"sales.revenue_sum\",\n  \"sales.streak\"\nFROM cp_value_streak\n) AS _outer",
  "cp/iif::postgres": "SELECT\n    \"sales.ordered_at\",\n    \"sales.streak\"\nFROM (\nWITH base AS (\n  SELECT\n    DATE_TRUNC('MONTH', sales.ordered_at) AS \"sales.ordered_at\",\n    SUM(sales.revenue) AS \"sales.revenue_sum\"\n  FROM sales AS sales\n  GROUP BY\n    DATE_TRUNC('MONTH', sales.ordered_at)\n), cp_reset_streak AS (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    SUM(\n      CASE\n        WHEN NOT CASE WHEN \"sales.revenue_sum\" > 0 THEN 1 ELSE 0 END IS NULL\n        AND CASE WHEN \"sales.revenue_sum\" > 0 THEN 1 ELSE 0 END <> 0\n        THEN 0\n        ELSE 1\n      END\n    ) OVER (ORDER BY \"sales.ordered_at\" ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW) AS \"_cp_reset_sales.streak\"\n  FROM base\n), cp_value_streak AS (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    CASE\n      WHEN NOT CASE WHEN \"sales.revenue_sum\" > 0 THEN 1 ELSE 0 END IS NULL\n      AND CASE WHEN \"sales.revenue_sum\" > 0 THEN 1 ELSE 0 END <> 0\n      THEN SUM(\n        CASE\n          WHEN NOT CASE WHEN \"sales.revenue_sum\" > 0 THEN 1 ELSE 0 END IS NULL\n          AND CASE WHEN \"sales.revenue_sum\" > 0 THEN 1 ELSE 0 END <> 0\n          THEN 1\n          ELSE 0\n        END\n      ) OVER (\n        PARTITION BY \"_cp_reset_sales.streak\"\n        ORDER BY \"sales.ordered_at\"\n        ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n      )\n      ELSE 0\n    END AS \"sales.streak\"\n  FROM cp_reset_streak\n)\nSELECT\n  \"sales.ordered_at\",\n  \"sales.revenue_sum\",\n  \"sales.streak\"\nFROM cp_value_streak\n) AS _outer",
  "cp/iif::sqlite": "SELECT\n    \"sales.ordered_at\",\n    \"sales.streak\"\nFROM (\nWITH base AS (\n  SELECT\n    STRFTIME('%Y-%m-01', sales.ordered_at) AS \"sales.ordered_at\",\n    SUM(sales.revenue) AS \"sales.revenue_sum\"\n  FROM sales AS sales\n  GROUP BY\n    STRFTIME('%Y-%m-01', sales.ordered_at)\n), cp_reset_streak AS (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    SUM(\n      CASE\n        WHEN NOT CASE WHEN \"sales.revenue_sum\" > 0 THEN 1 ELSE 0 END IS NULL\n        AND CASE WHEN \"sales.revenue_sum\" > 0 THEN 1 ELSE 0 END <> 0\n        THEN 0\n        ELSE 1\n      END\n    ) OVER (ORDER BY \"sales.ordered_at\" ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW) AS \"_cp_reset_sales.streak\"\n  FROM base\n), cp_value_streak AS (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    CASE\n      WHEN NOT CASE WHEN \"sales.revenue_sum\" > 0 THEN 1 ELSE 0 END IS NULL\n      AND CASE WHEN \"sales.revenue_sum\" > 0 THEN 1 ELSE 0 END <> 0\n      THEN SUM(\n        CASE\n          WHEN NOT CASE WHEN \"sales.revenue_sum\" > 0 THEN 1 ELSE 0 END IS NULL\n          AND CASE WHEN \"sales.revenue_sum\" > 0 THEN 1 ELSE 0 END <> 0\n          THEN 1\n          ELSE 0\n        END\n      ) OVER (\n        PARTITION BY \"_cp_reset_sales.streak\"\n        ORDER BY \"sales.ordered_at\"\n        ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n      )\n      ELSE 0\n    END AS \"sales.streak\"\n  FROM cp_reset_streak\n)\nSELECT\n  \"sales.ordered_at\",\n  \"sales.revenue_sum\",\n  \"sales.streak\"\nFROM cp_value_streak\n) AS _outer",
  "cp/iif::tsql": "WITH base AS (\n  SELECT\n    DATETRUNC(MONTH, sales.ordered_at) AS [sales___ordered_at],\n    SUM(sales.revenue) AS [sales___revenue_sum]\n  FROM sales AS sales\n  GROUP BY\n    DATETRUNC(MONTH, sales.ordered_at)\n), cp_reset_streak AS (\n  SELECT\n    [sales___ordered_at] AS [sales___ordered_at],\n    [sales___revenue_sum] AS [sales___revenue_sum],\n    SUM(\n      CASE\n        WHEN NOT CASE WHEN [sales___revenue_sum] > 0 THEN 1 ELSE 0 END IS NULL\n        AND CASE WHEN [sales___revenue_sum] > 0 THEN 1 ELSE 0 END <> 0\n        THEN 0\n        ELSE 1\n      END\n    ) OVER (ORDER BY [sales___ordered_at] ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW) AS [_cp_reset_sales___streak]\n  FROM base\n), cp_value_streak AS (\n  SELECT\n    [sales___ordered_at] AS [sales___ordered_at],\n    [sales___revenue_sum] AS [sales___revenue_sum],\n    CASE\n      WHEN NOT CASE WHEN [sales___revenue_sum] > 0 THEN 1 ELSE 0 END IS NULL\n      AND CASE WHEN [sales___revenue_sum] > 0 THEN 1 ELSE 0 END <> 0\n      THEN SUM(\n        CASE\n          WHEN NOT CASE WHEN [sales___revenue_sum] > 0 THEN 1 ELSE 0 END IS NULL\n          AND CASE WHEN [sales___revenue_sum] > 0 THEN 1 ELSE 0 END <> 0\n          THEN 1\n          ELSE 0\n        END\n      ) OVER (\n        PARTITION BY [_cp_reset_sales___streak]\n        ORDER BY [sales___ordered_at]\n        ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n      )\n      ELSE 0\n    END AS [sales___streak]\n  FROM cp_reset_streak\n)\nSELECT\n  [sales___ordered_at],\n  [sales___streak]\nFROM (\n  SELECT\n    [sales___ordered_at] AS [sales___ordered_at],\n    [sales___revenue_sum] AS [sales___revenue_sum],\n    [sales___streak] AS [sales___streak]\n  FROM cp_value_streak\n) AS _outer",
  "cp/in::bigquery": "SELECT\n    `sales___store`,\n    `sales___ordered_at`,\n    `sales___streak`\nFROM (\nWITH base AS (\n  SELECT\n    sales.store AS `sales___store`,\n    DATE_TRUNC(sales.ordered_at, MONTH) AS `sales___ordered_at`\n  FROM sales AS sales\n  GROUP BY\n    sales.store,\n    DATE_TRUNC(sales.ordered_at, MONTH)\n), cp_reset_streak AS (\n  SELECT\n    `sales___store`,\n    `sales___ordered_at`,\n    SUM(CASE WHEN COALESCE(`sales___store` IN ('A', 'B'), FALSE) THEN 0 ELSE 1 END) OVER (\n      PARTITION BY `sales___store`\n      ORDER BY `sales___ordered_at`\n      ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n    ) AS `_cp_reset_sales___streak`\n  FROM base\n), cp_value_streak AS (\n  SELECT\n    `sales___store`,\n    `sales___ordered_at`,\n    CASE\n      WHEN COALESCE(`sales___store` IN ('A', 'B'), FALSE)\n      THEN SUM(CASE WHEN COALESCE(`sales___store` IN ('A', 'B'), FALSE) THEN 1 ELSE 0 END) OVER (\n        PARTITION BY `sales___store`, `_cp_reset_sales___streak`\n        ORDER BY `sales___ordered_at`\n        ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n      )\n      ELSE 0\n    END AS `sales___streak`\n  FROM cp_reset_streak\n)\nSELECT\n  `sales___store`,\n  `sales___ordered_at`,\n  `sales___streak`\nFROM cp_value_streak\n) AS _outer",
  "cp/in::duckdb": "SELECT\n    \"sales.store\",\n    \"sales.ordered_at\",\n    \"sales.streak\"\nFROM (\nWITH base AS (\n  SELECT\n    sales.store AS \"sales.store\",\n    DATE_TRUNC('MONTH', sales.ordered_at) AS \"sales.ordered_at\"\n  FROM sales AS sales\n  GROUP BY\n    sales.store,\n    DATE_TRUNC('MONTH', sales.ordered_at)\n), cp_reset_streak AS (\n  SELECT\n    \"sales.store\",\n    \"sales.ordered_at\",\n    SUM(CASE WHEN COALESCE(\"sales.store\" IN ('A', 'B'), FALSE) THEN 0 ELSE 1 END) OVER (\n      PARTITION BY \"sales.store\"\n      ORDER BY \"sales.ordered_at\"\n      ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n    ) AS \"_cp_reset_sales.streak\"\n  FROM base\n), cp_value_streak AS (\n  SELECT\n    \"sales.store\",\n    \"sales.ordered_at\",\n    CASE\n      WHEN COALESCE(\"sales.store\" IN ('A', 'B'), FALSE)\n      THEN SUM(CASE WHEN COALESCE(\"sales.store\" IN ('A', 'B'), FALSE) THEN 1 ELSE 0 END) OVER (\n        PARTITION BY \"sales.store\", \"_cp_reset_sales.streak\"\n        ORDER BY \"sales.ordered_at\"\n        ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n      )\n      ELSE 0\n    END AS \"sales.streak\"\n  FROM cp_reset_streak\n)\nSELECT\n  \"sales.store\",\n  \"sales.ordered_at\",\n  \"sales.streak\"\nFROM cp_value_streak\n) AS _outer",
  "cp/in::postgres": "SELECT\n    \"sales.store\",\n    \"sales.ordered_at\",\n    \"sales.streak\"\nFROM (\nWITH base AS (\n  SELECT\n    sales.store AS \"sales.store\",\n    DATE_TRUNC('MONTH', sales.ordered_at) AS \"sales.ordered_at\"\n  FROM sales AS sales\n  GROUP BY\n    sales.store,\n    DATE_TRUNC('MONTH', sales.ordered_at)\n), cp_reset_streak AS (\n  SELECT\n    \"sales.store\",\n    \"sales.ordered_at\",\n    SUM(CASE WHEN COALESCE(\"sales.store\" IN ('A', 'B'), FALSE) THEN 0 ELSE 1 END) OVER (\n      PARTITION BY \"sales.store\"\n      ORDER BY \"sales.ordered_at\"\n      ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n    ) AS \"_cp_reset_sales.streak\"\n  FROM base\n), cp_value_streak AS (\n  SELECT\n    \"sales.store\",\n    \"sales.ordered_at\",\n    CASE\n      WHEN COALESCE(\"sales.store\" IN ('A', 'B'), FALSE)\n      THEN SUM(CASE WHEN COALESCE(\"sales.store\" IN ('A', 'B'), FALSE) THEN 1 ELSE 0 END) OVER (\n        PARTITION BY \"sales.store\", \"_cp_reset_sales.streak\"\n        ORDER BY \"sales.ordered_at\"\n        ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n      )\n      ELSE 0\n    END AS \"sales.streak\"\n  FROM cp_reset_streak\n)\nSELECT\n  \"sales.store\",\n  \"sales.ordered_at\",\n  \"sales.streak\"\nFROM cp_value_streak\n) AS _outer",
  "cp/in::sqlite": "SELECT\n    \"sales.store\",\n    \"sales.ordered_at\",\n    \"sales.streak\"\nFROM (\nWITH base AS (\n  SELECT\n    sales.store AS \"sales.store\",\n    STRFTIME('%Y-%m-01', sales.ordered_at) AS \"sales.ordered_at\"\n  FROM sales AS sales\n  GROUP BY\n    sales.store,\n    STRFTIME('%Y-%m-01', sales.ordered_at)\n), cp_reset_streak AS (\n  SELECT\n    \"sales.store\",\n    \"sales.ordered_at\",\n    SUM(CASE WHEN COALESCE(\"sales.store\" IN ('A', 'B'), FALSE) THEN 0 ELSE 1 END) OVER (\n      PARTITION BY \"sales.store\"\n      ORDER BY \"sales.ordered_at\"\n      ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n    ) AS \"_cp_reset_sales.streak\"\n  FROM base\n), cp_value_streak AS (\n  SELECT\n    \"sales.store\",\n    \"sales.ordered_at\",\n    CASE\n      WHEN COALESCE(\"sales.store\" IN ('A', 'B'), FALSE)\n      THEN SUM(CASE WHEN COALESCE(\"sales.store\" IN ('A', 'B'), FALSE) THEN 1 ELSE 0 END) OVER (\n        PARTITION BY \"sales.store\", \"_cp_reset_sales.streak\"\n        ORDER BY \"sales.ordered_at\"\n        ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n      )\n      ELSE 0\n    END AS \"sales.streak\"\n  FROM cp_reset_streak\n)\nSELECT\n  \"sales.store\",\n  \"sales.ordered_at\",\n  \"sales.streak\"\nFROM cp_value_streak\n) AS _outer",
  "cp/in::tsql": "WITH base AS (\n  SELECT\n    sales.store AS [sales___store],\n    DATETRUNC(MONTH, sales.ordered_at) AS [sales___ordered_at]\n  FROM sales AS sales\n  GROUP BY\n    sales.store,\n    DATETRUNC(MONTH, sales.ordered_at)\n), cp_reset_streak AS (\n  SELECT\n    [sales___store] AS [sales___store],\n    [sales___ordered_at] AS [sales___ordered_at],\n    SUM(CASE WHEN COALESCE([sales___store] IN ('A', 'B'), (\n      1 = 0\n    )) THEN 0 ELSE 1 END) OVER (\n      PARTITION BY [sales___store]\n      ORDER BY [sales___ordered_at]\n      ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n    ) AS [_cp_reset_sales___streak]\n  FROM base\n), cp_value_streak AS (\n  SELECT\n    [sales___store] AS [sales___store],\n    [sales___ordered_at] AS [sales___ordered_at],\n    CASE\n      WHEN COALESCE([sales___store] IN ('A', 'B'), (\n        1 = 0\n      ))\n      THEN SUM(CASE WHEN COALESCE([sales___store] IN ('A', 'B'), (\n        1 = 0\n      )) THEN 1 ELSE 0 END) OVER (\n        PARTITION BY [sales___store], [_cp_reset_sales___streak]\n        ORDER BY [sales___ordered_at]\n        ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n      )\n      ELSE 0\n    END AS [sales___streak]\n  FROM cp_reset_streak\n)\nSELECT\n  [sales___store],\n  [sales___ordered_at],\n  [sales___streak]\nFROM (\n  SELECT\n    [sales___store] AS [sales___store],\n    [sales___ordered_at] AS [sales___ordered_at],\n    [sales___streak] AS [sales___streak]\n  FROM cp_value_streak\n) AS _outer",
  "cp/negated_in::bigquery": "SELECT\n    `sales___store`,\n    `sales___ordered_at`,\n    `sales___streak`\nFROM (\nWITH base AS (\n  SELECT\n    sales.store AS `sales___store`,\n    DATE_TRUNC(sales.ordered_at, MONTH) AS `sales___ordered_at`\n  FROM sales AS sales\n  GROUP BY\n    sales.store,\n    DATE_TRUNC(sales.ordered_at, MONTH)\n), cp_reset_streak AS (\n  SELECT\n    `sales___store`,\n    `sales___ordered_at`,\n    SUM(CASE WHEN COALESCE(NOT `sales___store` IN ('B', 'C'), FALSE) THEN 0 ELSE 1 END) OVER (\n      PARTITION BY `sales___store`\n      ORDER BY `sales___ordered_at`\n      ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n    ) AS `_cp_reset_sales___streak`\n  FROM base\n), cp_value_streak AS (\n  SELECT\n    `sales___store`,\n    `sales___ordered_at`,\n    CASE\n      WHEN COALESCE(NOT `sales___store` IN ('B', 'C'), FALSE)\n      THEN SUM(CASE WHEN COALESCE(NOT `sales___store` IN ('B', 'C'), FALSE) THEN 1 ELSE 0 END) OVER (\n        PARTITION BY `sales___store`, `_cp_reset_sales___streak`\n        ORDER BY `sales___ordered_at`\n        ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n      )\n      ELSE 0\n    END AS `sales___streak`\n  FROM cp_reset_streak\n)\nSELECT\n  `sales___store`,\n  `sales___ordered_at`,\n  `sales___streak`\nFROM cp_value_streak\n) AS _outer",
  "cp/negated_in::duckdb": "SELECT\n    \"sales.store\",\n    \"sales.ordered_at\",\n    \"sales.streak\"\nFROM (\nWITH base AS (\n  SELECT\n    sales.store AS \"sales.store\",\n    DATE_TRUNC('MONTH', sales.ordered_at) AS \"sales.ordered_at\"\n  FROM sales AS sales\n  GROUP BY\n    sales.store,\n    DATE_TRUNC('MONTH', sales.ordered_at)\n), cp_reset_streak AS (\n  SELECT\n    \"sales.store\",\n    \"sales.ordered_at\",\n    SUM(CASE WHEN COALESCE(NOT \"sales.store\" IN ('B', 'C'), FALSE) THEN 0 ELSE 1 END) OVER (\n      PARTITION BY \"sales.store\"\n      ORDER BY \"sales.ordered_at\"\n      ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n    ) AS \"_cp_reset_sales.streak\"\n  FROM base\n), cp_value_streak AS (\n  SELECT\n    \"sales.store\",\n    \"sales.ordered_at\",\n    CASE\n      WHEN COALESCE(NOT \"sales.store\" IN ('B', 'C'), FALSE)\n      THEN SUM(CASE WHEN COALESCE(NOT \"sales.store\" IN ('B', 'C'), FALSE) THEN 1 ELSE 0 END) OVER (\n        PARTITION BY \"sales.store\", \"_cp_reset_sales.streak\"\n        ORDER BY \"sales.ordered_at\"\n        ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n      )\n      ELSE 0\n    END AS \"sales.streak\"\n  FROM cp_reset_streak\n)\nSELECT\n  \"sales.store\",\n  \"sales.ordered_at\",\n  \"sales.streak\"\nFROM cp_value_streak\n) AS _outer",
  "cp/negated_in::postgres": "SELECT\n    \"sales.store\",\n    \"sales.ordered_at\",\n    \"sales.streak\"\nFROM (\nWITH base AS (\n  SELECT\n    sales.store AS \"sales.store\",\n    DATE_TRUNC('MONTH', sales.ordered_at) AS \"sales.ordered_at\"\n  FROM sales AS sales\n  GROUP BY\n    sales.store,\n    DATE_TRUNC('MONTH', sales.ordered_at)\n), cp_reset_streak AS (\n  SELECT\n    \"sales.store\",\n    \"sales.ordered_at\",\n    SUM(CASE WHEN COALESCE(NOT \"sales.store\" IN ('B', 'C'), FALSE) THEN 0 ELSE 1 END) OVER (\n      PARTITION BY \"sales.store\"\n      ORDER BY \"sales.ordered_at\"\n      ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n    ) AS \"_cp_reset_sales.streak\"\n  FROM base\n), cp_value_streak AS (\n  SELECT\n    \"sales.store\",\n    \"sales.ordered_at\",\n    CASE\n      WHEN COALESCE(NOT \"sales.store\" IN ('B', 'C'), FALSE)\n      THEN SUM(CASE WHEN COALESCE(NOT \"sales.store\" IN ('B', 'C'), FALSE) THEN 1 ELSE 0 END) OVER (\n        PARTITION BY \"sales.store\", \"_cp_reset_sales.streak\"\n        ORDER BY \"sales.ordered_at\"\n        ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n      )\n      ELSE 0\n    END AS \"sales.streak\"\n  FROM cp_reset_streak\n)\nSELECT\n  \"sales.store\",\n  \"sales.ordered_at\",\n  \"sales.streak\"\nFROM cp_value_streak\n) AS _outer",
  "cp/negated_in::sqlite": "SELECT\n    \"sales.store\",\n    \"sales.ordered_at\",\n    \"sales.streak\"\nFROM (\nWITH base AS (\n  SELECT\n    sales.store AS \"sales.store\",\n    STRFTIME('%Y-%m-01', sales.ordered_at) AS \"sales.ordered_at\"\n  FROM sales AS sales\n  GROUP BY\n    sales.store,\n    STRFTIME('%Y-%m-01', sales.ordered_at)\n), cp_reset_streak AS (\n  SELECT\n    \"sales.store\",\n    \"sales.ordered_at\",\n    SUM(CASE WHEN COALESCE(NOT \"sales.store\" IN ('B', 'C'), FALSE) THEN 0 ELSE 1 END) OVER (\n      PARTITION BY \"sales.store\"\n      ORDER BY \"sales.ordered_at\"\n      ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n    ) AS \"_cp_reset_sales.streak\"\n  FROM base\n), cp_value_streak AS (\n  SELECT\n    \"sales.store\",\n    \"sales.ordered_at\",\n    CASE\n      WHEN COALESCE(NOT \"sales.store\" IN ('B', 'C'), FALSE)\n      THEN SUM(CASE WHEN COALESCE(NOT \"sales.store\" IN ('B', 'C'), FALSE) THEN 1 ELSE 0 END) OVER (\n        PARTITION BY \"sales.store\", \"_cp_reset_sales.streak\"\n        ORDER BY \"sales.ordered_at\"\n        ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n      )\n      ELSE 0\n    END AS \"sales.streak\"\n  FROM cp_reset_streak\n)\nSELECT\n  \"sales.store\",\n  \"sales.ordered_at\",\n  \"sales.streak\"\nFROM cp_value_streak\n) AS _outer",
  "cp/negated_in::tsql": "WITH base AS (\n  SELECT\n    sales.store AS [sales___store],\n    DATETRUNC(MONTH, sales.ordered_at) AS [sales___ordered_at]\n  FROM sales AS sales\n  GROUP BY\n    sales.store,\n    DATETRUNC(MONTH, sales.ordered_at)\n), cp_reset_streak AS (\n  SELECT\n    [sales___store] AS [sales___store],\n    [sales___ordered_at] AS [sales___ordered_at],\n    SUM(\n      CASE WHEN COALESCE(NOT [sales___store] IN ('B', 'C'), (\n        1 = 0\n      )) THEN 0 ELSE 1 END\n    ) OVER (\n      PARTITION BY [sales___store]\n      ORDER BY [sales___ordered_at]\n      ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n    ) AS [_cp_reset_sales___streak]\n  FROM base\n), cp_value_streak AS (\n  SELECT\n    [sales___store] AS [sales___store],\n    [sales___ordered_at] AS [sales___ordered_at],\n    CASE\n      WHEN COALESCE(NOT [sales___store] IN ('B', 'C'), (\n        1 = 0\n      ))\n      THEN SUM(\n        CASE WHEN COALESCE(NOT [sales___store] IN ('B', 'C'), (\n          1 = 0\n        )) THEN 1 ELSE 0 END\n      ) OVER (\n        PARTITION BY [sales___store], [_cp_reset_sales___streak]\n        ORDER BY [sales___ordered_at]\n        ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n      )\n      ELSE 0\n    END AS [sales___streak]\n  FROM cp_reset_streak\n)\nSELECT\n  [sales___store],\n  [sales___ordered_at],\n  [sales___streak]\nFROM (\n  SELECT\n    [sales___store] AS [sales___store],\n    [sales___ordered_at] AS [sales___ordered_at],\n    [sales___streak] AS [sales___streak]\n  FROM cp_value_streak\n) AS _outer",
  "cp/nested_in_and::bigquery": "SELECT\n    `sales___store`,\n    `sales___ordered_at`,\n    `sales___streak`\nFROM (\nWITH base AS (\n  SELECT\n    sales.store AS `sales___store`,\n    DATE_TRUNC(sales.ordered_at, MONTH) AS `sales___ordered_at`,\n    SUM(sales.revenue) AS `sales___revenue_sum`\n  FROM sales AS sales\n  GROUP BY\n    sales.store,\n    DATE_TRUNC(sales.ordered_at, MONTH)\n), cp_reset_streak AS (\n  SELECT\n    `sales___store`,\n    `sales___ordered_at`,\n    `sales___revenue_sum`,\n    SUM(\n      CASE\n        WHEN COALESCE(`sales___store` IN ('A', 'C') AND `sales___revenue_sum` > 0, FALSE)\n        THEN 0\n        ELSE 1\n      END\n    ) OVER (\n      PARTITION BY `sales___store`\n      ORDER BY `sales___ordered_at`\n      ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n    ) AS `_cp_reset_sales___streak`\n  FROM base\n), cp_value_streak AS (\n  SELECT\n    `sales___store`,\n    `sales___ordered_at`,\n    `sales___revenue_sum`,\n    CASE\n      WHEN COALESCE(`sales___store` IN ('A', 'C') AND `sales___revenue_sum` > 0, FALSE)\n      THEN SUM(\n        CASE\n          WHEN COALESCE(`sales___store` IN ('A', 'C') AND `sales___revenue_sum` > 0, FALSE)\n          THEN 1\n          ELSE 0\n        END\n      ) OVER (\n        PARTITION BY `sales___store`, `_cp_reset_sales___streak`\n        ORDER BY `sales___ordered_at`\n        ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n      )\n      ELSE 0\n    END AS `sales___streak`\n  FROM cp_reset_streak\n)\nSELECT\n  `sales___store`,\n  `sales___ordered_at`,\n  `sales___revenue_sum`,\n  `sales___streak`\nFROM cp_value_streak\n) AS _outer",
  "cp/nested_in_and::duckdb": "SELECT\n    \"sales.store\",\n    \"sales.ordered_at\",\n    \"sales.streak\"\nFROM (\nWITH base AS (\n  SELECT\n    sales.store AS \"sales.store\",\n    DATE_TRUNC('MONTH', sales.ordered_at) AS \"sales.ordered_at\",\n    SUM(sales.revenue) AS \"sales.revenue_sum\"\n  FROM sales AS sales\n  GROUP BY\n    sales.store,\n    DATE_TRUNC('MONTH', sales.ordered_at)\n), cp_reset_streak AS (\n  SELECT\n    \"sales.store\",\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    SUM(\n      CASE\n        WHEN COALESCE(\"sales.store\" IN ('A', 'C') AND \"sales.revenue_sum\" > 0, FALSE)\n        THEN 0\n        ELSE 1\n      END\n    ) OVER (\n      PARTITION BY \"sales.store\"\n      ORDER BY \"sales.ordered_at\"\n      ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n    ) AS \"_cp_reset_sales.streak\"\n  FROM base\n), cp_value_streak AS (\n  SELECT\n    \"sales.store\",\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    CASE\n      WHEN COALESCE(\"sales.store\" IN ('A', 'C') AND \"sales.revenue_sum\" > 0, FALSE)\n      THEN SUM(\n        CASE\n          WHEN COALESCE(\"sales.store\" IN ('A', 'C') AND \"sales.revenue_sum\" > 0, FALSE)\n          THEN 1\n          ELSE 0\n        END\n      ) OVER (\n        PARTITION BY \"sales.store\", \"_cp_reset_sales.streak\"\n        ORDER BY \"sales.ordered_at\"\n        ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n      )\n      ELSE 0\n    END AS \"sales.streak\"\n  FROM cp_reset_streak\n)\nSELECT\n  \"sales.store\",\n  \"sales.ordered_at\",\n  \"sales.revenue_sum\",\n  \"sales.streak\"\nFROM cp_value_streak\n) AS _outer",
  "cp/nested_in_and::postgres": "SELECT\n    \"sales.store\",\n    \"sales.ordered_at\",\n    \"sales.streak\"\nFROM (\nWITH base AS (\n  SELECT\n    sales.store AS \"sales.store\",\n    DATE_TRUNC('MONTH', sales.ordered_at) AS \"sales.ordered_at\",\n    SUM(sales.revenue) AS \"sales.revenue_sum\"\n  FROM sales AS sales\n  GROUP BY\n    sales.store,\n    DATE_TRUNC('MONTH', sales.ordered_at)\n), cp_reset_streak AS (\n  SELECT\n    \"sales.store\",\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    SUM(\n      CASE\n        WHEN COALESCE(\"sales.store\" IN ('A', 'C') AND \"sales.revenue_sum\" > 0, FALSE)\n        THEN 0\n        ELSE 1\n      END\n    ) OVER (\n      PARTITION BY \"sales.store\"\n      ORDER BY \"sales.ordered_at\"\n      ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n    ) AS \"_cp_reset_sales.streak\"\n  FROM base\n), cp_value_streak AS (\n  SELECT\n    \"sales.store\",\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    CASE\n      WHEN COALESCE(\"sales.store\" IN ('A', 'C') AND \"sales.revenue_sum\" > 0, FALSE)\n      THEN SUM(\n        CASE\n          WHEN COALESCE(\"sales.store\" IN ('A', 'C') AND \"sales.revenue_sum\" > 0, FALSE)\n          THEN 1\n          ELSE 0\n        END\n      ) OVER (\n        PARTITION BY \"sales.store\", \"_cp_reset_sales.streak\"\n        ORDER BY \"sales.ordered_at\"\n        ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n      )\n      ELSE 0\n    END AS \"sales.streak\"\n  FROM cp_reset_streak\n)\nSELECT\n  \"sales.store\",\n  \"sales.ordered_at\",\n  \"sales.revenue_sum\",\n  \"sales.streak\"\nFROM cp_value_streak\n) AS _outer",
  "cp/nested_in_and::sqlite": "SELECT\n    \"sales.store\",\n    \"sales.ordered_at\",\n    \"sales.streak\"\nFROM (\nWITH base AS (\n  SELECT\n    sales.store AS \"sales.store\",\n    STRFTIME('%Y-%m-01', sales.ordered_at) AS \"sales.ordered_at\",\n    SUM(sales.revenue) AS \"sales.revenue_sum\"\n  FROM sales AS sales\n  GROUP BY\n    sales.store,\n    STRFTIME('%Y-%m-01', sales.ordered_at)\n), cp_reset_streak AS (\n  SELECT\n    \"sales.store\",\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    SUM(\n      CASE\n        WHEN COALESCE(\"sales.store\" IN ('A', 'C') AND \"sales.revenue_sum\" > 0, FALSE)\n        THEN 0\n        ELSE 1\n      END\n    ) OVER (\n      PARTITION BY \"sales.store\"\n      ORDER BY \"sales.ordered_at\"\n      ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n    ) AS \"_cp_reset_sales.streak\"\n  FROM base\n), cp_value_streak AS (\n  SELECT\n    \"sales.store\",\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    CASE\n      WHEN COALESCE(\"sales.store\" IN ('A', 'C') AND \"sales.revenue_sum\" > 0, FALSE)\n      THEN SUM(\n        CASE\n          WHEN COALESCE(\"sales.store\" IN ('A', 'C') AND \"sales.revenue_sum\" > 0, FALSE)\n          THEN 1\n          ELSE 0\n        END\n      ) OVER (\n        PARTITION BY \"sales.store\", \"_cp_reset_sales.streak\"\n        ORDER BY \"sales.ordered_at\"\n        ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n      )\n      ELSE 0\n    END AS \"sales.streak\"\n  FROM cp_reset_streak\n)\nSELECT\n  \"sales.store\",\n  \"sales.ordered_at\",\n  \"sales.revenue_sum\",\n  \"sales.streak\"\nFROM cp_value_streak\n) AS _outer",
  "cp/nested_in_and::tsql": "WITH base AS (\n  SELECT\n    sales.store AS [sales___store],\n    DATETRUNC(MONTH, sales.ordered_at) AS [sales___ordered_at],\n    SUM(sales.revenue) AS [sales___revenue_sum]\n  FROM sales AS sales\n  GROUP BY\n    sales.store,\n    DATETRUNC(MONTH, sales.ordered_at)\n), cp_reset_streak AS (\n  SELECT\n    [sales___store] AS [sales___store],\n    [sales___ordered_at] AS [sales___ordered_at],\n    [sales___revenue_sum] AS [sales___revenue_sum],\n    SUM(\n      CASE\n        WHEN COALESCE([sales___store] IN ('A', 'C') AND [sales___revenue_sum] > 0, (\n          1 = 0\n        ))\n        THEN 0\n        ELSE 1\n      END\n    ) OVER (\n      PARTITION BY [sales___store]\n      ORDER BY [sales___ordered_at]\n      ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n    ) AS [_cp_reset_sales___streak]\n  FROM base\n), cp_value_streak AS (\n  SELECT\n    [sales___store] AS [sales___store],\n    [sales___ordered_at] AS [sales___ordered_at],\n    [sales___revenue_sum] AS [sales___revenue_sum],\n    CASE\n      WHEN COALESCE([sales___store] IN ('A', 'C') AND [sales___revenue_sum] > 0, (\n        1 = 0\n      ))\n      THEN SUM(\n        CASE\n          WHEN COALESCE([sales___store] IN ('A', 'C') AND [sales___revenue_sum] > 0, (\n            1 = 0\n          ))\n          THEN 1\n          ELSE 0\n        END\n      ) OVER (\n        PARTITION BY [sales___store], [_cp_reset_sales___streak]\n        ORDER BY [sales___ordered_at]\n        ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n      )\n      ELSE 0\n    END AS [sales___streak]\n  FROM cp_reset_streak\n)\nSELECT\n  [sales___store],\n  [sales___ordered_at],\n  [sales___streak]\nFROM (\n  SELECT\n    [sales___store] AS [sales___store],\n    [sales___ordered_at] AS [sales___ordered_at],\n    [sales___revenue_sum] AS [sales___revenue_sum],\n    [sales___streak] AS [sales___streak]\n  FROM cp_value_streak\n) AS _outer",
  "cp/not::bigquery": "SELECT\n    `sales___ordered_at`,\n    `sales___streak`\nFROM (\nWITH base AS (\n  SELECT\n    DATE_TRUNC(sales.ordered_at, MONTH) AS `sales___ordered_at`,\n    SUM(sales.revenue) AS `sales___revenue_sum`\n  FROM sales AS sales\n  GROUP BY\n    DATE_TRUNC(sales.ordered_at, MONTH)\n), cp_reset_streak AS (\n  SELECT\n    `sales___ordered_at`,\n    `sales___revenue_sum`,\n    SUM(CASE WHEN COALESCE(NOT `sales___revenue_sum` > 90, FALSE) THEN 0 ELSE 1 END) OVER (ORDER BY `sales___ordered_at` ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW) AS `_cp_reset_sales___streak`\n  FROM base\n), cp_value_streak AS (\n  SELECT\n    `sales___ordered_at`,\n    `sales___revenue_sum`,\n    CASE\n      WHEN COALESCE(NOT `sales___revenue_sum` > 90, FALSE)\n      THEN SUM(CASE WHEN COALESCE(NOT `sales___revenue_sum` > 90, FALSE) THEN 1 ELSE 0 END) OVER (\n        PARTITION BY `_cp_reset_sales___streak`\n        ORDER BY `sales___ordered_at`\n        ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n      )\n      ELSE 0\n    END AS `sales___streak`\n  FROM cp_reset_streak\n)\nSELECT\n  `sales___ordered_at`,\n  `sales___revenue_sum`,\n  `sales___streak`\nFROM cp_value_streak\n) AS _outer",
  "cp/not::duckdb": "SELECT\n    \"sales.ordered_at\",\n    \"sales.streak\"\nFROM (\nWITH base AS (\n  SELECT\n    DATE_TRUNC('MONTH', sales.ordered_at) AS \"sales.ordered_at\",\n    SUM(sales.revenue) AS \"sales.revenue_sum\"\n  FROM sales AS sales\n  GROUP BY\n    DATE_TRUNC('MONTH', sales.ordered_at)\n), cp_reset_streak AS (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    SUM(CASE WHEN COALESCE(NOT \"sales.revenue_sum\" > 90, FALSE) THEN 0 ELSE 1 END) OVER (ORDER BY \"sales.ordered_at\" ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW) AS \"_cp_reset_sales.streak\"\n  FROM base\n), cp_value_streak AS (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    CASE\n      WHEN COALESCE(NOT \"sales.revenue_sum\" > 90, FALSE)\n      THEN SUM(CASE WHEN COALESCE(NOT \"sales.revenue_sum\" > 90, FALSE) THEN 1 ELSE 0 END) OVER (\n        PARTITION BY \"_cp_reset_sales.streak\"\n        ORDER BY \"sales.ordered_at\"\n        ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n      )\n      ELSE 0\n    END AS \"sales.streak\"\n  FROM cp_reset_streak\n)\nSELECT\n  \"sales.ordered_at\",\n  \"sales.revenue_sum\",\n  \"sales.streak\"\nFROM cp_value_streak\n) AS _outer",
  "cp/not::postgres": "SELECT\n    \"sales.ordered_at\",\n    \"sales.streak\"\nFROM (\nWITH base AS (\n  SELECT\n    DATE_TRUNC('MONTH', sales.ordered_at) AS \"sales.ordered_at\",\n    SUM(sales.revenue) AS \"sales.revenue_sum\"\n  FROM sales AS sales\n  GROUP BY\n    DATE_TRUNC('MONTH', sales.ordered_at)\n), cp_reset_streak AS (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    SUM(CASE WHEN COALESCE(NOT \"sales.revenue_sum\" > 90, FALSE) THEN 0 ELSE 1 END) OVER (ORDER BY \"sales.ordered_at\" ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW) AS \"_cp_reset_sales.streak\"\n  FROM base\n), cp_value_streak AS (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    CASE\n      WHEN COALESCE(NOT \"sales.revenue_sum\" > 90, FALSE)\n      THEN SUM(CASE WHEN COALESCE(NOT \"sales.revenue_sum\" > 90, FALSE) THEN 1 ELSE 0 END) OVER (\n        PARTITION BY \"_cp_reset_sales.streak\"\n        ORDER BY \"sales.ordered_at\"\n        ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n      )\n      ELSE 0\n    END AS \"sales.streak\"\n  FROM cp_reset_streak\n)\nSELECT\n  \"sales.ordered_at\",\n  \"sales.revenue_sum\",\n  \"sales.streak\"\nFROM cp_value_streak\n) AS _outer",
  "cp/not::sqlite": "SELECT\n    \"sales.ordered_at\",\n    \"sales.streak\"\nFROM (\nWITH base AS (\n  SELECT\n    STRFTIME('%Y-%m-01', sales.ordered_at) AS \"sales.ordered_at\",\n    SUM(sales.revenue) AS \"sales.revenue_sum\"\n  FROM sales AS sales\n  GROUP BY\n    STRFTIME('%Y-%m-01', sales.ordered_at)\n), cp_reset_streak AS (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    SUM(CASE WHEN COALESCE(NOT \"sales.revenue_sum\" > 90, FALSE) THEN 0 ELSE 1 END) OVER (ORDER BY \"sales.ordered_at\" ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW) AS \"_cp_reset_sales.streak\"\n  FROM base\n), cp_value_streak AS (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    CASE\n      WHEN COALESCE(NOT \"sales.revenue_sum\" > 90, FALSE)\n      THEN SUM(CASE WHEN COALESCE(NOT \"sales.revenue_sum\" > 90, FALSE) THEN 1 ELSE 0 END) OVER (\n        PARTITION BY \"_cp_reset_sales.streak\"\n        ORDER BY \"sales.ordered_at\"\n        ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n      )\n      ELSE 0\n    END AS \"sales.streak\"\n  FROM cp_reset_streak\n)\nSELECT\n  \"sales.ordered_at\",\n  \"sales.revenue_sum\",\n  \"sales.streak\"\nFROM cp_value_streak\n) AS _outer",
  "cp/not::tsql": "WITH base AS (\n  SELECT\n    DATETRUNC(MONTH, sales.ordered_at) AS [sales___ordered_at],\n    SUM(sales.revenue) AS [sales___revenue_sum]\n  FROM sales AS sales\n  GROUP BY\n    DATETRUNC(MONTH, sales.ordered_at)\n), cp_reset_streak AS (\n  SELECT\n    [sales___ordered_at] AS [sales___ordered_at],\n    [sales___revenue_sum] AS [sales___revenue_sum],\n    SUM(CASE WHEN COALESCE(NOT [sales___revenue_sum] > 90, (\n      1 = 0\n    )) THEN 0 ELSE 1 END) OVER (ORDER BY [sales___ordered_at] ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW) AS [_cp_reset_sales___streak]\n  FROM base\n), cp_value_streak AS (\n  SELECT\n    [sales___ordered_at] AS [sales___ordered_at],\n    [sales___revenue_sum] AS [sales___revenue_sum],\n    CASE\n      WHEN COALESCE(NOT [sales___revenue_sum] > 90, (\n        1 = 0\n      ))\n      THEN SUM(CASE WHEN COALESCE(NOT [sales___revenue_sum] > 90, (\n        1 = 0\n      )) THEN 1 ELSE 0 END) OVER (\n        PARTITION BY [_cp_reset_sales___streak]\n        ORDER BY [sales___ordered_at]\n        ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n      )\n      ELSE 0\n    END AS [sales___streak]\n  FROM cp_reset_streak\n)\nSELECT\n  [sales___ordered_at],\n  [sales___streak]\nFROM (\n  SELECT\n    [sales___ordered_at] AS [sales___ordered_at],\n    [sales___revenue_sum] AS [sales___revenue_sum],\n    [sales___streak] AS [sales___streak]\n  FROM cp_value_streak\n) AS _outer",
  "cp/numeric_delta::bigquery": "SELECT\n    `sales___ordered_at`,\n    `sales___streak`\nFROM (\nWITH base AS (\n  SELECT\n    DATE_TRUNC(sales.ordered_at, MONTH) AS `sales___ordered_at`,\n    SUM(sales.revenue) AS `sales___revenue_sum`,\n    SUM(sales.cost) AS `sales___cost_sum`\n  FROM sales AS sales\n  GROUP BY\n    DATE_TRUNC(sales.ordered_at, MONTH)\n), cp_reset_streak AS (\n  SELECT\n    `sales___ordered_at`,\n    `sales___revenue_sum`,\n    `sales___cost_sum`,\n    SUM(\n      CASE\n        WHEN NOT `sales___revenue_sum` - `sales___cost_sum` IS NULL\n        AND `sales___revenue_sum` - `sales___cost_sum` <> 0\n        THEN 0\n        ELSE 1\n      END\n    ) OVER (ORDER BY `sales___ordered_at` ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW) AS `_cp_reset_sales___streak`\n  FROM base\n), cp_value_streak AS (\n  SELECT\n    `sales___ordered_at`,\n    `sales___revenue_sum`,\n    `sales___cost_sum`,\n    CASE\n      WHEN NOT `sales___revenue_sum` - `sales___cost_sum` IS NULL\n      AND `sales___revenue_sum` - `sales___cost_sum` <> 0\n      THEN SUM(\n        CASE\n          WHEN NOT `sales___revenue_sum` - `sales___cost_sum` IS NULL\n          AND `sales___revenue_sum` - `sales___cost_sum` <> 0\n          THEN 1\n          ELSE 0\n        END\n      ) OVER (\n        PARTITION BY `_cp_reset_sales___streak`\n        ORDER BY `sales___ordered_at`\n        ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n      )\n      ELSE 0\n    END AS `sales___streak`\n  FROM cp_reset_streak\n)\nSELECT\n  `sales___ordered_at`,\n  `sales___revenue_sum`,\n  `sales___cost_sum`,\n  `sales___streak`\nFROM cp_value_streak\n) AS _outer",
  "cp/numeric_delta::duckdb": "SELECT\n    \"sales.ordered_at\",\n    \"sales.streak\"\nFROM (\nWITH base AS (\n  SELECT\n    DATE_TRUNC('MONTH', sales.ordered_at) AS \"sales.ordered_at\",\n    SUM(sales.revenue) AS \"sales.revenue_sum\",\n    SUM(sales.cost) AS \"sales.cost_sum\"\n  FROM sales AS sales\n  GROUP BY\n    DATE_TRUNC('MONTH', sales.ordered_at)\n), cp_reset_streak AS (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    \"sales.cost_sum\",\n    SUM(\n      CASE\n        WHEN NOT \"sales.revenue_sum\" - \"sales.cost_sum\" IS NULL\n        AND \"sales.revenue_sum\" - \"sales.cost_sum\" <> 0\n        THEN 0\n        ELSE 1\n      END\n    ) OVER (ORDER BY \"sales.ordered_at\" ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW) AS \"_cp_reset_sales.streak\"\n  FROM base\n), cp_value_streak AS (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    \"sales.cost_sum\",\n    CASE\n      WHEN NOT \"sales.revenue_sum\" - \"sales.cost_sum\" IS NULL\n      AND \"sales.revenue_sum\" - \"sales.cost_sum\" <> 0\n      THEN SUM(\n        CASE\n          WHEN NOT \"sales.revenue_sum\" - \"sales.cost_sum\" IS NULL\n          AND \"sales.revenue_sum\" - \"sales.cost_sum\" <> 0\n          THEN 1\n          ELSE 0\n        END\n      ) OVER (\n        PARTITION BY \"_cp_reset_sales.streak\"\n        ORDER BY \"sales.ordered_at\"\n        ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n      )\n      ELSE 0\n    END AS \"sales.streak\"\n  FROM cp_reset_streak\n)\nSELECT\n  \"sales.ordered_at\",\n  \"sales.revenue_sum\",\n  \"sales.cost_sum\",\n  \"sales.streak\"\nFROM cp_value_streak\n) AS _outer",
  "cp/numeric_delta::postgres": "SELECT\n    \"sales.ordered_at\",\n    \"sales.streak\"\nFROM (\nWITH base AS (\n  SELECT\n    DATE_TRUNC('MONTH', sales.ordered_at) AS \"sales.ordered_at\",\n    SUM(sales.revenue) AS \"sales.revenue_sum\",\n    SUM(sales.cost) AS \"sales.cost_sum\"\n  FROM sales AS sales\n  GROUP BY\n    DATE_TRUNC('MONTH', sales.ordered_at)\n), cp_reset_streak AS (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    \"sales.cost_sum\",\n    SUM(\n      CASE\n        WHEN NOT \"sales.revenue_sum\" - \"sales.cost_sum\" IS NULL\n        AND \"sales.revenue_sum\" - \"sales.cost_sum\" <> 0\n        THEN 0\n        ELSE 1\n      END\n    ) OVER (ORDER BY \"sales.ordered_at\" ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW) AS \"_cp_reset_sales.streak\"\n  FROM base\n), cp_value_streak AS (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    \"sales.cost_sum\",\n    CASE\n      WHEN NOT \"sales.revenue_sum\" - \"sales.cost_sum\" IS NULL\n      AND \"sales.revenue_sum\" - \"sales.cost_sum\" <> 0\n      THEN SUM(\n        CASE\n          WHEN NOT \"sales.revenue_sum\" - \"sales.cost_sum\" IS NULL\n          AND \"sales.revenue_sum\" - \"sales.cost_sum\" <> 0\n          THEN 1\n          ELSE 0\n        END\n      ) OVER (\n        PARTITION BY \"_cp_reset_sales.streak\"\n        ORDER BY \"sales.ordered_at\"\n        ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n      )\n      ELSE 0\n    END AS \"sales.streak\"\n  FROM cp_reset_streak\n)\nSELECT\n  \"sales.ordered_at\",\n  \"sales.revenue_sum\",\n  \"sales.cost_sum\",\n  \"sales.streak\"\nFROM cp_value_streak\n) AS _outer",
  "cp/numeric_delta::sqlite": "SELECT\n    \"sales.ordered_at\",\n    \"sales.streak\"\nFROM (\nWITH base AS (\n  SELECT\n    STRFTIME('%Y-%m-01', sales.ordered_at) AS \"sales.ordered_at\",\n    SUM(sales.revenue) AS \"sales.revenue_sum\",\n    SUM(sales.cost) AS \"sales.cost_sum\"\n  FROM sales AS sales\n  GROUP BY\n    STRFTIME('%Y-%m-01', sales.ordered_at)\n), cp_reset_streak AS (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    \"sales.cost_sum\",\n    SUM(\n      CASE\n        WHEN NOT \"sales.revenue_sum\" - \"sales.cost_sum\" IS NULL\n        AND \"sales.revenue_sum\" - \"sales.cost_sum\" <> 0\n        THEN 0\n        ELSE 1\n      END\n    ) OVER (ORDER BY \"sales.ordered_at\" ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW) AS \"_cp_reset_sales.streak\"\n  FROM base\n), cp_value_streak AS (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    \"sales.cost_sum\",\n    CASE\n      WHEN NOT \"sales.revenue_sum\" - \"sales.cost_sum\" IS NULL\n      AND \"sales.revenue_sum\" - \"sales.cost_sum\" <> 0\n      THEN SUM(\n        CASE\n          WHEN NOT \"sales.revenue_sum\" - \"sales.cost_sum\" IS NULL\n          AND \"sales.revenue_sum\" - \"sales.cost_sum\" <> 0\n          THEN 1\n          ELSE 0\n        END\n      ) OVER (\n        PARTITION BY \"_cp_reset_sales.streak\"\n        ORDER BY \"sales.ordered_at\"\n        ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n      )\n      ELSE 0\n    END AS \"sales.streak\"\n  FROM cp_reset_streak\n)\nSELECT\n  \"sales.ordered_at\",\n  \"sales.revenue_sum\",\n  \"sales.cost_sum\",\n  \"sales.streak\"\nFROM cp_value_streak\n) AS _outer",
  "cp/numeric_delta::tsql": "WITH base AS (\n  SELECT\n    DATETRUNC(MONTH, sales.ordered_at) AS [sales___ordered_at],\n    SUM(sales.revenue) AS [sales___revenue_sum],\n    SUM(sales.cost) AS [sales___cost_sum]\n  FROM sales AS sales\n  GROUP BY\n    DATETRUNC(MONTH, sales.ordered_at)\n), cp_reset_streak AS (\n  SELECT\n    [sales___ordered_at] AS [sales___ordered_at],\n    [sales___revenue_sum] AS [sales___revenue_sum],\n    [sales___cost_sum] AS [sales___cost_sum],\n    SUM(\n      CASE\n        WHEN NOT [sales___revenue_sum] - [sales___cost_sum] IS NULL\n        AND [sales___revenue_sum] - [sales___cost_sum] <> 0\n        THEN 0\n        ELSE 1\n      END\n    ) OVER (ORDER BY [sales___ordered_at] ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW) AS [_cp_reset_sales___streak]\n  FROM base\n), cp_value_streak AS (\n  SELECT\n    [sales___ordered_at] AS [sales___ordered_at],\n    [sales___revenue_sum] AS [sales___revenue_sum],\n    [sales___cost_sum] AS [sales___cost_sum],\n    CASE\n      WHEN NOT [sales___revenue_sum] - [sales___cost_sum] IS NULL\n      AND [sales___revenue_sum] - [sales___cost_sum] <> 0\n      THEN SUM(\n        CASE\n          WHEN NOT [sales___revenue_sum] - [sales___cost_sum] IS NULL\n          AND [sales___revenue_sum] - [sales___cost_sum] <> 0\n          THEN 1\n          ELSE 0\n        END\n      ) OVER (\n        PARTITION BY [_cp_reset_sales___streak]\n        ORDER BY [sales___ordered_at]\n        ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n      )\n      ELSE 0\n    END AS [sales___streak]\n  FROM cp_reset_streak\n)\nSELECT\n  [sales___ordered_at],\n  [sales___streak]\nFROM (\n  SELECT\n    [sales___ordered_at] AS [sales___ordered_at],\n    [sales___revenue_sum] AS [sales___revenue_sum],\n    [sales___cost_sum] AS [sales___cost_sum],\n    [sales___streak] AS [sales___streak]\n  FROM cp_value_streak\n) AS _outer",
  "cp/or::bigquery": "SELECT\n    `sales___ordered_at`,\n    `sales___streak`\nFROM (\nWITH base AS (\n  SELECT\n    DATE_TRUNC(sales.ordered_at, MONTH) AS `sales___ordered_at`,\n    SUM(sales.revenue) AS `sales___revenue_sum`,\n    SUM(sales.cost) AS `sales___cost_sum`\n  FROM sales AS sales\n  GROUP BY\n    DATE_TRUNC(sales.ordered_at, MONTH)\n), cp_reset_streak AS (\n  SELECT\n    `sales___ordered_at`,\n    `sales___revenue_sum`,\n    `sales___cost_sum`,\n    SUM(\n      CASE\n        WHEN COALESCE(`sales___revenue_sum` > 90 OR `sales___cost_sum` > 40, FALSE)\n        THEN 0\n        ELSE 1\n      END\n    ) OVER (ORDER BY `sales___ordered_at` ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW) AS `_cp_reset_sales___streak`\n  FROM base\n), cp_value_streak AS (\n  SELECT\n    `sales___ordered_at`,\n    `sales___revenue_sum`,\n    `sales___cost_sum`,\n    CASE\n      WHEN COALESCE(`sales___revenue_sum` > 90 OR `sales___cost_sum` > 40, FALSE)\n      THEN SUM(\n        CASE\n          WHEN COALESCE(`sales___revenue_sum` > 90 OR `sales___cost_sum` > 40, FALSE)\n          THEN 1\n          ELSE 0\n        END\n      ) OVER (\n        PARTITION BY `_cp_reset_sales___streak`\n        ORDER BY `sales___ordered_at`\n        ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n      )\n      ELSE 0\n    END AS `sales___streak`\n  FROM cp_reset_streak\n)\nSELECT\n  `sales___ordered_at`,\n  `sales___revenue_sum`,\n  `sales___cost_sum`,\n  `sales___streak`\nFROM cp_value_streak\n) AS _outer",
  "cp/or::duckdb": "SELECT\n    \"sales.ordered_at\",\n    \"sales.streak\"\nFROM (\nWITH base AS (\n  SELECT\n    DATE_TRUNC('MONTH', sales.ordered_at) AS \"sales.ordered_at\",\n    SUM(sales.revenue) AS \"sales.revenue_sum\",\n    SUM(sales.cost) AS \"sales.cost_sum\"\n  FROM sales AS sales\n  GROUP BY\n    DATE_TRUNC('MONTH', sales.ordered_at)\n), cp_reset_streak AS (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    \"sales.cost_sum\",\n    SUM(\n      CASE\n        WHEN COALESCE(\"sales.revenue_sum\" > 90 OR \"sales.cost_sum\" > 40, FALSE)\n        THEN 0\n        ELSE 1\n      END\n    ) OVER (ORDER BY \"sales.ordered_at\" ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW) AS \"_cp_reset_sales.streak\"\n  FROM base\n), cp_value_streak AS (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    \"sales.cost_sum\",\n    CASE\n      WHEN COALESCE(\"sales.revenue_sum\" > 90 OR \"sales.cost_sum\" > 40, FALSE)\n      THEN SUM(\n        CASE\n          WHEN COALESCE(\"sales.revenue_sum\" > 90 OR \"sales.cost_sum\" > 40, FALSE)\n          THEN 1\n          ELSE 0\n        END\n      ) OVER (\n        PARTITION BY \"_cp_reset_sales.streak\"\n        ORDER BY \"sales.ordered_at\"\n        ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n      )\n      ELSE 0\n    END AS \"sales.streak\"\n  FROM cp_reset_streak\n)\nSELECT\n  \"sales.ordered_at\",\n  \"sales.revenue_sum\",\n  \"sales.cost_sum\",\n  \"sales.streak\"\nFROM cp_value_streak\n) AS _outer",
  "cp/or::postgres": "SELECT\n    \"sales.ordered_at\",\n    \"sales.streak\"\nFROM (\nWITH base AS (\n  SELECT\n    DATE_TRUNC('MONTH', sales.ordered_at) AS \"sales.ordered_at\",\n    SUM(sales.revenue) AS \"sales.revenue_sum\",\n    SUM(sales.cost) AS \"sales.cost_sum\"\n  FROM sales AS sales\n  GROUP BY\n    DATE_TRUNC('MONTH', sales.ordered_at)\n), cp_reset_streak AS (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    \"sales.cost_sum\",\n    SUM(\n      CASE\n        WHEN COALESCE(\"sales.revenue_sum\" > 90 OR \"sales.cost_sum\" > 40, FALSE)\n        THEN 0\n        ELSE 1\n      END\n    ) OVER (ORDER BY \"sales.ordered_at\" ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW) AS \"_cp_reset_sales.streak\"\n  FROM base\n), cp_value_streak AS (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    \"sales.cost_sum\",\n    CASE\n      WHEN COALESCE(\"sales.revenue_sum\" > 90 OR \"sales.cost_sum\" > 40, FALSE)\n      THEN SUM(\n        CASE\n          WHEN COALESCE(\"sales.revenue_sum\" > 90 OR \"sales.cost_sum\" > 40, FALSE)\n          THEN 1\n          ELSE 0\n        END\n      ) OVER (\n        PARTITION BY \"_cp_reset_sales.streak\"\n        ORDER BY \"sales.ordered_at\"\n        ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n      )\n      ELSE 0\n    END AS \"sales.streak\"\n  FROM cp_reset_streak\n)\nSELECT\n  \"sales.ordered_at\",\n  \"sales.revenue_sum\",\n  \"sales.cost_sum\",\n  \"sales.streak\"\nFROM cp_value_streak\n) AS _outer",
  "cp/or::sqlite": "SELECT\n    \"sales.ordered_at\",\n    \"sales.streak\"\nFROM (\nWITH base AS (\n  SELECT\n    STRFTIME('%Y-%m-01', sales.ordered_at) AS \"sales.ordered_at\",\n    SUM(sales.revenue) AS \"sales.revenue_sum\",\n    SUM(sales.cost) AS \"sales.cost_sum\"\n  FROM sales AS sales\n  GROUP BY\n    STRFTIME('%Y-%m-01', sales.ordered_at)\n), cp_reset_streak AS (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    \"sales.cost_sum\",\n    SUM(\n      CASE\n        WHEN COALESCE(\"sales.revenue_sum\" > 90 OR \"sales.cost_sum\" > 40, FALSE)\n        THEN 0\n        ELSE 1\n      END\n    ) OVER (ORDER BY \"sales.ordered_at\" ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW) AS \"_cp_reset_sales.streak\"\n  FROM base\n), cp_value_streak AS (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    \"sales.cost_sum\",\n    CASE\n      WHEN COALESCE(\"sales.revenue_sum\" > 90 OR \"sales.cost_sum\" > 40, FALSE)\n      THEN SUM(\n        CASE\n          WHEN COALESCE(\"sales.revenue_sum\" > 90 OR \"sales.cost_sum\" > 40, FALSE)\n          THEN 1\n          ELSE 0\n        END\n      ) OVER (\n        PARTITION BY \"_cp_reset_sales.streak\"\n        ORDER BY \"sales.ordered_at\"\n        ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n      )\n      ELSE 0\n    END AS \"sales.streak\"\n  FROM cp_reset_streak\n)\nSELECT\n  \"sales.ordered_at\",\n  \"sales.revenue_sum\",\n  \"sales.cost_sum\",\n  \"sales.streak\"\nFROM cp_value_streak\n) AS _outer",
  "cp/or::tsql": "WITH base AS (\n  SELECT\n    DATETRUNC(MONTH, sales.ordered_at) AS [sales___ordered_at],\n    SUM(sales.revenue) AS [sales___revenue_sum],\n    SUM(sales.cost) AS [sales___cost_sum]\n  FROM sales AS sales\n  GROUP BY\n    DATETRUNC(MONTH, sales.ordered_at)\n), cp_reset_streak AS (\n  SELECT\n    [sales___ordered_at] AS [sales___ordered_at],\n    [sales___revenue_sum] AS [sales___revenue_sum],\n    [sales___cost_sum] AS [sales___cost_sum],\n    SUM(\n      CASE\n        WHEN COALESCE([sales___revenue_sum] > 90 OR [sales___cost_sum] > 40, (\n          1 = 0\n        ))\n        THEN 0\n        ELSE 1\n      END\n    ) OVER (ORDER BY [sales___ordered_at] ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW) AS [_cp_reset_sales___streak]\n  FROM base\n), cp_value_streak AS (\n  SELECT\n    [sales___ordered_at] AS [sales___ordered_at],\n    [sales___revenue_sum] AS [sales___revenue_sum],\n    [sales___cost_sum] AS [sales___cost_sum],\n    CASE\n      WHEN COALESCE([sales___revenue_sum] > 90 OR [sales___cost_sum] > 40, (\n        1 = 0\n      ))\n      THEN SUM(\n        CASE\n          WHEN COALESCE([sales___revenue_sum] > 90 OR [sales___cost_sum] > 40, (\n            1 = 0\n          ))\n          THEN 1\n          ELSE 0\n        END\n      ) OVER (\n        PARTITION BY [_cp_reset_sales___streak]\n        ORDER BY [sales___ordered_at]\n        ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n      )\n      ELSE 0\n    END AS [sales___streak]\n  FROM cp_reset_streak\n)\nSELECT\n  [sales___ordered_at],\n  [sales___streak]\nFROM (\n  SELECT\n    [sales___ordered_at] AS [sales___ordered_at],\n    [sales___revenue_sum] AS [sales___revenue_sum],\n    [sales___cost_sum] AS [sales___cost_sum],\n    [sales___streak] AS [sales___streak]\n  FROM cp_value_streak\n) AS _outer",
  "reject/cp_boolean_numeric::bigquery": {
    "error": "ValueError",
    "message": "'consecutive_periods': a boolean-shaped predicate cannot appear in a value position (arithmetic operand, scalar-call argument, or IN / BETWEEN operand); only iif's condition and the top-level predicate accept a boolean. Got ArithmeticKey."
  },
  "reject/cp_boolean_numeric::duckdb": {
    "error": "ValueError",
    "message": "'consecutive_periods': a boolean-shaped predicate cannot appear in a value position (arithmetic operand, scalar-call argument, or IN / BETWEEN operand); only iif's condition and the top-level predicate accept a boolean. Got ArithmeticKey."
  },
  "reject/cp_boolean_numeric::postgres": {
    "error": "ValueError",
    "message": "'consecutive_periods': a boolean-shaped predicate cannot appear in a value position (arithmetic operand, scalar-call argument, or IN / BETWEEN operand); only iif's condition and the top-level predicate accept a boolean. Got ArithmeticKey."
  },
  "reject/cp_boolean_numeric::sqlite": {
    "error": "ValueError",
    "message": "'consecutive_periods': a boolean-shaped predicate cannot appear in a value position (arithmetic operand, scalar-call argument, or IN / BETWEEN operand); only iif's condition and the top-level predicate accept a boolean. Got ArithmeticKey."
  },
  "reject/cp_boolean_numeric::tsql": {
    "error": "ValueError",
    "message": "'consecutive_periods': a boolean-shaped predicate cannot appear in a value position (arithmetic operand, scalar-call argument, or IN / BETWEEN operand); only iif's condition and the top-level predicate accept a boolean. Got ArithmeticKey."
  },
  "reject/cp_string_family::bigquery": {
    "error": "ValueError",
    "message": "'consecutive_periods' cannot use a string-valued predicate (lower(...)): a string has no truthiness. Compare it explicitly (e.g. `length(...) > 0`) to form a predicate."
  },
  "reject/cp_string_family::duckdb": {
    "error": "ValueError",
    "message": "'consecutive_periods' cannot use a string-valued predicate (lower(...)): a string has no truthiness. Compare it explicitly (e.g. `length(...) > 0`) to form a predicate."
  },
  "reject/cp_string_family::postgres": {
    "error": "ValueError",
    "message": "'consecutive_periods' cannot use a string-valued predicate (lower(...)): a string has no truthiness. Compare it explicitly (e.g. `length(...) > 0`) to form a predicate."
  },
  "reject/cp_string_family::sqlite": {
    "error": "ValueError",
    "message": "'consecutive_periods' cannot use a string-valued predicate (lower(...)): a string has no truthiness. Compare it explicitly (e.g. `length(...) > 0`) to form a predicate."
  },
  "reject/cp_string_family::tsql": {
    "error": "ValueError",
    "message": "'consecutive_periods' cannot use a string-valued predicate (lower(...)): a string has no truthiness. Compare it explicitly (e.g. `length(...) > 0`) to form a predicate."
  },
  "reject/ts_cross_model_leaf::bigquery": {
    "error": "ValueError",
    "message": "'time_shift' does not support a cross-model aggregate leaf inside a composite input; compute it in an earlier stage of a multi-stage `source_queries` model and reference its output here."
  },
  "reject/ts_cross_model_leaf::duckdb": {
    "error": "ValueError",
    "message": "'time_shift' does not support a cross-model aggregate leaf inside a composite input; compute it in an earlier stage of a multi-stage `source_queries` model and reference its output here."
  },
  "reject/ts_cross_model_leaf::postgres": {
    "error": "ValueError",
    "message": "'time_shift' does not support a cross-model aggregate leaf inside a composite input; compute it in an earlier stage of a multi-stage `source_queries` model and reference its output here."
  },
  "reject/ts_cross_model_leaf::sqlite": {
    "error": "ValueError",
    "message": "'time_shift' does not support a cross-model aggregate leaf inside a composite input; compute it in an earlier stage of a multi-stage `source_queries` model and reference its output here."
  },
  "reject/ts_cross_model_leaf::tsql": {
    "error": "ValueError",
    "message": "'time_shift' does not support a cross-model aggregate leaf inside a composite input; compute it in an earlier stage of a multi-stage `source_queries` model and reference its output here."
  },
  "reject/ts_nested_transform::bigquery": {
    "error": "ValueError",
    "message": "Nesting a transform inside 'time_shift' is not supported. Compute the inner transform in an earlier stage of a multi-stage `source_queries` model and reference its output in this stage."
  },
  "reject/ts_nested_transform::duckdb": {
    "error": "ValueError",
    "message": "Nesting a transform inside 'time_shift' is not supported. Compute the inner transform in an earlier stage of a multi-stage `source_queries` model and reference its output in this stage."
  },
  "reject/ts_nested_transform::postgres": {
    "error": "ValueError",
    "message": "Nesting a transform inside 'time_shift' is not supported. Compute the inner transform in an earlier stage of a multi-stage `source_queries` model and reference its output in this stage."
  },
  "reject/ts_nested_transform::sqlite": {
    "error": "ValueError",
    "message": "Nesting a transform inside 'time_shift' is not supported. Compute the inner transform in an earlier stage of a multi-stage `source_queries` model and reference its output in this stage."
  },
  "reject/ts_nested_transform::tsql": {
    "error": "ValueError",
    "message": "Nesting a transform inside 'time_shift' is not supported. Compute the inner transform in an earlier stage of a multi-stage `source_queries` model and reference its output in this stage."
  },
  "ts/change_pct_ratio::bigquery": "SELECT\n    `sales___store`,\n    `sales___ordered_at`,\n    `sales___pct`\nFROM (\nWITH base AS (\n  SELECT\n    sales.store AS `sales___store`,\n    DATE_TRUNC(sales.ordered_at, MONTH) AS `sales___ordered_at`,\n    SUM(sales.revenue) AS `sales___revenue_sum`,\n    COUNT(*) AS `sales____count`\n  FROM sales AS sales\n  GROUP BY\n    sales.store,\n    DATE_TRUNC(sales.ordered_at, MONTH)\n), shifted__time_shift_inner AS (\n  SELECT\n    DATE_TRUNC(sales.ordered_at, MONTH) + INTERVAL 1 MONTH AS `sales___ordered_at`,\n    sales.store AS `sales___store`,\n    SUM(sales.revenue) / COUNT(*) AS `_time_shift_inner__ts`\n  FROM sales AS sales\n  GROUP BY\n    DATE_TRUNC(sales.ordered_at, MONTH) + INTERVAL 1 MONTH,\n    sales.store\n), sjoin__time_shift_inner AS (\n  SELECT\n    base.`sales___store`,\n    base.`sales___ordered_at`,\n    base.`sales___revenue_sum`,\n    base.`sales____count`,\n    shifted__time_shift_inner.`_time_shift_inner__ts` AS `sales____time_shift_inner`\n  FROM base\n  LEFT JOIN shifted__time_shift_inner\n    ON base.`sales___ordered_at` IS NOT DISTINCT FROM shifted__time_shift_inner.`sales___ordered_at`\n    AND base.`sales___store` IS NOT DISTINCT FROM shifted__time_shift_inner.`sales___store`\n), step1 AS (\n  SELECT\n    `sales___store`,\n    `sales___ordered_at`,\n    `sales___revenue_sum`,\n    `sales____count`,\n    `sales____time_shift_inner`,\n    (\n      `sales___revenue_sum` / `sales____count` - `sales____time_shift_inner`\n    ) / NULLIF(`sales____time_shift_inner`, 0) AS `sales___pct`\n  FROM sjoin__time_shift_inner\n)\nSELECT\n  `sales___store`,\n  `sales___ordered_at`,\n  `sales___revenue_sum`,\n  `sales____count`,\n  `sales____time_shift_inner`,\n  `sales___pct`\nFROM step1\n) AS _outer",
  "ts/change_pct_ratio::duckdb": "SELECT\n    \"sales.store\",\n    \"sales.ordered_at\",\n    \"sales.pct\"\nFROM (\nWITH base AS (\n  SELECT\n    sales.store AS \"sales.store\",\n    DATE_TRUNC('MONTH', sales.ordered_at) AS \"sales.ordered_at\",\n    SUM(sales.revenue) AS \"sales.revenue_sum\",\n    COUNT(*) AS \"sales._count\"\n  FROM sales AS sales\n  GROUP BY\n    sales.store,\n    DATE_TRUNC('MONTH', sales.ordered_at)\n), shifted__time_shift_inner AS (\n  SELECT\n    DATE_TRUNC('MONTH', sales.ordered_at) + INTERVAL 1 MONTH AS \"sales.ordered_at\",\n    sales.store AS \"sales.store\",\n    SUM(sales.revenue) / COUNT(*) AS \"_time_shift_inner__ts\"\n  FROM sales AS sales\n  GROUP BY\n    DATE_TRUNC('MONTH', sales.ordered_at) + INTERVAL 1 MONTH,\n    sales.store\n), sjoin__time_shift_inner AS (\n  SELECT\n    base.\"sales.store\",\n    base.\"sales.ordered_at\",\n    base.\"sales.revenue_sum\",\n    base.\"sales._count\",\n    shifted__time_shift_inner.\"_time_shift_inner__ts\" AS \"sales._time_shift_inner\"\n  FROM base\n  LEFT JOIN shifted__time_shift_inner\n    ON base.\"sales.ordered_at\" IS NOT DISTINCT FROM shifted__time_shift_inner.\"sales.ordered_at\"\n    AND base.\"sales.store\" IS NOT DISTINCT FROM shifted__time_shift_inner.\"sales.store\"\n), step1 AS (\n  SELECT\n    \"sales.store\",\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    \"sales._count\",\n    \"sales._time_shift_inner\",\n    (\n      \"sales.revenue_sum\" / \"sales._count\" - \"sales._time_shift_inner\"\n    ) / NULLIF(\"sales._time_shift_inner\", 0) AS \"sales.pct\"\n  FROM sjoin__time_shift_inner\n)\nSELECT\n  \"sales.store\",\n  \"sales.ordered_at\",\n  \"sales.revenue_sum\",\n  \"sales._count\",\n  \"sales._time_shift_inner\",\n  \"sales.pct\"\nFROM step1\n) AS _outer",
  "ts/change_pct_ratio::postgres": "SELECT\n    \"sales.store\",\n    \"sales.ordered_at\",\n    \"sales.pct\"\nFROM (\nWITH base AS (\n  SELECT\n    sales.store AS \"sales.store\",\n    DATE_TRUNC('MONTH', sales.ordered_at) AS \"sales.ordered_at\",\n    SUM(sales.revenue) AS \"sales.revenue_sum\",\n    COUNT(*) AS \"sales._count\"\n  FROM sales AS sales\n  GROUP BY\n    sales.store,\n    DATE_TRUNC('MONTH', sales.ordered_at)\n), shifted__time_shift_inner AS (\n  SELECT\n    DATE_TRUNC('MONTH', sales.ordered_at) + INTERVAL '1 MONTH' AS \"sales.ordered_at\",\n    sales.store AS \"sales.store\",\n    CAST(SUM(sales.revenue) AS DOUBLE PRECISION) / COUNT(*) AS \"_time_shift_inner__ts\"\n  FROM sales AS sales\n  GROUP BY\n    DATE_TRUNC('MONTH', sales.ordered_at) + INTERVAL '1 MONTH',\n    sales.store\n), sjoin__time_shift_inner AS (\n  SELECT\n    base.\"sales.store\",\n    base.\"sales.ordered_at\",\n    base.\"sales.revenue_sum\",\n    base.\"sales._count\",\n    shifted__time_shift_inner.\"_time_shift_inner__ts\" AS \"sales._time_shift_inner\"\n  FROM base\n  LEFT JOIN shifted__time_shift_inner\n    ON base.\"sales.ordered_at\" IS NOT DISTINCT FROM shifted__time_shift_inner.\"sales.ordered_at\"\n    AND base.\"sales.store\" IS NOT DISTINCT FROM shifted__time_shift_inner.\"sales.store\"\n), step1 AS (\n  SELECT\n    \"sales.store\",\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    \"sales._count\",\n    \"sales._time_shift_inner\",\n    CAST((\n      CAST(\"sales.revenue_sum\" AS DOUBLE PRECISION) / \"sales._count\" - \"sales._time_shift_inner\"\n    ) AS DOUBLE PRECISION) / NULLIF(\"sales._time_shift_inner\", 0) AS \"sales.pct\"\n  FROM sjoin__time_shift_inner\n)\nSELECT\n  \"sales.store\",\n  \"sales.ordered_at\",\n  \"sales.revenue_sum\",\n  \"sales._count\",\n  \"sales._time_shift_inner\",\n  \"sales.pct\"\nFROM step1\n) AS _outer",
  "ts/change_pct_ratio::sqlite": "SELECT\n    \"sales.store\",\n    \"sales.ordered_at\",\n    \"sales.pct\"\nFROM (\nWITH base AS (\n  SELECT\n    sales.store AS \"sales.store\",\n    STRFTIME('%Y-%m-01', sales.ordered_at) AS \"sales.ordered_at\",\n    SUM(sales.revenue) AS \"sales.revenue_sum\",\n    COUNT(*) AS \"sales._count\"\n  FROM sales AS sales\n  GROUP BY\n    sales.store,\n    STRFTIME('%Y-%m-01', sales.ordered_at)\n), shifted__time_shift_inner AS (\n  SELECT\n    DATE(STRFTIME('%Y-%m-01', sales.ordered_at), '1 months') AS \"sales.ordered_at\",\n    sales.store AS \"sales.store\",\n    CAST(SUM(sales.revenue) AS REAL) / COUNT(*) AS \"_time_shift_inner__ts\"\n  FROM sales AS sales\n  GROUP BY\n    DATE(STRFTIME('%Y-%m-01', sales.ordered_at), '1 months'),\n    sales.store\n), sjoin__time_shift_inner AS (\n  SELECT\n    base.\"sales.store\",\n    base.\"sales.ordered_at\",\n    base.\"sales.revenue_sum\",\n    base.\"sales._count\",\n    shifted__time_shift_inner.\"_time_shift_inner__ts\" AS \"sales._time_shift_inner\"\n  FROM base\n  LEFT JOIN shifted__time_shift_inner\n    ON base.\"sales.ordered_at\" IS shifted__time_shift_inner.\"sales.ordered_at\"\n    AND base.\"sales.store\" IS shifted__time_shift_inner.\"sales.store\"\n), step1 AS (\n  SELECT\n    \"sales.store\",\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    \"sales._count\",\n    \"sales._time_shift_inner\",\n    CAST((\n      CAST(\"sales.revenue_sum\" AS REAL) / \"sales._count\" - \"sales._time_shift_inner\"\n    ) AS REAL) / NULLIF(\"sales._time_shift_inner\", 0) AS \"sales.pct\"\n  FROM sjoin__time_shift_inner\n)\nSELECT\n  \"sales.store\",\n  \"sales.ordered_at\",\n  \"sales.revenue_sum\",\n  \"sales._count\",\n  \"sales._time_shift_inner\",\n  \"sales.pct\"\nFROM step1\n) AS _outer",
  "ts/change_pct_ratio::tsql": "WITH base AS (\n  SELECT\n    sales.store AS [sales___store],\n    DATETRUNC(MONTH, sales.ordered_at) AS [sales___ordered_at],\n    SUM(sales.revenue) AS [sales___revenue_sum],\n    COUNT(*) AS [sales____count]\n  FROM sales AS sales\n  GROUP BY\n    sales.store,\n    DATETRUNC(MONTH, sales.ordered_at)\n), shifted__time_shift_inner AS (\n  SELECT\n    DATEADD(MONTH, 1, DATETRUNC(MONTH, sales.ordered_at)) AS [sales___ordered_at],\n    sales.store AS [sales___store],\n    CAST(SUM(sales.revenue) AS FLOAT) / COUNT(*) AS [_time_shift_inner__ts]\n  FROM sales AS sales\n  GROUP BY\n    DATEADD(MONTH, 1, DATETRUNC(MONTH, sales.ordered_at)),\n    sales.store\n), sjoin__time_shift_inner AS (\n  SELECT\n    base.[sales___store] AS [sales___store],\n    base.[sales___ordered_at] AS [sales___ordered_at],\n    base.[sales___revenue_sum] AS [sales___revenue_sum],\n    base.[sales____count] AS [sales____count],\n    shifted__time_shift_inner.[_time_shift_inner__ts] AS [sales____time_shift_inner]\n  FROM base\n  LEFT JOIN shifted__time_shift_inner\n    ON (\n      base.[sales___ordered_at] = shifted__time_shift_inner.[sales___ordered_at]\n      OR (\n        base.[sales___ordered_at] IS NULL\n        AND shifted__time_shift_inner.[sales___ordered_at] IS NULL\n      )\n    )\n    AND (\n      base.[sales___store] = shifted__time_shift_inner.[sales___store]\n      OR (\n        base.[sales___store] IS NULL AND shifted__time_shift_inner.[sales___store] IS NULL\n      )\n    )\n), step1 AS (\n  SELECT\n    [sales___store] AS [sales___store],\n    [sales___ordered_at] AS [sales___ordered_at],\n    [sales___revenue_sum] AS [sales___revenue_sum],\n    [sales____count] AS [sales____count],\n    [sales____time_shift_inner] AS [sales____time_shift_inner],\n    CAST((\n      CAST([sales___revenue_sum] AS FLOAT) / [sales____count] - [sales____time_shift_inner]\n    ) AS FLOAT) / NULLIF([sales____time_shift_inner], 0) AS [sales___pct]\n  FROM sjoin__time_shift_inner\n)\nSELECT\n  [sales___store],\n  [sales___ordered_at],\n  [sales___pct]\nFROM (\n  SELECT\n    [sales___store] AS [sales___store],\n    [sales___ordered_at] AS [sales___ordered_at],\n    [sales___revenue_sum] AS [sales___revenue_sum],\n    [sales____count] AS [sales____count],\n    [sales____time_shift_inner] AS [sales____time_shift_inner],\n    [sales___pct] AS [sales___pct]\n  FROM step1\n) AS _outer",
  "ts/coalesce_missing::bigquery": "SELECT\n    `sales___ordered_at`,\n    `sales___prev`\nFROM (\nWITH base AS (\n  SELECT\n    DATE_TRUNC(sales.ordered_at, MONTH) AS `sales___ordered_at`,\n    SUM(sales.revenue) AS `sales___revenue_sum`\n  FROM sales AS sales\n  GROUP BY\n    DATE_TRUNC(sales.ordered_at, MONTH)\n), shifted_prev AS (\n  SELECT\n    DATE_TRUNC(sales.ordered_at, MONTH) + INTERVAL 1 MONTH AS `sales___ordered_at`,\n    COALESCE(SUM(sales.revenue), 0) AS `prev__ts`\n  FROM sales AS sales\n  GROUP BY\n    DATE_TRUNC(sales.ordered_at, MONTH) + INTERVAL 1 MONTH\n), sjoin_prev AS (\n  SELECT\n    base.`sales___ordered_at`,\n    base.`sales___revenue_sum`,\n    shifted_prev.`prev__ts` AS `sales___prev`\n  FROM base\n  LEFT JOIN shifted_prev\n    ON base.`sales___ordered_at` IS NOT DISTINCT FROM shifted_prev.`sales___ordered_at`\n)\nSELECT\n  `sales___ordered_at`,\n  `sales___revenue_sum`,\n  `sales___prev`\nFROM sjoin_prev\n) AS _outer",
  "ts/coalesce_missing::duckdb": "SELECT\n    \"sales.ordered_at\",\n    \"sales.prev\"\nFROM (\nWITH base AS (\n  SELECT\n    DATE_TRUNC('MONTH', sales.ordered_at) AS \"sales.ordered_at\",\n    SUM(sales.revenue) AS \"sales.revenue_sum\"\n  FROM sales AS sales\n  GROUP BY\n    DATE_TRUNC('MONTH', sales.ordered_at)\n), shifted_prev AS (\n  SELECT\n    DATE_TRUNC('MONTH', sales.ordered_at) + INTERVAL 1 MONTH AS \"sales.ordered_at\",\n    COALESCE(SUM(sales.revenue), 0) AS \"prev__ts\"\n  FROM sales AS sales\n  GROUP BY\n    DATE_TRUNC('MONTH', sales.ordered_at) + INTERVAL 1 MONTH\n), sjoin_prev AS (\n  SELECT\n    base.\"sales.ordered_at\",\n    base.\"sales.revenue_sum\",\n    shifted_prev.\"prev__ts\" AS \"sales.prev\"\n  FROM base\n  LEFT JOIN shifted_prev\n    ON base.\"sales.ordered_at\" IS NOT DISTINCT FROM shifted_prev.\"sales.ordered_at\"\n)\nSELECT\n  \"sales.ordered_at\",\n  \"sales.revenue_sum\",\n  \"sales.prev\"\nFROM sjoin_prev\n) AS _outer",
  "ts/coalesce_missing::postgres": "SELECT\n    \"sales.ordered_at\",\n    \"sales.prev\"\nFROM (\nWITH base AS (\n  SELECT\n    DATE_TRUNC('MONTH', sales.ordered_at) AS \"sales.ordered_at\",\n    SUM(sales.revenue) AS \"sales.revenue_sum\"\n  FROM sales AS sales\n  GROUP BY\n    DATE_TRUNC('MONTH', sales.ordered_at)\n), shifted_prev AS (\n  SELECT\n    DATE_TRUNC('MONTH', sales.ordered_at) + INTERVAL '1 MONTH' AS \"sales.ordered_at\",\n    COALESCE(SUM(sales.revenue), 0) AS \"prev__ts\"\n  FROM sales AS sales\n  GROUP BY\n    DATE_TRUNC('MONTH', sales.ordered_at) + INTERVAL '1 MONTH'\n), sjoin_prev AS (\n  SELECT\n    base.\"sales.ordered_at\",\n    base.\"sales.revenue_sum\",\n    shifted_prev.\"prev__ts\" AS \"sales.prev\"\n  FROM base\n  LEFT JOIN shifted_prev\n    ON base.\"sales.ordered_at\" IS NOT DISTINCT FROM shifted_prev.\"sales.ordered_at\"\n)\nSELECT\n  \"sales.ordered_at\",\n  \"sales.revenue_sum\",\n  \"sales.prev\"\nFROM sjoin_prev\n) AS _outer",
  "ts/coalesce_missing::sqlite": "SELECT\n    \"sales.ordered_at\",\n    \"sales.prev\"\nFROM (\nWITH base AS (\n  SELECT\n    STRFTIME('%Y-%m-01', sales.ordered_at) AS \"sales.ordered_at\",\n    SUM(sales.revenue) AS \"sales.revenue_sum\"\n  FROM sales AS sales\n  GROUP BY\n    STRFTIME('%Y-%m-01', sales.ordered_at)\n), shifted_prev AS (\n  SELECT\n    DATE(STRFTIME('%Y-%m-01', sales.ordered_at), '1 months') AS \"sales.ordered_at\",\n    COALESCE(SUM(sales.revenue), 0) AS \"prev__ts\"\n  FROM sales AS sales\n  GROUP BY\n    DATE(STRFTIME('%Y-%m-01', sales.ordered_at), '1 months')\n), sjoin_prev AS (\n  SELECT\n    base.\"sales.ordered_at\",\n    base.\"sales.revenue_sum\",\n    shifted_prev.\"prev__ts\" AS \"sales.prev\"\n  FROM base\n  LEFT JOIN shifted_prev\n    ON base.\"sales.ordered_at\" IS shifted_prev.\"sales.ordered_at\"\n)\nSELECT\n  \"sales.ordered_at\",\n  \"sales.revenue_sum\",\n  \"sales.prev\"\nFROM sjoin_prev\n) AS _outer",
  "ts/coalesce_missing::tsql": "WITH base AS (\n  SELECT\n    DATETRUNC(MONTH, sales.ordered_at) AS [sales___ordered_at],\n    SUM(sales.revenue) AS [sales___revenue_sum]\n  FROM sales AS sales\n  GROUP BY\n    DATETRUNC(MONTH, sales.ordered_at)\n), shifted_prev AS (\n  SELECT\n    DATEADD(MONTH, 1, DATETRUNC(MONTH, sales.ordered_at)) AS [sales___ordered_at],\n    COALESCE(SUM(sales.revenue), 0) AS [prev__ts]\n  FROM sales AS sales\n  GROUP BY\n    DATEADD(MONTH, 1, DATETRUNC(MONTH, sales.ordered_at))\n), sjoin_prev AS (\n  SELECT\n    base.[sales___ordered_at] AS [sales___ordered_at],\n    base.[sales___revenue_sum] AS [sales___revenue_sum],\n    shifted_prev.[prev__ts] AS [sales___prev]\n  FROM base\n  LEFT JOIN shifted_prev\n    ON (\n      base.[sales___ordered_at] = shifted_prev.[sales___ordered_at]\n      OR (\n        base.[sales___ordered_at] IS NULL AND shifted_prev.[sales___ordered_at] IS NULL\n      )\n    )\n)\nSELECT\n  [sales___ordered_at],\n  [sales___prev]\nFROM (\n  SELECT\n    [sales___ordered_at] AS [sales___ordered_at],\n    [sales___revenue_sum] AS [sales___revenue_sum],\n    [sales___prev] AS [sales___prev]\n  FROM sjoin_prev\n) AS _outer",
  "ts/crossing_join::bigquery": "SELECT\n    `sales___ordered_at`,\n    `sales___prev`\nFROM (\nWITH _base AS (\n  SELECT\n    DATE_TRUNC(sales.ordered_at, MONTH) AS `sales___ordered_at`\n  FROM sales AS sales\n  GROUP BY\n    DATE_TRUNC(sales.ordered_at, MONTH)\n), _cm_sales__revenue_wrevenue_sum AS (\n  SELECT\n    DATE_TRUNC(sales.ordered_at, MONTH) AS `sales___ordered_at`,\n    CAST(SUM(sales.revenue * regions.factor) AS FLOAT64) AS `sales___revenue_wrevenue_sum`\n  FROM sales AS sales\n  LEFT JOIN regions AS regions\n    ON sales.region_id = regions.id\n  GROUP BY\n    DATE_TRUNC(sales.ordered_at, MONTH)\n), base AS (\n  SELECT\n    _base.`sales___ordered_at`,\n    _cm_sales__revenue_wrevenue_sum.`sales___revenue_wrevenue_sum` AS `sales___revenue_wrevenue_sum`\n  FROM _base\n  LEFT JOIN _cm_sales__revenue_wrevenue_sum\n    ON _base.`sales___ordered_at` IS NOT DISTINCT FROM _cm_sales__revenue_wrevenue_sum.`sales___ordered_at`\n), shifted_prev AS (\n  SELECT\n    DATE_TRUNC(sales.ordered_at, MONTH) + INTERVAL 1 MONTH AS `sales___ordered_at`,\n    CAST(SUM(sales.revenue * regions.factor) AS FLOAT64) * 2 AS `prev__ts`\n  FROM sales AS sales\n  LEFT JOIN regions AS regions\n    ON sales.region_id = regions.id\n  GROUP BY\n    DATE_TRUNC(sales.ordered_at, MONTH) + INTERVAL 1 MONTH\n), sjoin_prev AS (\n  SELECT\n    base.`sales___ordered_at`,\n    base.`sales___revenue_wrevenue_sum`,\n    shifted_prev.`prev__ts` AS `sales___prev`\n  FROM base\n  LEFT JOIN shifted_prev\n    ON base.`sales___ordered_at` IS NOT DISTINCT FROM shifted_prev.`sales___ordered_at`\n)\nSELECT\n  `sales___ordered_at`,\n  `sales___revenue_wrevenue_sum`,\n  `sales___prev`\nFROM sjoin_prev\n) AS _outer",
  "ts/crossing_join::duckdb": "SELECT\n    \"sales.ordered_at\",\n    \"sales.prev\"\nFROM (\nWITH _base AS (\n  SELECT\n    DATE_TRUNC('MONTH', sales.ordered_at) AS \"sales.ordered_at\"\n  FROM sales AS sales\n  GROUP BY\n    DATE_TRUNC('MONTH', sales.ordered_at)\n), _cm_sales__revenue_wrevenue_sum AS (\n  SELECT\n    DATE_TRUNC('MONTH', sales.ordered_at) AS \"sales.ordered_at\",\n    CAST(SUM(sales.revenue * regions.factor) AS DOUBLE) AS \"sales.revenue_wrevenue_sum\"\n  FROM sales AS sales\n  LEFT JOIN regions AS regions\n    ON sales.region_id = regions.id\n  GROUP BY\n    DATE_TRUNC('MONTH', sales.ordered_at)\n), base AS (\n  SELECT\n    _base.\"sales.ordered_at\",\n    _cm_sales__revenue_wrevenue_sum.\"sales.revenue_wrevenue_sum\" AS \"sales.revenue_wrevenue_sum\"\n  FROM _base\n  LEFT JOIN _cm_sales__revenue_wrevenue_sum\n    ON _base.\"sales.ordered_at\" IS NOT DISTINCT FROM _cm_sales__revenue_wrevenue_sum.\"sales.ordered_at\"\n), shifted_prev AS (\n  SELECT\n    DATE_TRUNC('MONTH', sales.ordered_at) + INTERVAL 1 MONTH AS \"sales.ordered_at\",\n    CAST(SUM(sales.revenue * regions.factor) AS DOUBLE) * 2 AS \"prev__ts\"\n  FROM sales AS sales\n  LEFT JOIN regions AS regions\n    ON sales.region_id = regions.id\n  GROUP BY\n    DATE_TRUNC('MONTH', sales.ordered_at) + INTERVAL 1 MONTH\n), sjoin_prev AS (\n  SELECT\n    base.\"sales.ordered_at\",\n    base.\"sales.revenue_wrevenue_sum\",\n    shifted_prev.\"prev__ts\" AS \"sales.prev\"\n  FROM base\n  LEFT JOIN shifted_prev\n    ON base.\"sales.ordered_at\" IS NOT DISTINCT FROM shifted_prev.\"sales.ordered_at\"\n)\nSELECT\n  \"sales.ordered_at\",\n  \"sales.revenue_wrevenue_sum\",\n  \"sales.prev\"\nFROM sjoin_prev\n) AS _outer",
  "ts/crossing_join::postgres": "SELECT\n    \"sales.ordered_at\",\n    \"sales.prev\"\nFROM (\nWITH _base AS (\n  SELECT\n    DATE_TRUNC('MONTH', sales.ordered_at) AS \"sales.ordered_at\"\n  FROM sales AS sales\n  GROUP BY\n    DATE_TRUNC('MONTH', sales.ordered_at)\n), _cm_sales__revenue_wrevenue_sum AS (\n  SELECT\n    DATE_TRUNC('MONTH', sales.ordered_at) AS \"sales.ordered_at\",\n    CAST(SUM(sales.revenue * regions.factor) AS DOUBLE PRECISION) AS \"sales.revenue_wrevenue_sum\"\n  FROM sales AS sales\n  LEFT JOIN regions AS regions\n    ON sales.region_id = regions.id\n  GROUP BY\n    DATE_TRUNC('MONTH', sales.ordered_at)\n), base AS (\n  SELECT\n    _base.\"sales.ordered_at\",\n    _cm_sales__revenue_wrevenue_sum.\"sales.revenue_wrevenue_sum\" AS \"sales.revenue_wrevenue_sum\"\n  FROM _base\n  LEFT JOIN _cm_sales__revenue_wrevenue_sum\n    ON _base.\"sales.ordered_at\" IS NOT DISTINCT FROM _cm_sales__revenue_wrevenue_sum.\"sales.ordered_at\"\n), shifted_prev AS (\n  SELECT\n    DATE_TRUNC('MONTH', sales.ordered_at) + INTERVAL '1 MONTH' AS \"sales.ordered_at\",\n    CAST(SUM(sales.revenue * regions.factor) AS DOUBLE PRECISION) * 2 AS \"prev__ts\"\n  FROM sales AS sales\n  LEFT JOIN regions AS regions\n    ON sales.region_id = regions.id\n  GROUP BY\n    DATE_TRUNC('MONTH', sales.ordered_at) + INTERVAL '1 MONTH'\n), sjoin_prev AS (\n  SELECT\n    base.\"sales.ordered_at\",\n    base.\"sales.revenue_wrevenue_sum\",\n    shifted_prev.\"prev__ts\" AS \"sales.prev\"\n  FROM base\n  LEFT JOIN shifted_prev\n    ON base.\"sales.ordered_at\" IS NOT DISTINCT FROM shifted_prev.\"sales.ordered_at\"\n)\nSELECT\n  \"sales.ordered_at\",\n  \"sales.revenue_wrevenue_sum\",\n  \"sales.prev\"\nFROM sjoin_prev\n) AS _outer",
  "ts/crossing_join::sqlite": "SELECT\n    \"sales.ordered_at\",\n    \"sales.prev\"\nFROM (\nWITH _base AS (\n  SELECT\n    STRFTIME('%Y-%m-01', sales.ordered_at) AS \"sales.ordered_at\"\n  FROM sales AS sales\n  GROUP BY\n    STRFTIME('%Y-%m-01', sales.ordered_at)\n), _cm_sales__revenue_wrevenue_sum AS (\n  SELECT\n    STRFTIME('%Y-%m-01', sales.ordered_at) AS \"sales.ordered_at\",\n    CAST(SUM(sales.revenue * regions.factor) AS REAL) AS \"sales.revenue_wrevenue_sum\"\n  FROM sales AS sales\n  LEFT JOIN regions AS regions\n    ON sales.region_id = regions.id\n  GROUP BY\n    STRFTIME('%Y-%m-01', sales.ordered_at)\n), base AS (\n  SELECT\n    _base.\"sales.ordered_at\",\n    _cm_sales__revenue_wrevenue_sum.\"sales.revenue_wrevenue_sum\" AS \"sales.revenue_wrevenue_sum\"\n  FROM _base\n  LEFT JOIN _cm_sales__revenue_wrevenue_sum\n    ON _base.\"sales.ordered_at\" IS _cm_sales__revenue_wrevenue_sum.\"sales.ordered_at\"\n), shifted_prev AS (\n  SELECT\n    DATE(STRFTIME('%Y-%m-01', sales.ordered_at), '1 months') AS \"sales.ordered_at\",\n    CAST(SUM(sales.revenue * regions.factor) AS REAL) * 2 AS \"prev__ts\"\n  FROM sales AS sales\n  LEFT JOIN regions AS regions\n    ON sales.region_id = regions.id\n  GROUP BY\n    DATE(STRFTIME('%Y-%m-01', sales.ordered_at), '1 months')\n), sjoin_prev AS (\n  SELECT\n    base.\"sales.ordered_at\",\n    base.\"sales.revenue_wrevenue_sum\",\n    shifted_prev.\"prev__ts\" AS \"sales.prev\"\n  FROM base\n  LEFT JOIN shifted_prev\n    ON base.\"sales.ordered_at\" IS shifted_prev.\"sales.ordered_at\"\n)\nSELECT\n  \"sales.ordered_at\",\n  \"sales.revenue_wrevenue_sum\",\n  \"sales.prev\"\nFROM sjoin_prev\n) AS _outer",
  "ts/crossing_join::tsql": "WITH _base AS (\n  SELECT\n    DATETRUNC(MONTH, sales.ordered_at) AS [sales___ordered_at]\n  FROM sales AS sales\n  GROUP BY\n    DATETRUNC(MONTH, sales.ordered_at)\n), _cm_sales__revenue_wrevenue_sum AS (\n  SELECT\n    DATETRUNC(MONTH, sales.ordered_at) AS [sales___ordered_at],\n    CAST(SUM(sales.revenue * regions.factor) AS FLOAT) AS [sales___revenue_wrevenue_sum]\n  FROM sales AS sales\n  LEFT JOIN regions AS regions\n    ON sales.region_id = regions.id\n  GROUP BY\n    DATETRUNC(MONTH, sales.ordered_at)\n), base AS (\n  SELECT\n    _base.[sales___ordered_at] AS [sales___ordered_at],\n    _cm_sales__revenue_wrevenue_sum.[sales___revenue_wrevenue_sum] AS [sales___revenue_wrevenue_sum]\n  FROM _base\n  LEFT JOIN _cm_sales__revenue_wrevenue_sum\n    ON (\n      _base.[sales___ordered_at] = _cm_sales__revenue_wrevenue_sum.[sales___ordered_at]\n      OR (\n        _base.[sales___ordered_at] IS NULL\n        AND _cm_sales__revenue_wrevenue_sum.[sales___ordered_at] IS NULL\n      )\n    )\n), shifted_prev AS (\n  SELECT\n    DATEADD(MONTH, 1, DATETRUNC(MONTH, sales.ordered_at)) AS [sales___ordered_at],\n    CAST(SUM(sales.revenue * regions.factor) AS FLOAT) * 2 AS [prev__ts]\n  FROM sales AS sales\n  LEFT JOIN regions AS regions\n    ON sales.region_id = regions.id\n  GROUP BY\n    DATEADD(MONTH, 1, DATETRUNC(MONTH, sales.ordered_at))\n), sjoin_prev AS (\n  SELECT\n    base.[sales___ordered_at] AS [sales___ordered_at],\n    base.[sales___revenue_wrevenue_sum] AS [sales___revenue_wrevenue_sum],\n    shifted_prev.[prev__ts] AS [sales___prev]\n  FROM base\n  LEFT JOIN shifted_prev\n    ON (\n      base.[sales___ordered_at] = shifted_prev.[sales___ordered_at]\n      OR (\n        base.[sales___ordered_at] IS NULL AND shifted_prev.[sales___ordered_at] IS NULL\n      )\n    )\n)\nSELECT\n  [sales___ordered_at],\n  [sales___prev]\nFROM (\n  SELECT\n    [sales___ordered_at] AS [sales___ordered_at],\n    [sales___revenue_wrevenue_sum] AS [sales___revenue_wrevenue_sum],\n    [sales___prev] AS [sales___prev]\n  FROM sjoin_prev\n) AS _outer",
  "ts/ratio_by_store::bigquery": "SELECT\n    `sales___store`,\n    `sales___ordered_at`,\n    `sales___prev`\nFROM (\nWITH base AS (\n  SELECT\n    sales.store AS `sales___store`,\n    DATE_TRUNC(sales.ordered_at, MONTH) AS `sales___ordered_at`,\n    SUM(sales.revenue) AS `sales___revenue_sum`,\n    SUM(sales.qty) AS `sales___qty_sum`\n  FROM sales AS sales\n  GROUP BY\n    sales.store,\n    DATE_TRUNC(sales.ordered_at, MONTH)\n), shifted_prev AS (\n  SELECT\n    DATE_TRUNC(sales.ordered_at, MONTH) + INTERVAL 1 MONTH AS `sales___ordered_at`,\n    sales.store AS `sales___store`,\n    SUM(sales.revenue) / SUM(sales.qty) AS `prev__ts`\n  FROM sales AS sales\n  GROUP BY\n    DATE_TRUNC(sales.ordered_at, MONTH) + INTERVAL 1 MONTH,\n    sales.store\n), sjoin_prev AS (\n  SELECT\n    base.`sales___store`,\n    base.`sales___ordered_at`,\n    base.`sales___revenue_sum`,\n    base.`sales___qty_sum`,\n    shifted_prev.`prev__ts` AS `sales___prev`\n  FROM base\n  LEFT JOIN shifted_prev\n    ON base.`sales___ordered_at` IS NOT DISTINCT FROM shifted_prev.`sales___ordered_at`\n    AND base.`sales___store` IS NOT DISTINCT FROM shifted_prev.`sales___store`\n)\nSELECT\n  `sales___store`,\n  `sales___ordered_at`,\n  `sales___revenue_sum`,\n  `sales___qty_sum`,\n  `sales___prev`\nFROM sjoin_prev\n) AS _outer",
  "ts/ratio_by_store::duckdb": "SELECT\n    \"sales.store\",\n    \"sales.ordered_at\",\n    \"sales.prev\"\nFROM (\nWITH base AS (\n  SELECT\n    sales.store AS \"sales.store\",\n    DATE_TRUNC('MONTH', sales.ordered_at) AS \"sales.ordered_at\",\n    SUM(sales.revenue) AS \"sales.revenue_sum\",\n    SUM(sales.qty) AS \"sales.qty_sum\"\n  FROM sales AS sales\n  GROUP BY\n    sales.store,\n    DATE_TRUNC('MONTH', sales.ordered_at)\n), shifted_prev AS (\n  SELECT\n    DATE_TRUNC('MONTH', sales.ordered_at) + INTERVAL 1 MONTH AS \"sales.ordered_at\",\n    sales.store AS \"sales.store\",\n    SUM(sales.revenue) / SUM(sales.qty) AS \"prev__ts\"\n  FROM sales AS sales\n  GROUP BY\n    DATE_TRUNC('MONTH', sales.ordered_at) + INTERVAL 1 MONTH,\n    sales.store\n), sjoin_prev AS (\n  SELECT\n    base.\"sales.store\",\n    base.\"sales.ordered_at\",\n    base.\"sales.revenue_sum\",\n    base.\"sales.qty_sum\",\n    shifted_prev.\"prev__ts\" AS \"sales.prev\"\n  FROM base\n  LEFT JOIN shifted_prev\n    ON base.\"sales.ordered_at\" IS NOT DISTINCT FROM shifted_prev.\"sales.ordered_at\"\n    AND base.\"sales.store\" IS NOT DISTINCT FROM shifted_prev.\"sales.store\"\n)\nSELECT\n  \"sales.store\",\n  \"sales.ordered_at\",\n  \"sales.revenue_sum\",\n  \"sales.qty_sum\",\n  \"sales.prev\"\nFROM sjoin_prev\n) AS _outer",
  "ts/ratio_by_store::postgres": "SELECT\n    \"sales.store\",\n    \"sales.ordered_at\",\n    \"sales.prev\"\nFROM (\nWITH base AS (\n  SELECT\n    sales.store AS \"sales.store\",\n    DATE_TRUNC('MONTH', sales.ordered_at) AS \"sales.ordered_at\",\n    SUM(sales.revenue) AS \"sales.revenue_sum\",\n    SUM(sales.qty) AS \"sales.qty_sum\"\n  FROM sales AS sales\n  GROUP BY\n    sales.store,\n    DATE_TRUNC('MONTH', sales.ordered_at)\n), shifted_prev AS (\n  SELECT\n    DATE_TRUNC('MONTH', sales.ordered_at) + INTERVAL '1 MONTH' AS \"sales.ordered_at\",\n    sales.store AS \"sales.store\",\n    CAST(SUM(sales.revenue) AS DOUBLE PRECISION) / SUM(sales.qty) AS \"prev__ts\"\n  FROM sales AS sales\n  GROUP BY\n    DATE_TRUNC('MONTH', sales.ordered_at) + INTERVAL '1 MONTH',\n    sales.store\n), sjoin_prev AS (\n  SELECT\n    base.\"sales.store\",\n    base.\"sales.ordered_at\",\n    base.\"sales.revenue_sum\",\n    base.\"sales.qty_sum\",\n    shifted_prev.\"prev__ts\" AS \"sales.prev\"\n  FROM base\n  LEFT JOIN shifted_prev\n    ON base.\"sales.ordered_at\" IS NOT DISTINCT FROM shifted_prev.\"sales.ordered_at\"\n    AND base.\"sales.store\" IS NOT DISTINCT FROM shifted_prev.\"sales.store\"\n)\nSELECT\n  \"sales.store\",\n  \"sales.ordered_at\",\n  \"sales.revenue_sum\",\n  \"sales.qty_sum\",\n  \"sales.prev\"\nFROM sjoin_prev\n) AS _outer",
  "ts/ratio_by_store::sqlite": "SELECT\n    \"sales.store\",\n    \"sales.ordered_at\",\n    \"sales.prev\"\nFROM (\nWITH base AS (\n  SELECT\n    sales.store AS \"sales.store\",\n    STRFTIME('%Y-%m-01', sales.ordered_at) AS \"sales.ordered_at\",\n    SUM(sales.revenue) AS \"sales.revenue_sum\",\n    SUM(sales.qty) AS \"sales.qty_sum\"\n  FROM sales AS sales\n  GROUP BY\n    sales.store,\n    STRFTIME('%Y-%m-01', sales.ordered_at)\n), shifted_prev AS (\n  SELECT\n    DATE(STRFTIME('%Y-%m-01', sales.ordered_at), '1 months') AS \"sales.ordered_at\",\n    sales.store AS \"sales.store\",\n    CAST(SUM(sales.revenue) AS REAL) / SUM(sales.qty) AS \"prev__ts\"\n  FROM sales AS sales\n  GROUP BY\n    DATE(STRFTIME('%Y-%m-01', sales.ordered_at), '1 months'),\n    sales.store\n), sjoin_prev AS (\n  SELECT\n    base.\"sales.store\",\n    base.\"sales.ordered_at\",\n    base.\"sales.revenue_sum\",\n    base.\"sales.qty_sum\",\n    shifted_prev.\"prev__ts\" AS \"sales.prev\"\n  FROM base\n  LEFT JOIN shifted_prev\n    ON base.\"sales.ordered_at\" IS shifted_prev.\"sales.ordered_at\"\n    AND base.\"sales.store\" IS shifted_prev.\"sales.store\"\n)\nSELECT\n  \"sales.store\",\n  \"sales.ordered_at\",\n  \"sales.revenue_sum\",\n  \"sales.qty_sum\",\n  \"sales.prev\"\nFROM sjoin_prev\n) AS _outer",
  "ts/ratio_by_store::tsql": "WITH base AS (\n  SELECT\n    sales.store AS [sales___store],\n    DATETRUNC(MONTH, sales.ordered_at) AS [sales___ordered_at],\n    SUM(sales.revenue) AS [sales___revenue_sum],\n    SUM(sales.qty) AS [sales___qty_sum]\n  FROM sales AS sales\n  GROUP BY\n    sales.store,\n    DATETRUNC(MONTH, sales.ordered_at)\n), shifted_prev AS (\n  SELECT\n    DATEADD(MONTH, 1, DATETRUNC(MONTH, sales.ordered_at)) AS [sales___ordered_at],\n    sales.store AS [sales___store],\n    CAST(SUM(sales.revenue) AS FLOAT) / SUM(sales.qty) AS [prev__ts]\n  FROM sales AS sales\n  GROUP BY\n    DATEADD(MONTH, 1, DATETRUNC(MONTH, sales.ordered_at)),\n    sales.store\n), sjoin_prev AS (\n  SELECT\n    base.[sales___store] AS [sales___store],\n    base.[sales___ordered_at] AS [sales___ordered_at],\n    base.[sales___revenue_sum] AS [sales___revenue_sum],\n    base.[sales___qty_sum] AS [sales___qty_sum],\n    shifted_prev.[prev__ts] AS [sales___prev]\n  FROM base\n  LEFT JOIN shifted_prev\n    ON (\n      base.[sales___ordered_at] = shifted_prev.[sales___ordered_at]\n      OR (\n        base.[sales___ordered_at] IS NULL AND shifted_prev.[sales___ordered_at] IS NULL\n      )\n    )\n    AND (\n      base.[sales___store] = shifted_prev.[sales___store]\n      OR (\n        base.[sales___store] IS NULL AND shifted_prev.[sales___store] IS NULL\n      )\n    )\n)\nSELECT\n  [sales___store],\n  [sales___ordered_at],\n  [sales___prev]\nFROM (\n  SELECT\n    [sales___store] AS [sales___store],\n    [sales___ordered_at] AS [sales___ordered_at],\n    [sales___revenue_sum] AS [sales___revenue_sum],\n    [sales___qty_sum] AS [sales___qty_sum],\n    [sales___prev] AS [sales___prev]\n  FROM sjoin_prev\n) AS _outer",
  "ts/two_leaves::bigquery": "SELECT\n    `sales___ordered_at`,\n    `sales___prev`\nFROM (\nWITH _base AS (\n  SELECT\n    DATE_TRUNC(sales.ordered_at, MONTH) AS `sales___ordered_at`,\n    SUM(CASE WHEN sales.revenue > 15 THEN sales.revenue END) AS `sales___hi_rev_sum`\n  FROM sales AS sales\n  GROUP BY\n    DATE_TRUNC(sales.ordered_at, MONTH)\n), _cm_sales__revenue_wrevenue_sum AS (\n  SELECT\n    DATE_TRUNC(sales.ordered_at, MONTH) AS `sales___ordered_at`,\n    CAST(SUM(sales.revenue * regions.factor) AS FLOAT64) AS `sales___revenue_wrevenue_sum`\n  FROM sales AS sales\n  LEFT JOIN regions AS regions\n    ON sales.region_id = regions.id\n  GROUP BY\n    DATE_TRUNC(sales.ordered_at, MONTH)\n), base AS (\n  SELECT\n    _base.`sales___ordered_at`,\n    _base.`sales___hi_rev_sum`,\n    _cm_sales__revenue_wrevenue_sum.`sales___revenue_wrevenue_sum` AS `sales___revenue_wrevenue_sum`\n  FROM _base\n  LEFT JOIN _cm_sales__revenue_wrevenue_sum\n    ON _base.`sales___ordered_at` IS NOT DISTINCT FROM _cm_sales__revenue_wrevenue_sum.`sales___ordered_at`\n), shifted_prev AS (\n  SELECT\n    DATE_TRUNC(sales.ordered_at, MONTH) + INTERVAL 1 MONTH AS `sales___ordered_at`,\n    CAST(SUM(sales.revenue * regions.factor) AS FLOAT64) + SUM(CASE WHEN sales.revenue > 15 THEN sales.revenue END) AS `prev__ts`\n  FROM sales AS sales\n  LEFT JOIN regions AS regions\n    ON sales.region_id = regions.id\n  GROUP BY\n    DATE_TRUNC(sales.ordered_at, MONTH) + INTERVAL 1 MONTH\n), sjoin_prev AS (\n  SELECT\n    base.`sales___ordered_at`,\n    base.`sales___hi_rev_sum`,\n    base.`sales___revenue_wrevenue_sum`,\n    shifted_prev.`prev__ts` AS `sales___prev`\n  FROM base\n  LEFT JOIN shifted_prev\n    ON base.`sales___ordered_at` IS NOT DISTINCT FROM shifted_prev.`sales___ordered_at`\n)\nSELECT\n  `sales___ordered_at`,\n  `sales___hi_rev_sum`,\n  `sales___revenue_wrevenue_sum`,\n  `sales___prev`\nFROM sjoin_prev\n) AS _outer",
  "ts/two_leaves::duckdb": "SELECT\n    \"sales.ordered_at\",\n    \"sales.prev\"\nFROM (\nWITH _base AS (\n  SELECT\n    DATE_TRUNC('MONTH', sales.ordered_at) AS \"sales.ordered_at\",\n    SUM(CASE WHEN sales.revenue > 15 THEN sales.revenue END) AS \"sales.hi_rev_sum\"\n  FROM sales AS sales\n  GROUP BY\n    DATE_TRUNC('MONTH', sales.ordered_at)\n), _cm_sales__revenue_wrevenue_sum AS (\n  SELECT\n    DATE_TRUNC('MONTH', sales.ordered_at) AS \"sales.ordered_at\",\n    CAST(SUM(sales.revenue * regions.factor) AS DOUBLE) AS \"sales.revenue_wrevenue_sum\"\n  FROM sales AS sales\n  LEFT JOIN regions AS regions\n    ON sales.region_id = regions.id\n  GROUP BY\n    DATE_TRUNC('MONTH', sales.ordered_at)\n), base AS (\n  SELECT\n    _base.\"sales.ordered_at\",\n    _base.\"sales.hi_rev_sum\",\n    _cm_sales__revenue_wrevenue_sum.\"sales.revenue_wrevenue_sum\" AS \"sales.revenue_wrevenue_sum\"\n  FROM _base\n  LEFT JOIN _cm_sales__revenue_wrevenue_sum\n    ON _base.\"sales.ordered_at\" IS NOT DISTINCT FROM _cm_sales__revenue_wrevenue_sum.\"sales.ordered_at\"\n), shifted_prev AS (\n  SELECT\n    DATE_TRUNC('MONTH', sales.ordered_at) + INTERVAL 1 MONTH AS \"sales.ordered_at\",\n    CAST(SUM(sales.revenue * regions.factor) AS DOUBLE) + SUM(CASE WHEN sales.revenue > 15 THEN sales.revenue END) AS \"prev__ts\"\n  FROM sales AS sales\n  LEFT JOIN regions AS regions\n    ON sales.region_id = regions.id\n  GROUP BY\n    DATE_TRUNC('MONTH', sales.ordered_at) + INTERVAL 1 MONTH\n), sjoin_prev AS (\n  SELECT\n    base.\"sales.ordered_at\",\n    base.\"sales.hi_rev_sum\",\n    base.\"sales.revenue_wrevenue_sum\",\n    shifted_prev.\"prev__ts\" AS \"sales.prev\"\n  FROM base\n  LEFT JOIN shifted_prev\n    ON base.\"sales.ordered_at\" IS NOT DISTINCT FROM shifted_prev.\"sales.ordered_at\"\n)\nSELECT\n  \"sales.ordered_at\",\n  \"sales.hi_rev_sum\",\n  \"sales.revenue_wrevenue_sum\",\n  \"sales.prev\"\nFROM sjoin_prev\n) AS _outer",
  "ts/two_leaves::postgres": "SELECT\n    \"sales.ordered_at\",\n    \"sales.prev\"\nFROM (\nWITH _base AS (\n  SELECT\n    DATE_TRUNC('MONTH', sales.ordered_at) AS \"sales.ordered_at\",\n    SUM(CASE WHEN sales.revenue > 15 THEN sales.revenue END) AS \"sales.hi_rev_sum\"\n  FROM sales AS sales\n  GROUP BY\n    DATE_TRUNC('MONTH', sales.ordered_at)\n), _cm_sales__revenue_wrevenue_sum AS (\n  SELECT\n    DATE_TRUNC('MONTH', sales.ordered_at) AS \"sales.ordered_at\",\n    CAST(SUM(sales.revenue * regions.factor) AS DOUBLE PRECISION) AS \"sales.revenue_wrevenue_sum\"\n  FROM sales AS sales\n  LEFT JOIN regions AS regions\n    ON sales.region_id = regions.id\n  GROUP BY\n    DATE_TRUNC('MONTH', sales.ordered_at)\n), base AS (\n  SELECT\n    _base.\"sales.ordered_at\",\n    _base.\"sales.hi_rev_sum\",\n    _cm_sales__revenue_wrevenue_sum.\"sales.revenue_wrevenue_sum\" AS \"sales.revenue_wrevenue_sum\"\n  FROM _base\n  LEFT JOIN _cm_sales__revenue_wrevenue_sum\n    ON _base.\"sales.ordered_at\" IS NOT DISTINCT FROM _cm_sales__revenue_wrevenue_sum.\"sales.ordered_at\"\n), shifted_prev AS (\n  SELECT\n    DATE_TRUNC('MONTH', sales.ordered_at) + INTERVAL '1 MONTH' AS \"sales.ordered_at\",\n    CAST(SUM(sales.revenue * regions.factor) AS DOUBLE PRECISION) + SUM(CASE WHEN sales.revenue > 15 THEN sales.revenue END) AS \"prev__ts\"\n  FROM sales AS sales\n  LEFT JOIN regions AS regions\n    ON sales.region_id = regions.id\n  GROUP BY\n    DATE_TRUNC('MONTH', sales.ordered_at) + INTERVAL '1 MONTH'\n), sjoin_prev AS (\n  SELECT\n    base.\"sales.ordered_at\",\n    base.\"sales.hi_rev_sum\",\n    base.\"sales.revenue_wrevenue_sum\",\n    shifted_prev.\"prev__ts\" AS \"sales.prev\"\n  FROM base\n  LEFT JOIN shifted_prev\n    ON base.\"sales.ordered_at\" IS NOT DISTINCT FROM shifted_prev.\"sales.ordered_at\"\n)\nSELECT\n  \"sales.ordered_at\",\n  \"sales.hi_rev_sum\",\n  \"sales.revenue_wrevenue_sum\",\n  \"sales.prev\"\nFROM sjoin_prev\n) AS _outer",
  "ts/two_leaves::sqlite": "SELECT\n    \"sales.ordered_at\",\n    \"sales.prev\"\nFROM (\nWITH _base AS (\n  SELECT\n    STRFTIME('%Y-%m-01', sales.ordered_at) AS \"sales.ordered_at\",\n    SUM(CASE WHEN sales.revenue > 15 THEN sales.revenue END) AS \"sales.hi_rev_sum\"\n  FROM sales AS sales\n  GROUP BY\n    STRFTIME('%Y-%m-01', sales.ordered_at)\n), _cm_sales__revenue_wrevenue_sum AS (\n  SELECT\n    STRFTIME('%Y-%m-01', sales.ordered_at) AS \"sales.ordered_at\",\n    CAST(SUM(sales.revenue * regions.factor) AS REAL) AS \"sales.revenue_wrevenue_sum\"\n  FROM sales AS sales\n  LEFT JOIN regions AS regions\n    ON sales.region_id = regions.id\n  GROUP BY\n    STRFTIME('%Y-%m-01', sales.ordered_at)\n), base AS (\n  SELECT\n    _base.\"sales.ordered_at\",\n    _base.\"sales.hi_rev_sum\",\n    _cm_sales__revenue_wrevenue_sum.\"sales.revenue_wrevenue_sum\" AS \"sales.revenue_wrevenue_sum\"\n  FROM _base\n  LEFT JOIN _cm_sales__revenue_wrevenue_sum\n    ON _base.\"sales.ordered_at\" IS _cm_sales__revenue_wrevenue_sum.\"sales.ordered_at\"\n), shifted_prev AS (\n  SELECT\n    DATE(STRFTIME('%Y-%m-01', sales.ordered_at), '1 months') AS \"sales.ordered_at\",\n    CAST(SUM(sales.revenue * regions.factor) AS REAL) + SUM(CASE WHEN sales.revenue > 15 THEN sales.revenue END) AS \"prev__ts\"\n  FROM sales AS sales\n  LEFT JOIN regions AS regions\n    ON sales.region_id = regions.id\n  GROUP BY\n    DATE(STRFTIME('%Y-%m-01', sales.ordered_at), '1 months')\n), sjoin_prev AS (\n  SELECT\n    base.\"sales.ordered_at\",\n    base.\"sales.hi_rev_sum\",\n    base.\"sales.revenue_wrevenue_sum\",\n    shifted_prev.\"prev__ts\" AS \"sales.prev\"\n  FROM base\n  LEFT JOIN shifted_prev\n    ON base.\"sales.ordered_at\" IS shifted_prev.\"sales.ordered_at\"\n)\nSELECT\n  \"sales.ordered_at\",\n  \"sales.hi_rev_sum\",\n  \"sales.revenue_wrevenue_sum\",\n  \"sales.prev\"\nFROM sjoin_prev\n) AS _outer",
  "ts/two_leaves::tsql": "WITH _base AS (\n  SELECT\n    DATETRUNC(MONTH, sales.ordered_at) AS [sales___ordered_at],\n    SUM(CASE WHEN sales.revenue > 15 THEN sales.revenue END) AS [sales___hi_rev_sum]\n  FROM sales AS sales\n  GROUP BY\n    DATETRUNC(MONTH, sales.ordered_at)\n), _cm_sales__revenue_wrevenue_sum AS (\n  SELECT\n    DATETRUNC(MONTH, sales.ordered_at) AS [sales___ordered_at],\n    CAST(SUM(sales.revenue * regions.factor) AS FLOAT) AS [sales___revenue_wrevenue_sum]\n  FROM sales AS sales\n  LEFT JOIN regions AS regions\n    ON sales.region_id = regions.id\n  GROUP BY\n    DATETRUNC(MONTH, sales.ordered_at)\n), base AS (\n  SELECT\n    _base.[sales___ordered_at] AS [sales___ordered_at],\n    _base.[sales___hi_rev_sum] AS [sales___hi_rev_sum],\n    _cm_sales__revenue_wrevenue_sum.[sales___revenue_wrevenue_sum] AS [sales___revenue_wrevenue_sum]\n  FROM _base\n  LEFT JOIN _cm_sales__revenue_wrevenue_sum\n    ON (\n      _base.[sales___ordered_at] = _cm_sales__revenue_wrevenue_sum.[sales___ordered_at]\n      OR (\n        _base.[sales___ordered_at] IS NULL\n        AND _cm_sales__revenue_wrevenue_sum.[sales___ordered_at] IS NULL\n      )\n    )\n), shifted_prev AS (\n  SELECT\n    DATEADD(MONTH, 1, DATETRUNC(MONTH, sales.ordered_at)) AS [sales___ordered_at],\n    CAST(SUM(sales.revenue * regions.factor) AS FLOAT) + SUM(CASE WHEN sales.revenue > 15 THEN sales.revenue END) AS [prev__ts]\n  FROM sales AS sales\n  LEFT JOIN regions AS regions\n    ON sales.region_id = regions.id\n  GROUP BY\n    DATEADD(MONTH, 1, DATETRUNC(MONTH, sales.ordered_at))\n), sjoin_prev AS (\n  SELECT\n    base.[sales___ordered_at] AS [sales___ordered_at],\n    base.[sales___hi_rev_sum] AS [sales___hi_rev_sum],\n    base.[sales___revenue_wrevenue_sum] AS [sales___revenue_wrevenue_sum],\n    shifted_prev.[prev__ts] AS [sales___prev]\n  FROM base\n  LEFT JOIN shifted_prev\n    ON (\n      base.[sales___ordered_at] = shifted_prev.[sales___ordered_at]\n      OR (\n        base.[sales___ordered_at] IS NULL AND shifted_prev.[sales___ordered_at] IS NULL\n      )\n    )\n)\nSELECT\n  [sales___ordered_at],\n  [sales___prev]\nFROM (\n  SELECT\n    [sales___ordered_at] AS [sales___ordered_at],\n    [sales___hi_rev_sum] AS [sales___hi_rev_sum],\n    [sales___revenue_wrevenue_sum] AS [sales___revenue_wrevenue_sum],\n    [sales___prev] AS [sales___prev]\n  FROM sjoin_prev\n) AS _outer"
}
