{
  "cp/and_simple::bigquery": "WITH base AS (\n  SELECT\n    DATE_TRUNC(sales.ordered_at, MONTH) AS `sales___ordered_at`,\n    SUM(sales.revenue) AS `sales___revenue_sum`,\n    SUM(sales.cost) AS `sales___cost_sum`\n  FROM sales AS sales\n  GROUP BY\n    DATE_TRUNC(sales.ordered_at, MONTH)\n), cp_reset_streak AS (\n  SELECT\n    `sales___ordered_at`,\n    `sales___revenue_sum`,\n    `sales___cost_sum`,\n    SUM(CASE WHEN `sales___revenue_sum` > 40 AND `sales___cost_sum` > 10 THEN 0 ELSE 1 END) OVER (ORDER BY `sales___ordered_at` ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW) AS `_cp_reset_sales___streak`\n  FROM base\n), cp_value_streak AS (\n  SELECT\n    `sales___ordered_at`,\n    `sales___revenue_sum`,\n    `sales___cost_sum`,\n    CASE\n      WHEN `sales___revenue_sum` > 40 AND `sales___cost_sum` > 10\n      THEN SUM(CASE WHEN `sales___revenue_sum` > 40 AND `sales___cost_sum` > 10 THEN 1 ELSE 0 END) OVER (\n        PARTITION BY `_cp_reset_sales___streak`\n        ORDER BY `sales___ordered_at`\n        ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n      )\n      ELSE 0\n    END AS `sales___streak`\n  FROM cp_reset_streak\n)\nSELECT\n  `sales___ordered_at`,\n  `sales___streak`\nFROM (\n  SELECT\n    `sales___ordered_at`,\n    `sales___revenue_sum`,\n    `sales___cost_sum`,\n    `sales___streak`\n  FROM cp_value_streak\n) AS _outer",
  "cp/and_simple::duckdb": "WITH base AS (\n  SELECT\n    DATE_TRUNC('MONTH', sales.ordered_at) AS \"sales.ordered_at\",\n    SUM(sales.revenue) AS \"sales.revenue_sum\",\n    SUM(sales.cost) AS \"sales.cost_sum\"\n  FROM sales AS sales\n  GROUP BY\n    DATE_TRUNC('MONTH', sales.ordered_at)\n), cp_reset_streak AS (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    \"sales.cost_sum\",\n    SUM(CASE WHEN \"sales.revenue_sum\" > 40 AND \"sales.cost_sum\" > 10 THEN 0 ELSE 1 END) OVER (ORDER BY \"sales.ordered_at\" ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW) AS \"_cp_reset_sales.streak\"\n  FROM base\n), cp_value_streak AS (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    \"sales.cost_sum\",\n    CASE\n      WHEN \"sales.revenue_sum\" > 40 AND \"sales.cost_sum\" > 10\n      THEN SUM(CASE WHEN \"sales.revenue_sum\" > 40 AND \"sales.cost_sum\" > 10 THEN 1 ELSE 0 END) OVER (\n        PARTITION BY \"_cp_reset_sales.streak\"\n        ORDER BY \"sales.ordered_at\"\n        ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n      )\n      ELSE 0\n    END AS \"sales.streak\"\n  FROM cp_reset_streak\n)\nSELECT\n  \"sales.ordered_at\",\n  \"sales.streak\"\nFROM (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    \"sales.cost_sum\",\n    \"sales.streak\"\n  FROM cp_value_streak\n) AS _outer",
  "cp/and_simple::postgres": "WITH base AS (\n  SELECT\n    DATE_TRUNC('MONTH', sales.ordered_at) AS \"sales.ordered_at\",\n    SUM(sales.revenue) AS \"sales.revenue_sum\",\n    SUM(sales.cost) AS \"sales.cost_sum\"\n  FROM sales AS sales\n  GROUP BY\n    DATE_TRUNC('MONTH', sales.ordered_at)\n), cp_reset_streak AS (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    \"sales.cost_sum\",\n    SUM(CASE WHEN \"sales.revenue_sum\" > 40 AND \"sales.cost_sum\" > 10 THEN 0 ELSE 1 END) OVER (ORDER BY \"sales.ordered_at\" ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW) AS \"_cp_reset_sales.streak\"\n  FROM base\n), cp_value_streak AS (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    \"sales.cost_sum\",\n    CASE\n      WHEN \"sales.revenue_sum\" > 40 AND \"sales.cost_sum\" > 10\n      THEN SUM(CASE WHEN \"sales.revenue_sum\" > 40 AND \"sales.cost_sum\" > 10 THEN 1 ELSE 0 END) OVER (\n        PARTITION BY \"_cp_reset_sales.streak\"\n        ORDER BY \"sales.ordered_at\"\n        ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n      )\n      ELSE 0\n    END AS \"sales.streak\"\n  FROM cp_reset_streak\n)\nSELECT\n  \"sales.ordered_at\",\n  \"sales.streak\"\nFROM (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    \"sales.cost_sum\",\n    \"sales.streak\"\n  FROM cp_value_streak\n) AS _outer",
  "cp/and_simple::sqlite": "WITH base AS (\n  SELECT\n    STRFTIME('%Y-%m-01', sales.ordered_at) AS \"sales.ordered_at\",\n    SUM(sales.revenue) AS \"sales.revenue_sum\",\n    SUM(sales.cost) AS \"sales.cost_sum\"\n  FROM sales AS sales\n  GROUP BY\n    STRFTIME('%Y-%m-01', sales.ordered_at)\n), cp_reset_streak AS (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    \"sales.cost_sum\",\n    SUM(CASE WHEN \"sales.revenue_sum\" > 40 AND \"sales.cost_sum\" > 10 THEN 0 ELSE 1 END) OVER (ORDER BY \"sales.ordered_at\" ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW) AS \"_cp_reset_sales.streak\"\n  FROM base\n), cp_value_streak AS (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    \"sales.cost_sum\",\n    CASE\n      WHEN \"sales.revenue_sum\" > 40 AND \"sales.cost_sum\" > 10\n      THEN SUM(CASE WHEN \"sales.revenue_sum\" > 40 AND \"sales.cost_sum\" > 10 THEN 1 ELSE 0 END) OVER (\n        PARTITION BY \"_cp_reset_sales.streak\"\n        ORDER BY \"sales.ordered_at\"\n        ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n      )\n      ELSE 0\n    END AS \"sales.streak\"\n  FROM cp_reset_streak\n)\nSELECT\n  \"sales.ordered_at\",\n  \"sales.streak\"\nFROM (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    \"sales.cost_sum\",\n    \"sales.streak\"\n  FROM cp_value_streak\n) AS _outer",
  "cp/and_simple::tsql": "WITH base AS (\n  SELECT\n    DATETRUNC(month, sales.ordered_at) AS [sales___ordered_at],\n    SUM(sales.revenue) AS [sales___revenue_sum],\n    SUM(sales.cost) AS [sales___cost_sum]\n  FROM sales AS sales\n  GROUP BY\n    DATETRUNC(month, sales.ordered_at)\n), cp_reset_streak AS (\n  SELECT\n    [sales___ordered_at] AS [sales___ordered_at],\n    [sales___revenue_sum] AS [sales___revenue_sum],\n    [sales___cost_sum] AS [sales___cost_sum],\n    SUM(CASE WHEN [sales___revenue_sum] > 40 AND [sales___cost_sum] > 10 THEN 0 ELSE 1 END) OVER (ORDER BY [sales___ordered_at] ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW) AS [_cp_reset_sales___streak]\n  FROM base\n), cp_value_streak AS (\n  SELECT\n    [sales___ordered_at] AS [sales___ordered_at],\n    [sales___revenue_sum] AS [sales___revenue_sum],\n    [sales___cost_sum] AS [sales___cost_sum],\n    CASE\n      WHEN [sales___revenue_sum] > 40 AND [sales___cost_sum] > 10\n      THEN SUM(CASE WHEN [sales___revenue_sum] > 40 AND [sales___cost_sum] > 10 THEN 1 ELSE 0 END) OVER (\n        PARTITION BY [_cp_reset_sales___streak]\n        ORDER BY [sales___ordered_at]\n        ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n      )\n      ELSE 0\n    END AS [sales___streak]\n  FROM cp_reset_streak\n)\nSELECT\n  [sales___ordered_at],\n  [sales___streak]\nFROM (\n  SELECT\n    [sales___ordered_at],\n    [sales___revenue_sum],\n    [sales___cost_sum],\n    [sales___streak]\n  FROM cp_value_streak\n) AS _outer",
  "cp/bare_cumsum::bigquery": "WITH base AS (\n  SELECT\n    DATE_TRUNC(sales.ordered_at, MONTH) AS `sales___ordered_at`,\n    SUM(sales.revenue) AS `sales___revenue_sum`\n  FROM sales AS sales\n  GROUP BY\n    DATE_TRUNC(sales.ordered_at, MONTH)\n), step1 AS (\n  SELECT\n    `sales___ordered_at`,\n    `sales___revenue_sum`,\n    SUM(`sales___revenue_sum`) OVER (ORDER BY `sales___ordered_at`) AS `sales____cumsum_inner`\n  FROM base\n), cp_reset_streak AS (\n  SELECT\n    `sales___ordered_at`,\n    `sales___revenue_sum`,\n    `sales____cumsum_inner`,\n    SUM(\n      CASE\n        WHEN NOT `sales____cumsum_inner` IS NULL AND `sales____cumsum_inner` <> 0\n        THEN 0\n        ELSE 1\n      END\n    ) OVER (ORDER BY `sales___ordered_at` ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW) AS `_cp_reset_sales___streak`\n  FROM step1\n), cp_value_streak AS (\n  SELECT\n    `sales___ordered_at`,\n    `sales___revenue_sum`,\n    `sales____cumsum_inner`,\n    CASE\n      WHEN NOT `sales____cumsum_inner` IS NULL AND `sales____cumsum_inner` <> 0\n      THEN SUM(\n        CASE\n          WHEN NOT `sales____cumsum_inner` IS NULL AND `sales____cumsum_inner` <> 0\n          THEN 1\n          ELSE 0\n        END\n      ) OVER (\n        PARTITION BY `_cp_reset_sales___streak`\n        ORDER BY `sales___ordered_at`\n        ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n      )\n      ELSE 0\n    END AS `sales___streak`\n  FROM cp_reset_streak\n)\nSELECT\n  `sales___ordered_at`,\n  `sales___streak`\nFROM (\n  SELECT\n    `sales___ordered_at`,\n    `sales___revenue_sum`,\n    `sales____cumsum_inner`,\n    `sales___streak`\n  FROM cp_value_streak\n) AS _outer",
  "cp/bare_cumsum::duckdb": "WITH base AS (\n  SELECT\n    DATE_TRUNC('MONTH', sales.ordered_at) AS \"sales.ordered_at\",\n    SUM(sales.revenue) AS \"sales.revenue_sum\"\n  FROM sales AS sales\n  GROUP BY\n    DATE_TRUNC('MONTH', sales.ordered_at)\n), step1 AS (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    SUM(\"sales.revenue_sum\") OVER (ORDER BY \"sales.ordered_at\") AS \"sales._cumsum_inner\"\n  FROM base\n), cp_reset_streak AS (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    \"sales._cumsum_inner\",\n    SUM(\n      CASE\n        WHEN NOT \"sales._cumsum_inner\" IS NULL AND \"sales._cumsum_inner\" <> 0\n        THEN 0\n        ELSE 1\n      END\n    ) OVER (ORDER BY \"sales.ordered_at\" ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW) AS \"_cp_reset_sales.streak\"\n  FROM step1\n), cp_value_streak AS (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    \"sales._cumsum_inner\",\n    CASE\n      WHEN NOT \"sales._cumsum_inner\" IS NULL AND \"sales._cumsum_inner\" <> 0\n      THEN SUM(\n        CASE\n          WHEN NOT \"sales._cumsum_inner\" IS NULL AND \"sales._cumsum_inner\" <> 0\n          THEN 1\n          ELSE 0\n        END\n      ) OVER (\n        PARTITION BY \"_cp_reset_sales.streak\"\n        ORDER BY \"sales.ordered_at\"\n        ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n      )\n      ELSE 0\n    END AS \"sales.streak\"\n  FROM cp_reset_streak\n)\nSELECT\n  \"sales.ordered_at\",\n  \"sales.streak\"\nFROM (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    \"sales._cumsum_inner\",\n    \"sales.streak\"\n  FROM cp_value_streak\n) AS _outer",
  "cp/bare_cumsum::postgres": "WITH base AS (\n  SELECT\n    DATE_TRUNC('MONTH', sales.ordered_at) AS \"sales.ordered_at\",\n    SUM(sales.revenue) AS \"sales.revenue_sum\"\n  FROM sales AS sales\n  GROUP BY\n    DATE_TRUNC('MONTH', sales.ordered_at)\n), step1 AS (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    SUM(\"sales.revenue_sum\") OVER (ORDER BY \"sales.ordered_at\") AS \"sales._cumsum_inner\"\n  FROM base\n), cp_reset_streak AS (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    \"sales._cumsum_inner\",\n    SUM(\n      CASE\n        WHEN NOT \"sales._cumsum_inner\" IS NULL AND \"sales._cumsum_inner\" <> 0\n        THEN 0\n        ELSE 1\n      END\n    ) OVER (ORDER BY \"sales.ordered_at\" ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW) AS \"_cp_reset_sales.streak\"\n  FROM step1\n), cp_value_streak AS (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    \"sales._cumsum_inner\",\n    CASE\n      WHEN NOT \"sales._cumsum_inner\" IS NULL AND \"sales._cumsum_inner\" <> 0\n      THEN SUM(\n        CASE\n          WHEN NOT \"sales._cumsum_inner\" IS NULL AND \"sales._cumsum_inner\" <> 0\n          THEN 1\n          ELSE 0\n        END\n      ) OVER (\n        PARTITION BY \"_cp_reset_sales.streak\"\n        ORDER BY \"sales.ordered_at\"\n        ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n      )\n      ELSE 0\n    END AS \"sales.streak\"\n  FROM cp_reset_streak\n)\nSELECT\n  \"sales.ordered_at\",\n  \"sales.streak\"\nFROM (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    \"sales._cumsum_inner\",\n    \"sales.streak\"\n  FROM cp_value_streak\n) AS _outer",
  "cp/bare_cumsum::sqlite": "WITH base AS (\n  SELECT\n    STRFTIME('%Y-%m-01', sales.ordered_at) AS \"sales.ordered_at\",\n    SUM(sales.revenue) AS \"sales.revenue_sum\"\n  FROM sales AS sales\n  GROUP BY\n    STRFTIME('%Y-%m-01', sales.ordered_at)\n), step1 AS (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    SUM(\"sales.revenue_sum\") OVER (ORDER BY \"sales.ordered_at\") AS \"sales._cumsum_inner\"\n  FROM base\n), cp_reset_streak AS (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    \"sales._cumsum_inner\",\n    SUM(\n      CASE\n        WHEN NOT \"sales._cumsum_inner\" IS NULL AND \"sales._cumsum_inner\" <> 0\n        THEN 0\n        ELSE 1\n      END\n    ) OVER (ORDER BY \"sales.ordered_at\" ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW) AS \"_cp_reset_sales.streak\"\n  FROM step1\n), cp_value_streak AS (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    \"sales._cumsum_inner\",\n    CASE\n      WHEN NOT \"sales._cumsum_inner\" IS NULL AND \"sales._cumsum_inner\" <> 0\n      THEN SUM(\n        CASE\n          WHEN NOT \"sales._cumsum_inner\" IS NULL AND \"sales._cumsum_inner\" <> 0\n          THEN 1\n          ELSE 0\n        END\n      ) OVER (\n        PARTITION BY \"_cp_reset_sales.streak\"\n        ORDER BY \"sales.ordered_at\"\n        ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n      )\n      ELSE 0\n    END AS \"sales.streak\"\n  FROM cp_reset_streak\n)\nSELECT\n  \"sales.ordered_at\",\n  \"sales.streak\"\nFROM (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    \"sales._cumsum_inner\",\n    \"sales.streak\"\n  FROM cp_value_streak\n) AS _outer",
  "cp/bare_cumsum::tsql": "WITH base AS (\n  SELECT\n    DATETRUNC(month, sales.ordered_at) AS [sales___ordered_at],\n    SUM(sales.revenue) AS [sales___revenue_sum]\n  FROM sales AS sales\n  GROUP BY\n    DATETRUNC(month, sales.ordered_at)\n), step1 AS (\n  SELECT\n    [sales___ordered_at] AS [sales___ordered_at],\n    [sales___revenue_sum] AS [sales___revenue_sum],\n    SUM([sales___revenue_sum]) OVER (ORDER BY [sales___ordered_at]) AS [sales____cumsum_inner]\n  FROM base\n), cp_reset_streak AS (\n  SELECT\n    [sales___ordered_at] AS [sales___ordered_at],\n    [sales___revenue_sum] AS [sales___revenue_sum],\n    [sales____cumsum_inner] AS [sales____cumsum_inner],\n    SUM(\n      CASE\n        WHEN NOT [sales____cumsum_inner] IS NULL AND [sales____cumsum_inner] <> 0\n        THEN 0\n        ELSE 1\n      END\n    ) OVER (ORDER BY [sales___ordered_at] ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW) AS [_cp_reset_sales___streak]\n  FROM step1\n), cp_value_streak AS (\n  SELECT\n    [sales___ordered_at] AS [sales___ordered_at],\n    [sales___revenue_sum] AS [sales___revenue_sum],\n    [sales____cumsum_inner] AS [sales____cumsum_inner],\n    CASE\n      WHEN NOT [sales____cumsum_inner] IS NULL AND [sales____cumsum_inner] <> 0\n      THEN SUM(\n        CASE\n          WHEN NOT [sales____cumsum_inner] IS NULL AND [sales____cumsum_inner] <> 0\n          THEN 1\n          ELSE 0\n        END\n      ) OVER (\n        PARTITION BY [_cp_reset_sales___streak]\n        ORDER BY [sales___ordered_at]\n        ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n      )\n      ELSE 0\n    END AS [sales___streak]\n  FROM cp_reset_streak\n)\nSELECT\n  [sales___ordered_at],\n  [sales___streak]\nFROM (\n  SELECT\n    [sales___ordered_at],\n    [sales___revenue_sum],\n    [sales____cumsum_inner],\n    [sales___streak]\n  FROM cp_value_streak\n) AS _outer",
  "cp/change_gt_anchor::bigquery": "WITH base AS (\n  SELECT\n    DATE_TRUNC(sales.ordered_at, MONTH) AS `sales___ordered_at`,\n    SUM(sales.revenue) AS `sales___revenue_sum`\n  FROM sales AS sales\n  GROUP BY\n    DATE_TRUNC(sales.ordered_at, MONTH)\n), shifted__time_shift_inner AS (\n  SELECT\n    DATE_TRUNC(sales.ordered_at, MONTH) AS `sales___ordered_at`,\n    CAST(SUM(sales.revenue) AS FLOAT64) AS `sales___revenue_sum`\n  FROM sales AS sales\n  GROUP BY\n    DATE_TRUNC(sales.ordered_at, MONTH)\n), sjoin__time_shift_inner AS (\n  SELECT\n    base.`sales___ordered_at`,\n    base.`sales___revenue_sum`,\n    shifted__time_shift_inner.`sales___revenue_sum` AS `sales____time_shift_inner`\n  FROM base\n  LEFT JOIN shifted__time_shift_inner\n    ON base.`sales___ordered_at` - INTERVAL 1 MONTH IS NOT DISTINCT FROM shifted__time_shift_inner.`sales___ordered_at`\n), cp_reset_streak AS (\n  SELECT\n    `sales___ordered_at`,\n    `sales___revenue_sum`,\n    `sales____time_shift_inner`,\n    SUM(CASE WHEN `sales___revenue_sum` - `sales____time_shift_inner` > 0 THEN 0 ELSE 1 END) OVER (ORDER BY `sales___ordered_at` ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW) AS `_cp_reset_sales___streak`\n  FROM sjoin__time_shift_inner\n), cp_value_streak AS (\n  SELECT\n    `sales___ordered_at`,\n    `sales___revenue_sum`,\n    `sales____time_shift_inner`,\n    CASE\n      WHEN `sales___revenue_sum` - `sales____time_shift_inner` > 0\n      THEN SUM(CASE WHEN `sales___revenue_sum` - `sales____time_shift_inner` > 0 THEN 1 ELSE 0 END) OVER (\n        PARTITION BY `_cp_reset_sales___streak`\n        ORDER BY `sales___ordered_at`\n        ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n      )\n      ELSE 0\n    END AS `sales___streak`\n  FROM cp_reset_streak\n)\nSELECT\n  `sales___ordered_at`,\n  `sales___streak`\nFROM (\n  SELECT\n    `sales___ordered_at`,\n    `sales___revenue_sum`,\n    `sales____time_shift_inner`,\n    `sales___streak`\n  FROM cp_value_streak\n) AS _outer",
  "cp/change_gt_anchor::duckdb": "WITH base AS (\n  SELECT\n    DATE_TRUNC('MONTH', sales.ordered_at) AS \"sales.ordered_at\",\n    SUM(sales.revenue) AS \"sales.revenue_sum\"\n  FROM sales AS sales\n  GROUP BY\n    DATE_TRUNC('MONTH', sales.ordered_at)\n), shifted__time_shift_inner AS (\n  SELECT\n    DATE_TRUNC('MONTH', sales.ordered_at) AS \"sales.ordered_at\",\n    CAST(SUM(sales.revenue) AS DOUBLE) AS \"sales.revenue_sum\"\n  FROM sales AS sales\n  GROUP BY\n    DATE_TRUNC('MONTH', sales.ordered_at)\n), sjoin__time_shift_inner AS (\n  SELECT\n    base.\"sales.ordered_at\",\n    base.\"sales.revenue_sum\",\n    shifted__time_shift_inner.\"sales.revenue_sum\" AS \"sales._time_shift_inner\"\n  FROM base\n  LEFT JOIN shifted__time_shift_inner\n    ON base.\"sales.ordered_at\" - INTERVAL 1 MONTH IS NOT DISTINCT FROM shifted__time_shift_inner.\"sales.ordered_at\"\n), cp_reset_streak AS (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    \"sales._time_shift_inner\",\n    SUM(CASE WHEN \"sales.revenue_sum\" - \"sales._time_shift_inner\" > 0 THEN 0 ELSE 1 END) OVER (ORDER BY \"sales.ordered_at\" ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW) AS \"_cp_reset_sales.streak\"\n  FROM sjoin__time_shift_inner\n), cp_value_streak AS (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    \"sales._time_shift_inner\",\n    CASE\n      WHEN \"sales.revenue_sum\" - \"sales._time_shift_inner\" > 0\n      THEN SUM(CASE WHEN \"sales.revenue_sum\" - \"sales._time_shift_inner\" > 0 THEN 1 ELSE 0 END) OVER (\n        PARTITION BY \"_cp_reset_sales.streak\"\n        ORDER BY \"sales.ordered_at\"\n        ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n      )\n      ELSE 0\n    END AS \"sales.streak\"\n  FROM cp_reset_streak\n)\nSELECT\n  \"sales.ordered_at\",\n  \"sales.streak\"\nFROM (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    \"sales._time_shift_inner\",\n    \"sales.streak\"\n  FROM cp_value_streak\n) AS _outer",
  "cp/change_gt_anchor::postgres": "WITH base AS (\n  SELECT\n    DATE_TRUNC('MONTH', sales.ordered_at) AS \"sales.ordered_at\",\n    SUM(sales.revenue) AS \"sales.revenue_sum\"\n  FROM sales AS sales\n  GROUP BY\n    DATE_TRUNC('MONTH', sales.ordered_at)\n), shifted__time_shift_inner AS (\n  SELECT\n    DATE_TRUNC('MONTH', sales.ordered_at) AS \"sales.ordered_at\",\n    CAST(SUM(sales.revenue) AS DOUBLE PRECISION) AS \"sales.revenue_sum\"\n  FROM sales AS sales\n  GROUP BY\n    DATE_TRUNC('MONTH', sales.ordered_at)\n), sjoin__time_shift_inner AS (\n  SELECT\n    base.\"sales.ordered_at\",\n    base.\"sales.revenue_sum\",\n    shifted__time_shift_inner.\"sales.revenue_sum\" AS \"sales._time_shift_inner\"\n  FROM base\n  LEFT JOIN shifted__time_shift_inner\n    ON base.\"sales.ordered_at\" - INTERVAL '1 MONTH' IS NOT DISTINCT FROM shifted__time_shift_inner.\"sales.ordered_at\"\n), cp_reset_streak AS (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    \"sales._time_shift_inner\",\n    SUM(CASE WHEN \"sales.revenue_sum\" - \"sales._time_shift_inner\" > 0 THEN 0 ELSE 1 END) OVER (ORDER BY \"sales.ordered_at\" ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW) AS \"_cp_reset_sales.streak\"\n  FROM sjoin__time_shift_inner\n), cp_value_streak AS (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    \"sales._time_shift_inner\",\n    CASE\n      WHEN \"sales.revenue_sum\" - \"sales._time_shift_inner\" > 0\n      THEN SUM(CASE WHEN \"sales.revenue_sum\" - \"sales._time_shift_inner\" > 0 THEN 1 ELSE 0 END) OVER (\n        PARTITION BY \"_cp_reset_sales.streak\"\n        ORDER BY \"sales.ordered_at\"\n        ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n      )\n      ELSE 0\n    END AS \"sales.streak\"\n  FROM cp_reset_streak\n)\nSELECT\n  \"sales.ordered_at\",\n  \"sales.streak\"\nFROM (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    \"sales._time_shift_inner\",\n    \"sales.streak\"\n  FROM cp_value_streak\n) AS _outer",
  "cp/change_gt_anchor::sqlite": "WITH base AS (\n  SELECT\n    STRFTIME('%Y-%m-01', sales.ordered_at) AS \"sales.ordered_at\",\n    SUM(sales.revenue) AS \"sales.revenue_sum\"\n  FROM sales AS sales\n  GROUP BY\n    STRFTIME('%Y-%m-01', sales.ordered_at)\n), shifted__time_shift_inner AS (\n  SELECT\n    STRFTIME('%Y-%m-01', sales.ordered_at) AS \"sales.ordered_at\",\n    CAST(SUM(sales.revenue) AS REAL) AS \"sales.revenue_sum\"\n  FROM sales AS sales\n  GROUP BY\n    STRFTIME('%Y-%m-01', sales.ordered_at)\n), sjoin__time_shift_inner AS (\n  SELECT\n    base.\"sales.ordered_at\",\n    base.\"sales.revenue_sum\",\n    shifted__time_shift_inner.\"sales.revenue_sum\" AS \"sales._time_shift_inner\"\n  FROM base\n  LEFT JOIN shifted__time_shift_inner\n    ON DATE(base.\"sales.ordered_at\", '-1 months') IS shifted__time_shift_inner.\"sales.ordered_at\"\n), cp_reset_streak AS (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    \"sales._time_shift_inner\",\n    SUM(CASE WHEN \"sales.revenue_sum\" - \"sales._time_shift_inner\" > 0 THEN 0 ELSE 1 END) OVER (ORDER BY \"sales.ordered_at\" ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW) AS \"_cp_reset_sales.streak\"\n  FROM sjoin__time_shift_inner\n), cp_value_streak AS (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    \"sales._time_shift_inner\",\n    CASE\n      WHEN \"sales.revenue_sum\" - \"sales._time_shift_inner\" > 0\n      THEN SUM(CASE WHEN \"sales.revenue_sum\" - \"sales._time_shift_inner\" > 0 THEN 1 ELSE 0 END) OVER (\n        PARTITION BY \"_cp_reset_sales.streak\"\n        ORDER BY \"sales.ordered_at\"\n        ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n      )\n      ELSE 0\n    END AS \"sales.streak\"\n  FROM cp_reset_streak\n)\nSELECT\n  \"sales.ordered_at\",\n  \"sales.streak\"\nFROM (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    \"sales._time_shift_inner\",\n    \"sales.streak\"\n  FROM cp_value_streak\n) AS _outer",
  "cp/change_gt_anchor::tsql": "WITH base AS (\n  SELECT\n    DATETRUNC(month, sales.ordered_at) AS [sales___ordered_at],\n    SUM(sales.revenue) AS [sales___revenue_sum]\n  FROM sales AS sales\n  GROUP BY\n    DATETRUNC(month, sales.ordered_at)\n), shifted__time_shift_inner AS (\n  SELECT\n    DATETRUNC(month, sales.ordered_at) AS [sales___ordered_at],\n    CAST(SUM(sales.revenue) AS FLOAT) AS [sales___revenue_sum]\n  FROM sales AS sales\n  GROUP BY\n    DATETRUNC(month, sales.ordered_at)\n), sjoin__time_shift_inner AS (\n  SELECT\n    base.[sales___ordered_at] AS [sales___ordered_at],\n    base.[sales___revenue_sum] AS [sales___revenue_sum],\n    shifted__time_shift_inner.[sales___revenue_sum] AS [sales____time_shift_inner]\n  FROM base\n  LEFT JOIN shifted__time_shift_inner\n    ON (\n      DATEADD(MONTH, -1, base.[sales___ordered_at]) = shifted__time_shift_inner.[sales___ordered_at]\n      OR (\n        DATEADD(MONTH, -1, base.[sales___ordered_at]) IS NULL\n        AND shifted__time_shift_inner.[sales___ordered_at] IS NULL\n      )\n    )\n), cp_reset_streak AS (\n  SELECT\n    [sales___ordered_at] AS [sales___ordered_at],\n    [sales___revenue_sum] AS [sales___revenue_sum],\n    [sales____time_shift_inner] AS [sales____time_shift_inner],\n    SUM(CASE WHEN [sales___revenue_sum] - [sales____time_shift_inner] > 0 THEN 0 ELSE 1 END) OVER (ORDER BY [sales___ordered_at] ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW) AS [_cp_reset_sales___streak]\n  FROM sjoin__time_shift_inner\n), cp_value_streak AS (\n  SELECT\n    [sales___ordered_at] AS [sales___ordered_at],\n    [sales___revenue_sum] AS [sales___revenue_sum],\n    [sales____time_shift_inner] AS [sales____time_shift_inner],\n    CASE\n      WHEN [sales___revenue_sum] - [sales____time_shift_inner] > 0\n      THEN SUM(CASE WHEN [sales___revenue_sum] - [sales____time_shift_inner] > 0 THEN 1 ELSE 0 END) OVER (\n        PARTITION BY [_cp_reset_sales___streak]\n        ORDER BY [sales___ordered_at]\n        ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n      )\n      ELSE 0\n    END AS [sales___streak]\n  FROM cp_reset_streak\n)\nSELECT\n  [sales___ordered_at],\n  [sales___streak]\nFROM (\n  SELECT\n    [sales___ordered_at],\n    [sales___revenue_sum],\n    [sales____time_shift_inner],\n    [sales___streak]\n  FROM cp_value_streak\n) AS _outer",
  "cp/iif::bigquery": "WITH base AS (\n  SELECT\n    DATE_TRUNC(sales.ordered_at, MONTH) AS `sales___ordered_at`,\n    SUM(sales.revenue) AS `sales___revenue_sum`\n  FROM sales AS sales\n  GROUP BY\n    DATE_TRUNC(sales.ordered_at, MONTH)\n), cp_reset_streak AS (\n  SELECT\n    `sales___ordered_at`,\n    `sales___revenue_sum`,\n    SUM(\n      CASE\n        WHEN NOT CASE WHEN `sales___revenue_sum` > 0 THEN 1 ELSE 0 END IS NULL\n        AND CASE WHEN `sales___revenue_sum` > 0 THEN 1 ELSE 0 END <> 0\n        THEN 0\n        ELSE 1\n      END\n    ) OVER (ORDER BY `sales___ordered_at` ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW) AS `_cp_reset_sales___streak`\n  FROM base\n), cp_value_streak AS (\n  SELECT\n    `sales___ordered_at`,\n    `sales___revenue_sum`,\n    CASE\n      WHEN NOT CASE WHEN `sales___revenue_sum` > 0 THEN 1 ELSE 0 END IS NULL\n      AND CASE WHEN `sales___revenue_sum` > 0 THEN 1 ELSE 0 END <> 0\n      THEN SUM(\n        CASE\n          WHEN NOT CASE WHEN `sales___revenue_sum` > 0 THEN 1 ELSE 0 END IS NULL\n          AND CASE WHEN `sales___revenue_sum` > 0 THEN 1 ELSE 0 END <> 0\n          THEN 1\n          ELSE 0\n        END\n      ) OVER (\n        PARTITION BY `_cp_reset_sales___streak`\n        ORDER BY `sales___ordered_at`\n        ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n      )\n      ELSE 0\n    END AS `sales___streak`\n  FROM cp_reset_streak\n)\nSELECT\n  `sales___ordered_at`,\n  `sales___streak`\nFROM (\n  SELECT\n    `sales___ordered_at`,\n    `sales___revenue_sum`,\n    `sales___streak`\n  FROM cp_value_streak\n) AS _outer",
  "cp/iif::duckdb": "WITH base AS (\n  SELECT\n    DATE_TRUNC('MONTH', sales.ordered_at) AS \"sales.ordered_at\",\n    SUM(sales.revenue) AS \"sales.revenue_sum\"\n  FROM sales AS sales\n  GROUP BY\n    DATE_TRUNC('MONTH', sales.ordered_at)\n), cp_reset_streak AS (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    SUM(\n      CASE\n        WHEN NOT CASE WHEN \"sales.revenue_sum\" > 0 THEN 1 ELSE 0 END IS NULL\n        AND CASE WHEN \"sales.revenue_sum\" > 0 THEN 1 ELSE 0 END <> 0\n        THEN 0\n        ELSE 1\n      END\n    ) OVER (ORDER BY \"sales.ordered_at\" ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW) AS \"_cp_reset_sales.streak\"\n  FROM base\n), cp_value_streak AS (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    CASE\n      WHEN NOT CASE WHEN \"sales.revenue_sum\" > 0 THEN 1 ELSE 0 END IS NULL\n      AND CASE WHEN \"sales.revenue_sum\" > 0 THEN 1 ELSE 0 END <> 0\n      THEN SUM(\n        CASE\n          WHEN NOT CASE WHEN \"sales.revenue_sum\" > 0 THEN 1 ELSE 0 END IS NULL\n          AND CASE WHEN \"sales.revenue_sum\" > 0 THEN 1 ELSE 0 END <> 0\n          THEN 1\n          ELSE 0\n        END\n      ) OVER (\n        PARTITION BY \"_cp_reset_sales.streak\"\n        ORDER BY \"sales.ordered_at\"\n        ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n      )\n      ELSE 0\n    END AS \"sales.streak\"\n  FROM cp_reset_streak\n)\nSELECT\n  \"sales.ordered_at\",\n  \"sales.streak\"\nFROM (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    \"sales.streak\"\n  FROM cp_value_streak\n) AS _outer",
  "cp/iif::postgres": "WITH base AS (\n  SELECT\n    DATE_TRUNC('MONTH', sales.ordered_at) AS \"sales.ordered_at\",\n    SUM(sales.revenue) AS \"sales.revenue_sum\"\n  FROM sales AS sales\n  GROUP BY\n    DATE_TRUNC('MONTH', sales.ordered_at)\n), cp_reset_streak AS (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    SUM(\n      CASE\n        WHEN NOT CASE WHEN \"sales.revenue_sum\" > 0 THEN 1 ELSE 0 END IS NULL\n        AND CASE WHEN \"sales.revenue_sum\" > 0 THEN 1 ELSE 0 END <> 0\n        THEN 0\n        ELSE 1\n      END\n    ) OVER (ORDER BY \"sales.ordered_at\" ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW) AS \"_cp_reset_sales.streak\"\n  FROM base\n), cp_value_streak AS (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    CASE\n      WHEN NOT CASE WHEN \"sales.revenue_sum\" > 0 THEN 1 ELSE 0 END IS NULL\n      AND CASE WHEN \"sales.revenue_sum\" > 0 THEN 1 ELSE 0 END <> 0\n      THEN SUM(\n        CASE\n          WHEN NOT CASE WHEN \"sales.revenue_sum\" > 0 THEN 1 ELSE 0 END IS NULL\n          AND CASE WHEN \"sales.revenue_sum\" > 0 THEN 1 ELSE 0 END <> 0\n          THEN 1\n          ELSE 0\n        END\n      ) OVER (\n        PARTITION BY \"_cp_reset_sales.streak\"\n        ORDER BY \"sales.ordered_at\"\n        ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n      )\n      ELSE 0\n    END AS \"sales.streak\"\n  FROM cp_reset_streak\n)\nSELECT\n  \"sales.ordered_at\",\n  \"sales.streak\"\nFROM (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    \"sales.streak\"\n  FROM cp_value_streak\n) AS _outer",
  "cp/iif::sqlite": "WITH base AS (\n  SELECT\n    STRFTIME('%Y-%m-01', sales.ordered_at) AS \"sales.ordered_at\",\n    SUM(sales.revenue) AS \"sales.revenue_sum\"\n  FROM sales AS sales\n  GROUP BY\n    STRFTIME('%Y-%m-01', sales.ordered_at)\n), cp_reset_streak AS (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    SUM(\n      CASE\n        WHEN NOT CASE WHEN \"sales.revenue_sum\" > 0 THEN 1 ELSE 0 END IS NULL\n        AND CASE WHEN \"sales.revenue_sum\" > 0 THEN 1 ELSE 0 END <> 0\n        THEN 0\n        ELSE 1\n      END\n    ) OVER (ORDER BY \"sales.ordered_at\" ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW) AS \"_cp_reset_sales.streak\"\n  FROM base\n), cp_value_streak AS (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    CASE\n      WHEN NOT CASE WHEN \"sales.revenue_sum\" > 0 THEN 1 ELSE 0 END IS NULL\n      AND CASE WHEN \"sales.revenue_sum\" > 0 THEN 1 ELSE 0 END <> 0\n      THEN SUM(\n        CASE\n          WHEN NOT CASE WHEN \"sales.revenue_sum\" > 0 THEN 1 ELSE 0 END IS NULL\n          AND CASE WHEN \"sales.revenue_sum\" > 0 THEN 1 ELSE 0 END <> 0\n          THEN 1\n          ELSE 0\n        END\n      ) OVER (\n        PARTITION BY \"_cp_reset_sales.streak\"\n        ORDER BY \"sales.ordered_at\"\n        ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n      )\n      ELSE 0\n    END AS \"sales.streak\"\n  FROM cp_reset_streak\n)\nSELECT\n  \"sales.ordered_at\",\n  \"sales.streak\"\nFROM (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    \"sales.streak\"\n  FROM cp_value_streak\n) AS _outer",
  "cp/iif::tsql": "WITH base AS (\n  SELECT\n    DATETRUNC(month, sales.ordered_at) AS [sales___ordered_at],\n    SUM(sales.revenue) AS [sales___revenue_sum]\n  FROM sales AS sales\n  GROUP BY\n    DATETRUNC(month, sales.ordered_at)\n), cp_reset_streak AS (\n  SELECT\n    [sales___ordered_at] AS [sales___ordered_at],\n    [sales___revenue_sum] AS [sales___revenue_sum],\n    SUM(\n      CASE\n        WHEN NOT CASE WHEN [sales___revenue_sum] > 0 THEN 1 ELSE 0 END IS NULL\n        AND CASE WHEN [sales___revenue_sum] > 0 THEN 1 ELSE 0 END <> 0\n        THEN 0\n        ELSE 1\n      END\n    ) OVER (ORDER BY [sales___ordered_at] ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW) AS [_cp_reset_sales___streak]\n  FROM base\n), cp_value_streak AS (\n  SELECT\n    [sales___ordered_at] AS [sales___ordered_at],\n    [sales___revenue_sum] AS [sales___revenue_sum],\n    CASE\n      WHEN NOT CASE WHEN [sales___revenue_sum] > 0 THEN 1 ELSE 0 END IS NULL\n      AND CASE WHEN [sales___revenue_sum] > 0 THEN 1 ELSE 0 END <> 0\n      THEN SUM(\n        CASE\n          WHEN NOT CASE WHEN [sales___revenue_sum] > 0 THEN 1 ELSE 0 END IS NULL\n          AND CASE WHEN [sales___revenue_sum] > 0 THEN 1 ELSE 0 END <> 0\n          THEN 1\n          ELSE 0\n        END\n      ) OVER (\n        PARTITION BY [_cp_reset_sales___streak]\n        ORDER BY [sales___ordered_at]\n        ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n      )\n      ELSE 0\n    END AS [sales___streak]\n  FROM cp_reset_streak\n)\nSELECT\n  [sales___ordered_at],\n  [sales___streak]\nFROM (\n  SELECT\n    [sales___ordered_at],\n    [sales___revenue_sum],\n    [sales___streak]\n  FROM cp_value_streak\n) AS _outer",
  "cp/in::bigquery": "WITH base AS (\n  SELECT\n    sales.store AS `sales___store`,\n    DATE_TRUNC(sales.ordered_at, MONTH) AS `sales___ordered_at`\n  FROM sales AS sales\n  GROUP BY\n    sales.store,\n    DATE_TRUNC(sales.ordered_at, MONTH)\n), cp_reset_streak AS (\n  SELECT\n    `sales___store`,\n    `sales___ordered_at`,\n    SUM(CASE WHEN `sales___store` IN ('A', 'B') THEN 0 ELSE 1 END) OVER (\n      PARTITION BY `sales___store`\n      ORDER BY `sales___ordered_at`\n      ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n    ) AS `_cp_reset_sales___streak`\n  FROM base\n), cp_value_streak AS (\n  SELECT\n    `sales___store`,\n    `sales___ordered_at`,\n    CASE\n      WHEN `sales___store` IN ('A', 'B')\n      THEN SUM(CASE WHEN `sales___store` IN ('A', 'B') THEN 1 ELSE 0 END) OVER (\n        PARTITION BY `sales___store`, `_cp_reset_sales___streak`\n        ORDER BY `sales___ordered_at`\n        ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n      )\n      ELSE 0\n    END AS `sales___streak`\n  FROM cp_reset_streak\n)\nSELECT\n  `sales___store`,\n  `sales___ordered_at`,\n  `sales___streak`\nFROM (\n  SELECT\n    `sales___store`,\n    `sales___ordered_at`,\n    `sales___streak`\n  FROM cp_value_streak\n) AS _outer",
  "cp/in::duckdb": "WITH base AS (\n  SELECT\n    sales.store AS \"sales.store\",\n    DATE_TRUNC('MONTH', sales.ordered_at) AS \"sales.ordered_at\"\n  FROM sales AS sales\n  GROUP BY\n    sales.store,\n    DATE_TRUNC('MONTH', sales.ordered_at)\n), cp_reset_streak AS (\n  SELECT\n    \"sales.store\",\n    \"sales.ordered_at\",\n    SUM(CASE WHEN \"sales.store\" IN ('A', 'B') THEN 0 ELSE 1 END) OVER (\n      PARTITION BY \"sales.store\"\n      ORDER BY \"sales.ordered_at\"\n      ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n    ) AS \"_cp_reset_sales.streak\"\n  FROM base\n), cp_value_streak AS (\n  SELECT\n    \"sales.store\",\n    \"sales.ordered_at\",\n    CASE\n      WHEN \"sales.store\" IN ('A', 'B')\n      THEN SUM(CASE WHEN \"sales.store\" IN ('A', 'B') THEN 1 ELSE 0 END) OVER (\n        PARTITION BY \"sales.store\", \"_cp_reset_sales.streak\"\n        ORDER BY \"sales.ordered_at\"\n        ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n      )\n      ELSE 0\n    END AS \"sales.streak\"\n  FROM cp_reset_streak\n)\nSELECT\n  \"sales.store\",\n  \"sales.ordered_at\",\n  \"sales.streak\"\nFROM (\n  SELECT\n    \"sales.store\",\n    \"sales.ordered_at\",\n    \"sales.streak\"\n  FROM cp_value_streak\n) AS _outer",
  "cp/in::postgres": "WITH base AS (\n  SELECT\n    sales.store AS \"sales.store\",\n    DATE_TRUNC('MONTH', sales.ordered_at) AS \"sales.ordered_at\"\n  FROM sales AS sales\n  GROUP BY\n    sales.store,\n    DATE_TRUNC('MONTH', sales.ordered_at)\n), cp_reset_streak AS (\n  SELECT\n    \"sales.store\",\n    \"sales.ordered_at\",\n    SUM(CASE WHEN \"sales.store\" IN ('A', 'B') THEN 0 ELSE 1 END) OVER (\n      PARTITION BY \"sales.store\"\n      ORDER BY \"sales.ordered_at\"\n      ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n    ) AS \"_cp_reset_sales.streak\"\n  FROM base\n), cp_value_streak AS (\n  SELECT\n    \"sales.store\",\n    \"sales.ordered_at\",\n    CASE\n      WHEN \"sales.store\" IN ('A', 'B')\n      THEN SUM(CASE WHEN \"sales.store\" IN ('A', 'B') THEN 1 ELSE 0 END) OVER (\n        PARTITION BY \"sales.store\", \"_cp_reset_sales.streak\"\n        ORDER BY \"sales.ordered_at\"\n        ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n      )\n      ELSE 0\n    END AS \"sales.streak\"\n  FROM cp_reset_streak\n)\nSELECT\n  \"sales.store\",\n  \"sales.ordered_at\",\n  \"sales.streak\"\nFROM (\n  SELECT\n    \"sales.store\",\n    \"sales.ordered_at\",\n    \"sales.streak\"\n  FROM cp_value_streak\n) AS _outer",
  "cp/in::sqlite": "WITH base AS (\n  SELECT\n    sales.store AS \"sales.store\",\n    STRFTIME('%Y-%m-01', sales.ordered_at) AS \"sales.ordered_at\"\n  FROM sales AS sales\n  GROUP BY\n    sales.store,\n    STRFTIME('%Y-%m-01', sales.ordered_at)\n), cp_reset_streak AS (\n  SELECT\n    \"sales.store\",\n    \"sales.ordered_at\",\n    SUM(CASE WHEN \"sales.store\" IN ('A', 'B') THEN 0 ELSE 1 END) OVER (\n      PARTITION BY \"sales.store\"\n      ORDER BY \"sales.ordered_at\"\n      ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n    ) AS \"_cp_reset_sales.streak\"\n  FROM base\n), cp_value_streak AS (\n  SELECT\n    \"sales.store\",\n    \"sales.ordered_at\",\n    CASE\n      WHEN \"sales.store\" IN ('A', 'B')\n      THEN SUM(CASE WHEN \"sales.store\" IN ('A', 'B') THEN 1 ELSE 0 END) OVER (\n        PARTITION BY \"sales.store\", \"_cp_reset_sales.streak\"\n        ORDER BY \"sales.ordered_at\"\n        ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n      )\n      ELSE 0\n    END AS \"sales.streak\"\n  FROM cp_reset_streak\n)\nSELECT\n  \"sales.store\",\n  \"sales.ordered_at\",\n  \"sales.streak\"\nFROM (\n  SELECT\n    \"sales.store\",\n    \"sales.ordered_at\",\n    \"sales.streak\"\n  FROM cp_value_streak\n) AS _outer",
  "cp/in::tsql": "WITH base AS (\n  SELECT\n    sales.store AS [sales___store],\n    DATETRUNC(month, sales.ordered_at) AS [sales___ordered_at]\n  FROM sales AS sales\n  GROUP BY\n    sales.store,\n    DATETRUNC(month, sales.ordered_at)\n), cp_reset_streak AS (\n  SELECT\n    [sales___store] AS [sales___store],\n    [sales___ordered_at] AS [sales___ordered_at],\n    SUM(CASE WHEN [sales___store] IN ('A', 'B') THEN 0 ELSE 1 END) OVER (\n      PARTITION BY [sales___store]\n      ORDER BY [sales___ordered_at]\n      ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n    ) AS [_cp_reset_sales___streak]\n  FROM base\n), cp_value_streak AS (\n  SELECT\n    [sales___store] AS [sales___store],\n    [sales___ordered_at] AS [sales___ordered_at],\n    CASE\n      WHEN [sales___store] IN ('A', 'B')\n      THEN SUM(CASE WHEN [sales___store] IN ('A', 'B') THEN 1 ELSE 0 END) OVER (\n        PARTITION BY [sales___store], [_cp_reset_sales___streak]\n        ORDER BY [sales___ordered_at]\n        ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n      )\n      ELSE 0\n    END AS [sales___streak]\n  FROM cp_reset_streak\n)\nSELECT\n  [sales___store],\n  [sales___ordered_at],\n  [sales___streak]\nFROM (\n  SELECT\n    [sales___store],\n    [sales___ordered_at],\n    [sales___streak]\n  FROM cp_value_streak\n) AS _outer",
  "cp/is_not_null_and::bigquery": "WITH base AS (\n  SELECT\n    DATE_TRUNC(sales.ordered_at, MONTH) AS `sales___ordered_at`,\n    SUM(CASE WHEN sales.revenue > 15 THEN sales.revenue END) AS `sales___hi_rev_sum`,\n    SUM(sales.cost) AS `sales___cost_sum`\n  FROM sales AS sales\n  GROUP BY\n    DATE_TRUNC(sales.ordered_at, MONTH)\n), cp_reset_streak AS (\n  SELECT\n    `sales___ordered_at`,\n    `sales___hi_rev_sum`,\n    `sales___cost_sum`,\n    SUM(\n      CASE WHEN NOT `sales___hi_rev_sum` IS NULL AND `sales___cost_sum` > 0 THEN 0 ELSE 1 END\n    ) OVER (ORDER BY `sales___ordered_at` ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW) AS `_cp_reset_sales___streak`\n  FROM base\n), cp_value_streak AS (\n  SELECT\n    `sales___ordered_at`,\n    `sales___hi_rev_sum`,\n    `sales___cost_sum`,\n    CASE\n      WHEN NOT `sales___hi_rev_sum` IS NULL AND `sales___cost_sum` > 0\n      THEN SUM(\n        CASE WHEN NOT `sales___hi_rev_sum` IS NULL AND `sales___cost_sum` > 0 THEN 1 ELSE 0 END\n      ) OVER (\n        PARTITION BY `_cp_reset_sales___streak`\n        ORDER BY `sales___ordered_at`\n        ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n      )\n      ELSE 0\n    END AS `sales___streak`\n  FROM cp_reset_streak\n)\nSELECT\n  `sales___ordered_at`,\n  `sales___streak`\nFROM (\n  SELECT\n    `sales___ordered_at`,\n    `sales___hi_rev_sum`,\n    `sales___cost_sum`,\n    `sales___streak`\n  FROM cp_value_streak\n) AS _outer",
  "cp/is_not_null_and::duckdb": "WITH base AS (\n  SELECT\n    DATE_TRUNC('MONTH', sales.ordered_at) AS \"sales.ordered_at\",\n    SUM(CASE WHEN sales.revenue > 15 THEN sales.revenue END) AS \"sales.hi_rev_sum\",\n    SUM(sales.cost) AS \"sales.cost_sum\"\n  FROM sales AS sales\n  GROUP BY\n    DATE_TRUNC('MONTH', sales.ordered_at)\n), cp_reset_streak AS (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.hi_rev_sum\",\n    \"sales.cost_sum\",\n    SUM(\n      CASE WHEN NOT \"sales.hi_rev_sum\" IS NULL AND \"sales.cost_sum\" > 0 THEN 0 ELSE 1 END\n    ) OVER (ORDER BY \"sales.ordered_at\" ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW) AS \"_cp_reset_sales.streak\"\n  FROM base\n), cp_value_streak AS (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.hi_rev_sum\",\n    \"sales.cost_sum\",\n    CASE\n      WHEN NOT \"sales.hi_rev_sum\" IS NULL AND \"sales.cost_sum\" > 0\n      THEN SUM(\n        CASE WHEN NOT \"sales.hi_rev_sum\" IS NULL AND \"sales.cost_sum\" > 0 THEN 1 ELSE 0 END\n      ) OVER (\n        PARTITION BY \"_cp_reset_sales.streak\"\n        ORDER BY \"sales.ordered_at\"\n        ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n      )\n      ELSE 0\n    END AS \"sales.streak\"\n  FROM cp_reset_streak\n)\nSELECT\n  \"sales.ordered_at\",\n  \"sales.streak\"\nFROM (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.hi_rev_sum\",\n    \"sales.cost_sum\",\n    \"sales.streak\"\n  FROM cp_value_streak\n) AS _outer",
  "cp/is_not_null_and::postgres": "WITH base AS (\n  SELECT\n    DATE_TRUNC('MONTH', sales.ordered_at) AS \"sales.ordered_at\",\n    SUM(CASE WHEN sales.revenue > 15 THEN sales.revenue END) AS \"sales.hi_rev_sum\",\n    SUM(sales.cost) AS \"sales.cost_sum\"\n  FROM sales AS sales\n  GROUP BY\n    DATE_TRUNC('MONTH', sales.ordered_at)\n), cp_reset_streak AS (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.hi_rev_sum\",\n    \"sales.cost_sum\",\n    SUM(\n      CASE WHEN NOT \"sales.hi_rev_sum\" IS NULL AND \"sales.cost_sum\" > 0 THEN 0 ELSE 1 END\n    ) OVER (ORDER BY \"sales.ordered_at\" ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW) AS \"_cp_reset_sales.streak\"\n  FROM base\n), cp_value_streak AS (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.hi_rev_sum\",\n    \"sales.cost_sum\",\n    CASE\n      WHEN NOT \"sales.hi_rev_sum\" IS NULL AND \"sales.cost_sum\" > 0\n      THEN SUM(\n        CASE WHEN NOT \"sales.hi_rev_sum\" IS NULL AND \"sales.cost_sum\" > 0 THEN 1 ELSE 0 END\n      ) OVER (\n        PARTITION BY \"_cp_reset_sales.streak\"\n        ORDER BY \"sales.ordered_at\"\n        ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n      )\n      ELSE 0\n    END AS \"sales.streak\"\n  FROM cp_reset_streak\n)\nSELECT\n  \"sales.ordered_at\",\n  \"sales.streak\"\nFROM (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.hi_rev_sum\",\n    \"sales.cost_sum\",\n    \"sales.streak\"\n  FROM cp_value_streak\n) AS _outer",
  "cp/is_not_null_and::sqlite": "WITH base AS (\n  SELECT\n    STRFTIME('%Y-%m-01', sales.ordered_at) AS \"sales.ordered_at\",\n    SUM(CASE WHEN sales.revenue > 15 THEN sales.revenue END) AS \"sales.hi_rev_sum\",\n    SUM(sales.cost) AS \"sales.cost_sum\"\n  FROM sales AS sales\n  GROUP BY\n    STRFTIME('%Y-%m-01', sales.ordered_at)\n), cp_reset_streak AS (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.hi_rev_sum\",\n    \"sales.cost_sum\",\n    SUM(\n      CASE WHEN NOT \"sales.hi_rev_sum\" IS NULL AND \"sales.cost_sum\" > 0 THEN 0 ELSE 1 END\n    ) OVER (ORDER BY \"sales.ordered_at\" ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW) AS \"_cp_reset_sales.streak\"\n  FROM base\n), cp_value_streak AS (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.hi_rev_sum\",\n    \"sales.cost_sum\",\n    CASE\n      WHEN NOT \"sales.hi_rev_sum\" IS NULL AND \"sales.cost_sum\" > 0\n      THEN SUM(\n        CASE WHEN NOT \"sales.hi_rev_sum\" IS NULL AND \"sales.cost_sum\" > 0 THEN 1 ELSE 0 END\n      ) OVER (\n        PARTITION BY \"_cp_reset_sales.streak\"\n        ORDER BY \"sales.ordered_at\"\n        ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n      )\n      ELSE 0\n    END AS \"sales.streak\"\n  FROM cp_reset_streak\n)\nSELECT\n  \"sales.ordered_at\",\n  \"sales.streak\"\nFROM (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.hi_rev_sum\",\n    \"sales.cost_sum\",\n    \"sales.streak\"\n  FROM cp_value_streak\n) AS _outer",
  "cp/is_not_null_and::tsql": "WITH base AS (\n  SELECT\n    DATETRUNC(month, sales.ordered_at) AS [sales___ordered_at],\n    SUM(CASE WHEN sales.revenue > 15 THEN sales.revenue END) AS [sales___hi_rev_sum],\n    SUM(sales.cost) AS [sales___cost_sum]\n  FROM sales AS sales\n  GROUP BY\n    DATETRUNC(month, sales.ordered_at)\n), cp_reset_streak AS (\n  SELECT\n    [sales___ordered_at] AS [sales___ordered_at],\n    [sales___hi_rev_sum] AS [sales___hi_rev_sum],\n    [sales___cost_sum] AS [sales___cost_sum],\n    SUM(\n      CASE WHEN NOT [sales___hi_rev_sum] IS NULL AND [sales___cost_sum] > 0 THEN 0 ELSE 1 END\n    ) OVER (ORDER BY [sales___ordered_at] ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW) AS [_cp_reset_sales___streak]\n  FROM base\n), cp_value_streak AS (\n  SELECT\n    [sales___ordered_at] AS [sales___ordered_at],\n    [sales___hi_rev_sum] AS [sales___hi_rev_sum],\n    [sales___cost_sum] AS [sales___cost_sum],\n    CASE\n      WHEN NOT [sales___hi_rev_sum] IS NULL AND [sales___cost_sum] > 0\n      THEN SUM(\n        CASE WHEN NOT [sales___hi_rev_sum] IS NULL AND [sales___cost_sum] > 0 THEN 1 ELSE 0 END\n      ) OVER (\n        PARTITION BY [_cp_reset_sales___streak]\n        ORDER BY [sales___ordered_at]\n        ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n      )\n      ELSE 0\n    END AS [sales___streak]\n  FROM cp_reset_streak\n)\nSELECT\n  [sales___ordered_at],\n  [sales___streak]\nFROM (\n  SELECT\n    [sales___ordered_at],\n    [sales___hi_rev_sum],\n    [sales___cost_sum],\n    [sales___streak]\n  FROM cp_value_streak\n) AS _outer",
  "cp/is_null_top::bigquery": "WITH base AS (\n  SELECT\n    DATE_TRUNC(sales.ordered_at, MONTH) AS `sales___ordered_at`,\n    SUM(CASE WHEN sales.revenue > 15 THEN sales.revenue END) AS `sales___hi_rev_sum`\n  FROM sales AS sales\n  GROUP BY\n    DATE_TRUNC(sales.ordered_at, MONTH)\n), cp_reset_streak AS (\n  SELECT\n    `sales___ordered_at`,\n    `sales___hi_rev_sum`,\n    SUM(CASE WHEN `sales___hi_rev_sum` IS NULL THEN 0 ELSE 1 END) OVER (ORDER BY `sales___ordered_at` ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW) AS `_cp_reset_sales___streak`\n  FROM base\n), cp_value_streak AS (\n  SELECT\n    `sales___ordered_at`,\n    `sales___hi_rev_sum`,\n    CASE\n      WHEN `sales___hi_rev_sum` IS NULL\n      THEN SUM(CASE WHEN `sales___hi_rev_sum` IS NULL THEN 1 ELSE 0 END) OVER (\n        PARTITION BY `_cp_reset_sales___streak`\n        ORDER BY `sales___ordered_at`\n        ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n      )\n      ELSE 0\n    END AS `sales___streak`\n  FROM cp_reset_streak\n)\nSELECT\n  `sales___ordered_at`,\n  `sales___streak`\nFROM (\n  SELECT\n    `sales___ordered_at`,\n    `sales___hi_rev_sum`,\n    `sales___streak`\n  FROM cp_value_streak\n) AS _outer",
  "cp/is_null_top::duckdb": "WITH base AS (\n  SELECT\n    DATE_TRUNC('MONTH', sales.ordered_at) AS \"sales.ordered_at\",\n    SUM(CASE WHEN sales.revenue > 15 THEN sales.revenue END) AS \"sales.hi_rev_sum\"\n  FROM sales AS sales\n  GROUP BY\n    DATE_TRUNC('MONTH', sales.ordered_at)\n), cp_reset_streak AS (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.hi_rev_sum\",\n    SUM(CASE WHEN \"sales.hi_rev_sum\" IS NULL THEN 0 ELSE 1 END) OVER (ORDER BY \"sales.ordered_at\" ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW) AS \"_cp_reset_sales.streak\"\n  FROM base\n), cp_value_streak AS (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.hi_rev_sum\",\n    CASE\n      WHEN \"sales.hi_rev_sum\" IS NULL\n      THEN SUM(CASE WHEN \"sales.hi_rev_sum\" IS NULL THEN 1 ELSE 0 END) OVER (\n        PARTITION BY \"_cp_reset_sales.streak\"\n        ORDER BY \"sales.ordered_at\"\n        ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n      )\n      ELSE 0\n    END AS \"sales.streak\"\n  FROM cp_reset_streak\n)\nSELECT\n  \"sales.ordered_at\",\n  \"sales.streak\"\nFROM (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.hi_rev_sum\",\n    \"sales.streak\"\n  FROM cp_value_streak\n) AS _outer",
  "cp/is_null_top::postgres": "WITH base AS (\n  SELECT\n    DATE_TRUNC('MONTH', sales.ordered_at) AS \"sales.ordered_at\",\n    SUM(CASE WHEN sales.revenue > 15 THEN sales.revenue END) AS \"sales.hi_rev_sum\"\n  FROM sales AS sales\n  GROUP BY\n    DATE_TRUNC('MONTH', sales.ordered_at)\n), cp_reset_streak AS (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.hi_rev_sum\",\n    SUM(CASE WHEN \"sales.hi_rev_sum\" IS NULL THEN 0 ELSE 1 END) OVER (ORDER BY \"sales.ordered_at\" ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW) AS \"_cp_reset_sales.streak\"\n  FROM base\n), cp_value_streak AS (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.hi_rev_sum\",\n    CASE\n      WHEN \"sales.hi_rev_sum\" IS NULL\n      THEN SUM(CASE WHEN \"sales.hi_rev_sum\" IS NULL THEN 1 ELSE 0 END) OVER (\n        PARTITION BY \"_cp_reset_sales.streak\"\n        ORDER BY \"sales.ordered_at\"\n        ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n      )\n      ELSE 0\n    END AS \"sales.streak\"\n  FROM cp_reset_streak\n)\nSELECT\n  \"sales.ordered_at\",\n  \"sales.streak\"\nFROM (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.hi_rev_sum\",\n    \"sales.streak\"\n  FROM cp_value_streak\n) AS _outer",
  "cp/is_null_top::sqlite": "WITH base AS (\n  SELECT\n    STRFTIME('%Y-%m-01', sales.ordered_at) AS \"sales.ordered_at\",\n    SUM(CASE WHEN sales.revenue > 15 THEN sales.revenue END) AS \"sales.hi_rev_sum\"\n  FROM sales AS sales\n  GROUP BY\n    STRFTIME('%Y-%m-01', sales.ordered_at)\n), cp_reset_streak AS (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.hi_rev_sum\",\n    SUM(CASE WHEN \"sales.hi_rev_sum\" IS NULL THEN 0 ELSE 1 END) OVER (ORDER BY \"sales.ordered_at\" ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW) AS \"_cp_reset_sales.streak\"\n  FROM base\n), cp_value_streak AS (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.hi_rev_sum\",\n    CASE\n      WHEN \"sales.hi_rev_sum\" IS NULL\n      THEN SUM(CASE WHEN \"sales.hi_rev_sum\" IS NULL THEN 1 ELSE 0 END) OVER (\n        PARTITION BY \"_cp_reset_sales.streak\"\n        ORDER BY \"sales.ordered_at\"\n        ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n      )\n      ELSE 0\n    END AS \"sales.streak\"\n  FROM cp_reset_streak\n)\nSELECT\n  \"sales.ordered_at\",\n  \"sales.streak\"\nFROM (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.hi_rev_sum\",\n    \"sales.streak\"\n  FROM cp_value_streak\n) AS _outer",
  "cp/is_null_top::tsql": "WITH base AS (\n  SELECT\n    DATETRUNC(month, sales.ordered_at) AS [sales___ordered_at],\n    SUM(CASE WHEN sales.revenue > 15 THEN sales.revenue END) AS [sales___hi_rev_sum]\n  FROM sales AS sales\n  GROUP BY\n    DATETRUNC(month, sales.ordered_at)\n), cp_reset_streak AS (\n  SELECT\n    [sales___ordered_at] AS [sales___ordered_at],\n    [sales___hi_rev_sum] AS [sales___hi_rev_sum],\n    SUM(CASE WHEN [sales___hi_rev_sum] IS NULL THEN 0 ELSE 1 END) OVER (ORDER BY [sales___ordered_at] ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW) AS [_cp_reset_sales___streak]\n  FROM base\n), cp_value_streak AS (\n  SELECT\n    [sales___ordered_at] AS [sales___ordered_at],\n    [sales___hi_rev_sum] AS [sales___hi_rev_sum],\n    CASE\n      WHEN [sales___hi_rev_sum] IS NULL\n      THEN SUM(CASE WHEN [sales___hi_rev_sum] IS NULL THEN 1 ELSE 0 END) OVER (\n        PARTITION BY [_cp_reset_sales___streak]\n        ORDER BY [sales___ordered_at]\n        ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n      )\n      ELSE 0\n    END AS [sales___streak]\n  FROM cp_reset_streak\n)\nSELECT\n  [sales___ordered_at],\n  [sales___streak]\nFROM (\n  SELECT\n    [sales___ordered_at],\n    [sales___hi_rev_sum],\n    [sales___streak]\n  FROM cp_value_streak\n) AS _outer",
  "cp/negated_in::bigquery": "WITH base AS (\n  SELECT\n    sales.store AS `sales___store`,\n    DATE_TRUNC(sales.ordered_at, MONTH) AS `sales___ordered_at`\n  FROM sales AS sales\n  GROUP BY\n    sales.store,\n    DATE_TRUNC(sales.ordered_at, MONTH)\n), cp_reset_streak AS (\n  SELECT\n    `sales___store`,\n    `sales___ordered_at`,\n    SUM(CASE WHEN NOT `sales___store` IN ('B', 'C') THEN 0 ELSE 1 END) OVER (\n      PARTITION BY `sales___store`\n      ORDER BY `sales___ordered_at`\n      ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n    ) AS `_cp_reset_sales___streak`\n  FROM base\n), cp_value_streak AS (\n  SELECT\n    `sales___store`,\n    `sales___ordered_at`,\n    CASE\n      WHEN NOT `sales___store` IN ('B', 'C')\n      THEN SUM(CASE WHEN NOT `sales___store` IN ('B', 'C') THEN 1 ELSE 0 END) OVER (\n        PARTITION BY `sales___store`, `_cp_reset_sales___streak`\n        ORDER BY `sales___ordered_at`\n        ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n      )\n      ELSE 0\n    END AS `sales___streak`\n  FROM cp_reset_streak\n)\nSELECT\n  `sales___store`,\n  `sales___ordered_at`,\n  `sales___streak`\nFROM (\n  SELECT\n    `sales___store`,\n    `sales___ordered_at`,\n    `sales___streak`\n  FROM cp_value_streak\n) AS _outer",
  "cp/negated_in::duckdb": "WITH base AS (\n  SELECT\n    sales.store AS \"sales.store\",\n    DATE_TRUNC('MONTH', sales.ordered_at) AS \"sales.ordered_at\"\n  FROM sales AS sales\n  GROUP BY\n    sales.store,\n    DATE_TRUNC('MONTH', sales.ordered_at)\n), cp_reset_streak AS (\n  SELECT\n    \"sales.store\",\n    \"sales.ordered_at\",\n    SUM(CASE WHEN NOT \"sales.store\" IN ('B', 'C') THEN 0 ELSE 1 END) OVER (\n      PARTITION BY \"sales.store\"\n      ORDER BY \"sales.ordered_at\"\n      ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n    ) AS \"_cp_reset_sales.streak\"\n  FROM base\n), cp_value_streak AS (\n  SELECT\n    \"sales.store\",\n    \"sales.ordered_at\",\n    CASE\n      WHEN NOT \"sales.store\" IN ('B', 'C')\n      THEN SUM(CASE WHEN NOT \"sales.store\" IN ('B', 'C') THEN 1 ELSE 0 END) OVER (\n        PARTITION BY \"sales.store\", \"_cp_reset_sales.streak\"\n        ORDER BY \"sales.ordered_at\"\n        ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n      )\n      ELSE 0\n    END AS \"sales.streak\"\n  FROM cp_reset_streak\n)\nSELECT\n  \"sales.store\",\n  \"sales.ordered_at\",\n  \"sales.streak\"\nFROM (\n  SELECT\n    \"sales.store\",\n    \"sales.ordered_at\",\n    \"sales.streak\"\n  FROM cp_value_streak\n) AS _outer",
  "cp/negated_in::postgres": "WITH base AS (\n  SELECT\n    sales.store AS \"sales.store\",\n    DATE_TRUNC('MONTH', sales.ordered_at) AS \"sales.ordered_at\"\n  FROM sales AS sales\n  GROUP BY\n    sales.store,\n    DATE_TRUNC('MONTH', sales.ordered_at)\n), cp_reset_streak AS (\n  SELECT\n    \"sales.store\",\n    \"sales.ordered_at\",\n    SUM(CASE WHEN NOT \"sales.store\" IN ('B', 'C') THEN 0 ELSE 1 END) OVER (\n      PARTITION BY \"sales.store\"\n      ORDER BY \"sales.ordered_at\"\n      ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n    ) AS \"_cp_reset_sales.streak\"\n  FROM base\n), cp_value_streak AS (\n  SELECT\n    \"sales.store\",\n    \"sales.ordered_at\",\n    CASE\n      WHEN NOT \"sales.store\" IN ('B', 'C')\n      THEN SUM(CASE WHEN NOT \"sales.store\" IN ('B', 'C') THEN 1 ELSE 0 END) OVER (\n        PARTITION BY \"sales.store\", \"_cp_reset_sales.streak\"\n        ORDER BY \"sales.ordered_at\"\n        ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n      )\n      ELSE 0\n    END AS \"sales.streak\"\n  FROM cp_reset_streak\n)\nSELECT\n  \"sales.store\",\n  \"sales.ordered_at\",\n  \"sales.streak\"\nFROM (\n  SELECT\n    \"sales.store\",\n    \"sales.ordered_at\",\n    \"sales.streak\"\n  FROM cp_value_streak\n) AS _outer",
  "cp/negated_in::sqlite": "WITH base AS (\n  SELECT\n    sales.store AS \"sales.store\",\n    STRFTIME('%Y-%m-01', sales.ordered_at) AS \"sales.ordered_at\"\n  FROM sales AS sales\n  GROUP BY\n    sales.store,\n    STRFTIME('%Y-%m-01', sales.ordered_at)\n), cp_reset_streak AS (\n  SELECT\n    \"sales.store\",\n    \"sales.ordered_at\",\n    SUM(CASE WHEN NOT \"sales.store\" IN ('B', 'C') THEN 0 ELSE 1 END) OVER (\n      PARTITION BY \"sales.store\"\n      ORDER BY \"sales.ordered_at\"\n      ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n    ) AS \"_cp_reset_sales.streak\"\n  FROM base\n), cp_value_streak AS (\n  SELECT\n    \"sales.store\",\n    \"sales.ordered_at\",\n    CASE\n      WHEN NOT \"sales.store\" IN ('B', 'C')\n      THEN SUM(CASE WHEN NOT \"sales.store\" IN ('B', 'C') THEN 1 ELSE 0 END) OVER (\n        PARTITION BY \"sales.store\", \"_cp_reset_sales.streak\"\n        ORDER BY \"sales.ordered_at\"\n        ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n      )\n      ELSE 0\n    END AS \"sales.streak\"\n  FROM cp_reset_streak\n)\nSELECT\n  \"sales.store\",\n  \"sales.ordered_at\",\n  \"sales.streak\"\nFROM (\n  SELECT\n    \"sales.store\",\n    \"sales.ordered_at\",\n    \"sales.streak\"\n  FROM cp_value_streak\n) AS _outer",
  "cp/negated_in::tsql": "WITH base AS (\n  SELECT\n    sales.store AS [sales___store],\n    DATETRUNC(month, sales.ordered_at) AS [sales___ordered_at]\n  FROM sales AS sales\n  GROUP BY\n    sales.store,\n    DATETRUNC(month, sales.ordered_at)\n), cp_reset_streak AS (\n  SELECT\n    [sales___store] AS [sales___store],\n    [sales___ordered_at] AS [sales___ordered_at],\n    SUM(CASE WHEN NOT [sales___store] IN ('B', 'C') THEN 0 ELSE 1 END) OVER (\n      PARTITION BY [sales___store]\n      ORDER BY [sales___ordered_at]\n      ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n    ) AS [_cp_reset_sales___streak]\n  FROM base\n), cp_value_streak AS (\n  SELECT\n    [sales___store] AS [sales___store],\n    [sales___ordered_at] AS [sales___ordered_at],\n    CASE\n      WHEN NOT [sales___store] IN ('B', 'C')\n      THEN SUM(CASE WHEN NOT [sales___store] IN ('B', 'C') THEN 1 ELSE 0 END) OVER (\n        PARTITION BY [sales___store], [_cp_reset_sales___streak]\n        ORDER BY [sales___ordered_at]\n        ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n      )\n      ELSE 0\n    END AS [sales___streak]\n  FROM cp_reset_streak\n)\nSELECT\n  [sales___store],\n  [sales___ordered_at],\n  [sales___streak]\nFROM (\n  SELECT\n    [sales___store],\n    [sales___ordered_at],\n    [sales___streak]\n  FROM cp_value_streak\n) AS _outer",
  "cp/nested_in_and::bigquery": "WITH base AS (\n  SELECT\n    sales.store AS `sales___store`,\n    DATE_TRUNC(sales.ordered_at, MONTH) AS `sales___ordered_at`,\n    SUM(sales.revenue) AS `sales___revenue_sum`\n  FROM sales AS sales\n  GROUP BY\n    sales.store,\n    DATE_TRUNC(sales.ordered_at, MONTH)\n), cp_reset_streak AS (\n  SELECT\n    `sales___store`,\n    `sales___ordered_at`,\n    `sales___revenue_sum`,\n    SUM(\n      CASE WHEN `sales___store` IN ('A', 'C') AND `sales___revenue_sum` > 0 THEN 0 ELSE 1 END\n    ) OVER (\n      PARTITION BY `sales___store`\n      ORDER BY `sales___ordered_at`\n      ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n    ) AS `_cp_reset_sales___streak`\n  FROM base\n), cp_value_streak AS (\n  SELECT\n    `sales___store`,\n    `sales___ordered_at`,\n    `sales___revenue_sum`,\n    CASE\n      WHEN `sales___store` IN ('A', 'C') AND `sales___revenue_sum` > 0\n      THEN SUM(\n        CASE WHEN `sales___store` IN ('A', 'C') AND `sales___revenue_sum` > 0 THEN 1 ELSE 0 END\n      ) OVER (\n        PARTITION BY `sales___store`, `_cp_reset_sales___streak`\n        ORDER BY `sales___ordered_at`\n        ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n      )\n      ELSE 0\n    END AS `sales___streak`\n  FROM cp_reset_streak\n)\nSELECT\n  `sales___store`,\n  `sales___ordered_at`,\n  `sales___streak`\nFROM (\n  SELECT\n    `sales___store`,\n    `sales___ordered_at`,\n    `sales___revenue_sum`,\n    `sales___streak`\n  FROM cp_value_streak\n) AS _outer",
  "cp/nested_in_and::duckdb": "WITH base AS (\n  SELECT\n    sales.store AS \"sales.store\",\n    DATE_TRUNC('MONTH', sales.ordered_at) AS \"sales.ordered_at\",\n    SUM(sales.revenue) AS \"sales.revenue_sum\"\n  FROM sales AS sales\n  GROUP BY\n    sales.store,\n    DATE_TRUNC('MONTH', sales.ordered_at)\n), cp_reset_streak AS (\n  SELECT\n    \"sales.store\",\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    SUM(\n      CASE WHEN \"sales.store\" IN ('A', 'C') AND \"sales.revenue_sum\" > 0 THEN 0 ELSE 1 END\n    ) OVER (\n      PARTITION BY \"sales.store\"\n      ORDER BY \"sales.ordered_at\"\n      ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n    ) AS \"_cp_reset_sales.streak\"\n  FROM base\n), cp_value_streak AS (\n  SELECT\n    \"sales.store\",\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    CASE\n      WHEN \"sales.store\" IN ('A', 'C') AND \"sales.revenue_sum\" > 0\n      THEN SUM(\n        CASE WHEN \"sales.store\" IN ('A', 'C') AND \"sales.revenue_sum\" > 0 THEN 1 ELSE 0 END\n      ) OVER (\n        PARTITION BY \"sales.store\", \"_cp_reset_sales.streak\"\n        ORDER BY \"sales.ordered_at\"\n        ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n      )\n      ELSE 0\n    END AS \"sales.streak\"\n  FROM cp_reset_streak\n)\nSELECT\n  \"sales.store\",\n  \"sales.ordered_at\",\n  \"sales.streak\"\nFROM (\n  SELECT\n    \"sales.store\",\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    \"sales.streak\"\n  FROM cp_value_streak\n) AS _outer",
  "cp/nested_in_and::postgres": "WITH base AS (\n  SELECT\n    sales.store AS \"sales.store\",\n    DATE_TRUNC('MONTH', sales.ordered_at) AS \"sales.ordered_at\",\n    SUM(sales.revenue) AS \"sales.revenue_sum\"\n  FROM sales AS sales\n  GROUP BY\n    sales.store,\n    DATE_TRUNC('MONTH', sales.ordered_at)\n), cp_reset_streak AS (\n  SELECT\n    \"sales.store\",\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    SUM(\n      CASE WHEN \"sales.store\" IN ('A', 'C') AND \"sales.revenue_sum\" > 0 THEN 0 ELSE 1 END\n    ) OVER (\n      PARTITION BY \"sales.store\"\n      ORDER BY \"sales.ordered_at\"\n      ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n    ) AS \"_cp_reset_sales.streak\"\n  FROM base\n), cp_value_streak AS (\n  SELECT\n    \"sales.store\",\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    CASE\n      WHEN \"sales.store\" IN ('A', 'C') AND \"sales.revenue_sum\" > 0\n      THEN SUM(\n        CASE WHEN \"sales.store\" IN ('A', 'C') AND \"sales.revenue_sum\" > 0 THEN 1 ELSE 0 END\n      ) OVER (\n        PARTITION BY \"sales.store\", \"_cp_reset_sales.streak\"\n        ORDER BY \"sales.ordered_at\"\n        ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n      )\n      ELSE 0\n    END AS \"sales.streak\"\n  FROM cp_reset_streak\n)\nSELECT\n  \"sales.store\",\n  \"sales.ordered_at\",\n  \"sales.streak\"\nFROM (\n  SELECT\n    \"sales.store\",\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    \"sales.streak\"\n  FROM cp_value_streak\n) AS _outer",
  "cp/nested_in_and::sqlite": "WITH base AS (\n  SELECT\n    sales.store AS \"sales.store\",\n    STRFTIME('%Y-%m-01', sales.ordered_at) AS \"sales.ordered_at\",\n    SUM(sales.revenue) AS \"sales.revenue_sum\"\n  FROM sales AS sales\n  GROUP BY\n    sales.store,\n    STRFTIME('%Y-%m-01', sales.ordered_at)\n), cp_reset_streak AS (\n  SELECT\n    \"sales.store\",\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    SUM(\n      CASE WHEN \"sales.store\" IN ('A', 'C') AND \"sales.revenue_sum\" > 0 THEN 0 ELSE 1 END\n    ) OVER (\n      PARTITION BY \"sales.store\"\n      ORDER BY \"sales.ordered_at\"\n      ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n    ) AS \"_cp_reset_sales.streak\"\n  FROM base\n), cp_value_streak AS (\n  SELECT\n    \"sales.store\",\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    CASE\n      WHEN \"sales.store\" IN ('A', 'C') AND \"sales.revenue_sum\" > 0\n      THEN SUM(\n        CASE WHEN \"sales.store\" IN ('A', 'C') AND \"sales.revenue_sum\" > 0 THEN 1 ELSE 0 END\n      ) OVER (\n        PARTITION BY \"sales.store\", \"_cp_reset_sales.streak\"\n        ORDER BY \"sales.ordered_at\"\n        ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n      )\n      ELSE 0\n    END AS \"sales.streak\"\n  FROM cp_reset_streak\n)\nSELECT\n  \"sales.store\",\n  \"sales.ordered_at\",\n  \"sales.streak\"\nFROM (\n  SELECT\n    \"sales.store\",\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    \"sales.streak\"\n  FROM cp_value_streak\n) AS _outer",
  "cp/nested_in_and::tsql": "WITH base AS (\n  SELECT\n    sales.store AS [sales___store],\n    DATETRUNC(month, sales.ordered_at) AS [sales___ordered_at],\n    SUM(sales.revenue) AS [sales___revenue_sum]\n  FROM sales AS sales\n  GROUP BY\n    sales.store,\n    DATETRUNC(month, sales.ordered_at)\n), cp_reset_streak AS (\n  SELECT\n    [sales___store] AS [sales___store],\n    [sales___ordered_at] AS [sales___ordered_at],\n    [sales___revenue_sum] AS [sales___revenue_sum],\n    SUM(\n      CASE WHEN [sales___store] IN ('A', 'C') AND [sales___revenue_sum] > 0 THEN 0 ELSE 1 END\n    ) OVER (\n      PARTITION BY [sales___store]\n      ORDER BY [sales___ordered_at]\n      ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n    ) AS [_cp_reset_sales___streak]\n  FROM base\n), cp_value_streak AS (\n  SELECT\n    [sales___store] AS [sales___store],\n    [sales___ordered_at] AS [sales___ordered_at],\n    [sales___revenue_sum] AS [sales___revenue_sum],\n    CASE\n      WHEN [sales___store] IN ('A', 'C') AND [sales___revenue_sum] > 0\n      THEN SUM(\n        CASE WHEN [sales___store] IN ('A', 'C') AND [sales___revenue_sum] > 0 THEN 1 ELSE 0 END\n      ) OVER (\n        PARTITION BY [sales___store], [_cp_reset_sales___streak]\n        ORDER BY [sales___ordered_at]\n        ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n      )\n      ELSE 0\n    END AS [sales___streak]\n  FROM cp_reset_streak\n)\nSELECT\n  [sales___store],\n  [sales___ordered_at],\n  [sales___streak]\nFROM (\n  SELECT\n    [sales___store],\n    [sales___ordered_at],\n    [sales___revenue_sum],\n    [sales___streak]\n  FROM cp_value_streak\n) AS _outer",
  "cp/not::bigquery": "WITH base AS (\n  SELECT\n    DATE_TRUNC(sales.ordered_at, MONTH) AS `sales___ordered_at`,\n    SUM(sales.revenue) AS `sales___revenue_sum`\n  FROM sales AS sales\n  GROUP BY\n    DATE_TRUNC(sales.ordered_at, MONTH)\n), cp_reset_streak AS (\n  SELECT\n    `sales___ordered_at`,\n    `sales___revenue_sum`,\n    SUM(CASE WHEN NOT `sales___revenue_sum` > 90 THEN 0 ELSE 1 END) OVER (ORDER BY `sales___ordered_at` ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW) AS `_cp_reset_sales___streak`\n  FROM base\n), cp_value_streak AS (\n  SELECT\n    `sales___ordered_at`,\n    `sales___revenue_sum`,\n    CASE\n      WHEN NOT `sales___revenue_sum` > 90\n      THEN SUM(CASE WHEN NOT `sales___revenue_sum` > 90 THEN 1 ELSE 0 END) OVER (\n        PARTITION BY `_cp_reset_sales___streak`\n        ORDER BY `sales___ordered_at`\n        ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n      )\n      ELSE 0\n    END AS `sales___streak`\n  FROM cp_reset_streak\n)\nSELECT\n  `sales___ordered_at`,\n  `sales___streak`\nFROM (\n  SELECT\n    `sales___ordered_at`,\n    `sales___revenue_sum`,\n    `sales___streak`\n  FROM cp_value_streak\n) AS _outer",
  "cp/not::duckdb": "WITH base AS (\n  SELECT\n    DATE_TRUNC('MONTH', sales.ordered_at) AS \"sales.ordered_at\",\n    SUM(sales.revenue) AS \"sales.revenue_sum\"\n  FROM sales AS sales\n  GROUP BY\n    DATE_TRUNC('MONTH', sales.ordered_at)\n), cp_reset_streak AS (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    SUM(CASE WHEN NOT \"sales.revenue_sum\" > 90 THEN 0 ELSE 1 END) OVER (ORDER BY \"sales.ordered_at\" ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW) AS \"_cp_reset_sales.streak\"\n  FROM base\n), cp_value_streak AS (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    CASE\n      WHEN NOT \"sales.revenue_sum\" > 90\n      THEN SUM(CASE WHEN NOT \"sales.revenue_sum\" > 90 THEN 1 ELSE 0 END) OVER (\n        PARTITION BY \"_cp_reset_sales.streak\"\n        ORDER BY \"sales.ordered_at\"\n        ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n      )\n      ELSE 0\n    END AS \"sales.streak\"\n  FROM cp_reset_streak\n)\nSELECT\n  \"sales.ordered_at\",\n  \"sales.streak\"\nFROM (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    \"sales.streak\"\n  FROM cp_value_streak\n) AS _outer",
  "cp/not::postgres": "WITH base AS (\n  SELECT\n    DATE_TRUNC('MONTH', sales.ordered_at) AS \"sales.ordered_at\",\n    SUM(sales.revenue) AS \"sales.revenue_sum\"\n  FROM sales AS sales\n  GROUP BY\n    DATE_TRUNC('MONTH', sales.ordered_at)\n), cp_reset_streak AS (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    SUM(CASE WHEN NOT \"sales.revenue_sum\" > 90 THEN 0 ELSE 1 END) OVER (ORDER BY \"sales.ordered_at\" ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW) AS \"_cp_reset_sales.streak\"\n  FROM base\n), cp_value_streak AS (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    CASE\n      WHEN NOT \"sales.revenue_sum\" > 90\n      THEN SUM(CASE WHEN NOT \"sales.revenue_sum\" > 90 THEN 1 ELSE 0 END) OVER (\n        PARTITION BY \"_cp_reset_sales.streak\"\n        ORDER BY \"sales.ordered_at\"\n        ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n      )\n      ELSE 0\n    END AS \"sales.streak\"\n  FROM cp_reset_streak\n)\nSELECT\n  \"sales.ordered_at\",\n  \"sales.streak\"\nFROM (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    \"sales.streak\"\n  FROM cp_value_streak\n) AS _outer",
  "cp/not::sqlite": "WITH base AS (\n  SELECT\n    STRFTIME('%Y-%m-01', sales.ordered_at) AS \"sales.ordered_at\",\n    SUM(sales.revenue) AS \"sales.revenue_sum\"\n  FROM sales AS sales\n  GROUP BY\n    STRFTIME('%Y-%m-01', sales.ordered_at)\n), cp_reset_streak AS (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    SUM(CASE WHEN NOT \"sales.revenue_sum\" > 90 THEN 0 ELSE 1 END) OVER (ORDER BY \"sales.ordered_at\" ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW) AS \"_cp_reset_sales.streak\"\n  FROM base\n), cp_value_streak AS (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    CASE\n      WHEN NOT \"sales.revenue_sum\" > 90\n      THEN SUM(CASE WHEN NOT \"sales.revenue_sum\" > 90 THEN 1 ELSE 0 END) OVER (\n        PARTITION BY \"_cp_reset_sales.streak\"\n        ORDER BY \"sales.ordered_at\"\n        ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n      )\n      ELSE 0\n    END AS \"sales.streak\"\n  FROM cp_reset_streak\n)\nSELECT\n  \"sales.ordered_at\",\n  \"sales.streak\"\nFROM (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    \"sales.streak\"\n  FROM cp_value_streak\n) AS _outer",
  "cp/not::tsql": "WITH base AS (\n  SELECT\n    DATETRUNC(month, sales.ordered_at) AS [sales___ordered_at],\n    SUM(sales.revenue) AS [sales___revenue_sum]\n  FROM sales AS sales\n  GROUP BY\n    DATETRUNC(month, sales.ordered_at)\n), cp_reset_streak AS (\n  SELECT\n    [sales___ordered_at] AS [sales___ordered_at],\n    [sales___revenue_sum] AS [sales___revenue_sum],\n    SUM(CASE WHEN NOT [sales___revenue_sum] > 90 THEN 0 ELSE 1 END) OVER (ORDER BY [sales___ordered_at] ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW) AS [_cp_reset_sales___streak]\n  FROM base\n), cp_value_streak AS (\n  SELECT\n    [sales___ordered_at] AS [sales___ordered_at],\n    [sales___revenue_sum] AS [sales___revenue_sum],\n    CASE\n      WHEN NOT [sales___revenue_sum] > 90\n      THEN SUM(CASE WHEN NOT [sales___revenue_sum] > 90 THEN 1 ELSE 0 END) OVER (\n        PARTITION BY [_cp_reset_sales___streak]\n        ORDER BY [sales___ordered_at]\n        ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n      )\n      ELSE 0\n    END AS [sales___streak]\n  FROM cp_reset_streak\n)\nSELECT\n  [sales___ordered_at],\n  [sales___streak]\nFROM (\n  SELECT\n    [sales___ordered_at],\n    [sales___revenue_sum],\n    [sales___streak]\n  FROM cp_value_streak\n) AS _outer",
  "cp/numeric_delta::bigquery": "WITH base AS (\n  SELECT\n    DATE_TRUNC(sales.ordered_at, MONTH) AS `sales___ordered_at`,\n    SUM(sales.revenue) AS `sales___revenue_sum`,\n    SUM(sales.cost) AS `sales___cost_sum`\n  FROM sales AS sales\n  GROUP BY\n    DATE_TRUNC(sales.ordered_at, MONTH)\n), cp_reset_streak AS (\n  SELECT\n    `sales___ordered_at`,\n    `sales___revenue_sum`,\n    `sales___cost_sum`,\n    SUM(\n      CASE\n        WHEN NOT `sales___revenue_sum` - `sales___cost_sum` IS NULL\n        AND `sales___revenue_sum` - `sales___cost_sum` <> 0\n        THEN 0\n        ELSE 1\n      END\n    ) OVER (ORDER BY `sales___ordered_at` ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW) AS `_cp_reset_sales___streak`\n  FROM base\n), cp_value_streak AS (\n  SELECT\n    `sales___ordered_at`,\n    `sales___revenue_sum`,\n    `sales___cost_sum`,\n    CASE\n      WHEN NOT `sales___revenue_sum` - `sales___cost_sum` IS NULL\n      AND `sales___revenue_sum` - `sales___cost_sum` <> 0\n      THEN SUM(\n        CASE\n          WHEN NOT `sales___revenue_sum` - `sales___cost_sum` IS NULL\n          AND `sales___revenue_sum` - `sales___cost_sum` <> 0\n          THEN 1\n          ELSE 0\n        END\n      ) OVER (\n        PARTITION BY `_cp_reset_sales___streak`\n        ORDER BY `sales___ordered_at`\n        ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n      )\n      ELSE 0\n    END AS `sales___streak`\n  FROM cp_reset_streak\n)\nSELECT\n  `sales___ordered_at`,\n  `sales___streak`\nFROM (\n  SELECT\n    `sales___ordered_at`,\n    `sales___revenue_sum`,\n    `sales___cost_sum`,\n    `sales___streak`\n  FROM cp_value_streak\n) AS _outer",
  "cp/numeric_delta::duckdb": "WITH base AS (\n  SELECT\n    DATE_TRUNC('MONTH', sales.ordered_at) AS \"sales.ordered_at\",\n    SUM(sales.revenue) AS \"sales.revenue_sum\",\n    SUM(sales.cost) AS \"sales.cost_sum\"\n  FROM sales AS sales\n  GROUP BY\n    DATE_TRUNC('MONTH', sales.ordered_at)\n), cp_reset_streak AS (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    \"sales.cost_sum\",\n    SUM(\n      CASE\n        WHEN NOT \"sales.revenue_sum\" - \"sales.cost_sum\" IS NULL\n        AND \"sales.revenue_sum\" - \"sales.cost_sum\" <> 0\n        THEN 0\n        ELSE 1\n      END\n    ) OVER (ORDER BY \"sales.ordered_at\" ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW) AS \"_cp_reset_sales.streak\"\n  FROM base\n), cp_value_streak AS (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    \"sales.cost_sum\",\n    CASE\n      WHEN NOT \"sales.revenue_sum\" - \"sales.cost_sum\" IS NULL\n      AND \"sales.revenue_sum\" - \"sales.cost_sum\" <> 0\n      THEN SUM(\n        CASE\n          WHEN NOT \"sales.revenue_sum\" - \"sales.cost_sum\" IS NULL\n          AND \"sales.revenue_sum\" - \"sales.cost_sum\" <> 0\n          THEN 1\n          ELSE 0\n        END\n      ) OVER (\n        PARTITION BY \"_cp_reset_sales.streak\"\n        ORDER BY \"sales.ordered_at\"\n        ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n      )\n      ELSE 0\n    END AS \"sales.streak\"\n  FROM cp_reset_streak\n)\nSELECT\n  \"sales.ordered_at\",\n  \"sales.streak\"\nFROM (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    \"sales.cost_sum\",\n    \"sales.streak\"\n  FROM cp_value_streak\n) AS _outer",
  "cp/numeric_delta::postgres": "WITH base AS (\n  SELECT\n    DATE_TRUNC('MONTH', sales.ordered_at) AS \"sales.ordered_at\",\n    SUM(sales.revenue) AS \"sales.revenue_sum\",\n    SUM(sales.cost) AS \"sales.cost_sum\"\n  FROM sales AS sales\n  GROUP BY\n    DATE_TRUNC('MONTH', sales.ordered_at)\n), cp_reset_streak AS (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    \"sales.cost_sum\",\n    SUM(\n      CASE\n        WHEN NOT \"sales.revenue_sum\" - \"sales.cost_sum\" IS NULL\n        AND \"sales.revenue_sum\" - \"sales.cost_sum\" <> 0\n        THEN 0\n        ELSE 1\n      END\n    ) OVER (ORDER BY \"sales.ordered_at\" ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW) AS \"_cp_reset_sales.streak\"\n  FROM base\n), cp_value_streak AS (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    \"sales.cost_sum\",\n    CASE\n      WHEN NOT \"sales.revenue_sum\" - \"sales.cost_sum\" IS NULL\n      AND \"sales.revenue_sum\" - \"sales.cost_sum\" <> 0\n      THEN SUM(\n        CASE\n          WHEN NOT \"sales.revenue_sum\" - \"sales.cost_sum\" IS NULL\n          AND \"sales.revenue_sum\" - \"sales.cost_sum\" <> 0\n          THEN 1\n          ELSE 0\n        END\n      ) OVER (\n        PARTITION BY \"_cp_reset_sales.streak\"\n        ORDER BY \"sales.ordered_at\"\n        ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n      )\n      ELSE 0\n    END AS \"sales.streak\"\n  FROM cp_reset_streak\n)\nSELECT\n  \"sales.ordered_at\",\n  \"sales.streak\"\nFROM (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    \"sales.cost_sum\",\n    \"sales.streak\"\n  FROM cp_value_streak\n) AS _outer",
  "cp/numeric_delta::sqlite": "WITH base AS (\n  SELECT\n    STRFTIME('%Y-%m-01', sales.ordered_at) AS \"sales.ordered_at\",\n    SUM(sales.revenue) AS \"sales.revenue_sum\",\n    SUM(sales.cost) AS \"sales.cost_sum\"\n  FROM sales AS sales\n  GROUP BY\n    STRFTIME('%Y-%m-01', sales.ordered_at)\n), cp_reset_streak AS (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    \"sales.cost_sum\",\n    SUM(\n      CASE\n        WHEN NOT \"sales.revenue_sum\" - \"sales.cost_sum\" IS NULL\n        AND \"sales.revenue_sum\" - \"sales.cost_sum\" <> 0\n        THEN 0\n        ELSE 1\n      END\n    ) OVER (ORDER BY \"sales.ordered_at\" ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW) AS \"_cp_reset_sales.streak\"\n  FROM base\n), cp_value_streak AS (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    \"sales.cost_sum\",\n    CASE\n      WHEN NOT \"sales.revenue_sum\" - \"sales.cost_sum\" IS NULL\n      AND \"sales.revenue_sum\" - \"sales.cost_sum\" <> 0\n      THEN SUM(\n        CASE\n          WHEN NOT \"sales.revenue_sum\" - \"sales.cost_sum\" IS NULL\n          AND \"sales.revenue_sum\" - \"sales.cost_sum\" <> 0\n          THEN 1\n          ELSE 0\n        END\n      ) OVER (\n        PARTITION BY \"_cp_reset_sales.streak\"\n        ORDER BY \"sales.ordered_at\"\n        ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n      )\n      ELSE 0\n    END AS \"sales.streak\"\n  FROM cp_reset_streak\n)\nSELECT\n  \"sales.ordered_at\",\n  \"sales.streak\"\nFROM (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    \"sales.cost_sum\",\n    \"sales.streak\"\n  FROM cp_value_streak\n) AS _outer",
  "cp/numeric_delta::tsql": "WITH base AS (\n  SELECT\n    DATETRUNC(month, sales.ordered_at) AS [sales___ordered_at],\n    SUM(sales.revenue) AS [sales___revenue_sum],\n    SUM(sales.cost) AS [sales___cost_sum]\n  FROM sales AS sales\n  GROUP BY\n    DATETRUNC(month, sales.ordered_at)\n), cp_reset_streak AS (\n  SELECT\n    [sales___ordered_at] AS [sales___ordered_at],\n    [sales___revenue_sum] AS [sales___revenue_sum],\n    [sales___cost_sum] AS [sales___cost_sum],\n    SUM(\n      CASE\n        WHEN NOT [sales___revenue_sum] - [sales___cost_sum] IS NULL\n        AND [sales___revenue_sum] - [sales___cost_sum] <> 0\n        THEN 0\n        ELSE 1\n      END\n    ) OVER (ORDER BY [sales___ordered_at] ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW) AS [_cp_reset_sales___streak]\n  FROM base\n), cp_value_streak AS (\n  SELECT\n    [sales___ordered_at] AS [sales___ordered_at],\n    [sales___revenue_sum] AS [sales___revenue_sum],\n    [sales___cost_sum] AS [sales___cost_sum],\n    CASE\n      WHEN NOT [sales___revenue_sum] - [sales___cost_sum] IS NULL\n      AND [sales___revenue_sum] - [sales___cost_sum] <> 0\n      THEN SUM(\n        CASE\n          WHEN NOT [sales___revenue_sum] - [sales___cost_sum] IS NULL\n          AND [sales___revenue_sum] - [sales___cost_sum] <> 0\n          THEN 1\n          ELSE 0\n        END\n      ) OVER (\n        PARTITION BY [_cp_reset_sales___streak]\n        ORDER BY [sales___ordered_at]\n        ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n      )\n      ELSE 0\n    END AS [sales___streak]\n  FROM cp_reset_streak\n)\nSELECT\n  [sales___ordered_at],\n  [sales___streak]\nFROM (\n  SELECT\n    [sales___ordered_at],\n    [sales___revenue_sum],\n    [sales___cost_sum],\n    [sales___streak]\n  FROM cp_value_streak\n) AS _outer",
  "cp/or::bigquery": "WITH base AS (\n  SELECT\n    DATE_TRUNC(sales.ordered_at, MONTH) AS `sales___ordered_at`,\n    SUM(sales.revenue) AS `sales___revenue_sum`,\n    SUM(sales.cost) AS `sales___cost_sum`\n  FROM sales AS sales\n  GROUP BY\n    DATE_TRUNC(sales.ordered_at, MONTH)\n), cp_reset_streak AS (\n  SELECT\n    `sales___ordered_at`,\n    `sales___revenue_sum`,\n    `sales___cost_sum`,\n    SUM(CASE WHEN `sales___revenue_sum` > 90 OR `sales___cost_sum` > 40 THEN 0 ELSE 1 END) OVER (ORDER BY `sales___ordered_at` ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW) AS `_cp_reset_sales___streak`\n  FROM base\n), cp_value_streak AS (\n  SELECT\n    `sales___ordered_at`,\n    `sales___revenue_sum`,\n    `sales___cost_sum`,\n    CASE\n      WHEN `sales___revenue_sum` > 90 OR `sales___cost_sum` > 40\n      THEN SUM(CASE WHEN `sales___revenue_sum` > 90 OR `sales___cost_sum` > 40 THEN 1 ELSE 0 END) OVER (\n        PARTITION BY `_cp_reset_sales___streak`\n        ORDER BY `sales___ordered_at`\n        ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n      )\n      ELSE 0\n    END AS `sales___streak`\n  FROM cp_reset_streak\n)\nSELECT\n  `sales___ordered_at`,\n  `sales___streak`\nFROM (\n  SELECT\n    `sales___ordered_at`,\n    `sales___revenue_sum`,\n    `sales___cost_sum`,\n    `sales___streak`\n  FROM cp_value_streak\n) AS _outer",
  "cp/or::duckdb": "WITH base AS (\n  SELECT\n    DATE_TRUNC('MONTH', sales.ordered_at) AS \"sales.ordered_at\",\n    SUM(sales.revenue) AS \"sales.revenue_sum\",\n    SUM(sales.cost) AS \"sales.cost_sum\"\n  FROM sales AS sales\n  GROUP BY\n    DATE_TRUNC('MONTH', sales.ordered_at)\n), cp_reset_streak AS (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    \"sales.cost_sum\",\n    SUM(CASE WHEN \"sales.revenue_sum\" > 90 OR \"sales.cost_sum\" > 40 THEN 0 ELSE 1 END) OVER (ORDER BY \"sales.ordered_at\" ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW) AS \"_cp_reset_sales.streak\"\n  FROM base\n), cp_value_streak AS (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    \"sales.cost_sum\",\n    CASE\n      WHEN \"sales.revenue_sum\" > 90 OR \"sales.cost_sum\" > 40\n      THEN SUM(CASE WHEN \"sales.revenue_sum\" > 90 OR \"sales.cost_sum\" > 40 THEN 1 ELSE 0 END) OVER (\n        PARTITION BY \"_cp_reset_sales.streak\"\n        ORDER BY \"sales.ordered_at\"\n        ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n      )\n      ELSE 0\n    END AS \"sales.streak\"\n  FROM cp_reset_streak\n)\nSELECT\n  \"sales.ordered_at\",\n  \"sales.streak\"\nFROM (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    \"sales.cost_sum\",\n    \"sales.streak\"\n  FROM cp_value_streak\n) AS _outer",
  "cp/or::postgres": "WITH base AS (\n  SELECT\n    DATE_TRUNC('MONTH', sales.ordered_at) AS \"sales.ordered_at\",\n    SUM(sales.revenue) AS \"sales.revenue_sum\",\n    SUM(sales.cost) AS \"sales.cost_sum\"\n  FROM sales AS sales\n  GROUP BY\n    DATE_TRUNC('MONTH', sales.ordered_at)\n), cp_reset_streak AS (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    \"sales.cost_sum\",\n    SUM(CASE WHEN \"sales.revenue_sum\" > 90 OR \"sales.cost_sum\" > 40 THEN 0 ELSE 1 END) OVER (ORDER BY \"sales.ordered_at\" ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW) AS \"_cp_reset_sales.streak\"\n  FROM base\n), cp_value_streak AS (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    \"sales.cost_sum\",\n    CASE\n      WHEN \"sales.revenue_sum\" > 90 OR \"sales.cost_sum\" > 40\n      THEN SUM(CASE WHEN \"sales.revenue_sum\" > 90 OR \"sales.cost_sum\" > 40 THEN 1 ELSE 0 END) OVER (\n        PARTITION BY \"_cp_reset_sales.streak\"\n        ORDER BY \"sales.ordered_at\"\n        ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n      )\n      ELSE 0\n    END AS \"sales.streak\"\n  FROM cp_reset_streak\n)\nSELECT\n  \"sales.ordered_at\",\n  \"sales.streak\"\nFROM (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    \"sales.cost_sum\",\n    \"sales.streak\"\n  FROM cp_value_streak\n) AS _outer",
  "cp/or::sqlite": "WITH base AS (\n  SELECT\n    STRFTIME('%Y-%m-01', sales.ordered_at) AS \"sales.ordered_at\",\n    SUM(sales.revenue) AS \"sales.revenue_sum\",\n    SUM(sales.cost) AS \"sales.cost_sum\"\n  FROM sales AS sales\n  GROUP BY\n    STRFTIME('%Y-%m-01', sales.ordered_at)\n), cp_reset_streak AS (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    \"sales.cost_sum\",\n    SUM(CASE WHEN \"sales.revenue_sum\" > 90 OR \"sales.cost_sum\" > 40 THEN 0 ELSE 1 END) OVER (ORDER BY \"sales.ordered_at\" ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW) AS \"_cp_reset_sales.streak\"\n  FROM base\n), cp_value_streak AS (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    \"sales.cost_sum\",\n    CASE\n      WHEN \"sales.revenue_sum\" > 90 OR \"sales.cost_sum\" > 40\n      THEN SUM(CASE WHEN \"sales.revenue_sum\" > 90 OR \"sales.cost_sum\" > 40 THEN 1 ELSE 0 END) OVER (\n        PARTITION BY \"_cp_reset_sales.streak\"\n        ORDER BY \"sales.ordered_at\"\n        ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n      )\n      ELSE 0\n    END AS \"sales.streak\"\n  FROM cp_reset_streak\n)\nSELECT\n  \"sales.ordered_at\",\n  \"sales.streak\"\nFROM (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    \"sales.cost_sum\",\n    \"sales.streak\"\n  FROM cp_value_streak\n) AS _outer",
  "cp/or::tsql": "WITH base AS (\n  SELECT\n    DATETRUNC(month, sales.ordered_at) AS [sales___ordered_at],\n    SUM(sales.revenue) AS [sales___revenue_sum],\n    SUM(sales.cost) AS [sales___cost_sum]\n  FROM sales AS sales\n  GROUP BY\n    DATETRUNC(month, sales.ordered_at)\n), cp_reset_streak AS (\n  SELECT\n    [sales___ordered_at] AS [sales___ordered_at],\n    [sales___revenue_sum] AS [sales___revenue_sum],\n    [sales___cost_sum] AS [sales___cost_sum],\n    SUM(CASE WHEN [sales___revenue_sum] > 90 OR [sales___cost_sum] > 40 THEN 0 ELSE 1 END) OVER (ORDER BY [sales___ordered_at] ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW) AS [_cp_reset_sales___streak]\n  FROM base\n), cp_value_streak AS (\n  SELECT\n    [sales___ordered_at] AS [sales___ordered_at],\n    [sales___revenue_sum] AS [sales___revenue_sum],\n    [sales___cost_sum] AS [sales___cost_sum],\n    CASE\n      WHEN [sales___revenue_sum] > 90 OR [sales___cost_sum] > 40\n      THEN SUM(CASE WHEN [sales___revenue_sum] > 90 OR [sales___cost_sum] > 40 THEN 1 ELSE 0 END) OVER (\n        PARTITION BY [_cp_reset_sales___streak]\n        ORDER BY [sales___ordered_at]\n        ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n      )\n      ELSE 0\n    END AS [sales___streak]\n  FROM cp_reset_streak\n)\nSELECT\n  [sales___ordered_at],\n  [sales___streak]\nFROM (\n  SELECT\n    [sales___ordered_at],\n    [sales___revenue_sum],\n    [sales___cost_sum],\n    [sales___streak]\n  FROM cp_value_streak\n) AS _outer",
  "reject/cp_boolean_numeric::bigquery": {
    "error": "ValueError",
    "message": "'consecutive_periods': a boolean-shaped predicate cannot appear in a value position (arithmetic operand, scalar-call argument, or IN / BETWEEN operand); only iif's condition and the top-level predicate accept a boolean. Got ArithmeticKey."
  },
  "reject/cp_boolean_numeric::duckdb": {
    "error": "ValueError",
    "message": "'consecutive_periods': a boolean-shaped predicate cannot appear in a value position (arithmetic operand, scalar-call argument, or IN / BETWEEN operand); only iif's condition and the top-level predicate accept a boolean. Got ArithmeticKey."
  },
  "reject/cp_boolean_numeric::postgres": {
    "error": "ValueError",
    "message": "'consecutive_periods': a boolean-shaped predicate cannot appear in a value position (arithmetic operand, scalar-call argument, or IN / BETWEEN operand); only iif's condition and the top-level predicate accept a boolean. Got ArithmeticKey."
  },
  "reject/cp_boolean_numeric::sqlite": {
    "error": "ValueError",
    "message": "'consecutive_periods': a boolean-shaped predicate cannot appear in a value position (arithmetic operand, scalar-call argument, or IN / BETWEEN operand); only iif's condition and the top-level predicate accept a boolean. Got ArithmeticKey."
  },
  "reject/cp_boolean_numeric::tsql": {
    "error": "ValueError",
    "message": "'consecutive_periods': a boolean-shaped predicate cannot appear in a value position (arithmetic operand, scalar-call argument, or IN / BETWEEN operand); only iif's condition and the top-level predicate accept a boolean. Got ArithmeticKey."
  },
  "reject/cp_string_family::bigquery": {
    "error": "ValueError",
    "message": "'consecutive_periods' cannot use a string-valued predicate (lower(...)): a string has no truthiness. Compare it explicitly (e.g. `length(...) > 0`) to form a predicate."
  },
  "reject/cp_string_family::duckdb": {
    "error": "ValueError",
    "message": "'consecutive_periods' cannot use a string-valued predicate (lower(...)): a string has no truthiness. Compare it explicitly (e.g. `length(...) > 0`) to form a predicate."
  },
  "reject/cp_string_family::postgres": {
    "error": "ValueError",
    "message": "'consecutive_periods' cannot use a string-valued predicate (lower(...)): a string has no truthiness. Compare it explicitly (e.g. `length(...) > 0`) to form a predicate."
  },
  "reject/cp_string_family::sqlite": {
    "error": "ValueError",
    "message": "'consecutive_periods' cannot use a string-valued predicate (lower(...)): a string has no truthiness. Compare it explicitly (e.g. `length(...) > 0`) to form a predicate."
  },
  "reject/cp_string_family::tsql": {
    "error": "ValueError",
    "message": "'consecutive_periods' cannot use a string-valued predicate (lower(...)): a string has no truthiness. Compare it explicitly (e.g. `length(...) > 0`) to form a predicate."
  },
  "ts/change_pct_ratio::bigquery": "WITH base AS (\n  SELECT\n    sales.store AS `sales___store`,\n    DATE_TRUNC(sales.ordered_at, MONTH) AS `sales___ordered_at`,\n    SUM(sales.revenue) AS `sales___revenue_sum`,\n    COUNT(*) AS `sales____count`\n  FROM sales AS sales\n  GROUP BY\n    sales.store,\n    DATE_TRUNC(sales.ordered_at, MONTH)\n), shifted__time_shift_inner AS (\n  SELECT\n    sales.store AS `sales___store`,\n    DATE_TRUNC(sales.ordered_at, MONTH) AS `sales___ordered_at`,\n    SUM(sales.revenue) / COUNT(*) AS `sales___shifted`\n  FROM sales AS sales\n  GROUP BY\n    sales.store,\n    DATE_TRUNC(sales.ordered_at, MONTH)\n), sjoin__time_shift_inner AS (\n  SELECT\n    base.`sales___store`,\n    base.`sales___ordered_at`,\n    base.`sales___revenue_sum`,\n    base.`sales____count`,\n    shifted__time_shift_inner.`sales___shifted` AS `sales____time_shift_inner`\n  FROM base\n  LEFT JOIN shifted__time_shift_inner\n    ON base.`sales___store` IS NOT DISTINCT FROM shifted__time_shift_inner.`sales___store`\n    AND base.`sales___ordered_at` - INTERVAL 1 MONTH IS NOT DISTINCT FROM shifted__time_shift_inner.`sales___ordered_at`\n), step1 AS (\n  SELECT\n    `sales___store`,\n    `sales___ordered_at`,\n    `sales___revenue_sum`,\n    `sales____count`,\n    `sales____time_shift_inner`,\n    (\n      `sales___revenue_sum` / `sales____count` - `sales____time_shift_inner`\n    ) / NULLIF(`sales____time_shift_inner`, 0) AS `sales___pct`\n  FROM sjoin__time_shift_inner\n)\nSELECT\n  `sales___store`,\n  `sales___ordered_at`,\n  `sales___pct`\nFROM (\n  SELECT\n    `sales___store`,\n    `sales___ordered_at`,\n    `sales___revenue_sum`,\n    `sales____count`,\n    `sales____time_shift_inner`,\n    `sales___pct`\n  FROM step1\n) AS _outer",
  "ts/change_pct_ratio::duckdb": "WITH base AS (\n  SELECT\n    sales.store AS \"sales.store\",\n    DATE_TRUNC('MONTH', sales.ordered_at) AS \"sales.ordered_at\",\n    SUM(sales.revenue) AS \"sales.revenue_sum\",\n    COUNT(*) AS \"sales._count\"\n  FROM sales AS sales\n  GROUP BY\n    sales.store,\n    DATE_TRUNC('MONTH', sales.ordered_at)\n), shifted__time_shift_inner AS (\n  SELECT\n    sales.store AS \"sales.store\",\n    DATE_TRUNC('MONTH', sales.ordered_at) AS \"sales.ordered_at\",\n    SUM(sales.revenue) / COUNT(*) AS \"sales.shifted\"\n  FROM sales AS sales\n  GROUP BY\n    sales.store,\n    DATE_TRUNC('MONTH', sales.ordered_at)\n), sjoin__time_shift_inner AS (\n  SELECT\n    base.\"sales.store\",\n    base.\"sales.ordered_at\",\n    base.\"sales.revenue_sum\",\n    base.\"sales._count\",\n    shifted__time_shift_inner.\"sales.shifted\" AS \"sales._time_shift_inner\"\n  FROM base\n  LEFT JOIN shifted__time_shift_inner\n    ON base.\"sales.store\" IS NOT DISTINCT FROM shifted__time_shift_inner.\"sales.store\"\n    AND base.\"sales.ordered_at\" - INTERVAL 1 MONTH IS NOT DISTINCT FROM shifted__time_shift_inner.\"sales.ordered_at\"\n), step1 AS (\n  SELECT\n    \"sales.store\",\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    \"sales._count\",\n    \"sales._time_shift_inner\",\n    (\n      \"sales.revenue_sum\" / \"sales._count\" - \"sales._time_shift_inner\"\n    ) / NULLIF(\"sales._time_shift_inner\", 0) AS \"sales.pct\"\n  FROM sjoin__time_shift_inner\n)\nSELECT\n  \"sales.store\",\n  \"sales.ordered_at\",\n  \"sales.pct\"\nFROM (\n  SELECT\n    \"sales.store\",\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    \"sales._count\",\n    \"sales._time_shift_inner\",\n    \"sales.pct\"\n  FROM step1\n) AS _outer",
  "ts/change_pct_ratio::postgres": "WITH base AS (\n  SELECT\n    sales.store AS \"sales.store\",\n    DATE_TRUNC('MONTH', sales.ordered_at) AS \"sales.ordered_at\",\n    SUM(sales.revenue) AS \"sales.revenue_sum\",\n    COUNT(*) AS \"sales._count\"\n  FROM sales AS sales\n  GROUP BY\n    sales.store,\n    DATE_TRUNC('MONTH', sales.ordered_at)\n), shifted__time_shift_inner AS (\n  SELECT\n    sales.store AS \"sales.store\",\n    DATE_TRUNC('MONTH', sales.ordered_at) AS \"sales.ordered_at\",\n    CAST(SUM(sales.revenue) AS DOUBLE PRECISION) / COUNT(*) AS \"sales.shifted\"\n  FROM sales AS sales\n  GROUP BY\n    sales.store,\n    DATE_TRUNC('MONTH', sales.ordered_at)\n), sjoin__time_shift_inner AS (\n  SELECT\n    base.\"sales.store\",\n    base.\"sales.ordered_at\",\n    base.\"sales.revenue_sum\",\n    base.\"sales._count\",\n    shifted__time_shift_inner.\"sales.shifted\" AS \"sales._time_shift_inner\"\n  FROM base\n  LEFT JOIN shifted__time_shift_inner\n    ON base.\"sales.store\" IS NOT DISTINCT FROM shifted__time_shift_inner.\"sales.store\"\n    AND base.\"sales.ordered_at\" - INTERVAL '1 MONTH' IS NOT DISTINCT FROM shifted__time_shift_inner.\"sales.ordered_at\"\n), step1 AS (\n  SELECT\n    \"sales.store\",\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    \"sales._count\",\n    \"sales._time_shift_inner\",\n    CAST((\n      CAST(\"sales.revenue_sum\" AS DOUBLE PRECISION) / \"sales._count\" - \"sales._time_shift_inner\"\n    ) AS DOUBLE PRECISION) / NULLIF(\"sales._time_shift_inner\", 0) AS \"sales.pct\"\n  FROM sjoin__time_shift_inner\n)\nSELECT\n  \"sales.store\",\n  \"sales.ordered_at\",\n  \"sales.pct\"\nFROM (\n  SELECT\n    \"sales.store\",\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    \"sales._count\",\n    \"sales._time_shift_inner\",\n    \"sales.pct\"\n  FROM step1\n) AS _outer",
  "ts/change_pct_ratio::sqlite": "WITH base AS (\n  SELECT\n    sales.store AS \"sales.store\",\n    STRFTIME('%Y-%m-01', sales.ordered_at) AS \"sales.ordered_at\",\n    SUM(sales.revenue) AS \"sales.revenue_sum\",\n    COUNT(*) AS \"sales._count\"\n  FROM sales AS sales\n  GROUP BY\n    sales.store,\n    STRFTIME('%Y-%m-01', sales.ordered_at)\n), shifted__time_shift_inner AS (\n  SELECT\n    sales.store AS \"sales.store\",\n    STRFTIME('%Y-%m-01', sales.ordered_at) AS \"sales.ordered_at\",\n    CAST(SUM(sales.revenue) AS REAL) / COUNT(*) AS \"sales.shifted\"\n  FROM sales AS sales\n  GROUP BY\n    sales.store,\n    STRFTIME('%Y-%m-01', sales.ordered_at)\n), sjoin__time_shift_inner AS (\n  SELECT\n    base.\"sales.store\",\n    base.\"sales.ordered_at\",\n    base.\"sales.revenue_sum\",\n    base.\"sales._count\",\n    shifted__time_shift_inner.\"sales.shifted\" AS \"sales._time_shift_inner\"\n  FROM base\n  LEFT JOIN shifted__time_shift_inner\n    ON base.\"sales.store\" IS shifted__time_shift_inner.\"sales.store\"\n    AND DATE(base.\"sales.ordered_at\", '-1 months') IS shifted__time_shift_inner.\"sales.ordered_at\"\n), step1 AS (\n  SELECT\n    \"sales.store\",\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    \"sales._count\",\n    \"sales._time_shift_inner\",\n    CAST((\n      CAST(\"sales.revenue_sum\" AS REAL) / \"sales._count\" - \"sales._time_shift_inner\"\n    ) AS REAL) / NULLIF(\"sales._time_shift_inner\", 0) AS \"sales.pct\"\n  FROM sjoin__time_shift_inner\n)\nSELECT\n  \"sales.store\",\n  \"sales.ordered_at\",\n  \"sales.pct\"\nFROM (\n  SELECT\n    \"sales.store\",\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    \"sales._count\",\n    \"sales._time_shift_inner\",\n    \"sales.pct\"\n  FROM step1\n) AS _outer",
  "ts/change_pct_ratio::tsql": "WITH base AS (\n  SELECT\n    sales.store AS [sales___store],\n    DATETRUNC(month, sales.ordered_at) AS [sales___ordered_at],\n    SUM(sales.revenue) AS [sales___revenue_sum],\n    COUNT(*) AS [sales____count]\n  FROM sales AS sales\n  GROUP BY\n    sales.store,\n    DATETRUNC(month, sales.ordered_at)\n), shifted__time_shift_inner AS (\n  SELECT\n    sales.store AS [sales___store],\n    DATETRUNC(month, sales.ordered_at) AS [sales___ordered_at],\n    CAST(SUM(sales.revenue) AS FLOAT) / COUNT(*) AS [sales___shifted]\n  FROM sales AS sales\n  GROUP BY\n    sales.store,\n    DATETRUNC(month, sales.ordered_at)\n), sjoin__time_shift_inner AS (\n  SELECT\n    base.[sales___store] AS [sales___store],\n    base.[sales___ordered_at] AS [sales___ordered_at],\n    base.[sales___revenue_sum] AS [sales___revenue_sum],\n    base.[sales____count] AS [sales____count],\n    shifted__time_shift_inner.[sales___shifted] AS [sales____time_shift_inner]\n  FROM base\n  LEFT JOIN shifted__time_shift_inner\n    ON (\n      base.[sales___store] = shifted__time_shift_inner.[sales___store]\n      OR (\n        base.[sales___store] IS NULL AND shifted__time_shift_inner.[sales___store] IS NULL\n      )\n    )\n    AND (\n      DATEADD(MONTH, -1, base.[sales___ordered_at]) = shifted__time_shift_inner.[sales___ordered_at]\n      OR (\n        DATEADD(MONTH, -1, base.[sales___ordered_at]) IS NULL\n        AND shifted__time_shift_inner.[sales___ordered_at] IS NULL\n      )\n    )\n), step1 AS (\n  SELECT\n    [sales___store] AS [sales___store],\n    [sales___ordered_at] AS [sales___ordered_at],\n    [sales___revenue_sum] AS [sales___revenue_sum],\n    [sales____count] AS [sales____count],\n    [sales____time_shift_inner] AS [sales____time_shift_inner],\n    CAST((\n      CAST([sales___revenue_sum] AS FLOAT) / [sales____count] - [sales____time_shift_inner]\n    ) AS FLOAT) / NULLIF([sales____time_shift_inner], 0) AS [sales___pct]\n  FROM sjoin__time_shift_inner\n)\nSELECT\n  [sales___store],\n  [sales___ordered_at],\n  [sales___pct]\nFROM (\n  SELECT\n    [sales___store],\n    [sales___ordered_at],\n    [sales___revenue_sum],\n    [sales____count],\n    [sales____time_shift_inner],\n    [sales___pct]\n  FROM step1\n) AS _outer",
  "ts/coalesce_missing::bigquery": "WITH base AS (\n  SELECT\n    DATE_TRUNC(sales.ordered_at, MONTH) AS `sales___ordered_at`,\n    SUM(sales.revenue) AS `sales___revenue_sum`\n  FROM sales AS sales\n  GROUP BY\n    DATE_TRUNC(sales.ordered_at, MONTH)\n), shifted_prev AS (\n  SELECT\n    DATE_TRUNC(sales.ordered_at, MONTH) AS `sales___ordered_at`,\n    COALESCE(SUM(sales.revenue), 0) AS `sales___shifted`\n  FROM sales AS sales\n  GROUP BY\n    DATE_TRUNC(sales.ordered_at, MONTH)\n), sjoin_prev AS (\n  SELECT\n    base.`sales___ordered_at`,\n    base.`sales___revenue_sum`,\n    shifted_prev.`sales___shifted` AS `sales___prev`\n  FROM base\n  LEFT JOIN shifted_prev\n    ON base.`sales___ordered_at` - INTERVAL 1 MONTH IS NOT DISTINCT FROM shifted_prev.`sales___ordered_at`\n)\nSELECT\n  `sales___ordered_at`,\n  `sales___prev`\nFROM (\n  SELECT\n    `sales___ordered_at`,\n    `sales___revenue_sum`,\n    `sales___prev`\n  FROM sjoin_prev\n) AS _outer",
  "ts/coalesce_missing::duckdb": "WITH base AS (\n  SELECT\n    DATE_TRUNC('MONTH', sales.ordered_at) AS \"sales.ordered_at\",\n    SUM(sales.revenue) AS \"sales.revenue_sum\"\n  FROM sales AS sales\n  GROUP BY\n    DATE_TRUNC('MONTH', sales.ordered_at)\n), shifted_prev AS (\n  SELECT\n    DATE_TRUNC('MONTH', sales.ordered_at) AS \"sales.ordered_at\",\n    COALESCE(SUM(sales.revenue), 0) AS \"sales.shifted\"\n  FROM sales AS sales\n  GROUP BY\n    DATE_TRUNC('MONTH', sales.ordered_at)\n), sjoin_prev AS (\n  SELECT\n    base.\"sales.ordered_at\",\n    base.\"sales.revenue_sum\",\n    shifted_prev.\"sales.shifted\" AS \"sales.prev\"\n  FROM base\n  LEFT JOIN shifted_prev\n    ON base.\"sales.ordered_at\" - INTERVAL 1 MONTH IS NOT DISTINCT FROM shifted_prev.\"sales.ordered_at\"\n)\nSELECT\n  \"sales.ordered_at\",\n  \"sales.prev\"\nFROM (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    \"sales.prev\"\n  FROM sjoin_prev\n) AS _outer",
  "ts/coalesce_missing::postgres": "WITH base AS (\n  SELECT\n    DATE_TRUNC('MONTH', sales.ordered_at) AS \"sales.ordered_at\",\n    SUM(sales.revenue) AS \"sales.revenue_sum\"\n  FROM sales AS sales\n  GROUP BY\n    DATE_TRUNC('MONTH', sales.ordered_at)\n), shifted_prev AS (\n  SELECT\n    DATE_TRUNC('MONTH', sales.ordered_at) AS \"sales.ordered_at\",\n    COALESCE(SUM(sales.revenue), 0) AS \"sales.shifted\"\n  FROM sales AS sales\n  GROUP BY\n    DATE_TRUNC('MONTH', sales.ordered_at)\n), sjoin_prev AS (\n  SELECT\n    base.\"sales.ordered_at\",\n    base.\"sales.revenue_sum\",\n    shifted_prev.\"sales.shifted\" AS \"sales.prev\"\n  FROM base\n  LEFT JOIN shifted_prev\n    ON base.\"sales.ordered_at\" - INTERVAL '1 MONTH' IS NOT DISTINCT FROM shifted_prev.\"sales.ordered_at\"\n)\nSELECT\n  \"sales.ordered_at\",\n  \"sales.prev\"\nFROM (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    \"sales.prev\"\n  FROM sjoin_prev\n) AS _outer",
  "ts/coalesce_missing::sqlite": "WITH base AS (\n  SELECT\n    STRFTIME('%Y-%m-01', sales.ordered_at) AS \"sales.ordered_at\",\n    SUM(sales.revenue) AS \"sales.revenue_sum\"\n  FROM sales AS sales\n  GROUP BY\n    STRFTIME('%Y-%m-01', sales.ordered_at)\n), shifted_prev AS (\n  SELECT\n    STRFTIME('%Y-%m-01', sales.ordered_at) AS \"sales.ordered_at\",\n    COALESCE(SUM(sales.revenue), 0) AS \"sales.shifted\"\n  FROM sales AS sales\n  GROUP BY\n    STRFTIME('%Y-%m-01', sales.ordered_at)\n), sjoin_prev AS (\n  SELECT\n    base.\"sales.ordered_at\",\n    base.\"sales.revenue_sum\",\n    shifted_prev.\"sales.shifted\" AS \"sales.prev\"\n  FROM base\n  LEFT JOIN shifted_prev\n    ON DATE(base.\"sales.ordered_at\", '-1 months') IS shifted_prev.\"sales.ordered_at\"\n)\nSELECT\n  \"sales.ordered_at\",\n  \"sales.prev\"\nFROM (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    \"sales.prev\"\n  FROM sjoin_prev\n) AS _outer",
  "ts/coalesce_missing::tsql": "WITH base AS (\n  SELECT\n    DATETRUNC(month, sales.ordered_at) AS [sales___ordered_at],\n    SUM(sales.revenue) AS [sales___revenue_sum]\n  FROM sales AS sales\n  GROUP BY\n    DATETRUNC(month, sales.ordered_at)\n), shifted_prev AS (\n  SELECT\n    DATETRUNC(month, sales.ordered_at) AS [sales___ordered_at],\n    COALESCE(SUM(sales.revenue), 0) AS [sales___shifted]\n  FROM sales AS sales\n  GROUP BY\n    DATETRUNC(month, sales.ordered_at)\n), sjoin_prev AS (\n  SELECT\n    base.[sales___ordered_at] AS [sales___ordered_at],\n    base.[sales___revenue_sum] AS [sales___revenue_sum],\n    shifted_prev.[sales___shifted] AS [sales___prev]\n  FROM base\n  LEFT JOIN shifted_prev\n    ON (\n      DATEADD(MONTH, -1, base.[sales___ordered_at]) = shifted_prev.[sales___ordered_at]\n      OR (\n        DATEADD(MONTH, -1, base.[sales___ordered_at]) IS NULL\n        AND shifted_prev.[sales___ordered_at] IS NULL\n      )\n    )\n)\nSELECT\n  [sales___ordered_at],\n  [sales___prev]\nFROM (\n  SELECT\n    [sales___ordered_at],\n    [sales___revenue_sum],\n    [sales___prev]\n  FROM sjoin_prev\n) AS _outer",
  "ts/crossing_join::bigquery": "WITH _base AS (\n  SELECT\n    DATE_TRUNC(sales.ordered_at, MONTH) AS `sales___ordered_at`\n  FROM sales AS sales\n  GROUP BY\n    DATE_TRUNC(sales.ordered_at, MONTH)\n), _cm_sales__revenue_wrevenue_sum_f_regions_factor AS (\n  SELECT\n    DATE_TRUNC(sales.ordered_at, MONTH) AS `sales___ordered_at`,\n    CAST(SUM(sales.revenue * regions.factor) AS FLOAT64) AS `sales___revenue_wrevenue_sum_f_regions_factor`\n  FROM sales AS sales\n  LEFT JOIN regions AS regions\n    ON sales.region_id = regions.id\n  GROUP BY\n    DATE_TRUNC(sales.ordered_at, MONTH)\n), base AS (\n  SELECT\n    _base.`sales___ordered_at`,\n    _cm_sales__revenue_wrevenue_sum_f_regions_factor.`sales___revenue_wrevenue_sum_f_regions_factor` AS `sales___revenue_wrevenue_sum_f_regions_factor`\n  FROM _base\n  LEFT JOIN _cm_sales__revenue_wrevenue_sum_f_regions_factor\n    ON _base.`sales___ordered_at` IS NOT DISTINCT FROM _cm_sales__revenue_wrevenue_sum_f_regions_factor.`sales___ordered_at`\n), shifted_prev AS (\n  SELECT\n    DATE_TRUNC(sales.ordered_at, MONTH) AS `sales___ordered_at`,\n    SUM(sales.revenue * regions.factor) * 2 AS `sales___shifted`\n  FROM sales AS sales\n  LEFT JOIN regions AS regions\n    ON sales.region_id = regions.id\n  GROUP BY\n    DATE_TRUNC(sales.ordered_at, MONTH)\n), sjoin_prev AS (\n  SELECT\n    base.`sales___ordered_at`,\n    base.`sales___revenue_wrevenue_sum_f_regions_factor`,\n    shifted_prev.`sales___shifted` AS `sales___prev`\n  FROM base\n  LEFT JOIN shifted_prev\n    ON base.`sales___ordered_at` - INTERVAL 1 MONTH IS NOT DISTINCT FROM shifted_prev.`sales___ordered_at`\n)\nSELECT\n  `sales___ordered_at`,\n  `sales___prev`\nFROM (\n  SELECT\n    `sales___ordered_at`,\n    `sales___revenue_wrevenue_sum_f_regions_factor`,\n    `sales___prev`\n  FROM sjoin_prev\n) AS _outer",
  "ts/crossing_join::duckdb": "WITH _base AS (\n  SELECT\n    DATE_TRUNC('MONTH', sales.ordered_at) AS \"sales.ordered_at\"\n  FROM sales AS sales\n  GROUP BY\n    DATE_TRUNC('MONTH', sales.ordered_at)\n), _cm_sales__revenue_wrevenue_sum_f_regions_factor AS (\n  SELECT\n    DATE_TRUNC('MONTH', sales.ordered_at) AS \"sales.ordered_at\",\n    CAST(SUM(sales.revenue * regions.factor) AS DOUBLE) AS \"sales.revenue_wrevenue_sum_f_regions_factor\"\n  FROM sales AS sales\n  LEFT JOIN regions AS regions\n    ON sales.region_id = regions.id\n  GROUP BY\n    DATE_TRUNC('MONTH', sales.ordered_at)\n), base AS (\n  SELECT\n    _base.\"sales.ordered_at\",\n    _cm_sales__revenue_wrevenue_sum_f_regions_factor.\"sales.revenue_wrevenue_sum_f_regions_factor\" AS \"sales.revenue_wrevenue_sum_f_regions_factor\"\n  FROM _base\n  LEFT JOIN _cm_sales__revenue_wrevenue_sum_f_regions_factor\n    ON _base.\"sales.ordered_at\" IS NOT DISTINCT FROM _cm_sales__revenue_wrevenue_sum_f_regions_factor.\"sales.ordered_at\"\n), shifted_prev AS (\n  SELECT\n    DATE_TRUNC('MONTH', sales.ordered_at) AS \"sales.ordered_at\",\n    SUM(sales.revenue * regions.factor) * 2 AS \"sales.shifted\"\n  FROM sales AS sales\n  LEFT JOIN regions AS regions\n    ON sales.region_id = regions.id\n  GROUP BY\n    DATE_TRUNC('MONTH', sales.ordered_at)\n), sjoin_prev AS (\n  SELECT\n    base.\"sales.ordered_at\",\n    base.\"sales.revenue_wrevenue_sum_f_regions_factor\",\n    shifted_prev.\"sales.shifted\" AS \"sales.prev\"\n  FROM base\n  LEFT JOIN shifted_prev\n    ON base.\"sales.ordered_at\" - INTERVAL 1 MONTH IS NOT DISTINCT FROM shifted_prev.\"sales.ordered_at\"\n)\nSELECT\n  \"sales.ordered_at\",\n  \"sales.prev\"\nFROM (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.revenue_wrevenue_sum_f_regions_factor\",\n    \"sales.prev\"\n  FROM sjoin_prev\n) AS _outer",
  "ts/crossing_join::postgres": "WITH _base AS (\n  SELECT\n    DATE_TRUNC('MONTH', sales.ordered_at) AS \"sales.ordered_at\"\n  FROM sales AS sales\n  GROUP BY\n    DATE_TRUNC('MONTH', sales.ordered_at)\n), _cm_sales__revenue_wrevenue_sum_f_regions_factor AS (\n  SELECT\n    DATE_TRUNC('MONTH', sales.ordered_at) AS \"sales.ordered_at\",\n    CAST(SUM(sales.revenue * regions.factor) AS DOUBLE PRECISION) AS \"sales.revenue_wrevenue_sum_f_regions_factor\"\n  FROM sales AS sales\n  LEFT JOIN regions AS regions\n    ON sales.region_id = regions.id\n  GROUP BY\n    DATE_TRUNC('MONTH', sales.ordered_at)\n), base AS (\n  SELECT\n    _base.\"sales.ordered_at\",\n    _cm_sales__revenue_wrevenue_sum_f_regions_factor.\"sales.revenue_wrevenue_sum_f_regions_factor\" AS \"sales.revenue_wrevenue_sum_f_regions_factor\"\n  FROM _base\n  LEFT JOIN _cm_sales__revenue_wrevenue_sum_f_regions_factor\n    ON _base.\"sales.ordered_at\" IS NOT DISTINCT FROM _cm_sales__revenue_wrevenue_sum_f_regions_factor.\"sales.ordered_at\"\n), shifted_prev AS (\n  SELECT\n    DATE_TRUNC('MONTH', sales.ordered_at) AS \"sales.ordered_at\",\n    SUM(sales.revenue * regions.factor) * 2 AS \"sales.shifted\"\n  FROM sales AS sales\n  LEFT JOIN regions AS regions\n    ON sales.region_id = regions.id\n  GROUP BY\n    DATE_TRUNC('MONTH', sales.ordered_at)\n), sjoin_prev AS (\n  SELECT\n    base.\"sales.ordered_at\",\n    base.\"sales.revenue_wrevenue_sum_f_regions_factor\",\n    shifted_prev.\"sales.shifted\" AS \"sales.prev\"\n  FROM base\n  LEFT JOIN shifted_prev\n    ON base.\"sales.ordered_at\" - INTERVAL '1 MONTH' IS NOT DISTINCT FROM shifted_prev.\"sales.ordered_at\"\n)\nSELECT\n  \"sales.ordered_at\",\n  \"sales.prev\"\nFROM (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.revenue_wrevenue_sum_f_regions_factor\",\n    \"sales.prev\"\n  FROM sjoin_prev\n) AS _outer",
  "ts/crossing_join::sqlite": "WITH _base AS (\n  SELECT\n    STRFTIME('%Y-%m-01', sales.ordered_at) AS \"sales.ordered_at\"\n  FROM sales AS sales\n  GROUP BY\n    STRFTIME('%Y-%m-01', sales.ordered_at)\n), _cm_sales__revenue_wrevenue_sum_f_regions_factor AS (\n  SELECT\n    STRFTIME('%Y-%m-01', sales.ordered_at) AS \"sales.ordered_at\",\n    CAST(SUM(sales.revenue * regions.factor) AS REAL) AS \"sales.revenue_wrevenue_sum_f_regions_factor\"\n  FROM sales AS sales\n  LEFT JOIN regions AS regions\n    ON sales.region_id = regions.id\n  GROUP BY\n    STRFTIME('%Y-%m-01', sales.ordered_at)\n), base AS (\n  SELECT\n    _base.\"sales.ordered_at\",\n    _cm_sales__revenue_wrevenue_sum_f_regions_factor.\"sales.revenue_wrevenue_sum_f_regions_factor\" AS \"sales.revenue_wrevenue_sum_f_regions_factor\"\n  FROM _base\n  LEFT JOIN _cm_sales__revenue_wrevenue_sum_f_regions_factor\n    ON _base.\"sales.ordered_at\" IS _cm_sales__revenue_wrevenue_sum_f_regions_factor.\"sales.ordered_at\"\n), shifted_prev AS (\n  SELECT\n    STRFTIME('%Y-%m-01', sales.ordered_at) AS \"sales.ordered_at\",\n    SUM(sales.revenue * regions.factor) * 2 AS \"sales.shifted\"\n  FROM sales AS sales\n  LEFT JOIN regions AS regions\n    ON sales.region_id = regions.id\n  GROUP BY\n    STRFTIME('%Y-%m-01', sales.ordered_at)\n), sjoin_prev AS (\n  SELECT\n    base.\"sales.ordered_at\",\n    base.\"sales.revenue_wrevenue_sum_f_regions_factor\",\n    shifted_prev.\"sales.shifted\" AS \"sales.prev\"\n  FROM base\n  LEFT JOIN shifted_prev\n    ON DATE(base.\"sales.ordered_at\", '-1 months') IS shifted_prev.\"sales.ordered_at\"\n)\nSELECT\n  \"sales.ordered_at\",\n  \"sales.prev\"\nFROM (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.revenue_wrevenue_sum_f_regions_factor\",\n    \"sales.prev\"\n  FROM sjoin_prev\n) AS _outer",
  "ts/crossing_join::tsql": "WITH _base AS (\n  SELECT\n    DATETRUNC(month, sales.ordered_at) AS [sales___ordered_at]\n  FROM sales AS sales\n  GROUP BY\n    DATETRUNC(month, sales.ordered_at)\n), _cm_sales__revenue_wrevenue_sum_f_regions_factor AS (\n  SELECT\n    DATETRUNC(month, sales.ordered_at) AS [sales___ordered_at],\n    CAST(SUM(sales.revenue * regions.factor) AS FLOAT) AS [sales___revenue_wrevenue_sum_f_regions_factor]\n  FROM sales AS sales\n  LEFT JOIN regions AS regions\n    ON sales.region_id = regions.id\n  GROUP BY\n    DATETRUNC(month, sales.ordered_at)\n), base AS (\n  SELECT\n    _base.[sales___ordered_at] AS [sales___ordered_at],\n    _cm_sales__revenue_wrevenue_sum_f_regions_factor.[sales___revenue_wrevenue_sum_f_regions_factor] AS [sales___revenue_wrevenue_sum_f_regions_factor]\n  FROM _base\n  LEFT JOIN _cm_sales__revenue_wrevenue_sum_f_regions_factor\n    ON (\n      _base.[sales___ordered_at] = _cm_sales__revenue_wrevenue_sum_f_regions_factor.[sales___ordered_at]\n      OR (\n        _base.[sales___ordered_at] IS NULL\n        AND _cm_sales__revenue_wrevenue_sum_f_regions_factor.[sales___ordered_at] IS NULL\n      )\n    )\n), shifted_prev AS (\n  SELECT\n    DATETRUNC(month, sales.ordered_at) AS [sales___ordered_at],\n    SUM(sales.revenue * regions.factor) * 2 AS [sales___shifted]\n  FROM sales AS sales\n  LEFT JOIN regions AS regions\n    ON sales.region_id = regions.id\n  GROUP BY\n    DATETRUNC(month, sales.ordered_at)\n), sjoin_prev AS (\n  SELECT\n    base.[sales___ordered_at] AS [sales___ordered_at],\n    base.[sales___revenue_wrevenue_sum_f_regions_factor] AS [sales___revenue_wrevenue_sum_f_regions_factor],\n    shifted_prev.[sales___shifted] AS [sales___prev]\n  FROM base\n  LEFT JOIN shifted_prev\n    ON (\n      DATEADD(MONTH, -1, base.[sales___ordered_at]) = shifted_prev.[sales___ordered_at]\n      OR (\n        DATEADD(MONTH, -1, base.[sales___ordered_at]) IS NULL\n        AND shifted_prev.[sales___ordered_at] IS NULL\n      )\n    )\n)\nSELECT\n  [sales___ordered_at],\n  [sales___prev]\nFROM (\n  SELECT\n    [sales___ordered_at],\n    [sales___revenue_wrevenue_sum_f_regions_factor],\n    [sales___prev]\n  FROM sjoin_prev\n) AS _outer",
  "ts/ratio_by_store::bigquery": "WITH base AS (\n  SELECT\n    sales.store AS `sales___store`,\n    DATE_TRUNC(sales.ordered_at, MONTH) AS `sales___ordered_at`,\n    SUM(sales.revenue) AS `sales___revenue_sum`,\n    SUM(sales.qty) AS `sales___qty_sum`\n  FROM sales AS sales\n  GROUP BY\n    sales.store,\n    DATE_TRUNC(sales.ordered_at, MONTH)\n), shifted_prev AS (\n  SELECT\n    sales.store AS `sales___store`,\n    DATE_TRUNC(sales.ordered_at, MONTH) AS `sales___ordered_at`,\n    SUM(sales.revenue) / SUM(sales.qty) AS `sales___shifted`\n  FROM sales AS sales\n  GROUP BY\n    sales.store,\n    DATE_TRUNC(sales.ordered_at, MONTH)\n), sjoin_prev AS (\n  SELECT\n    base.`sales___store`,\n    base.`sales___ordered_at`,\n    base.`sales___revenue_sum`,\n    base.`sales___qty_sum`,\n    shifted_prev.`sales___shifted` AS `sales___prev`\n  FROM base\n  LEFT JOIN shifted_prev\n    ON base.`sales___store` IS NOT DISTINCT FROM shifted_prev.`sales___store`\n    AND base.`sales___ordered_at` - INTERVAL 1 MONTH IS NOT DISTINCT FROM shifted_prev.`sales___ordered_at`\n)\nSELECT\n  `sales___store`,\n  `sales___ordered_at`,\n  `sales___prev`\nFROM (\n  SELECT\n    `sales___store`,\n    `sales___ordered_at`,\n    `sales___revenue_sum`,\n    `sales___qty_sum`,\n    `sales___prev`\n  FROM sjoin_prev\n) AS _outer",
  "ts/ratio_by_store::duckdb": "WITH base AS (\n  SELECT\n    sales.store AS \"sales.store\",\n    DATE_TRUNC('MONTH', sales.ordered_at) AS \"sales.ordered_at\",\n    SUM(sales.revenue) AS \"sales.revenue_sum\",\n    SUM(sales.qty) AS \"sales.qty_sum\"\n  FROM sales AS sales\n  GROUP BY\n    sales.store,\n    DATE_TRUNC('MONTH', sales.ordered_at)\n), shifted_prev AS (\n  SELECT\n    sales.store AS \"sales.store\",\n    DATE_TRUNC('MONTH', sales.ordered_at) AS \"sales.ordered_at\",\n    SUM(sales.revenue) / SUM(sales.qty) AS \"sales.shifted\"\n  FROM sales AS sales\n  GROUP BY\n    sales.store,\n    DATE_TRUNC('MONTH', sales.ordered_at)\n), sjoin_prev AS (\n  SELECT\n    base.\"sales.store\",\n    base.\"sales.ordered_at\",\n    base.\"sales.revenue_sum\",\n    base.\"sales.qty_sum\",\n    shifted_prev.\"sales.shifted\" AS \"sales.prev\"\n  FROM base\n  LEFT JOIN shifted_prev\n    ON base.\"sales.store\" IS NOT DISTINCT FROM shifted_prev.\"sales.store\"\n    AND base.\"sales.ordered_at\" - INTERVAL 1 MONTH IS NOT DISTINCT FROM shifted_prev.\"sales.ordered_at\"\n)\nSELECT\n  \"sales.store\",\n  \"sales.ordered_at\",\n  \"sales.prev\"\nFROM (\n  SELECT\n    \"sales.store\",\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    \"sales.qty_sum\",\n    \"sales.prev\"\n  FROM sjoin_prev\n) AS _outer",
  "ts/ratio_by_store::postgres": "WITH base AS (\n  SELECT\n    sales.store AS \"sales.store\",\n    DATE_TRUNC('MONTH', sales.ordered_at) AS \"sales.ordered_at\",\n    SUM(sales.revenue) AS \"sales.revenue_sum\",\n    SUM(sales.qty) AS \"sales.qty_sum\"\n  FROM sales AS sales\n  GROUP BY\n    sales.store,\n    DATE_TRUNC('MONTH', sales.ordered_at)\n), shifted_prev AS (\n  SELECT\n    sales.store AS \"sales.store\",\n    DATE_TRUNC('MONTH', sales.ordered_at) AS \"sales.ordered_at\",\n    CAST(SUM(sales.revenue) AS DOUBLE PRECISION) / SUM(sales.qty) AS \"sales.shifted\"\n  FROM sales AS sales\n  GROUP BY\n    sales.store,\n    DATE_TRUNC('MONTH', sales.ordered_at)\n), sjoin_prev AS (\n  SELECT\n    base.\"sales.store\",\n    base.\"sales.ordered_at\",\n    base.\"sales.revenue_sum\",\n    base.\"sales.qty_sum\",\n    shifted_prev.\"sales.shifted\" AS \"sales.prev\"\n  FROM base\n  LEFT JOIN shifted_prev\n    ON base.\"sales.store\" IS NOT DISTINCT FROM shifted_prev.\"sales.store\"\n    AND base.\"sales.ordered_at\" - INTERVAL '1 MONTH' IS NOT DISTINCT FROM shifted_prev.\"sales.ordered_at\"\n)\nSELECT\n  \"sales.store\",\n  \"sales.ordered_at\",\n  \"sales.prev\"\nFROM (\n  SELECT\n    \"sales.store\",\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    \"sales.qty_sum\",\n    \"sales.prev\"\n  FROM sjoin_prev\n) AS _outer",
  "ts/ratio_by_store::sqlite": "WITH base AS (\n  SELECT\n    sales.store AS \"sales.store\",\n    STRFTIME('%Y-%m-01', sales.ordered_at) AS \"sales.ordered_at\",\n    SUM(sales.revenue) AS \"sales.revenue_sum\",\n    SUM(sales.qty) AS \"sales.qty_sum\"\n  FROM sales AS sales\n  GROUP BY\n    sales.store,\n    STRFTIME('%Y-%m-01', sales.ordered_at)\n), shifted_prev AS (\n  SELECT\n    sales.store AS \"sales.store\",\n    STRFTIME('%Y-%m-01', sales.ordered_at) AS \"sales.ordered_at\",\n    CAST(SUM(sales.revenue) AS REAL) / SUM(sales.qty) AS \"sales.shifted\"\n  FROM sales AS sales\n  GROUP BY\n    sales.store,\n    STRFTIME('%Y-%m-01', sales.ordered_at)\n), sjoin_prev AS (\n  SELECT\n    base.\"sales.store\",\n    base.\"sales.ordered_at\",\n    base.\"sales.revenue_sum\",\n    base.\"sales.qty_sum\",\n    shifted_prev.\"sales.shifted\" AS \"sales.prev\"\n  FROM base\n  LEFT JOIN shifted_prev\n    ON base.\"sales.store\" IS shifted_prev.\"sales.store\"\n    AND DATE(base.\"sales.ordered_at\", '-1 months') IS shifted_prev.\"sales.ordered_at\"\n)\nSELECT\n  \"sales.store\",\n  \"sales.ordered_at\",\n  \"sales.prev\"\nFROM (\n  SELECT\n    \"sales.store\",\n    \"sales.ordered_at\",\n    \"sales.revenue_sum\",\n    \"sales.qty_sum\",\n    \"sales.prev\"\n  FROM sjoin_prev\n) AS _outer",
  "ts/ratio_by_store::tsql": "WITH base AS (\n  SELECT\n    sales.store AS [sales___store],\n    DATETRUNC(month, sales.ordered_at) AS [sales___ordered_at],\n    SUM(sales.revenue) AS [sales___revenue_sum],\n    SUM(sales.qty) AS [sales___qty_sum]\n  FROM sales AS sales\n  GROUP BY\n    sales.store,\n    DATETRUNC(month, sales.ordered_at)\n), shifted_prev AS (\n  SELECT\n    sales.store AS [sales___store],\n    DATETRUNC(month, sales.ordered_at) AS [sales___ordered_at],\n    CAST(SUM(sales.revenue) AS FLOAT) / SUM(sales.qty) AS [sales___shifted]\n  FROM sales AS sales\n  GROUP BY\n    sales.store,\n    DATETRUNC(month, sales.ordered_at)\n), sjoin_prev AS (\n  SELECT\n    base.[sales___store] AS [sales___store],\n    base.[sales___ordered_at] AS [sales___ordered_at],\n    base.[sales___revenue_sum] AS [sales___revenue_sum],\n    base.[sales___qty_sum] AS [sales___qty_sum],\n    shifted_prev.[sales___shifted] AS [sales___prev]\n  FROM base\n  LEFT JOIN shifted_prev\n    ON (\n      base.[sales___store] = shifted_prev.[sales___store]\n      OR (\n        base.[sales___store] IS NULL AND shifted_prev.[sales___store] IS NULL\n      )\n    )\n    AND (\n      DATEADD(MONTH, -1, base.[sales___ordered_at]) = shifted_prev.[sales___ordered_at]\n      OR (\n        DATEADD(MONTH, -1, base.[sales___ordered_at]) IS NULL\n        AND shifted_prev.[sales___ordered_at] IS NULL\n      )\n    )\n)\nSELECT\n  [sales___store],\n  [sales___ordered_at],\n  [sales___prev]\nFROM (\n  SELECT\n    [sales___store],\n    [sales___ordered_at],\n    [sales___revenue_sum],\n    [sales___qty_sum],\n    [sales___prev]\n  FROM sjoin_prev\n) AS _outer",
  "ts/two_leaves::bigquery": "WITH _base AS (\n  SELECT\n    DATE_TRUNC(sales.ordered_at, MONTH) AS `sales___ordered_at`,\n    SUM(CASE WHEN sales.revenue > 15 THEN sales.revenue END) AS `sales___hi_rev_sum`\n  FROM sales AS sales\n  GROUP BY\n    DATE_TRUNC(sales.ordered_at, MONTH)\n), _cm_sales__revenue_wrevenue_sum_f_regions_factor AS (\n  SELECT\n    DATE_TRUNC(sales.ordered_at, MONTH) AS `sales___ordered_at`,\n    CAST(SUM(sales.revenue * regions.factor) AS FLOAT64) AS `sales___revenue_wrevenue_sum_f_regions_factor`\n  FROM sales AS sales\n  LEFT JOIN regions AS regions\n    ON sales.region_id = regions.id\n  GROUP BY\n    DATE_TRUNC(sales.ordered_at, MONTH)\n), base AS (\n  SELECT\n    _base.`sales___ordered_at`,\n    _base.`sales___hi_rev_sum`,\n    _cm_sales__revenue_wrevenue_sum_f_regions_factor.`sales___revenue_wrevenue_sum_f_regions_factor` AS `sales___revenue_wrevenue_sum_f_regions_factor`\n  FROM _base\n  LEFT JOIN _cm_sales__revenue_wrevenue_sum_f_regions_factor\n    ON _base.`sales___ordered_at` IS NOT DISTINCT FROM _cm_sales__revenue_wrevenue_sum_f_regions_factor.`sales___ordered_at`\n), shifted_prev AS (\n  SELECT\n    DATE_TRUNC(sales.ordered_at, MONTH) AS `sales___ordered_at`,\n    SUM(sales.revenue * regions.factor) + SUM(CASE WHEN sales.revenue > 15 THEN sales.revenue END) AS `sales___shifted`\n  FROM sales AS sales\n  LEFT JOIN regions AS regions\n    ON sales.region_id = regions.id\n  GROUP BY\n    DATE_TRUNC(sales.ordered_at, MONTH)\n), sjoin_prev AS (\n  SELECT\n    base.`sales___ordered_at`,\n    base.`sales___hi_rev_sum`,\n    base.`sales___revenue_wrevenue_sum_f_regions_factor`,\n    shifted_prev.`sales___shifted` AS `sales___prev`\n  FROM base\n  LEFT JOIN shifted_prev\n    ON base.`sales___ordered_at` - INTERVAL 1 MONTH IS NOT DISTINCT FROM shifted_prev.`sales___ordered_at`\n)\nSELECT\n  `sales___ordered_at`,\n  `sales___prev`\nFROM (\n  SELECT\n    `sales___ordered_at`,\n    `sales___hi_rev_sum`,\n    `sales___revenue_wrevenue_sum_f_regions_factor`,\n    `sales___prev`\n  FROM sjoin_prev\n) AS _outer",
  "ts/two_leaves::duckdb": "WITH _base AS (\n  SELECT\n    DATE_TRUNC('MONTH', sales.ordered_at) AS \"sales.ordered_at\",\n    SUM(CASE WHEN sales.revenue > 15 THEN sales.revenue END) AS \"sales.hi_rev_sum\"\n  FROM sales AS sales\n  GROUP BY\n    DATE_TRUNC('MONTH', sales.ordered_at)\n), _cm_sales__revenue_wrevenue_sum_f_regions_factor AS (\n  SELECT\n    DATE_TRUNC('MONTH', sales.ordered_at) AS \"sales.ordered_at\",\n    CAST(SUM(sales.revenue * regions.factor) AS DOUBLE) AS \"sales.revenue_wrevenue_sum_f_regions_factor\"\n  FROM sales AS sales\n  LEFT JOIN regions AS regions\n    ON sales.region_id = regions.id\n  GROUP BY\n    DATE_TRUNC('MONTH', sales.ordered_at)\n), base AS (\n  SELECT\n    _base.\"sales.ordered_at\",\n    _base.\"sales.hi_rev_sum\",\n    _cm_sales__revenue_wrevenue_sum_f_regions_factor.\"sales.revenue_wrevenue_sum_f_regions_factor\" AS \"sales.revenue_wrevenue_sum_f_regions_factor\"\n  FROM _base\n  LEFT JOIN _cm_sales__revenue_wrevenue_sum_f_regions_factor\n    ON _base.\"sales.ordered_at\" IS NOT DISTINCT FROM _cm_sales__revenue_wrevenue_sum_f_regions_factor.\"sales.ordered_at\"\n), shifted_prev AS (\n  SELECT\n    DATE_TRUNC('MONTH', sales.ordered_at) AS \"sales.ordered_at\",\n    SUM(sales.revenue * regions.factor) + SUM(CASE WHEN sales.revenue > 15 THEN sales.revenue END) AS \"sales.shifted\"\n  FROM sales AS sales\n  LEFT JOIN regions AS regions\n    ON sales.region_id = regions.id\n  GROUP BY\n    DATE_TRUNC('MONTH', sales.ordered_at)\n), sjoin_prev AS (\n  SELECT\n    base.\"sales.ordered_at\",\n    base.\"sales.hi_rev_sum\",\n    base.\"sales.revenue_wrevenue_sum_f_regions_factor\",\n    shifted_prev.\"sales.shifted\" AS \"sales.prev\"\n  FROM base\n  LEFT JOIN shifted_prev\n    ON base.\"sales.ordered_at\" - INTERVAL 1 MONTH IS NOT DISTINCT FROM shifted_prev.\"sales.ordered_at\"\n)\nSELECT\n  \"sales.ordered_at\",\n  \"sales.prev\"\nFROM (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.hi_rev_sum\",\n    \"sales.revenue_wrevenue_sum_f_regions_factor\",\n    \"sales.prev\"\n  FROM sjoin_prev\n) AS _outer",
  "ts/two_leaves::postgres": "WITH _base AS (\n  SELECT\n    DATE_TRUNC('MONTH', sales.ordered_at) AS \"sales.ordered_at\",\n    SUM(CASE WHEN sales.revenue > 15 THEN sales.revenue END) AS \"sales.hi_rev_sum\"\n  FROM sales AS sales\n  GROUP BY\n    DATE_TRUNC('MONTH', sales.ordered_at)\n), _cm_sales__revenue_wrevenue_sum_f_regions_factor AS (\n  SELECT\n    DATE_TRUNC('MONTH', sales.ordered_at) AS \"sales.ordered_at\",\n    CAST(SUM(sales.revenue * regions.factor) AS DOUBLE PRECISION) AS \"sales.revenue_wrevenue_sum_f_regions_factor\"\n  FROM sales AS sales\n  LEFT JOIN regions AS regions\n    ON sales.region_id = regions.id\n  GROUP BY\n    DATE_TRUNC('MONTH', sales.ordered_at)\n), base AS (\n  SELECT\n    _base.\"sales.ordered_at\",\n    _base.\"sales.hi_rev_sum\",\n    _cm_sales__revenue_wrevenue_sum_f_regions_factor.\"sales.revenue_wrevenue_sum_f_regions_factor\" AS \"sales.revenue_wrevenue_sum_f_regions_factor\"\n  FROM _base\n  LEFT JOIN _cm_sales__revenue_wrevenue_sum_f_regions_factor\n    ON _base.\"sales.ordered_at\" IS NOT DISTINCT FROM _cm_sales__revenue_wrevenue_sum_f_regions_factor.\"sales.ordered_at\"\n), shifted_prev AS (\n  SELECT\n    DATE_TRUNC('MONTH', sales.ordered_at) AS \"sales.ordered_at\",\n    SUM(sales.revenue * regions.factor) + SUM(CASE WHEN sales.revenue > 15 THEN sales.revenue END) AS \"sales.shifted\"\n  FROM sales AS sales\n  LEFT JOIN regions AS regions\n    ON sales.region_id = regions.id\n  GROUP BY\n    DATE_TRUNC('MONTH', sales.ordered_at)\n), sjoin_prev AS (\n  SELECT\n    base.\"sales.ordered_at\",\n    base.\"sales.hi_rev_sum\",\n    base.\"sales.revenue_wrevenue_sum_f_regions_factor\",\n    shifted_prev.\"sales.shifted\" AS \"sales.prev\"\n  FROM base\n  LEFT JOIN shifted_prev\n    ON base.\"sales.ordered_at\" - INTERVAL '1 MONTH' IS NOT DISTINCT FROM shifted_prev.\"sales.ordered_at\"\n)\nSELECT\n  \"sales.ordered_at\",\n  \"sales.prev\"\nFROM (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.hi_rev_sum\",\n    \"sales.revenue_wrevenue_sum_f_regions_factor\",\n    \"sales.prev\"\n  FROM sjoin_prev\n) AS _outer",
  "ts/two_leaves::sqlite": "WITH _base AS (\n  SELECT\n    STRFTIME('%Y-%m-01', sales.ordered_at) AS \"sales.ordered_at\",\n    SUM(CASE WHEN sales.revenue > 15 THEN sales.revenue END) AS \"sales.hi_rev_sum\"\n  FROM sales AS sales\n  GROUP BY\n    STRFTIME('%Y-%m-01', sales.ordered_at)\n), _cm_sales__revenue_wrevenue_sum_f_regions_factor AS (\n  SELECT\n    STRFTIME('%Y-%m-01', sales.ordered_at) AS \"sales.ordered_at\",\n    CAST(SUM(sales.revenue * regions.factor) AS REAL) AS \"sales.revenue_wrevenue_sum_f_regions_factor\"\n  FROM sales AS sales\n  LEFT JOIN regions AS regions\n    ON sales.region_id = regions.id\n  GROUP BY\n    STRFTIME('%Y-%m-01', sales.ordered_at)\n), base AS (\n  SELECT\n    _base.\"sales.ordered_at\",\n    _base.\"sales.hi_rev_sum\",\n    _cm_sales__revenue_wrevenue_sum_f_regions_factor.\"sales.revenue_wrevenue_sum_f_regions_factor\" AS \"sales.revenue_wrevenue_sum_f_regions_factor\"\n  FROM _base\n  LEFT JOIN _cm_sales__revenue_wrevenue_sum_f_regions_factor\n    ON _base.\"sales.ordered_at\" IS _cm_sales__revenue_wrevenue_sum_f_regions_factor.\"sales.ordered_at\"\n), shifted_prev AS (\n  SELECT\n    STRFTIME('%Y-%m-01', sales.ordered_at) AS \"sales.ordered_at\",\n    SUM(sales.revenue * regions.factor) + SUM(CASE WHEN sales.revenue > 15 THEN sales.revenue END) AS \"sales.shifted\"\n  FROM sales AS sales\n  LEFT JOIN regions AS regions\n    ON sales.region_id = regions.id\n  GROUP BY\n    STRFTIME('%Y-%m-01', sales.ordered_at)\n), sjoin_prev AS (\n  SELECT\n    base.\"sales.ordered_at\",\n    base.\"sales.hi_rev_sum\",\n    base.\"sales.revenue_wrevenue_sum_f_regions_factor\",\n    shifted_prev.\"sales.shifted\" AS \"sales.prev\"\n  FROM base\n  LEFT JOIN shifted_prev\n    ON DATE(base.\"sales.ordered_at\", '-1 months') IS shifted_prev.\"sales.ordered_at\"\n)\nSELECT\n  \"sales.ordered_at\",\n  \"sales.prev\"\nFROM (\n  SELECT\n    \"sales.ordered_at\",\n    \"sales.hi_rev_sum\",\n    \"sales.revenue_wrevenue_sum_f_regions_factor\",\n    \"sales.prev\"\n  FROM sjoin_prev\n) AS _outer",
  "ts/two_leaves::tsql": "WITH _base AS (\n  SELECT\n    DATETRUNC(month, sales.ordered_at) AS [sales___ordered_at],\n    SUM(CASE WHEN sales.revenue > 15 THEN sales.revenue END) AS [sales___hi_rev_sum]\n  FROM sales AS sales\n  GROUP BY\n    DATETRUNC(month, sales.ordered_at)\n), _cm_sales__revenue_wrevenue_sum_f_regions_factor AS (\n  SELECT\n    DATETRUNC(month, sales.ordered_at) AS [sales___ordered_at],\n    CAST(SUM(sales.revenue * regions.factor) AS FLOAT) AS [sales___revenue_wrevenue_sum_f_regions_factor]\n  FROM sales AS sales\n  LEFT JOIN regions AS regions\n    ON sales.region_id = regions.id\n  GROUP BY\n    DATETRUNC(month, sales.ordered_at)\n), base AS (\n  SELECT\n    _base.[sales___ordered_at] AS [sales___ordered_at],\n    _base.[sales___hi_rev_sum] AS [sales___hi_rev_sum],\n    _cm_sales__revenue_wrevenue_sum_f_regions_factor.[sales___revenue_wrevenue_sum_f_regions_factor] AS [sales___revenue_wrevenue_sum_f_regions_factor]\n  FROM _base\n  LEFT JOIN _cm_sales__revenue_wrevenue_sum_f_regions_factor\n    ON (\n      _base.[sales___ordered_at] = _cm_sales__revenue_wrevenue_sum_f_regions_factor.[sales___ordered_at]\n      OR (\n        _base.[sales___ordered_at] IS NULL\n        AND _cm_sales__revenue_wrevenue_sum_f_regions_factor.[sales___ordered_at] IS NULL\n      )\n    )\n), shifted_prev AS (\n  SELECT\n    DATETRUNC(month, sales.ordered_at) AS [sales___ordered_at],\n    SUM(sales.revenue * regions.factor) + SUM(CASE WHEN sales.revenue > 15 THEN sales.revenue END) AS [sales___shifted]\n  FROM sales AS sales\n  LEFT JOIN regions AS regions\n    ON sales.region_id = regions.id\n  GROUP BY\n    DATETRUNC(month, sales.ordered_at)\n), sjoin_prev AS (\n  SELECT\n    base.[sales___ordered_at] AS [sales___ordered_at],\n    base.[sales___hi_rev_sum] AS [sales___hi_rev_sum],\n    base.[sales___revenue_wrevenue_sum_f_regions_factor] AS [sales___revenue_wrevenue_sum_f_regions_factor],\n    shifted_prev.[sales___shifted] AS [sales___prev]\n  FROM base\n  LEFT JOIN shifted_prev\n    ON (\n      DATEADD(MONTH, -1, base.[sales___ordered_at]) = shifted_prev.[sales___ordered_at]\n      OR (\n        DATEADD(MONTH, -1, base.[sales___ordered_at]) IS NULL\n        AND shifted_prev.[sales___ordered_at] IS NULL\n      )\n    )\n)\nSELECT\n  [sales___ordered_at],\n  [sales___prev]\nFROM (\n  SELECT\n    [sales___ordered_at],\n    [sales___hi_rev_sum],\n    [sales___revenue_wrevenue_sum_f_regions_factor],\n    [sales___prev]\n  FROM sjoin_prev\n) AS _outer"
}
