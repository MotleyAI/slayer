views {
  view landscape {
    title 'SLayer landscape'
    include *
  }

  view query_pipeline {
    title 'Query pipeline'
    include core, engine, sql, ir, storage
  }

  view core_focus {
    title 'Core in context'
    include core, core -> *, * -> core
  }
}
