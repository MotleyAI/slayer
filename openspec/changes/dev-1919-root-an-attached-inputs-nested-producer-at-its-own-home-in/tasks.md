## 1. Tests (spec-tests stage — RED without the change)

- [ ] 1.1 Add `tests/test_dev1919_home_rooted_attached_inputs.py` on the dev1840 dataset (SQLite + DuckDB via `tests/_dev1841_fixtures.make_exec_engine`): headline by `status` under broadcast = `broadcast_wavg_global()` on both cells + broadcast warning; error mode refuses naming `status`; headline by `customers.tier` identical under all three modes with hand-computed oracles (gold 63.75, silver 415/3, bronze 40, NULL tier NULL) and no warnings; verify RED (raises today) / GREEN after 2.x
- [ ] 1.2 Same file: the mixed twin `sum(customers.spend * sum(amount, partition_by=customers.regions.name))` by `status` under broadcast (33780 both cells + warning) and by `customers.tier` under every mode (15300, 16600, 1880); verify RED/GREEN
- [ ] 1.3 Same file: D4 pin — the headline by `customers.tier` under `associate` on `tests/_dev1841_fixtures.keyless_root_models()` executes with broadcast-identical values and no association warning; verify RED (unique-key refusal today) / GREEN
- [ ] 1.4 Same file: plan shape — `plan_query` under broadcast and error: the `customers`-rooted attach (kernel not an association kernel) nests a producer with `producer_root_model == "orders"` (mirror of `tests/test_dev1910_association_plan.py::TestNestedAttachedParameterProducer`); `assert_scope_closed` + no `__regroup__` leak on the sqlite SQL; adding the measure is cardinality-neutral; verify RED/GREEN
- [ ] 1.5 Same file, Codex-folded shapes, PROBE FIRST and record the outcome in this task: recursive nesting (`customers.spend:weighted_avg(weight=weighted_avg(amount, weight=sum(customers.regions.pop, partition_by=customers.regions.name), partition_by=customers.regions.name))`, expected 6616.67/80 ≈ 82.71 broadcast to both status cells — c4's NULL innermost weight excludes it); ranked-transform parameter (`weight=rank(sum(amount, partition_by=customers.regions.name))`, rank DESC: North 1, NULL-name region 2, South 3 → by status 945/14 = 67.5; by tier gold 475/8, silver 97.5, bronze 40); windowed outer (`customers.spend:weighted_avg(window='1y', weight=…)` on a `customers.signup_at` month TD → 100, 125, 28080/267, 33780/407, NULL bucket NULL, identical every mode). An UNRELATED failure → file its own issue and mark the scenario xfail(strict=True) with that ref, never narrow the scenario
- [ ] 1.6 Same file, fail-closed pins in every mode: transform parameter whose grain the home does not determine (`cumsum(sum(amount, partition_by=[customers.regions.name, ordered_at]))` on an `ordered_at` month TD → determination error naming the parameter); unanalysable dependency inside the parameter (`tests/_dev1900_fixtures.unparseable_derived_models`, `weight=sum(customers.regions.unparseable, …)` or the local `unparseable` column → analyzability error naming the column); parameter whose own partition key fans from its own home (`amount:weighted_avg(weight=sum(customers.spend, partition_by=status))` → the DEV-1911 mode-invariant error); verify each is a typed error (some are GREEN already — that is fine, they pin D2)
- [ ] 1.7 Same file, D5 pins: `customers.spend:last(ordered_at)` names `ordered_at`, `customers`, "not attributable from" AND the hop `orders`; `customers.regions.bad_pop:last(ordered_at)` (dev1900 models) names `ordered_at`, not `region_events`; verify RED (hop-only message today for the first once rerooted; today the second names region_events) / GREEN
- [ ] 1.8 Consented edits: `tests/test_dev1859_attached_param_exec.py` (delete `test_default_mode_refuses_the_host_rooted_parameter`; drop the strict xfail from `test_default_mode_broadcasts_the_global_value`; drop the DEV-1906 sentences); `tests/test_dev1892_residue.py` (flip `test_default_mode_refuses_the_host_rooted_parameter` to executes + broadcast warning; re-point the class docstring); `tests/test_dev1832_cross_model_exec.py` (flip `test_attached_parameter_rejected_under_broadcast` to a per-mode identity check on `customers.tier`: equal non-NULL values, no warnings, both backends); `tests/_dev1871_raise_ledger.py` (delete the `check_attached_inputs_attributable` row; update the ranks/reads row to the D5 wording); verify `grep -rn DEV-1906 tests/` is empty
- [ ] 1.9 Goldens: `tests/test_dev1859_golden_sql.py` — add `param/broadcast` (headline, mode None, dims [status]) and `mixed/cross_model_constituent` (mixed twin, mode None, dims [status]); `ALLOWED_DELTAS` for `param/associate::<7 dialects>`; `tests/test_dev1900_golden_sql.py` — `ALLOWED_DELTAS` for `fanning/cross_model_kwarg::<7 dialects>`; the re-bless itself happens in 2.6
- [ ] 1.10 Codex-review the tests against this plan; run `poetry run pytest -m "not integration" -n auto` and confirm the only reds are the intended ones; commit the new + edited test files by name

## 2. Implementation (spec-implement stage)

- [ ] 2.1 D1: in `_synthesize_cross_model_producer` compute `agg_rooted = reroot_from_root(...)` once above the arms; association arm → `.model_copy(update={"locus": "host"})`; broadcast arm → host locus only when `target_path != source_anchor_path(agg.source)`; delete `_association_arm`'s own reroot; verify `tests/test_dev1910_*.py` + `tests/test_dev1841_*.py` green
- [ ] 2.2 D2: `_assert_cross_model_inputs_safe` masks `attached_inputs(agg_rooted)` with `LiteralKey(value=Decimal(1))` via `substitute_value_keys` before computing the closure / analyzability / hop checks; remove the association arm's `_safety_rooted` block; keep each arm's single call where it is; verify 1.2 and 1.6 green
- [ ] 2.3 D3: delete `_first_unattributable_attached_leaf` (stages.py), `check_attached_inputs_attributable` (elaborate_env.py) and the import; verify `tests/test_dev1871_raise_parity.py` + `tests/test_law_guard_ratchet.py` green, `architecture/index.yaml` guards baseline untouched
- [ ] 2.4 D4: `associate = mode == "associate" and bool(unattributable)`; `associated_measure = None if explicit else alias`; update the arm comments (terse); verify 1.3 green and `tests/test_dev1832_cross_model_exec.py` green
- [ ] 2.5 D5: `_first_unattributable_arg_leaf` returns `[(leaf, reason)]` with `reason` from `key_broadcast_reason` (or `broadcast_reason` when `host_model is None`); `_assert_cross_model_inputs_safe` runs it first and the hop check only when it returns nothing; `check_cross_model_inputs_safe(unattributable_arg_leaves: Sequence[Tuple[str, str]])` leaf arm = "ranks/reads by {leaf}, which is not attributable from {root} ({reason}); {remedy}."; verify 1.7, `tests/test_dev1838_kernels.py`, `tests/test_dev1900_input_safety.py` green
- [ ] 2.6 Re-bless goldens with `SLAYER_UPDATE_GOLDEN=1 poetry run pytest tests/test_dev1859_golden_sql.py tests/test_dev1900_golden_sql.py`; review the diff: dev1859 `param/associate` alias-only, dev1900 `fanning/cross_model_kwarg` = the D5 leaf message naming `bad_pop` and `region_events`, new cases generate SQL (not raises); verify `test_param_cases_generate` green
- [ ] 2.7 Docs: replace the two-clause sentence in the `docs/concepts/queries.md` associate paragraph ("An attached (aggregate-valued) parameter the entity grain determines … names the `associate` remedy.") with: "An attached (aggregate-valued) parameter or source constituent is computed at its own home and attached per home row in every mode, so `customers.spend:weighted_avg(weight=sum(amount, partition_by=customers.regions.name))` weights each customer by its region's order total under `broadcast`, `associate` and `error` alike."; verify `grep -n "attributable from the aggregate's root" docs/` is empty
- [ ] 2.8 arc42 (edit APPROVED by Egor 2026-09-19 during spec-plan — apply exactly this and nothing else): in `architecture/semantics.arc42.md` Axiom 9, after `[enforced: test:tests/test_law_guard_ratchet.py]` insert `[enforced: test:tests/test_dev1919_home_rooted_attached_inputs.py]` (same line, before "Typed residue"); verify `poetry run python tools/arch_check.py` green
- [ ] 2.9 Gates: `poetry run pytest -m "not integration" -n auto` all green; `poetry run ruff check slayer/ tests/`; `poetry run basedpyright` (no new errors vs baseline — do not write the baseline); `poetry run python tools/arch_check.py`; `npx -y likec4@1.47.0 validate architecture`; Codex pass on the working tree; then commit by named file and STOP for the push/PR go-ahead

## 3. Review (spec-review stage)

- [ ] 3.1 `/process-reviews` until CodeRabbit, Sonar, Codex and CI are green; every check shows `pass`
- [ ] 3.2 With Egor's explicit OK: `openspec archive` the change, commit, push
