## 1. Tests (spec-tests stage — RED without the change)

- [x] 1.1 (RED verified: the guard message today) Add `tests/test_dev1919_home_rooted_attached_inputs.py` on the dev1840 dataset (SQLite + DuckDB via `tests/_dev1841_fixtures.make_exec_engine`): headline by `status` under broadcast = `broadcast_wavg_global()` on both cells + broadcast warning; error mode refuses naming `status`; headline by `customers.tier` identical under all three modes with hand-computed oracles (gold 63.75, silver 415/3, bronze 40, NULL tier NULL) and no warnings; verify RED (raises today) / GREEN after 2.x
- [x] 1.2 (RED verified) Same file: the mixed twin `sum(customers.spend * sum(amount, partition_by=customers.regions.name))` by `status` under broadcast (33780 both cells + warning) and by `customers.tier` under every mode (15300, 16600, 1880); verify RED/GREEN
- [x] 1.3 Same file: D4 pin — the headline by `customers.tier` under `associate` on `tests/_dev1919_fixtures.keyless_declared_models()` (no unique key on customers, `orders → customers` declared many-to-one — a bare keyless customers makes the hop unproven and the parameter's own key fails at bind) executes with broadcast-identical values and no association warning; by `status` under `associate` the unique-key refusal still fires; verified RED (unique-key refusal today) / GREEN under the probe
- [x] 1.4 (RED verified; kernel pinned as `PlainProducerKernel`, D6) Same file: plan shape — `plan_query` under broadcast and error: the `customers`-rooted attach (kernel not an association kernel) nests a producer with `producer_root_model == "orders"` (mirror of `tests/test_dev1910_association_plan.py::TestNestedAttachedParameterProducer`); `assert_scope_closed` + no `__regroup__` leak on the sqlite SQL; adding the measure is cardinality-neutral; verify RED/GREEN
- [x] 1.5 PROBED 2026-09-19 (design.md › Spec-tests probe): recursive nesting GREEN under the patch (82.7083 by status; by tier gold 3316.67/53.33, silver 123.75, bronze NULL); ranked-transform parameter fails at bind → deferred to DEV-1903 (Egor), strict xfail; windowed parameter fails at the sum/avg allowlist → deferred to DEV-1915 (Egor), strict xfail, and the windowed CONSTITUENT `sum(customers.spend * sum(amount, partition_by=customers.regions.name), window='1y')` (10000 / 25000 / 28080 / 33780, NULL bucket NULL, every mode, no warning) is pinned executing. Original brief: recursive nesting (`customers.spend:weighted_avg(weight=weighted_avg(amount, weight=sum(customers.regions.pop, partition_by=customers.regions.name), partition_by=customers.regions.name))`, expected 6616.67/80 ≈ 82.71 broadcast to both status cells — c4's NULL innermost weight excludes it); ranked-transform parameter (`weight=rank(sum(amount, partition_by=customers.regions.name))`, rank DESC: North 1, NULL-name region 2, South 3 → by status 945/14 = 67.5; by tier gold 475/8, silver 97.5, bronze 40); windowed outer (`customers.spend:weighted_avg(window='1y', weight=…)` on a `customers.signup_at` month TD → 100, 125, 28080/267, 33780/407, NULL bucket NULL, identical every mode). An UNRELATED failure → file its own issue and mark the scenario xfail(strict=True) with that ref, never narrow the scenario
- [x] 1.6 Same file, fail-closed pins in every mode (corrected after the probe): transform parameter whose grain the home does not determine (`cumsum(sum(amount, partition_by=[customers.regions.name, ordered_at]))` on an `ordered_at` month TD → determination error naming the parameter; strict xfail → DEV-1903, bind refuses the transform first); unanalysable dependency inside the parameter (`tests/_dev1900_fixtures.unparseable_derived_models`, `weight=sum(customers.regions.unparseable, partition_by=customers.regions.name)` → analyzability error naming the column); parameter whose own partition key fans from its own home (`amount:weighted_avg(weight=sum(customers.regions.pop, partition_by=customers.regions.bad_pop))` → the DEV-1911 mode-invariant error naming `region_events`); the host-safe sibling `amount:weighted_avg(weight=sum(customers.spend, partition_by=status))` is MODE-AWARE (broadcast/error: explicit-key error naming `status`; associate: executes 82/7 ok, 85/3 new — Axiom 8, already enforced by `test_dev1911`); D2b pin: `customers.spend:weighted_avg(weight=sum(customers.spend, partition_by=status))` → determination error naming `weight` in all six (mode × status/tier) cells (RED today: the guard's message names neither the grain nor `partition_by=`; under error by status the dimension refusal fires instead)
- [x] 1.7 (RED verified; the second names the hop `customers`) Same file, D5 pins: `customers.spend:last(ordered_at)` names `ordered_at`, `customers`, "not attributable from" AND the hop `orders`; `customers.regions.bad_pop:last(ordered_at)` (dev1900 models) names `ordered_at`, not `region_events`; verify RED (hop-only message today for the first once rerooted; today the second names region_events) / GREEN
- [x] 1.8 (applied; modified files left unstaged for the spec-implement gate) Consented edits: `tests/test_dev1859_attached_param_exec.py` (delete `test_default_mode_refuses_the_host_rooted_parameter`; drop the strict xfail from `test_default_mode_broadcasts_the_global_value`; drop the DEV-1906 sentences); `tests/test_dev1892_residue.py` (flip `test_default_mode_refuses_the_host_rooted_parameter` to executes + broadcast warning; re-point the class docstring); `tests/test_dev1832_cross_model_exec.py` (flip `test_attached_parameter_rejected_under_broadcast` to a per-mode identity check on `customers.tier`: equal non-NULL values, no warnings, both backends); `tests/_dev1871_raise_ledger.py` (delete the `check_attached_inputs_attributable` row; update the ranks/reads row to the D5 wording); verify `grep -rn DEV-1906 tests/` is empty
- [x] 1.9 (applied; new cases unblessed until 2.6) Goldens: `tests/test_dev1859_golden_sql.py` — add `param/broadcast` (headline, mode None, dims [status]) and `mixed/cross_model_constituent` (mixed twin, mode None, dims [status]); `ALLOWED_DELTAS` for `param/associate::<7 dialects>`; `tests/test_dev1900_golden_sql.py` — `ALLOWED_DELTAS` for `fanning/cross_model_kwarg::<7 dialects>`; the re-bless itself happens in 2.6
- [x] 1.10 Codex-reviewed 2026-09-19 (tests: D5 hop assertion, D6 `PlainProducerKernel` pin, D4 first/last control added, ledger-wording finding declined — see D5; plan edits: D2b rewritten as one home-determination rule, requirement sentences reconciled with Axiom 8); full unit suite 87 red / 19183 green, every red in the six intended files; new test files committed, edited test files left unstaged

## 2. Implementation (spec-implement stage)

- [x] 2.1 D1: in `_synthesize_cross_model_producer` compute `agg_rooted = reroot_from_root(...)` once above the arms; association arm → `.model_copy(update={"locus": "host"})`; broadcast arm → host locus only when `target_path != source_anchor_path(agg.source)`; delete `_association_arm`'s own reroot; verify `tests/test_dev1910_*.py` + `tests/test_dev1841_*.py` green
- [x] 2.2b D2b: one home-determination predicate for explicitly grained attached (aggregate-valued) parameters — each resolved-grain member attributable from the home (closure-aware, as `safe_pairs` judges a dimension; recurse into an aggregate-valued key's grain; expression keys never determined; ungrained parameters determined, DEV-1859 decision 12) — called at the top of the shared tail in both arms (the association arm's `_param_is_determined` keeps column-valued/default parameters only), raising `check_parameter_determined` in every mode; verify the 1.6 D2b pins green in all six cells and `test_dev1859_attached_param_exec.py::TestUndeterminedParameterRejected` + `tests/test_dev1892_*.py` unchanged
- [x] 2.2 D2: `_assert_cross_model_inputs_safe` masks `attached_inputs(agg_rooted)` with `LiteralKey(value=Decimal(1))` via `substitute_value_keys` before computing the closure / analyzability / hop checks; remove the association arm's `_safety_rooted` block; keep each arm's single call where it is; verify 1.2 and 1.6 green
- [x] 2.3 D3: delete `_first_unattributable_attached_leaf` (stages.py), `check_attached_inputs_attributable` (elaborate_env.py) and the import; verify `tests/test_dev1871_raise_parity.py` + `tests/test_law_guard_ratchet.py` green, `architecture/index.yaml` guards baseline untouched
- [x] 2.4 D4: `associate = mode == "associate" and bool(unattributable)`; `associated_measure = None if explicit else alias`; update the arm comments (terse); verify 1.3 green and `tests/test_dev1832_cross_model_exec.py` green
- [x] 2.5 D5: `_first_unattributable_arg_leaf` returns `[(leaf, reason)]` with `reason` from `key_broadcast_reason` (or `broadcast_reason` when `host_model is None`); `_assert_cross_model_inputs_safe` runs it first and the hop check only when it returns nothing; `check_cross_model_inputs_safe(unattributable_arg_leaves: Sequence[Tuple[str, str]])` leaf arm = "ranks/reads by {leaf}, which is not attributable from {root} ({reason}); {remedy}."; verify 1.7, `tests/test_dev1838_kernels.py`, `tests/test_dev1900_input_safety.py` green
- [x] 2.6 Re-bless goldens with `SLAYER_UPDATE_GOLDEN=1 poetry run pytest tests/test_dev1859_golden_sql.py tests/test_dev1900_golden_sql.py`; review the diff: dev1859 `param/associate` alias-only, dev1900 `fanning/cross_model_kwarg` = the D5 leaf message naming `bad_pop` and `region_events`, new cases generate SQL (not raises); verify `test_param_cases_generate` green
- [x] 2.7 Docs: replace the two-clause sentence in the `docs/concepts/queries.md` associate paragraph ("An attached (aggregate-valued) parameter the entity grain determines … names the `associate` remedy.") with: "An attached (aggregate-valued) parameter or source constituent is computed at its own home and attached per home row in every mode, so `customers.spend:weighted_avg(weight=sum(amount, partition_by=customers.regions.name))` weights each customer by its region's order total under `broadcast`, `associate` and `error` alike."; verify `grep -n "attributable from the aggregate's root" docs/` is empty
- [x] 2.8 arc42 (edit APPROVED by Egor 2026-09-19 during spec-plan — apply exactly this and nothing else): in `architecture/semantics.arc42.md` Axiom 9, after `[enforced: test:tests/test_law_guard_ratchet.py]` insert `[enforced: test:tests/test_dev1919_home_rooted_attached_inputs.py]` (same line, before "Typed residue"); verify `poetry run python tools/arch_check.py` green
- [x] 2.9 Gates: `poetry run pytest -m "not integration" -n auto` all green; `poetry run ruff check slayer/ tests/`; `poetry run basedpyright` (no new errors vs baseline — do not write the baseline); `poetry run python tools/arch_check.py`; `npx -y likec4@1.47.0 validate architecture`; Codex pass on the working tree; then commit by named file and STOP for the push/PR go-ahead

## 3. Review (spec-review stage)

- [ ] 3.1 `/process-reviews` until CodeRabbit, Sonar, Codex and CI are green; every check shows `pass`
- [x] 3.1a Codex finding, option 1 approved by Egor 2026-09-19 (design D8): `_fold_positional_agg_args` refuses any positional value on an aggregation declaring no parameters (ranked first/last take at most one positional, a column ranking key; an aggregate, literal or extra value there is refused — Codex round 2) — with the guard gone, `customers.spend:sum(sum(customers.spend, partition_by=status))` executed multiplied (gold 445 vs 245, no warning); tests: `tests/test_agg_positional_params.py::TestPositionalErrors` (5 new) + dev1919 every-mode pin; spec delta `aggregations/functional-form` (two scenarios); verified RED without the binder change
- [ ] 3.2 With Egor's explicit OK: `openspec archive` the change, commit, push
