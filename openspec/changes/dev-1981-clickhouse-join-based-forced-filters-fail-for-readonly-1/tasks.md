## 1. Failing tests (spec-tests)

Approved test-logic changes (spec-plan, 2026-09-26) — any other test-logic change needs a fresh OK:
- `tests/test_session_policy.py`: `test_clickhouse_join_appends_settings_and_calls_hook` → asserts `IN`, no `SETTINGS`; `test_non_clickhouse_join_calls_hook_no_settings` and `test_clickhouse_column_only_does_not_append_settings_or_call_hook` → drop the callback assertions, keep the no-`SETTINGS` ones.
- `tests/test_session_policy_engine.py`: delete the `_clickhouse_correlated_guard` tests; `_parse_clickhouse_version` tests → `parse_server_profile` tests (same version inputs/expectations); `_ch_version_cache` / preflight tests → client profile-cache tests (cached once, failure not cached, failure blocks the semi-join); ClickHouse `test_apply_policy_*` → assert `IN`, no setting.
- `tests/integration/test_integration_rls.py`: `test_execute_invokes_clickhouse_preflight` / `test_get_column_types_invokes_clickhouse_preflight` → assert the probe is NOT invoked for a policy-only query.
- `tests/test_dev1840_clickhouse_gate.py`: `_version_probe` fake answers the profile statements (readonly 0, setting 0 → existing cases keep their meaning); the cached-across-queries test counts profile probes; `test_rls_attachment_keeps_one_settings_clause` → the RLS rewrite adds an `IN` and keeps the planner's single `SETTINGS … = 1`.
- DEV-1977: `tests/dialects/test_statement_timeout_hooks.py` permission-hook tests → `server_profile_sql` / `parse_server_profile` / `timeout_permitted(profile)` (readonly 0/1/2 matrix kept); `tests/_ch_fake_http.py` answers the profile statements (existing `readonly` knob + new `version` / `correlated` knobs), `permission_checks()` counts profile probes so `tests/test_clickhouse_statement_timeout.py` assertions stay.

- [x] 1.1 `session_policy` unit tests: ClickHouse single-hop, multi-hop and multi-rule join policies emit the guarded `GLOBAL IN` (`toNullable(to_col)` projection, `GLOBAL INNER JOIN` between hops, `to_col IS NOT NULL`, tenant predicate on the terminal hop), no `SETTINGS`, no `EXISTS`; anchor/whitelist/fail-closed unchanged; Postgres/DuckDB output unchanged; ClickHouse output passes `assert_scope_closed(…, allow_rls_correlation=False)` — verify they fail now
- [x] 1.2 Dialect hook unit tests: gated flag (ClickHouse only); `server_profile_sql`, `correlated_setting_sql` (None below 25.4); tolerant `parse_server_profile` (NULL, `""`, `"0"`/`"1"`, `"true"`/`"false"`, Python bools, ints, malformed version, missing columns → unknown, never raise); `timeout_permitted(profile)`; `correlated_subquery_refusal` matrix (<25.4, unknown version, 25.4 setting 0 ro 1 → reason incl. upgrade remedy, 25.8+ setting 0 ro 1 → reason without upgrade remedy, ro 0/2 → None, setting 1 ro 1 → None, setting unknown → reason); attach hook (UNION placement, pre-existing `= 0` forced to 1, other settings kept) — verify they fail now
- [x] 1.3 Client unit tests (fake HTTP ClickHouse, sync + async): one profile fill per engine incl. concurrent threads; profile fill runs through the real timeout wrapper without recursion/deadlock; `server_profile()` reuses the execution cache and vice versa; correlated-setting statement only on 25.4+; its failure leaves the field unknown while timeouts still apply; a failed base probe propagates and is not cached — verify they fail now
- [x] 1.4 Engine unit tests: policy-only ClickHouse query runs no profile probe; semi-join plan on ClickHouse with a refusing profile raises `SlayerError` on execute, dry-run and explain, before any statement reaches the database, and the message names the pushed filter, `allow_experimental_correlated_subqueries`, `readonly`, and the remedies; profile failure → "could not be determined" error and the next query re-probes; non-gated dialects run no probe; rendered semi-join SQL carries exactly one `SETTINGS … = 1`, attached without re-parsing rendered SQL (extend `tests/test_law_no_statement_reparse.py` or add a law test); law test: no `dialect ==` / `"clickhouse"` literal in `session_policy.py` or the engine's correlated gate — verify they fail now
- [x] 1.5 Live ClickHouse integration (`tests/integration/test_integration_clickhouse.py`): on 24-alpine, a `readonly = 1` user under a join policy gets exactly the tenant's rows (single- and multi-hop); `transform_null_in = 1` with nullable keys and a NULL in the anchor path returns exact row ids with no NULL-keyed leak; `Distributed` target + anchor under a join policy (check the image has a local cluster; if not, STOP and ask); new module-scoped container fixture parametrized over the 25.4 and 25.8 images — on 25.4: `readonly = 1` join policy returns scoped rows, `readonly = 1` semi-join query raises the new error pre-execution, `readonly = 2` runs it, `readonly = 1` with the setting enabled in the user's profile runs it with the `SETTINGS` attached; on 25.8: `readonly = 1` join policy returns scoped rows, and a `readonly = 1` semi-join query with default settings runs with the `SETTINGS` attached (proves real probe output parses) — verify they fail now
- [x] 1.6 Apply the approved test-logic changes listed above
- [x] 1.7 Codex review of the tests against `specs/`; resolve findings with the user

## 2. Implementation (spec-implement)

- [x] 2.1 Moves with the deterministic-refactor skill (never by hand): `_attach_ch_correlated_setting` + `_settings_holder` → `slayer/sql/dialects/clickhouse.py`; `_parse_clickhouse_version` → `slayer/sql/dialects/clickhouse.py`; `plan_has_semi_join_filters` → `slayer/ir`; verify the type-check gate and the full unit suite are green after the moves alone
- [x] 2.2 `slayer/sql/dialects/`: gated flag, `ServerProfile`, profile/verdict/attach hooks (D1, D4, D6); delete `timeout_permission_sql` / `timeout_permitted(value)`; verify 1.2 passes
- [x] 2.3 `slayer/sql/session_policy.py`: guarded `IN` for gated dialects (`in_subquery_key` hook → ClickHouse `toNullable`, `global_in_subqueries` flag → `GLOBAL IN` / `GLOBAL JOIN`); delete `on_correlated_emitted` and the ClickHouse `SETTINGS` branch (D2, D3); verify 1.1 passes
- [x] 2.4 `slayer/sql/client.py`: profile cache + sync/async fill helpers via `_exec_verbatim*`; `SlayerSQLClient.server_profile()`; timeout path reads `timeout_permitted(profile)`; failure not cached (D4); verify 1.3 passes
- [x] 2.5 `slayer/sql/generator.py`: `generate_planned_stages` attaches the setting on the statement AST for gated dialects with semi-join plans (D6)
- [x] 2.6 `slayer/engine/query_engine.py`: delete `_ch_version_cache`, `_preflight_clickhouse_correlated`, `_clickhouse_correlated_guard`, `_policy_has_join_rules`, the policy-only preflight calls and the rendered-SQL re-parse; dialect-agnostic semi-join gate via `client.server_profile()` + `correlated_subquery_refusal` (D3, D4, D6); verify 1.4 passes and the full unit suite is green
- [x] 2.7 Docs: `docs/concepts/row-level-security.md` ClickHouse section (join targets use a non-correlated semi-join; no version floor; works for `readonly = 1` and `Distributed` tables) + "How it works" wording; one sentence at `docs/concepts/queries.md:730` on the 25.4–25.7 `readonly = 1` refusal and remedies; RLS notebook markdown cell (~line 504) fixed, then re-run and save outputs
- [x] 2.8 Gates: full unit suite, ClickHouse integration (1.5) + integration suite (CI invocation), `ruff`, `basedpyright` (no new errors vs baseline), `tools/arch_check.py`, conventions gate; verify 1.5 passes

## 3. Review (spec-review)

- [ ] 3.1 /process-reviews until every source is green
- [ ] 3.2 Comment on DEV-1981 that it is done by this PR (after archive)
- [ ] 3.3 `openspec archive dev-1981-clickhouse-join-based-forced-filters-fail-for-readonly-1` with the user's explicit go-ahead
