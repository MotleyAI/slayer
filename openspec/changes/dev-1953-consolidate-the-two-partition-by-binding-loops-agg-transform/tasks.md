## 1. Tests (spec-tests stage — RED without the change)

- [x] 1.1 Add `tests/test_dev1953_partition_alias.py` (SQLite + DuckDB via `tests/_dev1847_fixtures.make_exec_engine`, `ureg = upper(region)` declared as a computed dimension, oracles derived from `_SALES_ROWS_WIDE` and pinned against the delta constants): measure (rank cells per region incl. VOID Xi 1), aggregation-parameter (`weighted_avg` 56 / 120÷7 / 36 / 7 / NULL), filter (five rank-1 rows), order (rank-1 rows first), dimension-position member (`(ureg, r)` cells), mixed list `[ureg, product]` bound grain == the aggregate twin's, symmetry (`bind_expr` with `dimension_alias_map` of `sum(amount, partition_by=ureg)` and `rank(sum(amount), partition_by=ureg)` yield equal `partition_keys`), both construct-naming error texts, undeclared name → `UnknownReferenceError`; verify every alias test is RED today
- [x] 1.2 Same file, `spend_band` (attach-carrying): filter and order positions execute (hi/Gamma 100, lo/Alpha 70); measure and parameter positions bind the key to the dimension's value and fail with the narrow current planner errors (D5, forward pointer DEV-1960); verify RED (unknown reference today)
- [x] 1.3 Add `tests/test_dev1953_partition_membership.py`: non-member as measure and as filter (`product` over `[city, region]`) → error naming `rank`, `product`, `city, region`, the remedy; dimension-position non-member plain (`region` over `[city, product]`) and alias (`ureg` over `[city, region]`) → same error, never `RuntimeError`; member executes (rank cells within region on `[city, region, product]`); ungrained inner over `[band]` alone → existing query-dimension error, over `[region, band]` executes; windowed inner (orders graph, monthly) accepts `ordered_at`; nested `last` drops its axis (`ordered_at` rejected, `customers.regions.name` accepted, checked at `bind_query_inputs` level); residue error precedes membership; aggregate-free input `rank(city, partition_by=region)` executes (query grain) and a literal input `rank(1, partition_by=region)` binds (execution → DEV-1962); verify every membership test is RED today (the non-member shapes execute or raise the wrong error)
- [x] 1.4 Same file: `parse_expr` rejects `rank(sum(amount), partition_by=region, partition_by=city)`, `sum(amount, partition_by=region, partition_by=city)` and `amount:sum(partition_by=region, partition_by=city)` with an error naming the call and `partition_by`; verify RED (parses today)
- [x] 1.5 Codex-review both files against the plan; full unit suite: every red is in the two new files; commit the two new files by name

## 2. Implementation (spec-implement stage)

- [x] 2.1 D1: `_bind_partition_keys` in `slayer/engine/binding.py`; `_bind_agg` and the rank arm of `_bind_transform` call it; `_bind_agg_partition_keys` and the inline loop deleted; `partition_keys: Grain = Grain.EMPTY`; stale comment at the top of `_bind_transform` updated; verify 1.1 alias tests and the symmetry test green, `tests/test_binding.py` + `tests/test_transforms_planner.py` unchanged
- [x] 2.2 D2: repeated-keyword rejection in the Call branch of `_convert` (`slayer/engine/syntax.py`, before the placeholder / functional dispatch); verify 1.4 green and `tests/test_syntax*.py` unchanged
- [x] 2.3 D3: `transform_operand_grain` in `slayer/core/keys.py` (exported); verify the windowed-inner and nested-`last` cases of 1.3 green at bind level
- [x] 2.4 D4 + D7: `check_transform_partition_keys_in_operand_grain` in `slayer/engine/elaborate_env.py`; call site in `slayer/engine/bind_inputs.py` immediately after the `_rw` rewrite, over declared measures (dimensions included), bound filters and order specs with `_query_grain` and `active_td_key`; ledger row in `tests/_dev1871_raise_ledger.py`; verify 1.3 green, `tests/test_dev1871_raise_parity.py` green, and the DEV-1837 `test_explicit_partition_by_on_transform_wins` still green (D6)
- [x] 2.5 1.2 pins: verify the filter / order executed values green and the two narrow planner-error pins green (if either position now executes, STOP and report — the pin must not be loosened silently)
- [x] 2.6 Docs: `docs/concepts/formulas.md` transforms section — one sentence "the rank family's `partition_by=` may name a computed dimension, exactly as an aggregation's can" and one sentence stating the operand-grain membership rule; verify `grep -n "operand grain" docs/concepts/formulas.md` hits the new sentence
- [ ] 2.7 arc42 (needs an explicit per-edit OK first — present the exact one-line diff, apply only on a yes, skip on a no): append `[enforced: test:tests/test_dev1953_partition_membership.py]` to Axiom 11 in `architecture/semantics.arc42.md`; verify `poetry run python tools/arch_check.py` green
- [x] 2.8 Gates: `poetry run pytest -m "not integration" -n auto` all green (a red in an existing test → STOP and ask, never relax it); `poetry run ruff check slayer/ tests/`; `poetry run basedpyright` (no new errors vs baseline, never write the baseline); `poetry run python tools/arch_check.py`; `npx -y likec4@1.47.0 validate architecture`; Codex pass on the working tree; then commit by named file and STOP for the push / PR go-ahead

## 3. Review (spec-review stage)

- [ ] 3.1 `/process-reviews` until CodeRabbit, Sonar, Codex and CI are green; every check shows `pass`
- [ ] 3.2 With Egor's explicit OK: `openspec archive` the change, commit, push
