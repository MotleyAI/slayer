## 1. Tests (spec-tests stage — RED without the change)

- [ ] 1.1 Remove the two `xfail(strict=True, …DEV-1903)` markers from `tests/test_dev1919_home_rooted_attached_inputs.py` (`TestRankedTransformParameter`, `test_transform_parameter_grain_not_determined`); no other edit to that file; verify both classes are RED with the bind refusal today
- [ ] 1.2 Add `tests/_dev1946_fixtures.py`: formula constants for every delta scenario and raw-row oracles over `tests/_dev1840_fixtures` (orders → customers → regions) and `tests/_dev1847_fixtures` (sales), each oracle returning the literal constants the spec cites (rank of a NULL-valued cell = 1 + the number of non-NULL cells); verify the oracles reproduce the spec constants by a direct assertion in the fixture module's own test class
- [ ] 1.3 Add `tests/test_dev1946_transform_parameter.py` (SQLite + DuckDB via the existing `make_exec_engine` fixtures) — positional ≡ keyword (identical bound `AggregateKey`, result keys, values); associate by `status` (700 / 9, 82.5 + association warning); local root by region / by region and city / global (810 / 43); collapsing `last` (63.79 by month + warning; 3285 / 54, 3000 / 27, 40 by tier and month); cumsum without axis → time-axis error in every mode, as a measure and as a filter; determined cumsum (100, 150, 1900 / 45, 5700 / 140; filter `> 50`; raw ORDER BY desc); ungrained inner (tier executes 61.25 / 115 / 40; status refused naming `weight`, every mode); windowed inner refused naming `weight`, every mode; windowed outer (100, 125, 510 / 7, 945 / 14, every mode, no warning); filter and order positions of RANKED; nested transform parameter (73.01 broadcast; 62.19 / 123.75 / 40 by tier every mode); cross-model transform producer on a local root (281 / 16; 116 / 11, 33 by status, every mode); own `partition_by=` outside the query dims (760 / 11 global; 61.25 / 115 / 40 by tier); assert literal constants AND the oracle; verify every new test is RED today
- [ ] 1.4 Same file, plan shape: `plan_query` under default and error → the `customers`-rooted attach is a `PlainProducerKernel`; `assert_scope_closed` + no `__regroup__` leak on the sqlite SQL for the ranked, collapsing, cumsum, windowed-outer and associate shapes; the association kernel and the trailing-window kernel carry a `PickedParam` named `weight`; adding the measure is cardinality-neutral; verify RED / GREEN
- [ ] 1.5 Same file, re-aggregation outer (sales graph): `weighted_avg(sum(amount, partition_by=[city, region]), weight=rank(count(id, partition_by=[city, region])))` by region → North 55, South 580 / 7, East 60, Gap 64 / 7, Void NULL, scope-closed, the carrier plan lists the transform constituent; the `partition_by=product` twin → determination error naming `weight` and the grain; verify RED (scope leak / determination error today) / GREEN
- [ ] 1.6 Same file, bind level: a `TransformCall` kwarg binds to a `TransformKey` kwarg; `customers.spend:last(rank(sum(amount, partition_by=customers.regions.name)))` is refused at bind with the "ranks by a column" error; `weight=qty * 2`-style expression argument keeps the refusal naming the accepted kinds; verify RED / GREEN where applicable
- [ ] 1.7 Goldens: add `param/ranked_transform` (orders, mode None, dims [status], RANKED) and `param/local_ranked_transform` (sales, dims [region], `weighted_avg(amount, weight=rank(sum(amount, partition_by=region)))`) to `tests/test_dev1859_golden_sql.py`; unblessed until 2.10; verify `test_param_cases_generate` covers them
- [ ] 1.8 Codex-review the tests against the plan; full unit suite: every red is in the intended files; commit the new test files by name (edited files left unstaged for the spec-implement gate)

## 2. Implementation (spec-implement stage)

- [ ] 2.1 D1: `_bind_agg_arg` binds `TransformCall` via `_bind_transform(..., dim_alias_map=dim_alias_map)`; refusal message lists grained transforms; `_AggregateArgValue` admits `TransformKey` (comment updated); verify 1.6 green and `tests/test_binding.py` + `tests/test_agg_positional_params.py` unchanged
- [ ] 2.2 D7: `refs.agg_kwarg_canonical_str` gains the `TransformKey` arm (`_transform_key_canonical_str`, incl. rank partition keys and the time key); verify `tests/test_agg_kwarg_canonical_golden.py` + naming tests green
- [ ] 2.3 D2: `keys.map_attached_inputs`; `normalize_transform_constituents` and `lower_collapsing_constituents` rewrite through it (post-order, per position, partition keys untouched); verify the collapsing, ungrained-inner and nested scenarios green and `tests/test_dev1832_transform_source.py` + `tests/test_dev1928_*.py` unchanged
- [ ] 2.4 D3: `_temporal_axis_transforms` iterates `attached_inputs(k)`; verify the no-axis scenario green (measure and filter) and `tests/test_dev1832_transform_source.py` unchanged
- [ ] 2.5 D6: `attached_operand_keys` collects aggregates and transforms; verify the own-partition_by scenario green and `tests/test_dev1825_*`, `tests/test_dev1859_*` unchanged
- [ ] 2.6 D4: `resolve_aggregation_params` admits `TransformKey`; the association arm skips `check_parameter_determined` for attached kinds and picks the transform; the trailing-window kernel picks it; the re-aggregation outer appends it to the carrier's constituents; verify the associate, windowed-outer and re-aggregation scenarios green
- [ ] 2.7 D5: `keys.attached_parameter_grain`; `_check_attached_params_determined` and `_param_is_determined` consume it (context threaded: projected keys + `prebound.main_time_key`); verify the pinned cumsum cell, the ungrained-by-status, windowed-inner and re-aggregation-undetermined scenarios green in every mode, and `tests/test_dev1919_home_rooted_attached_inputs.py` fully green
- [ ] 2.8 D8: verify no `DEV-1903` citation remains in `tests/` or `slayer/` (`grep -rn "DEV-1903" tests/ slayer/` empty)
- [ ] 2.9 D9 arc42 (apply exactly the approved edit): Axiom 11.4 wording + the `enforced:` tag; verify `poetry run python tools/arch_check.py` green
- [ ] 2.10 Bless the two new goldens with `SLAYER_UPDATE_GOLDEN=1 poetry run pytest tests/test_dev1859_golden_sql.py`; review the diff: only the two new keys added, no existing key moved (a moved key → stop and report)
- [ ] 2.11 Docs: `docs/concepts/formulas.md` — the parameter sentence gains "an aggregate or a grained transform (`weight=rank(sum(amount, partition_by=region))`)"; the re-aggregation parameter sentence gains "or a grained transform over such cells"; `docs/concepts/queries.md` — "(aggregate-valued)" → "(aggregate- or transform-valued)"; verify `grep -n "aggregate-valued" docs/concepts/queries.md` is empty
- [ ] 2.12 Gates: `poetry run pytest -m "not integration" -n auto` all green; `poetry run ruff check slayer/ tests/`; `poetry run basedpyright` (no new errors vs baseline — never write the baseline); `poetry run python tools/arch_check.py`; `npx -y likec4@1.47.0 validate architecture`; Codex pass on the working tree; then commit by named file and STOP for the push / PR go-ahead

## 3. Review (spec-review stage)

- [ ] 3.1 `/process-reviews` until CodeRabbit, Sonar, Codex and CI are green; every check shows `pass`
- [ ] 3.2 With Egor's explicit OK: `openspec archive` the change, commit, push
