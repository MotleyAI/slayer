## 1. Failing tests first (spec-tests stage)

- [x] 1.0 MANDATORY baseline: DEV-1958 must be on `main` before any DEV-1903 test is written (the checker text, `_series_mode`, the shifted producer and the D6 rule are its baseline). If it is not, STOP the stage and say so — never write against pre-1958 names. Merge `origin/main` into this branch (merge commit, never rebase), run the full non-integration suite, and record the green baseline count in a comment on DEV-1903
- [x] 1.1 `tests/test_dev1903_discovery.py`: `discover_roots` disposition matrix — one shape per (phase, routing) pair; dual role (computed dimension + measure) yields two dispositions; a standalone re-aggregation root that is also a constituent of an inline row-attach root yields both routings (row+row and combined+row variants); the three consumer-context exclusions (order-by-name / field-typed filter → row-scope, measure-typed filter dimension subtree not walked, measure use keeps dual-role combined); `consumer_public_names` carries every consuming measure name for a constituent shared by two public measures; a row-attach root inline with inputs yielded vs producer-bound with inputs omitted; `time_shift` candidates carry `series`; deleted symbols absent (`combined_consumer_aggregates`, `dimension_regroup_roots`, `dimension_partitioned_aggregates`, `CombinedConsumers`, `_bare_combined_roots`, `_discover_roots`, `_answers_need_nested_regroups`, `local_crossing_input_paths`, `descend_aggregates`); verify every test fails today (ImportError) and the plan pins pass after item 1
- [x] 1.2 `tests/test_dev1903_producer_flag.py`: no entry point (`elaborate_query`, `compile_query`, `plan_query`, `compile_synthesized`, `elaborate_synthesized`) accepts either old flag or any producer-mode boolean; `compile_prebound` is not exported from `stages`; `compile_synthesized` requires `population` (no default); `compile_query` rejects an `ElaboratedProducer`; each environment type rejects the other query carrier; `ProducerContext.enclosing_grain` is required and non-optional; the cross-model producer passes `NoInheritedPopulation`, the other five `InheritedPopulation`; the top-only steps run at the top and never through the producer entry — filter split (`elaborate_synthesized` keeps a crossing conjunct unsplit), disposal (a fanning row filter yields `semi_join_filters` at the top, none through `compile_synthesized(population=NoInheritedPopulation(…))`), strip, total routing (blinded discovery raises at the top, not in a producer); a two-stage DAG whose non-root stage needs filter split and disposal gets each exactly once; with call counters on the top-only helpers, a late-wrap shape and a shifted shape show one call per authored stage and zero from nested producers; the probe-A shape (`avg(sum(amount, partition_by=[city, region]), partition_by=product)` by `[region, product]`, broadcast) plans with exactly one inline aggregate slot in the outer producer and executes (SQLite + DuckDB, values as `tests/test_dev1847_modes.py` pins); a carrier with two constituent grains whose union is the carrier grain nests both; a producer's own answer at its grain never nests when compiled through `compile_synthesized` (crossing-input `loss_payment_amt:sum` / `region_pay:sum`, parametrized — replaces the two deleted suppression tests); the post-1958 shifted-producer regressions of 2.2; verify RED today
- [x] 1.3 `tests/test_dev1903_opacity.py` (SQLite + DuckDB, `tests/_dev1919_fixtures` / dev1840 dataset): a local aggregate whose attached SOURCE input's inner crosses a join, whose attached KWARG crosses, and whose attached TRANSFORM input crosses — each executes with the hand-derived value, the outer is an inline aggregate slot and the input a row-phase attach with its own producer; a `Column.filter` dependency riding `ColumnSqlKey` inside the attached input still fails closed / routes as today; the DEV-1910 `locus="host"` wrap has no second producer and its safety check still fires; verify RED today (plan shape: host-rooted producer)
- [x] 1.4 `tests/test_dev1903_transform_inputs.py`: `check_transform_inputs` raises the post-1958 row-leaf message and the boolean message byte-identically (assert against the ledger rows); innermost attribution — `first(cumsum(weight))`, `rank(cumsum(weight))`, `time_shift(cumsum(weight), -1)` name `cumsum`; traversal matrix — a row-leaf transform inside an aggregate arg, kwarg, expression source, predicate, and under `first` / `last`; partition and time keys never leaves; `first` / `last` over an attached input never raises; precedence — `first(cumsum(weight))` fails with the unified row-leaf error naming `cumsum`, never the aggregation expression-source error; verify RED today (ImportError / outer op named)
- [x] 1.5 `tests/test_dev1903_first_last_dispatch.py`: `parse_expr` yields `AggCall` for both spellings and every shape; bind matrix — aggregated input → `TransformKey`, `last(amount)` / `last(amount, shipped_at)` → ranked `AggregateKey`, mixed composite and `first(1)` → expression-source error, `first(sum(x), partition_by=store)` → transform kwarg error; the spec scenarios — `first(rev)` / `first(rev * 2)` over a saved measure plan and execute (SQLite + DuckDB) identically to `first(revenue:sum)` / `first(revenue:sum * 2)`; the `cumsum` parity matrix (design D5) — for each operand shape (local saved measure, dotted cross-model saved measure, saved formula that is a transform, saved formula that is a composite, recursive saved measure, selected-measure alias in filter and in order position, ineligible unselected saved measure in filter / order) `first(X)` yields the same key kind or the same error as `cumsum(X)`; `_is_mixed_agg_source` absent; the P2 enforcement test (no grain decision in `syntax.py`: both spellings collapse to one node); verify RED today
- [x] 1.6 Consented re-pin: `tests/test_dev1847_gate.py::TestFirstLastDispatchUnchanged` asserts the BOUND key kind (`TransformKey` for an aggregated input, `AggregateKey` for `last(amount)`) instead of the parsed node
- [x] 1.7 Consented re-pin: `tests/test_dev1859_walker.py` — a nested transform is opaque to `_first_row_leaf` (judged on its own visit); docstring updated
- [x] 1.8 Consented re-pin: `tests/test_dev1868_series_shift_exec.py::test_nested_transform_over_row_leaf` (and `test_composite_hiding_transform_over_row_leaf`) assert `cumsum` is named; the other substring pins (`row`, `source_queries`) unchanged
- [x] 1.9 Consented edits (Codex-reviewed): `tests/test_filtered_local_isolation.py` — delete `test_disable_kwarg_suppresses_trigger` and `test_disable_flag_suppresses_widened_trigger` (replaced by 1.2's parametrized invariant), re-pin `test_disable_flag_keeps_target_rooted` to the producer entry (`compile_synthesized`) and rename it (producer mode keeps target-rooted discovery), retitle `TestRecursionSuppression`, remove `_agg_slot_for` if unused; `tests/test_dev1747_order_entry.py` — re-pin/rename `test_the_disabled_flag_does_not_suppress_target_rooted_plans` to the producer entry (`compile_synthesized`), fix the stale "local discovery off" docstring on `test_the_wrap_producer_holds_its_key_inline`
- [x] 1.10 Consented edit: `tests/test_dev1836_total_routing.py` — monkeypatch the symbol `stages` resolves for the one walk with a stub returning `[]`; drop `_blind_consumers` / `CombinedConsumers` / the `ir_bound` import; assertions unchanged
- [x] 1.10a Consented edit: `tests/test_dev1859_transform_row_leaf.py` — replace the `_SHIFT_FAMILY_OPS` import with a local `SHIFT_OPS = frozenset({"time_shift", "change", "change_pct"})`; `TestDerivedSet` asserts `SHIFT_OPS ⊆ ALL_TRANSFORMS` and `NON_SHIFT_OPS ∩ SHIFT_OPS = ∅` (the constant's identity is no longer pinned)
- [x] 1.11 `tests/_dev1871_raise_ledger.py`: re-anchor the two transform rows to `check_transform_inputs`; verify `tests/test_dev1871_raise_parity.py` is RED until item 3 lands
- [x] 1.12 Codex review of the tests against this plan; fold findings; commit

## 2. Baseline

- [x] 2.1 Re-merge `origin/main` at the start of spec-implement (merge commit, never rebase); verify the full non-integration suite and every DEV-1958 test green before touching planner code
- [x] 2.2 Confirm the post-1958 regressions in `tests/test_dev1903_producer_flag.py` (written in 1.2 against the merged baseline): a shifted producer with an own-grain ranked answer, a strict-constituent ranked answer, a windowed constituent, and carried attaches — only strict constituents nest, the answer itself does not, `attach_phase="shifted"` / `shift_of` / `answer_slot_id` unchanged; verify RED where the always-on flag changes today's nesting, else document as pins

## 3. Item 4 — first/last dispatch by type (design D5)

- [x] 3.1 `slayer/core/keys.py`: `is_attached_source(source)`; `is_reaggregation_key` reuses it; unit tests in `tests/test_keys.py`
- [x] 3.2 `slayer/engine/syntax.py`: `_convert_call` emits `AggCall` for `first` / `last` always; delete `_FIRST_LAST`, `_is_mixed_agg_source`; keep `_contains_agg_or_transform` for the unknown-name rung; update the NOSONAR rationale
- [x] 3.3 `slayer/engine/binding.py`: split `_bind_transform` into input binding + parameter binding; `_bind_agg` routes a ranked aggregation through the dedicated operand path (transform-input contexts) and dispatches on `is_attached_source` of the bound operand; row-grain arm unchanged; verify 1.5, 1.6, `tests/test_dev1859_plan_structure.py::TestFirstOverMixedKeepsExpressionError`, `tests/test_dev1748_*` and the "First and last keep transform dispatch" scenario green
- [x] 3.4 Grep `docs/` for "argument shape" / first-last dispatch wording; one sentence if any page states the parse-time rule

## 4. Item 3 — one transform-input checker (design D4)

- [x] 4.1 `slayer/engine/elaborate_env.py`: `check_transform_inputs` + `_TRANSFORM_INPUT_RULES`; `_first_row_leaf` opaque at a nested transform; delete `check_transform_row_leaf`, `check_time_shift_input`, `_check_shift_family_key`, `_SHIFT_FAMILY_OPS`, `_FIRST_LAST_OPS`; `slayer/engine/bind_inputs.py` makes one call; verify 1.4, 1.7, 1.8, 1.11 and raise parity green

## 5. Item 2 — one producer flag (design D3)

- [x] 5.1 Delete both flags from every entry point (`slayer/engine/compile/__init__.py` included); add `elaborate_synthesized`, `ElaboratedStage` / `ElaboratedProducer`, `ProducerContext`, `InheritedPopulation` / `NoInheritedPopulation`; split `compile_prebound` into the private `_route_top_level` / `_route_producer` / `_emit_planned` core; the top-level path owns filter split, disposal, strip and the total-routing assert; `compile_synthesized` requires `population`; delete the six caller-side predicates + `_answers_need_nested_regroups`; verify 1.2 entry-point tests green
- [x] 5.2 `_route_producer` nesting rule under `ProducerContext.enclosing_grain`: the D3 nesting rule (row always; combined off-grain unless the producer's own answer) (+ D6 clause + windowed-transform-input clause); verify the probe-A shape (1.2), `tests/test_dev1847_modes.py`, every golden byte-identical, full suite green
- [x] 5.3 1.9 / 1.10 edits land with this item; verify the whole `tests/test_filtered_local_isolation.py`, `tests/test_dev1747_order_entry.py`, `tests/test_dev1836_total_routing.py` green

## 6. Item 1 — one discovery walk (design D1, D2, D6)

- [x] 6.1 `slayer/engine/compile/discovery.py`: `RootDisposition` + `discover_roots`; verify 1.1 green
- [x] 6.1a Shared position classification (D1): `walk_consumer_positions` in `core/keys.py`; `consumer_roots` / `PositionClasses` / `position_classes` / `combined_partitioned_consumers` / `position_typing_context` in `elaborate_env`; rewire `bind_inputs` leniency, `elaborate`, `elaborate_env` typing and `discovery`; delete `ir/prebound.py::position_typing_context`; regression tests pinning a re-aggregation in a computed dimension (non-query-dimension `partition_by`; dual role with a measure and with an order; filter over it) and discovery dispositions under a re-aggregation's partition keys
- [x] 6.2 `_plan_regroups` consumes the disposition list: grouping by (phase, routing), consumer names merged, synthesis loops unchanged; delete the four walks, `CombinedConsumers`, `_bare_combined_roots`, `_discover_roots`, the `_local_broadcasts` scan, `producer_bound` / `mixed_inline_inner` arithmetic; split into discovery → grouping → synthesis (NOSONAR gone); verify every golden byte-identical and the full suite green
- [x] 6.3 D2 opacity: `aggregate_input_closure` loses `descend_aggregates`; `local_crossing_input_paths` deleted; the crossing-root predicate judges each node by its own closure; verify 1.3 green, goldens byte-identical (record any movement in `divergences.md` in this folder)
- [x] 6.4 D6: `compile/shift.py::plan_shifted_producers` takes the walk's non-series shift candidates; verify every `tests/test_dev1958_*` test and 2.2 green

## 7. Architecture, docs, gates

- [ ] 7.1 Present the two arc42 diffs (`ir.arc42.md` §1 wording; `engine.arc42.md` P2 enforced tag) for explicit approval; land only after the OK; `poetry run python tools/arch_check.py` and `npx -y likec4@1.47.0 validate architecture` green
- [x] 7.2 `poetry run ruff check slayer/ tests/` clean; `poetry run basedpyright` no new errors vs baseline; `guards.baseline` untouched
- [x] 7.3 `poetry run pytest -m "not integration" -n auto` fully green; then the CI integration invocation from `.github/workflows/ci.yml`
