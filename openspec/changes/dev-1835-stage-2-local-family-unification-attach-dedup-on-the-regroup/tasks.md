# Tasks — dev-1835 Stage 2

## 1. Tests first (TDD — land before implementation)

- [x] 1.1 `tests/_dev1835_fixtures.py`: hand-computed oracles on the DEV-1739/1824 dataset for the desugared families (bare `wm`/`rk` at RC/RCM grains incl. NULL region/city), the guard-dissolution shapes (transform/composite/filter over bare `wm`/`rk`, `time_shift(amount:last, -1)`), `change`/`change_pct` over attached inputs, and the union-grain broadcasts (mixed windowed/first-last inner grains); verify two oracles by hand in review
- [x] 1.2 Matrix flips in `tests/test_dev1837_dimension_measure_matrix.py`: the 10 DEV-1835 cells (incl. the DEV-1839-added `mixed` family's wm/rk) move from strict-xfail to supported (oracle or solo-equality + no `__regroup__` + `assert_scope_closed` + cardinality neutrality), on SQLite and DuckDB; verify: cells fail for the right reason pre-implementation (NotImplementedError, not setup)
- [x] 1.3 `tests/test_dev1835_desugar.py`: bare-vs-explicit twin equivalence (values identical; combined query renders ONE shared producer), migrated-family executed-value pins, keyless/no-dims edge (single-row CROSS JOIN), order-only and filter-only bare references (hidden producers; order-only bare windowed pins the fix for today's silent window drop), filtered measure beside unfiltered companions (cardinality + companion values unchanged), D10 dual-role aggregate alongside a bare windowed measure; SQLite + DuckDB; verify: fail-for-the-right-reason
- [x] 1.4 `tests/test_dev1835_guard_dissolution.py`: executed values for G4/G5/G6/G7 shapes (+ post-projection twin) and `time_shift`-over-ranked; `change`/`change_pct`-over-attached fix (fails today with `RenderContextMissingFacilityError`); mixed OR keeps the no-common-scope directive; AND conjuncts split (filter-only + row predicate; windowed + plain aggregate); verify: fail-for-the-right-reason
- [x] 1.5 `tests/test_dev1835_dedup.py`: same-aggregate-both-roles → one producer relation in SQL; bare+explicit twin → one; negative cases (differing window duration / measure filter / explicit ranking arg → separate producers, each correct); verify: fails pre-implementation (two producers today)
- [x] 1.6 `tests/test_dev1835_union_grain.py` (D9, DEV-1839 lift): windowed-inner + plain sibling at same declared grain → (region, bucket) union broadcast; first/last-inner + different-grain sibling; unresolvable-TD failure message; verify: strict-xfail or fail-for-the-right-reason until task 3.7
- [x] 1.7 Guard tests updated in `tests/test_dev1837_guards.py` + a `tests/test_dev1835_guards.py`: deleted arms/messages asserted absent package-wide (windowed/ranked coexistence arm, G4/G5/G6/G7, `time_shift`-over-ranked, the residual DEV-1839 union guard); G1/G8/G2 + ranked no-ranking-column pinned verbatim; G3 re-pointed to DEV-1836 (the cross-model partition messages already point there); `test_dev1824_remaining_guards.py` G4/G5 preservation pins retired; verify by running the files
- [x] 1.8 `tests/test_dev1835_golden_sql.py` + `tests/golden/dev1835_sql_baseline.json`: flagship shapes (bare wm, bare rk, band×wm, dedup dual-role, cumsum-over-bare-wm) plus generation-smoke on tsql/bigquery/one case-folding dialect (parses, single flat WITH, scope-closed); verify: harness runs (baselines blessed during implementation)
- [x] 1.9 DEV-1732 frame-bound and explicit-ranking-time semantics as executed-value tests (`tests/test_dev1835_semantic_pins.py`, incl. the already-working ranked composition shapes); verify: pass today AND post-migration

## 2. Implementation — desugar, producers, deletion, dedup

> Progress markers: `[x]` done, `[~]` partial (see HANDOVER.md), `[ ]` not started.
> No executed-value regressions in DEV-1824/1839/1748-execution.

- [x] 2.1 Desugar pass (design D1): bare windowed/ranked measure/order/filter refs become combined-attach roots at the full projected grain (slot provenance per D5, active TD via the D5 bucket); G1/G8/G2 validate on the desugar path; order-only-windowed binding preserves `window=` (session 2). DONE.
- [x] 2.2 Producer synthesis from original expressions + nested LOCAL row attaches (design D4): PLANNING + RENDERING done. Windowed/ranked kernels go regroup-aware (`render_value_key` for computed-dim grains, nested row producers joined); the collapse folds row attaches into a hoisted WITH so no `_wm_`/`_rk_` relation surfaces. All 32 matrix wm/rk cells pass (session 2).
- [~] 2.3 Windowed collapse + uniform `_cm_` naming (design D3) — DONE (`_collapses_to_windowed_cte` / `_render_collapsed_windowed_plan`; ranked internals renamed off `_rk_`).
- [~] 2.4 Consumer-level deletion (design D2): the DEV-1835 coexistence arm, G4/G5/G6/G7, post-projection twin, `time_shift`-over-ranked are DELETED and G3 re-pointed. The isolation enum members / OrderScope-OrderEnv pair cleanup is NOT yet done (the top-level windowed/ranked plan lists are empty post-desugar, so those arms are already dead code — a tidy-up, not a correctness gap).
- [x] 2.5 Plan-time dedup (design D6): identity grouping + partition-free dedup + simple dual-role row+combined → 1 producer. CROSS-LEVEL dedup resolved by pruning a functionally-determined computed-dim grain key (`_prune_functionally_determined_grain`) so the redundant nested producer is never synthesized (respects the "no cross-producer-internal structural dedup" non-goal); `test_dual_role_beside_bare_windowed_is_two_producers` passes (session 2).
- [x] 2.6 Guard dissolution + `change`/`change_pct` fix (design D7): guards dissolved; transform-over-attached (change/change_pct/time_shift over partitioned / bare wm / bare last) renders via the transform chain with the combined producer joined into the shifted CTE. 14 guard_dissolution tests pass (session 2).
- [x] 2.7 Full non-integration suite green; golden divergences enumerated (452, pure `_wm_`/`_rk_`→`_cm_` shape; values proven unchanged by 274 executed-value tests) → user batch-approved → re-blessed; SQL-shape suites rewritten preserving intent (session 2). Also fixed a D9 filter-conjunction-split regression from session 1's `fe7dce16` (`_has_partitioned_ref` narrowed to combined-only, dropping computed-dim filters). Verify: `poetry run pytest -m "not integration"` clean.

## 3. DEV-1839 lift + wrap-up

- [x] 3.7 Effective-grain union + broadcast for windowed/first-last inner aggregates (design D9): `_effective_root_grain` folds the active bucket into a windowed transform-root's union grain; a windowed inner nests into its own windowed producer (own-grain exception for windowed transform inputs); nested-attach validation admits a windowed attach at the full grain; the residual union guard is deleted. `test_dev1835_union_grain.py` passes on both engines (session 2). Also fixed a generator hoisting bug (`_uniquify_producer_base_ctes` clobbered a nested windowed producer's shadowed inline `_base` refs).
- [ ] 3.8 Perf corpus re-recorded; regressions reported in the PR description; verify: `tests/perf/compare` results committed — PENDING USER DECISION (heavy network A/B audit vs. D3's predicted delta: one extra source scan per windowed producer group).
- [x] 3.9 Docs: `.claude/skills/slayer-query.md` deferred-list + `docs/architecture/composable-attach.md` roadmap updated to the lifted surface; `docs/concepts/formulas.md` / `queries.md` grep-clean (no stale can't-combine claims); verify: grep clean.
- [x] 3.10 `openspec validate dev-1835-... --strict` green; `poetry run ruff check slayer/ tests/` clean.
