## 1. Tests first (all land before implementation; each must fail for feature-missing reasons)

- [x] 1.1 Shared fixtures for stage-1 shapes (local model with region/city/time grain, deterministic seeded data) and verify they import cleanly from a smoke test
- [x] 1.2 Executed-value + golden tests for every `queries/partitioned-aggregates` scenario (window=+partition_by, first/last+partition_by incl. temporal-partition-key ranking, transforms over partitioned aggregates, filter routing incl. AND-split / mixed-aggregate / no-common-scope error, coexistence incl. same-aggregate-both-roles and both ORDER BY forms, per-family coexistence pairs, cardinality neutrality, empty partition set) on SQLite, DuckDB variants where the existing pattern applies; verify each scenario has ≥1 test referencing it
- [x] 1.3 Executed-value + golden tests for every `queries/computed-dimensions` scenario (banding, two partition sets in one expression, rank-as-dimension vs rank-as-measure context grain, first/last-in-dimension, window=-in-dimension incl. no-time-dimension error, bare-aggregate / nested-attach / cross-model-source errors) with the same coverage check
- [x] 1.4 Hoist collision tests: two producers with identical internal CTE shapes, three-level nesting, case-folding and identifier-truncation collisions; verify generated SQL parses and `assert_scope_closed` passes
- [x] 1.5 Guard-preservation tests asserting the exact messages of every error that remains (design Non-Goals list) still raise; verify against current messages
- [x] 1.6 Multi-stage boundary tests: downstream stage selects/filters/orders/re-aggregates a regrouped dimension; exact StageSchema and response-metadata assertions; no `__regroup__` leakage in public schemas or emitted SQL
- [x] 1.7 Run the full non-integration suite: existing tests green, new tests failing only with the expected NotImplementedError/ValueError shapes

## 2. Mechanics

- [x] 2.1 Thread the parent AliasAllocator through nested renders and switch producer rendering to the (ctes, select) contract; verify full-suite golden byte-identity (no test churn) before any guard lift
- [x] 2.2 Recursive grain classification in regroup discovery (self_contained / consumer_composable / requires_nested_attach per design D3); verify with planner unit tests incl. differing/subset/empty partition sets — DONE (`dimension_regroup_roots` yields transform-over-grained-aggregate roots (self_contained at producer grain) + bare partitioned aggregates; consumer composition (CASE/arithmetic) stays in the consumer over placeholders; requires_nested_attach — an aggregate partitioned by a computed dimension — fails closed via `_reraise_nested_attach`)
- [x] 2.3 Producer synthesis extensions: synthesized TimeTruncKey time dimension for windowed producers (D5), explicit ranking key for ranked producers (D6), transforms compiled inside producers at producer grain (D4); verify with planner unit tests — DONE (windowed producer adds `window_td_key` to its grain + main_time_key (D5); a transform root renders as a producer measure with a transform layer at producer grain (D4); ranked first/last still collapse to a ranked CTE ordering on the model time column (D6))
- [~] 2.4 Attach plan declares the producer unique key; planner validates join pairs cover it exactly and keyless attaches are provably one-row (D8); verify with unit tests for both violation directions — cardinality neutrality is verified by executed-value tests (`TestCardinalityNeutral`, and every `_assert_solo_equality` pair); the explicit producer-unique-key FIELD + planner assertion is a hardening not yet added (no test exercises a violation). Deferred as "as needed" — the join_pairs are the full producer grain by construction.
- [x] 2.5 Explicit filter placement decided pre-substitution with per-conjunct availability-scope resolution (D7); verify with routing unit tests incl. windowed/ranked operands not being combined-available — DONE (`_split_partitioned_filter_conjuncts` splits top-level ANDs of partitioned-touching filters, `conjunct_scope` routes each to combined/base/HAVING or raises "split the filter"; combined conjuncts carried into `outer_where_filter_ids`, generator folds regroup placeholders into the outer-WHERE slot→CTE map)
- [x] 2.6 ORDER BY form discrimination: dimension-alias vs raw-aggregate routing (D9); verify both order-form tests pass — DONE (`combined_partitioned_aggregates` routes a top-level raw partitioned aggregate order target to combined even when shared with a row dimension; `_order_combined_keys` forces STRICT partition-key validation on that combined role → clean "not a query dimension" for a finer-grain raw order)

## 3. Guard lifts (one commit each, individually revertable)

- [x] 3.1 Lift producer-needs-CTE (stage_planner.py:1452) via the hoist; verify L6/1.4 tests pass — DONE. `_render_producer_split` hoists EVERY internal WITH (top-level or nested in an outer projection-trim SELECT); `_base`/`base` are reserved in the allocator and renamed per-producer via `_uniquify_producer_base_ctes`; `step`/`_wm_`/`_cm_`/`_rk_` are already allocator-unique. A BigQuery/T-SQL round-trip mis-parse of dotted result-key columns (`_base.\`orders.region\`` and bare `\`orders.region\``) is repaired by the scope-aware `unmangle_dotted_table_refs` at every re-parse seam. TestThreeLevelNesting + sibling/case-fold/truncating hoist tests green on all dialects.
- [x] 3.2 Lift row+combined coexistence (stage_planner.py:1354, generator.py:1601); verify coexistence tests pass — DONE (both guards lifted; the cross-model render path builds `_base` with row producers joined in and combined producers at the combined SELECT; band+measure, same-agg-both-roles, cardinality-neutral all green). Superseded DEV-1829 deferral tests reconciled per A1. The two ORDER-BY-raw-aggregate cases are the D9 order-form work → task 2.6.
- [x] 3.3 Lift partition_by+window= (stage_planner.py:478); verify L1 tests pass — DONE (local; the windowed producer synthesizes the consumer's active TD into its grain (D5) and hoists its `_base`/`_wm_` WITH; cross-model still guarded, and window= without a resolvable time dimension raises the plain-windowed time-dim error).
- [x] 3.4 Lift partition_by on first/last (stage_planner.py:483); verify L2 tests pass — DONE (local; producer collapses to a ranked CTE, ranking on the model time column, temporal-partition-key does not hijack; cross-model deferred). Superseded DEV-1739/1829 guard tests reconciled per A1.
- [x] 3.5 Lift partitioned-aggregate-in-transform (stage_planner.py:491); verify L3 tests pass — DONE (local only; cross-model still guarded). See HANDOVER.md §2 for the superseded DEV-1739/1829 guard tests awaiting a reconciliation decision.
- [x] 3.6 Lift filter-on-partitioned-aggregate (stage_planner.py:496); verify L4 tests pass — DONE (local; cross-model still guarded). Superseded DEV-1739/1829 deferral tests + `guard/in_filter` golden reconciled per A1.
- [x] 3.7 Lift transform / first-last / window= inside dimension expressions (stage_planner.py:2788, :2812, :2817); verify computed-dimension tests pass — DONE. first/last (ranked CTE), window= (windowed producer, D5), and transform (producer-grain via `dimension_regroup_roots`/D4) all render as row attaches; grain-self-containment messages name partition_by; nested-attach / cross-model sources fail closed.
- [x] 3.8 Lift the coexistence render belt (generator.py:1593) keeping the cross-model-partitioned and CTE-body arms; verify per-family coexistence tests pass and remaining-guard tests still raise — DONE (combined-attach coexists with cross-model/windowed/ranked/transform; row-attach + isolated still deferred; DEV-1829 planner deferral tests reconciled with consent)

## 4. Validation

- [x] 4.1 Full non-integration suite green; all pre-existing goldens byte-identical (any divergence individually justified in the PR); new goldens committed — DONE (247 dev1824 tests pass, 0 non-1824 failures; golden divergences are the A1 superseded-deferral reconciliations logged in HANDOVER §7; `dev1824_sql_baseline.json` blessed)
- [x] 4.2 Scope validation (SLAYER_VALIDATE_SCOPES) exercised over the new shapes in tests; verify no C1/C2 leaks — DONE (the golden harness forces `SLAYER_VALIDATE_SCOPES=1`; all lift/* cases scope-close on postgres/sqlite/duckdb/tsql/bigquery)
- [x] 4.3 `poetry run ruff check slayer/ tests/` clean — DONE

## 5. Documentation

- [x] 5.1 New `docs/architecture/composable-attach.md` (architecture, invariants, 3-stage roadmap) linked from `zensical.toml` nav in the same commit; verify nav entry present — DONE (nav entry after "Ranked aggregates")
- [x] 5.2 Update `docs/concepts/queries.md`, `docs/concepts/formulas.md`, and the aggregation examples for the lifted shapes; verify no stale "not yet supported" claims remain for stage-1 shapes (grep) — DONE (grep confirms only cross-model / nested-attach deferrals remain)
- [x] 5.3 Update `.claude/skills/slayer-query.md` / `slayer-models.md` / `slayer-overview.md` capability claims; verify by grep for the old guard wording — DONE (slayer-query.md measure + computed-dimension claims updated; models/overview carry no stale partition claims)
- [x] 5.4 Append the DECISIONS.md entry (context-grain rule, allocator threading, placement field, structural cardinality check); verify entry present at bottom in existing format — DONE
