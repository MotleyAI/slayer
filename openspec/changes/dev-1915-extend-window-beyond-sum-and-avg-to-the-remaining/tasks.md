## 1. Failing suites (spec-tests stage)

Seed for the executed module (`tests/_dev1915_fixtures.py`; monthly buckets, 2024 leap
year; interval lower bounds Jan 2023-11-03, Feb 2023-12-02, Mar 2024-01-02):
`orders` (id, amount, qty, created_at, updated_at, region) = 1: 100, 1, 2024-01-01,
2024-01-02, US · 2: 200, 2, 2024-01-15, 2024-01-16, US · 3: 300, 3, 2024-02-15, NULL, EU ·
4: 400, 4, 2024-03-15, 2024-03-25, EU · 5: 300, 5, 2024-03-20, 2024-03-16, US; intervals
Jan {1,2}, Feb {1,2,3}, Mar {2,3,4,5}. `customers` (id, spend, signup_at, tier) as the
DEV-1836 seed, `orders.customer_id` → customers. Model aggregations: `trimmed_mean`
(`AVG(CASE WHEN {value} BETWEEN {lo} AND {hi} THEN {value} END)`, defaults lo=0, hi=1000)
on `orders`; a custom aggregation with a definition-default column parameter on
`customers`.

- [ ] 1.1 `tests/_dev1915_fixtures.py`: models, seeds (SQLite + DuckDB), `make_exec_engine`-style fixture body, and an oracle smoke test deriving every value from the raw rows (count {2,3,4}, count_distinct {2,3,3}, min {100,100,200}, max {200,300,400}, avg/median {150,200,300}, stddev_samp {70.71,100,81.65}, var_pop {2500,6666.67,5000}, corr {1,1,0.6325}, covar_samp {50,100,66.67}, weighted_avg {166.67,233.33,314.29}, trimmed_mean(150,350) {200,250,266.67}, first {100,100,200}, last {200,300,300}, last(updated_at) {200,200,400}, region-partitioned count US/Jan 2, EU/Feb 1, EU/Mar 2, US/Mar 2, attached-weight `weighted_avg(weight=qty:sum(partition_by=region))` {150,195.65,296.67}); verify the smoke test passes on the current tree
- [ ] 1.2 `tests/test_dev1915_windowed_exec.py` (SQLite + DuckDB in-process, unit suite): one executed test per family in 1.1, plus `window='1d'` empty-interval (column count 0, `*:count` 0, sum NULL, last NULL, every bucket present), `last(updated_at)` mixed-NULL and the EU/Feb all-NULL-key cell (300), `first(updated_at)` February pinned per dialect (SQLite 300 NULLs-first, DuckDB 100 NULLs-last), percentile(p=0.5) = median, custom with and without overrides, custom-on-joined-target with a definition-default column param, cross-model `customers.spend:count(window='1y')` {1,2,4} and `last(customers.signup_at, window='1y')` {100,150,40} over the signup month, partition_by count, filter-only `amount:count(window='90d') >= 3` (Feb, Mar), order-only `amount:max(window='90d')` desc (Mar, Feb, Jan; no windowed column in the response), cardinality neutrality (adding a windowed count changes no row count or sibling value), two-stage `rev:count(window='60d')` {1,2,2,1} and `rev:first(window='60d')` {100,100,200,400} over the DEV-1471 stage fixture equal to the model-backed evaluation, and two fail-closed probes (windowed `corr` whose `other=` crosses a fanning hop from `customers`; windowed `last` whose explicit ranking column does) asserting the existing input-safety errors; verify every non-probe test fails on the current tree with the sum/avg error
- [ ] 1.3 `tests/test_dev1915_golden_sql.py` + `tests/golden/dev1915_sql_baseline.json` over the seven harness dialects: count, min, max, count_distinct, count_distinct_approx, star_count, median, percentile_p09, stddev_samp, var_pop, corr, covar_samp, weighted_avg, weighted_avg_attached_weight, custom_trimmed_mean, custom_default_literal_p, custom_joined_default_column, first, last, last_explicit_time, partitioned_count, cross_model_last, expression_source_count_distinct, order_only_max, filter_only_count; module-level assertions that every `first`/`last` case records SQL and that only median/percentile on mysql/tsql/bigquery and corr/covar on mysql record raises; verify the baseline is generated as recorded raises (sum/avg error) on the current tree and the assertions are red
- [ ] 1.4 Consented edits: delete `test_g1_windowed_sum_avg_only` + `G1_MESSAGE` (`tests/test_dev1835_guards.py`, docstring updated); delete `test_windowable_flags_are_exact`, `test_window_agg_class_replaces_the_hardcode`, `test_non_windowable_aggregation_fails_closed` and the `window_agg_class` import (`tests/test_dev1744_value_expr.py`); re-point `test_windowed_non_sum_avg_raises`, `test_custom_aggregation_with_window_raises` and the `g1_non_sum_avg` arm (`tests/test_sql_generator.py`) and `test_non_sum_avg_windowed_order_target_still_raises` (`tests/test_dev1733_order_only_transform_composite.py`) from raises to renders (scope-closed, no placeholder leak); verify each re-pointed test is red on the current tree
- [ ] 1.5 `tests/_dev1871_raise_ledger.py`: drop the "only supported for sum and avg" row and re-anchor the two duration rows to `check_window_duration`; verify `tests/test_dev1871_raise_parity.py` is red on the current tree

## 2. Planner (D3, D4)

- [ ] 2.1 `slayer/ir/planned.py`: `TrailingWindowProducerKernel` += `ranking_time_key: Optional[ValueKey] = None`, `picked_params: List[PickedParam] = []`; verify pyright and the existing kernel fingerprint tests pass
- [ ] 2.2 `slayer/engine/compile/stages.py`: `_trailing_window_kernel` takes `root_model` + `bundle`; sets `ranking_time_key` for `first`/`last` via `resolve_ranking_time_key` (row keys = the producer's ordered row keys, as `_ranked_kernel`); builds `picked_params` from `resolve_aggregation_params` on the source owner (source anchor path walked from the producer root) and remaps each picked key through the sub-plan's regroup substitutions as the association path does; verify the plan-structure of `last(window=)` carries the ranking key and the attached-weight case carries a remapped picked param
- [ ] 2.3 `slayer/engine/elaborate_env.py`: `check_windowed_key_supported` → `check_window_duration(*, window_val)` (duration checks only), call site in `_guard_windowed_measures` updated; verify 1.5 green and the malformed/empty/non-string duration tests still pass

## 3. Renderer (D1, D2, D3, D4)

- [ ] 3.1 `slayer/sql/generator.py` `_render_window_measure_cte_from_planned`: star source → literal `1 AS _w_value` before any scope resolve; picked params → `_w_p<i>` via `_render_picked_param_value` in the `_src` scope; `first`/`last` → `_w_rank` via `_ranked_scope_expr(cast_derived=False)`; verify the golden SQL for star_count, weighted_avg and last projects those columns
- [ ] 3.2 Multiset outer: resolve the aggregation definition on the source owner, build `AggRenderSpec(model_name="_src", name="_w_value", sql="_w_value" for column count else None, aggregation_def, type=slot type, column_type=None, agg_kwargs = literal kwargs (kind="str") ∪ picked (kind="expr"), window/partition_by stripped)` and render through `_build_agg`, cast as today; verify every non-ranked executed family in 1.2 passes on SQLite + DuckDB and the dialect-gap goldens record the plain-form errors
- [ ] 3.3 Ranked outer: inner SELECT over `_base LEFT JOIN _src` with `build_rank_column` partitioned by the base grain, `ranked_ordered` on `_src._w_rank`, `build_ranked_pick` GROUP BY grain, `_ranked_value_cast_type`; verify the first/last, explicit-time, NULL-key, empty-interval and cross-model-last executed cases pass on both engines and the golden assertion that no first/last case raises is green
- [ ] 3.4 `slayer/sql/render/aggregates.py`: delete `window_class`, `windowable`, `window_agg_class`, the sum/avg comment and the generator import; verify 1.4 green and `AGG_REGISTRY` still agrees with `BUILTIN_AGGREGATIONS` both ways
- [ ] 3.5 Record the golden matrix as SQL through `ALLOWED_DELTAS` (added, regenerated, emptied); verify the baseline diff is only the dev1915 file and every other golden is byte-identical

## 4. Docs, accounting, gates

- [ ] 4.1 Docs, one concise sentence per change: `docs/concepts/formulas.md` (section "Windowed sum and average" → "Windowed aggregations"; every aggregation, first/last reading, empty-interval semantics, per-row parameters; the stale follow-ups paragraph and the inbound anchor in `docs/concepts/queries.md` corrected), `docs/examples/07_aggregations/aggregations.md` (table row and paragraph generalised), `slayer/mcp/server.py` help line (every aggregation takes `window=`); verify `grep -rn "sum and avg\|only sum" docs/ slayer/` is empty and `zensical` nav needs no change (no new page)
- [ ] 4.2 Confirm no arc42 / `index.yaml` edit is needed and `guards.baseline` is unchanged; run `poetry run python tools/arch_check.py`; verify green
- [ ] 4.3 `openspec validate dev-1915-extend-window-beyond-sum-and-avg-to-the-remaining --strict`, `poetry run pytest -m "not integration" -n auto`, `poetry run ruff check slayer/ tests/`, conventions gate, `poetry run basedpyright` (baseline holds or shrinks); verify all green, then the Codex working-tree pass before push
