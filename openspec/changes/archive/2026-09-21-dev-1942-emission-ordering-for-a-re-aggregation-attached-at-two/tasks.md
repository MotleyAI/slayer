## 1. Failing tests first (spec-tests stage)

- [x] 1.1 `tests/test_dev1942_dual_phase_reagg.py` (dual-engine, monthly + customers fixtures): issue shape executes (a: North 10 / South 5 / West NULL; b: North 100/200/300, South 25/75, West NULL); standalone in filter (`min(X, partition_by=region) > 5`) and order positions beside the mixed measure; the shape plus an unrelated `cumsum(amount:sum)` measure (steps prelude path); plan pins — exactly two attaches share one placeholder with phases {row, combined}, that placeholder slot staged BASE; dry-run SQL on every Tier-1 dialect has exactly two `_cm_` CTEs with the carrier before the re-aggregation CTE and passes the scope validator; verify RED today (guard error) and the pins fail for the right reason
- [x] 1.2 Same module, population cases: `sum(spend * avg(spend:sum(partition_by=[tier, plan_code]), partition_by=tier))` over `[tier]` with `orders.status = 'ok'` = gold 18050 / silver 26450 (and `!= 27550`), EXISTS in every `_cm_` CTE, no `orders` join alias, semi-join entries name `m`; the same with the standalone `avg` beside it (95 / 115, one `_cm_` for the avg, entries name `a` and `m`); standalone `min(...)` under `tier = 'bronze' or orders.status = 'ok'` yields the dropped-filter warning; verify RED today (27550, missing entries / warning)
- [x] 1.3 `tests/test_dev1942_golden_sql.py` + `tests/golden/dev1942_sql_baseline.json` (7 dialects): `dual_phase/measure`, `dual_phase/filter_position`, `dual_phase/order_position`, `dual_phase/with_transform`, `population/mixed_reagg_constituent`, `population/dual_phase_with_filter`; verify the harness records raises today and the baseline blesses after 3–5
- [x] 1.4 `tests/test_dev1942_consumer_substitution.py`: `substitute_consumer_keys` mirrors `walk_consumer_keys` — replaced at match; opaque below an attach-owning aggregate's source/args/kwargs; partition keys traversed; plain composites and transforms traversed; scalars pass through; identity object when nothing matches; deep vs consumer difference pinned on the issue shape (deep erases the nested root, consumer keeps it); verify RED (ImportError) today
- [x] 1.5 `tests/test_dev1942_cte_dependencies.py`: split entries carry deps through a nested hoist (two producers each with its own `_base` and different nested deps → distinct re-keyed edges); the combined preparer's consumer node declares its hoisted names; validator unit tests (forward reference raises `CteOrderError`, ordered chain passes, physical table not flagged, schema-qualified same basename not flagged, derived-table alias shadowing not flagged, nested `WITH` scope resolved, quoted / case-folded names); the dual-phase statement passes on every dialect; a multi-stage DAG query whose non-root stage carries the dual-phase re-aggregation executes on both engines and passes the validator; verify RED today
  - Calibration (spec-tests): the two white-box clauses (split-entry re-keying, combined preparer declares hoisted names) are enforced OBSERVABLY, not by pinning private generator signatures — `assert_dependency_ordered_ctes` (suite-wide + per Tier-1 dialect) plus the adversarial dual-phase shape and a two-distinct-hoists shape where each carrier must precede its OWN consumer (insertion order alone lands a carrier last → the exact DEV-1942 bug). Keep P6 enforcement behavioral in spec-implement (don't add a brittle registry-internals unit test unless the structural cut makes one cheap and stable).
- [x] 1.6 Flip `TestReaggAxiomCompliance.test_reagg_used_standalone_and_mixed_fails_closed` in `tests/test_dev1832_transform_source.py` to executed values on both engines (rename `…_executes`); delete the `check_reaggregation_not_standalone_and_mixed` row in `tests/_dev1871_raise_ledger.py`; verify the ledger parity test is RED until the guard is deleted
- [x] 1.7 Codex review of the tests against this plan; fold findings; commit

## 2. Planner — substitution mirrors discovery (design D1, D6, D7)

- [x] 2.1 Add `substitute_consumer_keys` to `slayer/core/keys.py` beside `walk_consumer_keys`; verify 1.4 green
- [x] 2.2 `_substitute_prebound` and `substitute_in_bound_filter` take `substitute=`; the re-aggregation pre-substitution in `_plan_regroups` passes the consumer-scoped one; verify the 1.1 plan pins green (two attaches, BASE placeholder) and `test_dev1859*`, `test_dev1847*`, `test_dev1832*`, `test_dev1928`-era pins unchanged
- [x] 2.3 Thread `population_filters` into the `reagg_constituents` synthesis loop; set `dropped_filter_warnings` and `population_semi_join_measures` on both re-aggregation synthesis paths and the carrier attach (consuming public names); verify 1.2 green
- [x] 2.4 Delete `check_reaggregation_not_standalone_and_mixed` (elaborate_env), its import + call (bind_inputs); verify ledger parity and `test_law_guard_ratchet` green, `guards.baseline` untouched

## 3. Renderer — declared dependencies through hoisting (design D2–D5)

- [x] 3.1 Statement-scoped dependency registry: stack + assembly wrapper over all four `assemble_with_chain` sites; push/pop in `_render_producer_split` and the stage loop; `_uniquify_producer_base_ctes` returns its rename map; `_split_statement_ctes` returns `List[CteEntry]` with re-keyed deps and fails closed on a duplicate name across `WITH` nodes; verify the registry unit tests in 1.5 green
- [x] 3.2 `_prepare_combined_regroup_attaches` returns `Node`s (hoisted + consumer with declared deps); both `cm_regroup_ctes` sites splice; `_prepare_regroup_attaches` gives hoisted nodes their own deps; verify 1.1 CTE-order pins and the dual-phase golden green on all dialects and DuckDB execution green
- [x] 3.3 `generate_planned_stages` assembles by declared edges (root split without base rename; stage relation declares hoisted + sibling read; root entries declare stage relations); verify the multi-stage test in 1.5 green and every DAG / notebook golden byte-identical
- [x] 3.4 `scope_check.assert_dependency_ordered_ctes` (scope-aware) hooked into `maybe_validate_scopes`; verify 1.5 validator tests green and the full unit suite green under the harness flag
- [x] 3.5 Re-bless goldens whose CTE order shifted under declared deps (record the file list here: `tests/golden/dev1839_sql_baseline.json` (lift/dual_role)); verify each shifted case still executes on SQLite + DuckDB via its execution twin
  - Also re-bless the six `tests/golden/dev1942_sql_baseline.json` cases (five raise→SQL, `population/mixed_reagg_constituent` SQL→SQL) via `ALLOWED_DELTAS`, then FLIP `test_dev1942_golden_sql.py::test_fail_closed_cases_record_a_raise` to `assert not isinstance(value, dict)` (rename `…_generate_sql`): once the guard is gone these all emit SQL, so the record-a-raise pin must invert.

## 4. Docs, architecture, gates

- [x] 4.1 `docs/concepts/formulas.md` (line 67 sentence): one clause — the same re-aggregation may be selected on its own and used inside a mixed source in one query; verify the page renders (`zensical`/mkdocs build not required; grep the clause)
- [x] 4.2 `architecture/sql.arc42.md` P6 `[review]` → `[enforced: test:tests/test_dev1942_cte_dependencies.py]` (approved exact diff); verify `poetry run python tools/arch_check.py` green
- [x] 4.3 Full non-integration suite, `ruff`, `basedpyright` (no new errors vs baseline), `openspec validate --strict`; verify all green
- [x] 4.4 Follow-up check: if any plain-producer diagnostic (broadcast / associate / degenerate entries) is still missing on the constituent path, file the Linear issue with an actionable comment and note it here
  - Check outcome: nothing missing — `_synthesize_reaggregation_producer` sets `broadcast_measure` / `associated_measure` / `degenerate_measure` on both the root and the constituent attach. The only residue is naming (a mixed constituent's broadcast/associate/degenerate entries carry the re-aggregation's internal alias, not the consuming measure name — parallel to the semi-join naming DEV-1944 tracks). Raised with Egor for a follow-up decision rather than filed unilaterally.
