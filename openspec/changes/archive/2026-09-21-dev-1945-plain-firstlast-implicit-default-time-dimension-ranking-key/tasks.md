## 1. Tests (spec-tests stage — RED without the change)

- [x] 1.1 Add `tests/_dev1945_fixtures.py`: `orders` (id PK, amount, created_at, derived `li_ts` = `line_items.created_at`, a filtered column `li_flag_ts` whose `Column.filter` reads `line_items.qty`, an unparseable derived `bad_ts`, and `cust_signup` = `customers.signup_at`), `line_items` (id PK, order_id, qty, created_at, derived `sh_ts` = `shipments.shipped_at`), `shipments` (id PK, line_item_id, shipped_at), `customers` (id PK, signup_at); joins `orders → line_items` on `id = order_id` and `line_items → shipments` on `id = line_item_id` UNDECLARED (reverse-PK-only, unproven), `orders → customers` on `customer_id = id` (proven); a `models(...)` builder that sets each model's `default_time_dimension` per case; seeded SQLite via `tests._engine_helpers.make_seeded_sqlite_engine` with hand-computed oracles (orders 1: 10.0 / 2024-01-01 / customer 1 signed up 2023-06-01; orders 2: 30.0 / 2024-01-02 / customer 2 signed up 2023-07-01; line items 1,2 on order 1 dated 2024-01-05 / 2024-01-03, item 3 on order 2 dated 2024-01-04); verify the module imports and `_dev1945` models save with only the DEV-1930 warning
- [x] 1.2 Add `tests/test_dev1945_ranking_key_safety.py` — refusal rows: host-rooted `amount:last` and `amount:first` with `default_time_dimension="li_ts"` and no time/temporal dimension → `ValueError` naming `li_ts`, `line_items`, `orders` (the "ranks/reads by" form); target-rooted `line_items.qty:last` with `line_items.default_time_dimension="sh_ts"` → the cross-model form naming `sh_ts`, `line_items`, `shipments`; verify RED today (both render SQL) / GREEN after 2.x
- [x] 1.3 Same file: `dimensions=["line_items.created_at"]` + `amount:last` parametrised over `to_many_handling` broadcast/error/associate → refused naming `created_at` and `line_items`; the windowed twin `amount:last(window='30d')` with that dimension AND a `created_at` month time dimension → refused (D8); `time_dimensions=[line_items.created_at month]` + `amount:last` → refused; verify RED today (all rank inside the fan) / GREEN
- [x] 1.4 Same file: explicit `amount:last(li_ts)` → message names `li_ts` and `line_items` (RED today: hop-only message); explicit `amount:last(line_items.created_at)` → names `created_at` and `line_items` (GREEN today, regression guard); `default_time_dimension="li_flag_ts"` + `amount:last` → refused naming `li_flag_ts` and `line_items` (RED); `default_time_dimension="bad_ts"` + `amount:last` → the unanalyzable-dependency error naming `bad_ts` (RED: render-time failure today)
- [x] 1.5 Same file: executed rows with pinned values and SQL-shape assertions (D7): local default `created_at` → 30.0; no default + `filters=["created_at > '2023-01-01'"]` → 30.0 (the hidden-slot candidate); `default_time_dimension="cust_signup"` (proven hop) → 30.0, SQL joins `customers` in the ranked CTE and has no `CAST` around the ranking expression; `amount:last(window='30d')` on a `created_at` month time dimension with `default_time_dimension="li_ts"` → executes (`2024-01-01` bucket = 30.0) and the SQL never joins `line_items`; `amount:last(cust_signup, window='30d')` → executes, `_src` joins `customers`, no `CAST` around `_w_rank`; verify all GREEN today except none — this row is the over-refusal guard
- [x] 1.6 Same file, unit rows on `resolve_ranking_time_key`: a derived default yields `ColumnSqlKey(path=(), model=<root>, column_name=<default>)`, a plain default yields `ColumnKey`; the function has no `target_path` parameter (`inspect.signature`); verify RED today
- [x] 1.7 Consented edit (2026-09-21): delete the class `TestRankingKeyFullPathValidation` in `tests/test_dev1476_first_last_explicit_time.py` (its single test `test_invalid_leaf_past_first_hop_is_rejected_at_plan_time` is the only caller of the dead branch) and any now-unused import; verify `poetry run pytest tests/test_dev1476_first_last_explicit_time.py` green
- [x] 1.8 Codex-review the tests against this plan; run the FULL unit suite (`poetry run pytest -m "not integration" -n auto`) and confirm every red is in the new module; `git add` the two new files by name (edited files stay unstaged for the spec-implement gate); commit; STOP at reset point 2

## 2. Implementation (spec-implement stage)

- [x] 2.1 D3/D4 `slayer/engine/ranked_planner.py`: model default → `column_default_key(path=(), leaf=root_model.default_time_dimension, base=root_model)`; delete the `target_path` parameter and both its branches, `_resolves_on`, `_ranking_key_name`, the "not resolvable" raise and the now-unused imports (`AmbiguousJoinPathError`, `walk`, `reroot_value_key`); docstring = the one precedence list; verify 1.6 green and `poetry run ruff check slayer/`
- [x] 2.2 D1 `slayer/engine/compile/stages.py`: add `_ranking_key_crossing(*, key, root_model, bundle, alias) -> Optional[Tuple[str, str]]` (closure via `key_closure`; `None` → `check_input_dependencies_analyzable`; first path not `safe_reachable` → `(column_leaf(key), path[-1])`); replace the explicit-args loop in `_assert_local_producer_inputs_safe` with one call on `explicit_ranking_time_arg(agg)`; verify 1.4's explicit rows green and `tests/test_dev1900_*.py`, `tests/test_dev1748_*.py`, `tests/test_dev1476_*.py` green
- [x] 2.3 D5/D6 same file: add `_checked_ranking_time_key(*, producer_plan, agg_key, root_model, bundle, alias, target_rooted)` (resolve as today, judge via 2.2, raise through `check_local_producer_inputs_safe` / `check_cross_model_inputs_safe` with `key_broadcast_reason`); `_ranked_kernel` and `_trailing_window_kernel` call it and gain `alias` + `target_rooted`; the four call sites pass `canonical_aggregate_alias(<host-coordinate agg>, profile="stage_formula")` and `target_rooted=True` inside `_synthesize_cross_model_producer`; verify 1.2, 1.3, 1.5 green and `grep -n "raise " slayer/engine/compile/stages.py` unchanged in count (`tests/test_dev1871_raise_parity.py` green)
- [x] 2.4 Docs: append to the ranking-resolution paragraph in `docs/concepts/models.md` (the "ordering resolves via" sentence) exactly one sentence: "Whichever column wins is an input of the aggregation: if it, or a derived definition it names, crosses a join hop that is not provably to-one from the aggregation's root, the query fails with the input-safety error exactly as an explicit ranking argument would."; verify the page still renders in the docs nav (no `zensical.toml` change needed)
- [x] 2.5 arc42 (edit APPROVED by Egor 2026-09-21 during spec-plan — apply exactly this and nothing else): in `architecture/semantics.arc42.md` Axiom 2, after the line `   [enforced: test:tests/test_dev1832_home.py]` insert the line `   [enforced: test:tests/test_dev1945_ranking_key_safety.py]`; verify `poetry run python tools/arch_check.py` green
- [x] 2.6 Gates: `poetry run pytest -m "not integration" -n auto` all green (a golden diff in dev1748/dev1915 is a finding to investigate, not a re-bless); `poetry run ruff check slayer/ tests/`; `poetry run basedpyright` (no new errors vs baseline — never write the baseline); `poetry run python tools/arch_check.py`; `npx -y likec4@1.47.0 validate architecture`; Codex pass on the working tree; then commit by named file and STOP for the push/PR go-ahead (reset point 3)

## 3. Review (spec-review stage)

- [x] 3.1 `/process-reviews` until CodeRabbit, Sonar, Codex and CI are green; confirm every check shows `pass`
- [x] 3.2 With Egor's explicit OK: `openspec archive` the change, commit, push
