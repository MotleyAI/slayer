# Tasks

## 1. Tests first (TDD — all initially failing, one per spec scenario)

- [x] 1.1 `tests/test_dev1902_join_key_contract.py` — construction: `is_base_column_sql` / `physical_column_sql` / `Column.is_base` / `Column.physical_name` (None, bare, double-quoted, expression → raise); `ModelJoin` rejects a dotted entry; `SlayerModel` rejects an undeclared, expression, and filtered source key with model/target/column/remedy in the message; `apply_extension_overlay` rejects an extension join with an undeclared source key; fails until 2.1 / 2.2
- [x] 1.2 Same file — target side: `save_model` on SQLite storage raises `JoinKeyError` for an expression target key; `audit_join_safety` emits an error finding for an undeclared target key; `physical_join_pairs` raises `JoinKeyError` for an unresolvable key and a query crossing such a join fails before SQL; fails until 2.3 / 2.4
- [x] 1.3 Same file — drift and refinement: a double-quoted self-name column present in the live table is not drift and keeps its join (`diff_sql_table_model`); type refinement narrows a double-quoted `DOUBLE` column naming a live integer column; `_detection_skip_reason` still skips a non-base target key; fails until 2.6
- [x] 1.4 `tests/test_dev1902_join_key_spelling.py` — executed on SQLite + DuckDB via `make_exec_engine` (`tests/_dev1832_fixtures.py` shape, new fixture models with renamed keys): renamed source FK; renamed target PK (provably to-one, no broadcast warning, exact values); both renamed over two hops; associate mode over a renamed-PK root; population pushdown (EXISTS spine) over renamed keys; cardinality detection over renamed keys detects `many_to_one`; each asserts hand-computed values; this file is P16's enforcing test; fails until 2.3–2.5
- [x] 1.5 Same file — unit: `_side_stats_sql` quotes a mixed-case physical key; `_unique_key_sets` returns column names; `provably_to_one` / `grain_determines` seeds with a renamed target key and a renamed FK with no map (the DEV-1892 review-fix cases stay green); fails until 2.4
- [x] 1.6 `tests/test_dev1902_golden_sql.py` via `bind_golden_tests` with its own baseline: ON clause with a renamed key, semi-join spine over renamed keys, association shape over a renamed PK root, a mixed-case physical key quoted (postgres dialect); baseline generated with `SLAYER_UPDATE_GOLDEN=1` once the shapes are approved; existing golden modules asserted byte-identical
- [x] 1.7 `tests/test_dev1902_join_key_migration.py` — YAML and SQLite backends: a v10 document with a source-side physical spelling is rewritten and written back at v11; target-side physical spelling rewritten against the peer's stored columns; a mirror pair spelled two ways collapses to one edge; an unmatched source entry fails to load with the remedy while siblings load; a `name == sql` store is untouched apart from the version; fails until 2.7
- [x] 1.8 Importer tests in the existing modules: `tests/test_cube_converter.py` — member-name join pairs (`test_join_member_resolves_to_physical_column` re-pinned to `[["customer_id", "id"]]` + `sql: cust_pk` on the column — consent given 2026-09-23) and a non-member operand synthesises a hidden key column, on the cube's model and on a view facade carrying the join (`tests/test_cube_views.py`); `tests/test_dbt_entities.py` / `tests/test_dbt_converter.py` — hidden FK column synthesised for a foreign entity, non-bare `expr` skips with a warning (list any column-count assertions that flip for consent); `tests/test_osi_converter.py` — a relationship keyed on an expression column is skipped with the warning; ingestion — a `_count` FK column yields `count_col` in both the column and the join key; fails until 2.8
- [x] 1.9 `tests/test_planned.py::TestJoinRequirement` deleted (consent given 2026-09-23) in the same commit as 2.9
- [x] 1.10 Existing fixtures brought under the contract (consent given 2026-09-23, option A): every fixture whose join names an undeclared key column declares it (unit suite: ~350 tests in ~22 files, chiefly `orders.customer_id`, `loss_payment.Claim_Amount_Identifier`; integration fixtures likewise); `tests/test_dev1892_review_fixes.py::test_renamed_fk_seeds_to_one_hop` keys on the logical `region_id`; the `tests/test_detect_join_cardinality.py` expression-key skip case becomes a construction rejection. Any rendered-output expectation that moves because a declared column appears is listed for a separate consent; Cube/dbt importer tests are left to 2.8

## 2. Implementation

- [x] 2.1 `slayer/core/models.py`: `is_base_column_sql`, `physical_column_sql` (on `core.refs.IDENTIFIER_RE`), `Column.is_base` / `Column.physical_name`, `_check_join_keys`, `ModelJoin` field validator applies `_validate_column_name`, `SlayerModel` after-validator calls `_check_join_keys`; `slayer/core/errors.py`: `JoinKeyError`; `slayer/ir/source_bundle.py`: `apply_extension_overlay` calls `_check_join_keys`; verify 1.1 green
- [x] 2.2 `slayer/core/join_walker.py`: `physical_join_pairs`; verify the seam cases of 1.2 green
- [x] 2.3 `slayer/sql/generator.py` `_build_from_and_joins` through the seam; `slayer/engine/compile/stages.py` `_forward_hops` / `_reverse_hops` register physical pairs; `slayer/ir/planned.py` `SemiJoinHop` doc PHYSICAL; verify the base-join, spine and pushdown cases of 1.4 green on both engines
- [x] 2.4 `slayer/engine/join_safety.py`: logical `_unique_key_sets`, direct comparisons in `provably_to_one` / `_entity_seeded` / `_hop_pins`, delete `_physical_name` / `_physical_grain_leaves` / `_BARE_IDENT_RE`, audit finding for a non-base key; `slayer/engine/query_engine.py`: `_validate_join_keys` at save, `_detect_one_join` through `Column.physical_name`, `_detection_skip_reason` via `Column.is_base`; verify 1.2, 1.5 and the association / detection cases of 1.4 green
- [x] 2.5 Run `poetry run pytest tests/test_dev1892_review_fixes.py tests/test_dev1836_safety_predicate.py tests/test_dev1853_oriented_safety.py tests/test_dev1910_association_plan.py` to confirm the seed and kernel tests stay green with no map
- [x] 2.6 `slayer/engine/schema_drift.py` and `slayer/storage/type_refinement.py`: `_column_is_base` delegates; the four raw `sql` lookups go through `physical_column_sql`; verify 1.3 green and `tests/test_schema_drift_error.py`, `tests/test_validate_models.py`, `tests/test_storage_type_refinement.py` green
- [x] 2.7 `slayer/storage/migrations.py`: v11 + no-op converter; `slayer/storage/base.py`: `canonical_join_pairs`, `_canonicalize_join_key_spellings` before `_dedup_exact_inverse_joins`, `_stored_counterpart` canonicalises the peer's joins; verify 1.7 green on both backends and `tests/test_migrations.py` green
- [x] 2.8 `slayer/cube/converter.py` (+ `slayer/cube/refs.py` docstring), `slayer/dbt/entities.py` / `slayer/dbt/converter.py`, `slayer/osi/converter.py`, `slayer/engine/ingestion.py` (`_logical_column_name`, dotted branch deleted); verify 1.8 green
- [x] 2.9 `slayer/ir/planned.py`: delete `JoinRequirement`; verify 1.9 and `tests/test_planned.py` green
- [x] 2.10 `architecture/system.arc42.md` §3 item 16, the approved wording from design.md decision 10 verbatim, in the commit that adds 1.4; verify `poetry run python tools/arch_check.py` green

## 3. Docs and spec hygiene

- [x] 3.1 One sentence each: `docs/concepts/models.md` Joins section (keys name columns by `name`, base columns only, `sql` applied at emission) and `version` "currently `9`" → `11` (two places); `docs/cube/cube_import.md` ("member references resolve to their physical columns" → keys are member names, non-base members drop the join); `slayer/mcp/server.py` join-dict docstring (keys are column names); verify by grep that no doc still says join keys are physical
- [x] 3.2 `openspec validate dev-1902-pin-modeljoinjoin-pairs-to-one-column-name-spelling --strict` green after any wording adjustments made during implementation

## 4. Verification

- [x] 4.1 Full non-integration suite green: `poetry run pytest -m "not integration" -n auto`; existing golden modules byte-identical (no `ALLOWED_DELTAS` entries needed)
- [x] 4.2 `poetry run ruff check slayer/ tests/`, `poetry run python tools/arch_check.py`, `poetry run basedpyright` (no new errors vs baseline; shrink in touched files), conventions gate clear; `legacy_arrows` / `guards` baselines unchanged
- [x] 4.3 Codex read-only pass over the working tree before the push; PR vs `main` opened with Egor's go-ahead
