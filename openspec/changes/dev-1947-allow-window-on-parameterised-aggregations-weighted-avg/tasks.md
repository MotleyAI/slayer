## 1. Pins (spec-tests stage)

Close-out: the behaviour landed in DEV-1915 and the strict xfail was dropped on main, so
every executed and plan-structure test below PASSES on the current tree by design — do NOT
manufacture a red (no reverting DEV-1915, no xfail). The only red on the current tree is
1.5: the golden module fails with "is not in the golden baseline" for the fourteen new keys
until spec-implement blesses them.

- [ ] 1.1 `tests/_dev1919_fixtures.py`: add `WINDOWED_CUSTOM_PARAM = "customers.spend:wsum(window='1y', weight=sum(amount, partition_by=customers.regions.name))"` and `windowed_custom_param_by_month()` — its own docstring, built from `_region_weight()`, `_months()`, `_trailing_year()` and `_sum_product()` (trailing-1y Σ spend × region order total by signup month), NOT an alias of `windowed_constituent_by_month`; export both in `__all__`; verify the oracle returns {2024-01: 10000, 2024-02: 25000, 2024-03: 28080, 2024-04: 33780}
- [ ] 1.2 `tests/test_dev1919_home_rooted_attached_inputs.py`: a `wsum_engine` fixture (`params=["sqlite", "duckdb"]`, `make_orders_engine(request, models=customers_wsum_models())`, `customers_wsum_models` imported from `tests._dev1859_fixtures`); in `TestWindowedOuter`, `test_windowed_custom_parameter_every_mode` parametrized over `MODES` over `signup_month_td()`: `assert_cells(month_vals(resp), {**windowed_custom_param_by_month(), None: None})` and `_assert_no_mode_warnings(resp)`; verify 6 green (2 engines × 3 modes)
- [ ] 1.3 Same class, `test_windowed_parameter_in_filter_and_order_positions` on `orders_engine`, default mode, `AMOUNT` selected over `signup_month_td()`: `filters=[f"{WINDOWED_PARAM} > 110"]` keeps exactly the `2024-02` bucket with `a == 55`; `order=[{"column": WINDOWED_PARAM, "direction": "desc"}]` yields the non-NULL months `["2024-02", "2024-03", "2024-01", "2024-04"]` with every `a` unchanged; each response's row keys are exactly `{"orders.a", "orders.customers.signup_at"}` (no windowed column); verify green on both engines
- [ ] 1.4 Same class, `test_windowed_parameter_reads_the_row_attach` (sync, no engine): `plan_query(query=mode_q(None, time_dimensions=signup_month_td(), measures=[_m(WINDOWED_PARAM)]), bundle=ResolvedSourceBundle(source_model=models[0], referenced_models=models[1:]))` over `dev1840_models()` (`ResolvedSourceBundle` from `slayer.ir.source_bundle`, as `tests/test_dev1892_plan_structure.py` does); assert exactly one `regroup_attach_plans` entry, `kernel.kind == "trailing-window"`, `[p.name for p in kernel.picked_params] == ["weight"]`, the attach's `producer_plan.regroup_attach_plans` has exactly one entry with exactly one substitution, that substitution's `original_key` is an `AggregateKey` with `agg == "sum"`, and its `placeholder == kernel.picked_params[0].key`; verify green
- [ ] 1.5 `tests/test_dev1859_golden_sql.py`: add cases `"param/windowed"` (`models="orders"`, `source="orders"`, `mode=None`, `kw={"time_dimensions": signup_month_td(), "measures": [{"formula": WINDOWED_PARAM, "name": "w"}]}`) and `"param/windowed_custom"` (`models="orders_wsum"`, same kw with `WINDOWED_CUSTOM_PARAM`), importing the constants from `tests._dev1919_fixtures` and `signup_month_td` from `tests._dev1840_fixtures`; no `ALLOWED_DELTAS` entry (new keys fold in); verify the module is RED only with "is not in the golden baseline" for the 14 new keys
- [ ] 1.6 `poetry run pytest tests/test_dev1919_home_rooted_attached_inputs.py tests/test_dev1859_golden_sql.py -q` and `poetry run ruff check tests/`; verify the only failures are 1.5's missing keys

## 2. Bless (spec-implement stage — no change under `slayer/`)

- [ ] 2.1 `SLAYER_UPDATE_GOLDEN=1 poetry run pytest tests/test_dev1859_golden_sql.py -q`, then review the 14 new entries in `tests/golden/dev1859_sql_baseline.json` before committing: each is a `customers`-rooted trailing-window producer whose `_src` projects `_w_p0` from the row-attached `orders`-rooted region-total producer, with no `__regroup__` text; every pre-existing key byte-identical (`git diff --stat` touches only the new keys); verify the module is green without the env var and `test_param_cases_generate` passes
- [ ] 2.2 Gates: `poetry run pytest -m "not integration" -n auto`, `poetry run ruff check slayer/ tests/`, `poetry run basedpyright` (baseline holds or shrinks), `poetry run python tools/arch_check.py`; verify all green (the conventions gate `bash ~/.claude/skills/process-reviews/scripts/check-conventions.sh <PR>` runs at spec-review once the PR exists), then the Codex working-tree pass before push

## 3. Accounting

- [ ] 3.1 Confirm no docs change is needed (`grep -rn "sum and avg\|only sum" docs/ slayer/` is empty; `docs/concepts/formulas.md` already states every aggregation takes `window=` and attached-aggregate parameters read per interval row) and no arc42 / LikeC4 / `index.yaml` edit; verify `openspec validate dev-1947-allow-window-on-parameterised-aggregations-weighted-avg --strict` is green
