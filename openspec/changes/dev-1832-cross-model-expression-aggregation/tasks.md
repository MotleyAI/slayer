## 1. Fixtures and failing suites (spec-tests stage)

- [ ] 1.1 `tests/_dev1832_fixtures.py`: DEV-1847 graph + DEV-1900 fanning hop, plus `customers.discount`, filtered `customers.north_spend`, a to-one `stores` branch with `rent`, `sales.q_amount` (existing), a `wsum` custom aggregation on `customers`; SQLite + DuckDB seeds and hand-computed oracles for every scenario; verify with a fixtures smoke test that re-derives each oracle from the raw rows
- [ ] 1.2 `tests/test_dev1832_anchor.py`: unit tests for `source_anchor_path` / `source_leaf_paths` (column, star, literal-only, host-mixed, target-only, two-branch expressions) and the AST guard over `slayer/engine` + `slayer/sql` rejecting every direct `.source` path read outside the accessor; verify the guard is red on the current tree
- [ ] 1.3 `tests/test_dev1832_home.py`: `Aggregate.home_path` per home scenario (target, host, two-branch, parameter-widened, default-widened, spelling equivalence, no-home fallback), parallel named edges yield distinct paths, a re-elaborated sub-plan yields root-relative paths, nested cross-model producer reads the term; verify they fail on the current tree
- [ ] 1.4 `tests/test_dev1832_cross_model_exec.py`: executed values on SQLite + DuckDB for every scenario of *Row-level expressions can be aggregated* and *Expression source typing* (incl. fanning-leaf and unanalysable-leaf errors, `first` rejection, associate mode, cardinality invariant, `assert_scope_closed`, no placeholder leak); verify they fail on the current tree
- [ ] 1.5 `tests/test_dev1832_column_filter.py`: every `models/column-filters` scenario, each of the three behaviour changes as a fail-without-fix test, single-column SQL identity, filtered leaf on a joined model incl. the fanning-filter error; verify they fail on the current tree
- [ ] 1.6 `tests/test_dev1832_transform_source.py`: the running-total oracle, the ungrained degenerate warning, mixed transform constituent, joined-model row leaf in a mixed source, row-leaf rejection, projected-key legality, axis error, windowed-inner-under-transform probe, `first`/`last` dispatch unchanged, plan structure (one transform-root producer inside the carrier, one flat `WITH`); verify they fail on the current tree
- [ ] 1.7 `tests/test_dev1832_golden_sql.py` + `tests/golden/dev1832_sql_baseline.json` over postgres/sqlite/duckdb/tsql/bigquery for the lifted shapes; verify the baseline is recorded once the implementation is green (initially recorded raises)
- [ ] 1.8 Re-point pins with the agreed consent: `tests/test_dev1847_gate.py` (nested transform, cross-model, filtered operand → accepted; row-leaf variant still raises via the checker), `tests/test_dev1859_plan_structure.py::test_nested_transform_in_source_stays_rejected`; verify they fail on the current tree

## 2. Source anchor (D1)

- [x] 2.1 Add `source_anchor_path` / `source_leaf_paths` to `slayer/core/keys.py`; verify `test_dev1832_anchor.py` unit tests pass
- [x] 2.2 Sweep every site enumerated by the AST guard onto the accessor (keys classifiers, `join_safety`, `compile/stages.py`, `compile/staging.py`, `binding`, `elaborate_env._key_display`, generator passes, `naming`, `render/value_expr`); verify the AST guard is green and the full unit suite is unchanged

## 3. Home in the elaborator (D2, D3)

- [x] 3.1 `slayer/ir/terms.py`: `Aggregate.home_path`; new `slayer/engine/home.py` with the S2 rule seeded from leaf paths, parameters and defaults (moved out of `compile/stages.py`); `elaborate.py` resolves homes and passes them to `build_environment`; verify `test_dev1832_home.py` elaborator cases pass and `arch_check` model-truth stays green (arch_check green 2026-09-16; Axiom 2 rewrite applied and approved; constituent-grain inputs are 3.1b)
- [x] 3.1b `home_path_for` adds every attached constituent's grain members as inputs (Axiom 2.3): `_effective_root_grain` moves from `compile/stages.py` to `core/keys.py` as `effective_root_grain`; `resolve_aggregate_homes` takes the query's dimension keys, time-dimension keys and active bucket from `elaborate.py`; pins in `tests/test_dev1832_home.py` (status grain → root, `customers.tier` grain → customers, ungrained → query dims); verify `test_joined_model_row_leaf_mixed_by_status` passes on SQLite and DuckDB
- [x] 3.2 `compile_synthesized` helper; every sub-plan call site uses it (five after the DEV-1910 merge); `compile_prebound` requires `env`, local typing branch deleted; verify the nested-producer home test passes and existing goldens are byte-identical (needed a new `engine.compile -> engine.elaborate` model arrow — approved by Egor 2026-09-16; `arch_check` OK, diagrams regenerated)
- [x] 3.3 `_synthesize_cross_model_producer` reads `home_path` from the term; `_home_path` / `_default_home_candidate_paths` removed from the compiler; drop the binder's dotted-leaf rejection; verify `test_dev1832_cross_model_exec.py` passes on SQLite and DuckDB (structure done; the remaining reds are 3.3a, 3.3b and 3.5)
- [x] 3.3a `sql/generator.py` custom-formula substitution parenthesises every non-atomic `{value}` / parameter fragment (`SUM((spend - pop) * spend)`), and the `wsum` oracle in `test_dev1832_cross_model_exec.py` iterates every customer like `_spend_minus_pop_by_tier` (consented and applied 2026-09-16: gold −18475, silver −2100, bronze NULL); verify `test_custom_aggregation_resolves_on_the_anchor` on both backends and no golden moves except through `ALLOWED_DELTAS`
- [x] 3.3b `_assert_local_producer_inputs_safe` checks the expression source's own row leaves (`include_source=True`) for a non-host-locus crossing-local-root, so a fanning / unproven / unanalysable source leaf fails closed while an explicit host-grain wrap (`locus="host"`) stays exempt; the closure already walks expression operands (`key_closure` via `_child_keys`), so `_explicit_input_refs` is unchanged; verify `test_fanning_leaf_fails_closed` and `test_unanalysable_leaf_fails_closed` raise the existing typed errors and `test_dev1838` host-grain wraps stay legal
- [x] 3.4 Merge DEV-1910 into this branch (its branch head `ec519dcb`, stacked — PR base follows) per the conflict recipe in the Linear mini-plan; extend the anchor guard to reject `key_host_path(<expr>.source)`; verify the guard, `test_dev1910_*`, `test_dev1841_*`, `test_dev1892_*`, `test_dev1900_*` green
- [x] 3.5 The association arm also triggers under `associate` when an attached input is unattributable from the home (no unattributable dimension → no associated warning); verify `test_attached_parameter_accepted_under_associate` on both backends and `test_attached_parameter_rejected_under_broadcast` still raises

## 4. Column.filter desugar (D5)

- [x] 4.1 `Column.needs_expansion` (value non-trivial or filter set); both binder `ColumnSqlKey` decisions read it; `column_dependency` adds filter-reference edges (a filter naming its own column is a cycle; a filtered physical column becomes a root/node) and raises on a broken dotted filter path at save; `_column_key_closure` walks value AND filter; cycle/dependency scenarios pass
- [x] 4.2 CASE wrapper (`column_expansion.wrap_column_filter` / `expand_column_definition_sync`) at the three expansion seams — `_process_reference_site`, `scope._anchor` ColumnSqlKey arm, generator `_expand_derived_column_sql` — filter expanded at the owner path; the declared-type CAST rides the value so single-column SQL is `SUM(CASE WHEN … THEN col END)` (no outer cast); single-column identity + derived-over-filtered pass
- [x] 4.3 Deleted `AggregateKey.column_filter_key`, `_resolve_column_filter_key`, closure filter arms + `compute_column_filter_join_paths`, generator `_wrap_filter` / count(*) CASE / ranked picked-value CASE / param-mask CASE / `_expand_column_filter_sql` / `_qualify_column_filter_sql`, `AggRenderSpec.filter_sql`, compiler identity tuples, renderer guard; retired `SqlExprKey` + `parse_sql_expr` (`canonicalize_sql` kept). `test_dev1832_column_filter.py` 33/33 green; ~18 obsolete-machinery test files updated (Egor consent 2026-09-16). Golden re-bless deferred to 6.4 (adds `dev1859 param/mixed_plus` + `mixed/outer_partition` alias-hash deltas)

## 5. Transform constituents (D4)

Design D4 (a–e) settled with Egor 2026-09-17: explicit axis, normalisation, collapsing
desugar, all in this change. The working tree at head `bcd66547` carries task-5 WIP in
`core/keys.py`, `engine/syntax.py`, `engine/binding.py`, `engine/home.py`,
`engine/compile/stages.py`, `engine/elaborate_env.py` — ALL SIX ARE KEPT (the
handover-5 "revert elaborate_env.py" instruction is withdrawn: its constituent arm is
D4d). Monthly fixture rows: North Jan 10 / Feb 20 / Mar 30, South Jan 5 / Feb 15;
`X` below = `amount:sum(partition_by=[region, ordered_at])`.

- [x] 5.0 Normative prerequisite (Codex: the axiom must precede the code): present ONE exact `architecture/semantics.arc42.md` diff — the Axiom 11 rewrite as 11.1–11.5 (draft approved by Egor 2026-09-17; verbatim text in the "design session 6" Linear comment), the Axiom 2.3 transform clause re-pointed at "its result grain (Axiom 11)", and the `enforced:` tags on Axiom 6 and Axiom 11 (`tests/test_dev1832_transform_source.py`) — wait for the OK, apply, `poetry run python tools/arch_check.py`
- [x] 5.1 Admission (D4): keep the WIP; `engine/syntax.py` `AggCall.source` union gains `TransformCall`; `core/keys.py` `_AggregateSource` gains `TransformKey` (`model_rebuild` already runs); `compile/stages.py` gains `_constituent_alias(c)` (aggregate → `canonical_aggregate_alias(c, profile="stage_formula")` or `c.agg`; transform → `c.op`) used by `_synthesize_reaggregation_producer` and `_build_carrier_attach`, whose `enable_producer_regroups` compares `constituent_grain(c, …)` to the union grain instead of `c.partition_keys`; verify the parse/classifier cases in `test_dev1832_transform_source.py`, `test_dev1847_gate.py`, `test_dev1859_plan_structure.py`
- [x] 5.2 Time plumbing (D4d): `engine/bind_inputs._attach_time_keys` gains an `AggregateKey` arm mapping source, args and kwargs (identity-preserving, like `map_children`); `engine/elaborate_env._time_search_children` yields an aggregate's source/args/kwargs; verify the generated SQL for `sum(cumsum(X) - 1)` carries `ORDER BY` inside the window, and the same measure with no `time_dimensions` raises "requires an unambiguous time dimension"
- [x] 5.3 Axis check (D4a/D4d): keep the WIP `_temporal_axis_transforms` + position-neutral message in `check_dimension_temporal_axis`, but the constituent arm yields EVERY `TransformKey` reachable inside each transform constituent (`walk_value_keys(c)`), mirroring the dimension arm, so a nested time transform cannot evade the rule; update the `tests/_dev1871_raise_ledger.py` row; re-record the `dev1839` `guard/dim_temporal_axis_missing` golden raises through `ALLOWED_DELTAS`; verify `test_dev1871_raise_parity.py`, `test_law_guard_ratchet.py` (the `^A time-ordered transform .* accumulates within its own grain\.$` entry still matches), that `sum(cumsum(amount:sum(partition_by=region)))` and the nested `sum(cumsum(last(amount:sum(partition_by=region)) - X))` with a month dimension both raise the axis error, while every top-level `cumsum(amount:sum(partition_by=region))` pin (`test_dev1739_guards.py:102`, `test_dev1829_guards.py:64`, `test_dev1868_*`) is untouched
- [x] 5.4 Normalisation (D4b): `core/keys.normalize_transform_constituents(key, *, query_grain)` — post-order over `map_children`; for each `AggregateKey` root and each `TransformKey` in `operand_constituents(root.source)`, every `AggregateKey` under `walk_value_keys(t.input)` with `partition_keys is None`, `window_kwarg_of(a) is None` and `not source_anchor_path(a.source)` gets `partition_keys=query_grain`; `effective_root_grain` transform arm: no grained inner → `Grain.of([*projected_dim_keys, *projected_td_keys])`; wired in `bind_inputs.bind_query_inputs` BEFORE the `_rw` partition-key pass (Codex: the synthesized keys must go through `assert_partition_key_attributable` / `check_partition_key_resolves` like a user-written `partition_by=`) through `_map_bound_keys(..., skip_dimensions=True)` (new flag) with `query_grain = Grain.of([*_dim_key_set, *_td_key_set])`, and `_reagg_operand_keys` computed AFTER it; verify `sum(cumsum(amount:sum))` = 15/50/80 + degenerate warning, `sum(rank(region))` identity + warning, the mixed-operand executed test `sum(rank(X - amount:sum))` by month with `region != 'West'` = Jan 5 / Feb 9 / Mar 1 (ranks over the five `(region, month)` cells of `X − month total`; without normalisation every cell is 0 and the sums are 2 / 2 / 1), a query dimension not attributable from the host (Graph A, rooted at `orders`, dims `[customers.regions.region_events.value]`-style fanning dim) with `sum(cumsum(amount:sum))` raising the partition-key attributability error, an ORDER BY on the alias of `sum(cumsum(amount:sum))` interning onto the measure's slot (no duplicate column), and a user-written `partition_by=region` inside a constituent still lenient after normalisation
- [x] 5.5 Collapsing (D4c): `core/enums.AXIS_COLLAPSING_TRANSFORMS = frozenset({"first", "last"})` beside `RANK_FAMILY_TRANSFORMS`; `core/keys.lower_collapsing_constituents(key)` — post-order; for each `AggregateKey` root and each top-level `TransformKey` `t` in `operand_constituents(root.source)` with `t.op` collapsing and `t.time_key` set, substitute `t` inside `root.source` with `AggregateKey(source=t, agg="max", partition_keys=regroup_root_grain(t) - {t.time_key})`; wired after the `_rw` pass for every position (dimensions included; ties along the axis are impossible because cells are grouped, and an all-NULL picked value stays NULL under `max`); PROBE FIRST with a plan-structure assertion that the synthesized `max` is discovered as a re-aggregation root INSIDE the carrier sub-plan (its `partition_by=region` is a carrier grain key there, so `check_reaggregation_partition_key_is_query_dim` passes) and never at the top level; verify on SQLite and DuckDB: `sum(last(X))` = 45 every month with the broadcast warning naming the month dimension (the preserving treatment gives March 30 — the fail-without-fix pin; the `West` NULL cell exercises the all-NULL pick), `sum(first(X))` = 15 every month + warning, `sum(cumsum(X) - last(X))` = Jan −30 / Feb 5 / Mar 30 with no warning, the mixed row source `sum(amount * last(X))` = Jan 375 / Feb 825 / Mar 900, filter-only `sum(last(X)) > 40` (all months survive, values unchanged), ORDER BY-only on `sum(last(X))` (compiles, one slot), the computed dimension banding `sum(last(X), partition_by=[region])` (groups 30 → North's 3 rows, 15 → South's 2, NULL → West), plus `band(sum(last(amount:sum)))` still raising the dimension residue error; cardinality invariant, one flat `WITH`, `assert_scope_closed`, no placeholder leak. If the nested re-aggregation constituent cannot render, add a dedicated collapse step in `_build_carrier_attach` (outer GROUP BY on the collapsed grain over the transform-root producer); never the preserving treatment, never a deferral
- [x] 5.6 Family coverage: add the monthly row `(6, "West", "2024-02", NULL)` (every existing oracle unchanged — `SUM` ignores NULL; the smoke test's derivations skip `None`); executed constituent tests with fixture oracles re-derived in `test_dev1832_fixtures_smoke.py` — `sum(change(X))` = Feb 20 / Mar 10 (Jan NULL), `sum(change_pct(X))` = Feb 3.0 / Mar 0.5, `sum(time_shift(X, -1))` = Feb 15 / Mar 20, `sum(lag(X, 1))` = Feb 15 / Mar 20, `sum(lead(X, 1))` = Jan 35 / Feb 30 (Mar NULL), `sum(consecutive_periods(X > 12))` = Jan 0 / Feb 2 / Mar 2; `TestWindowedInnerProbe`: drop the xfail and pin the windowed time-dimension error; add a cross-model grained inner probe (`sum(cumsum(customers.spend:sum(partition_by=[customers.tier, ordered_at])))` rooted at `orders` by month) pinned as executed values if it runs correctly, else as its typed error
- [x] 5.7 Re-spell (D4a): `tests/_dev1832_fixtures.py` / `test_dev1832_transform_source.py` `GRAINED_CUMSUM` and `test_dev1832_golden_sql.py` `lifted/transform_constituent` → `sum(cumsum(X) - 1)` (oracle 13/48/59 unchanged); the axis-fail test → `sum(cumsum(amount:sum(partition_by=region)))`; the deltas in `specs/queries/semantics`, `specs/queries/partitioned-aggregates`, `specs/aggregations/expression-aggregation` (done at plan time); verify every `month(ordered_at)]` spelling is gone from the change and from `tests/`
- [x] 5.8 Follow-up issue + worktree for the shapes 5.6 pins as errors (windowed inner under a transform constituent; cross-model grained inner if it fails; the explicit `time_dimension=` axis idea) PLUS the D4c mixed-row-source collapse deferred under option A, blockedBy DEV-1832, with a detailed actionable comment that names each pinned error as an Axiom 9 closure gap to close (Codex finding) and the pinned tests to flip; verify the issue key is recorded in the PR description — DEV-1928 (parented under DEV-1873; worktree `egor__dev-1928-…` cut from this head)

## 6. Accounting, docs, gates

- [x] 6.1 Remove the stale ratchet allowlist entry; verify `test_law_guard_ratchet.py` passes with `guards.baseline` unchanged
- [x] 6.2 Docs: one sentence each in `docs/concepts/formulas.md` (boundaries paragraph), `docs/concepts/models.md` (filter row + paragraph), `docs/concepts/queries.md` (cross-model measures); `Column.filter` field comment; verify by grep that no "not yet supported" wording for the three shapes remains
- [x] 6.3 Confirm the 5.0 arc42 edit is applied and `arch_check` is green at the gate (the diff itself is task 5.0)
- [x] 6.4 Record the `dev1832` golden baseline (lifted/* now SQL) plus the `dev1839` axis-message and `dev1859` alias-hash deltas through `ALLOWED_DELTAS`; run `poetry run pytest -m "not integration" -n auto`, `ruff`, conventions gate, `basedpyright` (baseline shrinks or holds), `arch_check`; verify all green, then the Codex working-tree pass before push
