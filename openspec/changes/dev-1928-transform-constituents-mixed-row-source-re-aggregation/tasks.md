## 1. Failing suites (spec-tests stage)

Fixture rows: North Jan 10 / Feb 20 / Mar 30, South Jan 5 / Feb 15, West Feb NULL;
`X` = `amount:sum(partition_by=[region, ordered_at])`.

- [x] 1.1 `tests/test_dev1832_fixtures_smoke.py`: derive every new oracle from the raw rows — `sum(amount * last(X))` (375 / 825 / 900), `sum(amount * min(X, partition_by=region))`, the empty-grain `sum(amount * last(amount:sum(partition_by=ordered_at)))`, `weighted_avg(amount, weight=min(X, partition_by=region))`, the mixed+combined `sum(amount * min(X, partition_by=region)) + amount:sum(partition_by=region)` over `[region]`, and the windowed `sum(rank(amount:sum(window='90d', partition_by=region)))`; verify the smoke test passes on the current tree
- [x] 1.2 `tests/test_dev1832_transform_source.py`: flip `test_collapse_mixed_with_row_leaf_fails_closed` → executed 375 / 825 / 900 on SQLite + DuckDB; add the hand-written, empty-grain, parameter, mixed+combined, filter-only and ORDER-BY-only cases; plan-structure pins (one re-aggregation producer at its own grain row-attached on its keys — zero join pairs for the empty grain — never attached at the enclosing level, one flat `WITH`, `assert_scope_closed`, no placeholder leak, cardinality invariant); broadcast- and error-mode tests with the fanning-dimension fixture; verify each fails on the current tree (the guard or the grain-cover assertion)
- [x] 1.3 Same file: `test_windowed_inner_fails_closed` → executed hand-derived values, exactly three buckets, every value non-NULL, both engines, plus pins that the nested producer's projected grain and join keys contain the exact bucket key, one flat `WITH`, closed scopes, no leak. The nested-producer pin asserts the top-level attach is **COMBINED** phase (Egor, 2026-09-18): no row leaf → a second-order aggregation over the operand cells (Axiom 2.4), never a row attach
- [x] 1.4 Same file: move `test_cross_model_grained_inner_fails_closed` into a narrowly named permanent-boundary class whose docstring states the Axiom 8 reason (the inner's explicit `ordered_at` key is an `orders` column reachable from the `customers` home only across the fanning `customers → orders` hop — a mode-invariant input-safety error) and asserts the error names `ordered_at` in `broadcast`, `error` AND `associate` modes; add the to-one positive pin `sum(cumsum(amount:sum(partition_by=[customers.tier, ordered_at])))` rooted at `orders` (executes today → `{Jan 33, Feb 55, Mar 85, Apr 120}`); add the contrast pin that a non-time cross-model key (`customers.spend:sum(partition_by=status)`) still associates. Probe RESOLVED (task-1.4 escalation, Egor 2026-09-18): associate mode raises the same fanning-hop error, so the boundary holds in every mode — the associate-executed scenario is dropped and the well-defined value (`{Jan 100, Feb 150, Mar 200, Apr 420}`) is tracked forward as DEV-1941. Verify the boundary test passes in all three modes and the positive/contrast pins pass on the current tree
- [x] 1.5 `tests/test_dev1832_golden_sql.py`: add named cases `lifted/mixed_reagg`, `lifted/mixed_collapse`, `lifted/windowed_inner` (recorded raises until the implementation lands); verify the baseline diff is their addition only
- [x] 1.6 `tests/_dev1871_raise_ledger.py`: remove the `check_collapsing_transform_not_row_mixed` row; verify `tests/test_dev1871_raise_parity.py` is red on the current tree (the raise still exists)

## 2. Mixed source + re-aggregation constituent (D1–D3, D6)

- [x] 2.1 Delete `elaborate_env.check_collapsing_transform_not_row_mixed`, its import and its call in `bind_inputs.py`; verify the collapse test now fails on the grain-cover assertion and `test_dev1871_raise_parity.py` is green
- [x] 2.2 `compile/stages._plan_regroups`: route a re-aggregation attached input of a row-attach root through `_synthesize_reaggregation_producer` in an attached-constituent context (projection = the constituent's own grain; query-dimension rule skipped, attributability kept); verify the hand-written and collapse executed cases pass on SQLite + DuckDB
- [x] 2.3 Join pairs from the synthesized producer's projected grain (`ordered_pks` → row slots), zero pairs for an empty grain (scalar cross join, no empty `ON`); verify the empty-grain case and the plan-structure pins pass on both engines
- [x] 2.4 Parameter, mixed+combined, position-parity and mode cases; verify all of 1.2 green and that `_assert_broadcast_coherence` still fires for the combined term (the mixed+combined pin)
- [x] 2.5 Codex review found a bounded boundary: a re-aggregation used BOTH standalone (combined phase) and as a mixed constituent (row phase) needs its shared producer at two phases, whose nested-producer CTE emits out of dependency order on strict dialects (DuckDB rejects the forward reference SQLite tolerates). Fail closed in the checker (`elaborate_env.check_reaggregation_not_standalone_and_mixed`, a `ValueError` — no ratchet bump) with a raise-ledger row, a `pytest.raises` pin in `test_dev1832_transform_source.py`, and DEV-1942 filed for the emission-ordering fix. Never a crash / dialect-inconsistent SQL

## 3. Windowed inner under a transform constituent (D4)

- [x] 3.1 Thread the query's active time dimension into the nested transform-root producer via `_regroup_producer_prebound` (`_build_carrier_attach` passes `window_td_key` when a constituent has a windowed inner) so the bucket is in its GROUP BY, projection and join pairs; verify 1.3 passes on both engines and every existing windowed and transform pin is unchanged
- [x] 3.2 Probe outcome: the windowed re-aggregation is well-defined, not undefined — it is the axiom-correct second-order aggregation over the operand cells (COMBINED phase, Axiom 2.4). Escalated the plan-structure phase (spec-tests pinned "row"); Egor resolved it as COMBINED and asked to extend tests + fixtures for full axiom compliance. Added `monthly_multi` (two rows per (region, month) cell) + `TestReaggAxiomCompliance` (exec: windowed pure re-aggregation counts cells `{Jan 7, Jun 3}`, never the doubled row count; mixed `sum(amount * min(X, partition_by=region))` counts rows `{Jan 125, Jun 400}`; plan: pure = combined, mixed = row) + `TestDev1928AxiomComplianceOracles` (double-entry oracle derivations)

## 4. Boundary, docs, accounting, gates

- [x] 4.1 Confirm 1.4 green (boundary class and positive pin); verify the error message still names `ordered_at` and the remedy with no deferral wording
- [x] 4.2 Docs: `docs/concepts/formulas.md` — re-aggregation constituents in a mixed source and windowed inners under a transform constituent now execute, while a target-homed inner naming a host time axis stays a typed error; the windowed-follow-ups paragraph carves out the now-executing constituent case; grep confirms no deferred / follow-up wording for these shapes remains
- [x] 4.3 Record the three named golden cases as SQL through `ALLOWED_DELTAS` (added, regenerated, removed); verified the baseline diff is only those 21 entries (raise → SQL) and every other golden is byte-identical
- [ ] 4.4 Confirm no arc42 or `index.yaml` edit is needed (`guards.baseline` unchanged); run `poetry run python tools/arch_check.py`; verify green
- [ ] 4.5 `poetry run pytest -m "not integration" -n auto`, `ruff check slayer/ tests/`, conventions gate, `basedpyright` (baseline holds or shrinks); verify all green, then the Codex working-tree pass before push
